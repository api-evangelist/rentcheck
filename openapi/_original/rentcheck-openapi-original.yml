openapi: 3.1.0
info:
  title: RentCheck REST API
  version: 1.0.0
  description: >
    
    ## Mission

    At RentCheck, our mission is plain and simple: To make renting fair and transparent for everyone involved. 

    RentCheck is a property inspection solution that helps property managers save time and resources with easy self-guided inspections that residents can perform from their smartphone. 


    With RentCheck, property managers can avoid tenant coordination, eliminate drive time, and standardize their inspection process. 

    We provides real-time visibility to property managers and owners while bringing transparency to the security deposit deduction process.


    ## API

    The RentCheck API lets developers tap into the RentCheck ecosystem, building their own RentCheck-powered applications to enable inspection scheduling and creation and to leverage inspection data for a variety of use cases in the property management, maintenance, and insurance spaces.


    The RentCheck REST API supports JSON requests and responses and features a resource-oriented design that generally adheres to the RFC 7321 HTTP/1.1 standard. 

    Our API resources provide access to many RentCheck features, including units, buildings, communities, inspections, and residents.


    ## Credentials

    In addition to the Bearer Auth, RentCheck will need to send you an application ID and secret. These are required to generate the required application headers (x-app-id & x-app-secret).

    These values can be obtained from the [RentCheck API integration page](https://app.getrentcheck.com/account/integrations/rentcheck-api).


    ## Rate Limiting

    The RentCheck API enforces rate limits to ensure fair usage and prevent abuse. The rate limits are as follows:

    - **Requests per second**: 8

    - **Requests per minute**: 256

    - **Requests per 10 minutes**: 1024


    If you exceed the rate limits, you will receive a 429 Too Many Requests response.


    ## Pagination

    When interacting with endpoints that return a list of items, the results are paginated to help manage large data sets efficiently. The following parameters control pagination:

    - **page_size** (integer): Defines the number of items returned per page. The maximum allowed value is 250. If a value larger than 250 is provided, it will be automatically clamped to 250. This ensures that the system performs optimally and prevents the server from being overwhelmed by too many items in a single response.
      - **Maximum**: `250`
    - **page_number** (integer): Indicates the page number to retrieve. Pagination starts at `page 0`. If not specified, the first page (`page 0`) is returned by default.

    ### Example Request

    ```http

    GET /api/v1/inspections?page_size=300&page_number=2

    ```

    In this example, although the `page_size` parameter is set to `300` **for a query with 1500 total results**, the system will return only `250` items per page, as `300` exceeds the maximum allowed value.

    #### Example Response

    ```json

    {
        "status": 200,
        "data": [...],
        "count": 250,
        "total_results": 1500
    }

    ```

    This response shows that the `page_size` has been clamped to `250`, despite the initial request for `300`.
  contact:
    name: RentCheck Support
    email: support@getrentcheck.com
servers:
  - url: https://prod-public-api.getrentcheck.com
    description: Production server
components:
  securitySchemes:
    bearerAuth:
      description: Authorization key needed to use the API
      type: http
      scheme: bearer
      bearerFormat: JWT
    x-app-id:
      description: Represents the identification of you application
      type: apiKey
      name: x-app-id
      in: header
      required: true
    x-app-secret:
      description: Represents the secret for your application
      type: apiKey
      name: x-app-secret
      in: header
      required: true
  responses:
    "401":
      description: Unauthorized
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: integer
                description: HTTP status code
                example: 401
              error:
                type: string
                description: Error message
                enum:
                  - token is not valid
                  - authorization header is empty
                  - authorization header is not valid
                  - "format for Authorization must be: Bearer [token]"
                  - refresh token is not valid for resources
                  - not well-formed token
                example: token is not valid
    "403":
      description: Forbidden
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: integer
                description: HTTP status code
                example: 403
              error:
                type: string
                description: Error message describing why the caller is not allowed to perform
                  the action.
    "404":
      description: Not Found
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: integer
                description: HTTP status code
                example: 404
              error:
                type: string
                description: Error message describing the missing resource
                example: property not found
    synced_property_delete_forbidden:
      description: Forbidden — at least one requested property is currently synced
        from an external integration (Latchel/Yardi/AppFolio/etc.) and cannot be
        deleted without first disconnecting the sync.
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: integer
                description: HTTP status code
                example: 403
              error:
                type: string
                enum:
                  - Cannot delete property when integration sync is active
                example: Cannot delete property when integration sync is active
    synced_property_merge_forbidden:
      description: Forbidden — either the origin or target property of a merge is
        currently synced from an external integration.
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: integer
                description: HTTP status code
                example: 403
              error:
                type: string
                enum:
                  - Synced property cannot be used for merge
                example: Synced property cannot be used for merge
    synced_resource_removal_forbidden:
      description: Forbidden — the resource is currently synced from an external
        integration and cannot be deleted or merged without first disconnecting
        the sync. Prefer the action-specific `synced_property_delete_forbidden`
        / `synced_property_merge_forbidden` responses; this alias is kept for
        backward compatibility.
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: integer
                description: HTTP status code
                example: 403
              error:
                type: string
                enum:
                  - Cannot delete property when integration sync is active
                  - Synced property cannot be used for merge
                example: Cannot delete property when integration sync is active
    inspection_access_unauthorized:
      description: Unauthorized — standard auth-token failures, plus the inspection
        access check. When the caller cannot access the target inspection, this
        surfaces as 401 with the message `User does not have permissions to
        access inspection`.
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: integer
                description: HTTP status code
                example: 401
              error:
                type: string
                enum:
                  - token is not valid
                  - authorization header is empty
                  - authorization header is not valid
                  - "format for Authorization must be: Bearer [token]"
                  - refresh token is not valid for resources
                  - not well-formed token
                  - User does not have permissions to access inspection
                example: User does not have permissions to access inspection
    resident_lease_unauthorized:
      description: Unauthorized — either a standard auth-token failure, or the
        authenticated renter does not have access to the property the lease
        belongs to (or the lease belongs to a different property).
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: integer
                description: HTTP status code
                example: 401
              error:
                type: string
                enum:
                  - token is not valid
                  - authorization header is empty
                  - authorization header is not valid
                  - "format for Authorization must be: Bearer [token]"
                  - refresh token is not valid for resources
                  - not well-formed token
                  - User does not have access to this property
                  - property not found
                  - User either lacks property access or the lease belongs to a
                    different property
                  - Lease belongs to a different property than the one provided
                example: User either lacks property access or the lease belongs to a different
                  property
  parameters:
    V3PropertyListPageSize:
      in: query
      name: page_size
      schema:
        type: integer
        default: 20
        minimum: 0
      description: The number of elements in the data collection. Sizes above `250`
        are clamped to `250` to keep responses bounded (matches the API-wide
        pagination policy documented in the OpenAPI intro).
    V3PropertyListPageNumber:
      in: query
      name: page_number
      schema:
        type: integer
        default: 0
        minimum: 0
      description: Zero-indexed page number.
    V3PropertyListSortBy:
      in: query
      name: sort_by
      schema:
        type: string
        default: address
        enum:
          - address
          - name
          - last_completed_inspection
      description: Sort results by one of the available fields.
    V3PropertyListSortType:
      in: query
      name: sort_type
      schema:
        type: string
        default: asc
        pattern: ^(?:[Aa][Ss][Cc]|[Dd][Ee][Ss][Cc])$
        example: asc
      description: Sort direction. Case-insensitive, so `asc`/`ASC`/`Asc` and
        `desc`/`DESC`/`Desc` are all accepted.
    V3PropertyListTeamValue:
      in: query
      name: filters[team][value]
      schema:
        type: string
      description: List of team ids to filter by. Multiple values are concatenated
        with `|`. When `filters[team][condition]` is supplied, this must resolve
        to at least one id, so an empty list surfaces as 400
        `"filters.teamIds.value" must contain at least 1 items`.
    V3PropertyListTeamCondition:
      in: query
      name: filters[team][condition]
      schema:
        type: string
        enum:
          - any_of
          - none_of
      description: Condition to apply to the team filter.
    V3PropertyListTeamIdLegacy:
      in: query
      name: team_id[]
      deprecated: true
      schema:
        type: array
        items:
          type: string
      description: Legacy team id filter. Prefer `filters[team][value]`.
    V3PropertyListLastCompletedStart:
      in: query
      name: last_completed_inspection_start_date
      schema:
        oneOf:
          - type: string
            format: date
          - type: string
            format: date-time
          - type: string
            enum:
              - ""
      description: ISO-8601 date (`YYYY-MM-DD`) or date-time lower bound for the last
        completed inspection filter. Both date and date-time forms are accepted,
        and an empty string is treated as "no filter".
    V3PropertyListLastCompletedEnd:
      in: query
      name: last_completed_inspection_end_date
      schema:
        oneOf:
          - type: string
            format: date
          - type: string
            format: date-time
          - type: string
            enum:
              - ""
      description: ISO-8601 date (`YYYY-MM-DD`) or date-time upper bound for the last
        completed inspection filter. Both date and date-time forms are accepted,
        and an empty string is treated as "no filter".
    V3PropertyListSynced:
      in: query
      name: synced
      schema:
        type: string
        enum:
          - synced
          - not synced
      description: Filter by external integration sync status.
    V3PropertyListExtraFields:
      in: query
      name: extra_fields[]
      schema:
        type: array
        items:
          type: string
      description: Optional list of extra fields to include in each digest (e.g.
        `last_completed_inspection`).
  schemas:
    oauth2_refresh_token_request_model:
      type: object
      required:
        - refresh_token
      properties:
        refresh_token:
          type: string
          description: The refresh token is used to request a fresh access token.
    oauth2_refresh_token_response_model:
      type: object
      required:
        - access_token
        - refresh_token
      properties:
        access_token:
          type: string
          description: The access token is used to validate a request to the RentCheck API.
        refresh_token:
          type: string
          description: The refresh token is used to request a fresh access token.
    oauth2_email_password_auth_request_model:
      description: Credentials for `POST /v1/oAuth2/emailPasswordAuth`. Either the
        account password OR the integration password (never both) must accompany
        the email.
      oneOf:
        - type: object
          required:
            - email
            - password
          properties:
            email:
              type: string
              format: email
              description: The user's email.
              example: user@email.com
            password:
              type: string
              description: The account password used on login.
              example: password123
        - type: object
          required:
            - email
            - integration_password
          properties:
            email:
              type: string
              format: email
              description: The user's email.
              example: user@email.com
            integration_password:
              type: string
              description: The integration password issued to the user via the RentCheck API
                integrations page. Callers presenting this value are
                authenticated as the underlying RentCheck user with no
                additional login round-trip.
              example: integration-abcdef123456
    oauth2_email_password_auth_response_model:
      type: object
      required:
        - access_token
        - refresh_token
      properties:
        access_token:
          type: string
          description: The access token is used to validate a request to the RentCheck API.
        refresh_token:
          type: string
          description: The refresh token is used to request a fresh access token.
    unit_create_request_model:
      type: object
      required:
        - property_type
        - address
        - city
        - zip_code
        - bedrooms
        - full_bathrooms
      properties:
        x_id:
          type: string
          description: External property ID that maps to RentCheck properties
        team_id:
          type: string
          description: Id of the Team who created or administers the Entity.
          example: 05fDGZRKp4nVmlmEwjsv
        building_id:
          type: string
          description: Optional ID of the building the unit belongs to. When supplied, the
            unit inherits the building team and is created under it.
          example: 00BRcZPSakXz6w7RYoE
        property_type:
          type: string
          description: 'The property type designates which type of property the Unit is.
            Write-only: the legacy `""` value is read-only and rejected on
            create — an empty string is rejected.'
          enum:
            - Single-Family
            - Multi-Family
            - Apartment or Condo
            - Commercial
          example: Single-Family
        address:
          type: string
          description: This is the street address for the Unit.
          example: 2001 Red Gates
        address_2:
          type: string
          description: Additional address information for the Unit.
          example: Apt. 4B
        city:
          type: string
          description: This is the city of the Unit.
          example: New Orleans
        region:
          type: string
          description: The broader region or area in which the Unit is located.
          example: Orleans
        state:
          type: string
          description: Two-letter US state code. Optional alias of `region` for callers
            that prefer the postal-style name.
          example: CA
        zip_code:
          type: string
          description: This is the Postal Code for the Unit.
          example: "70130"
        rooms:
          type: array
          items:
            type: string
          deprecated: true
          description: "Deprecated: derived from `rooms_info` on read; use `rooms_info`
            instead. An array of strings representing the room configuration for
            the property. Only rooms that are not bedrooms and bathrooms can be
            set this way."
          example:
            - Living Room
            - Den
            - Balcony
        rooms_info:
          type: array
          description: "Structured per-room configuration and, when supplied, the
            authoritative room set: `rooms`/`room_names` are derived from these
            entries. `name` is the source of truth (required); `type` is
            recomputed from it and `label` defaults to `name`, so both are
            optional. The bedroom/bathroom counts always come from the dedicated
            `bedrooms`/`full_bathrooms`/`half_bathrooms` fields, never from this
            array. Mutually exclusive with `rooms` — sending both is a
            validation error."
          items:
            $ref: "#/components/schemas/room_info_request"
        bedrooms:
          type: integer
          minimum: 0
          maximum: 10
          description: The number of bedrooms the Unit has. Must be an integer in `[0, 10]`.
          example: 2
        full_bathrooms:
          type: number
          minimum: 0
          maximum: 10
          description: The number of full bathrooms the Unit has. Must be a number in `[0,
            10]` — decimals are allowed.
          example: 2
        half_bathrooms:
          type: integer
          minimum: 0
          maximum: 10
          description: The number of half bathrooms the Unit has. Must be an integer in
            `[0, 10]`.
          example: 1
        rentvine_sync:
          $ref: "#/components/schemas/rentvine_sync"
    unit_update_request_model:
      type: object
      properties:
        x_id:
          type: string
          description: External property ID that maps to RentCheck properties
        building_id:
          type: string
          description: Entity ID of the building the unit belongs to.
          example: 00BRcZPSakXz6w7RYoE
        property_type:
          type: string
          description: 'The property type designates which type of property the Unit is.
            Write-only: the legacy `""` value is read-only and rejected on
            update — an empty string is rejected.'
          enum:
            - Single-Family
            - Multi-Family
            - Apartment or Condo
            - Commercial
          example: Single-Family
        address:
          type: string
          description: This is the street address for the Unit.
          example: 2001 Red Gates
        address_2:
          type: string
          description: Additional address information for the Unit.
          example: Apt. 4B
        city:
          type: string
          description: This is the city of the Unit.
          example: New Orleans
        region:
          type: string
          description: The broader region or area in which the Unit is located.
          example: Orleans
        state:
          type: string
          description: Two-letter US state code. Optional alias of `region` for callers
            that prefer the postal-style name.
          example: CA
        zip_code:
          type: string
          description: This is the Postal Code for the Unit.
          example: "70130"
        rooms:
          type: array
          items:
            type: string
          deprecated: true
          description: "Deprecated: derived from `rooms_info` on read; use `rooms_info`
            instead. An array of strings representing the room configuration for
            the property. Only rooms that are not bedrooms and bathrooms can be
            set this way."
          example:
            - Living Room
            - Den
            - Balcony
        rooms_info:
          type: array
          description: "Structured per-room configuration and, when supplied, the
            authoritative room set: `rooms`/`room_names` are derived from these
            entries. `name` is the source of truth (required); `type` is
            recomputed from it and `label` defaults to `name`, so both are
            optional. The bedroom/bathroom counts always come from the dedicated
            `bedrooms`/`full_bathrooms`/`half_bathrooms` fields, never from this
            array. Mutually exclusive with `rooms` — sending both is a
            validation error. Omit both `rooms` and `rooms_info` to leave the
            existing room arrays unchanged (only the counts are updated)."
          items:
            $ref: "#/components/schemas/room_info_request"
        bedrooms:
          type: integer
          minimum: 0
          maximum: 10
          description: The number of bedrooms the Unit has. Must be an integer in `[0, 10]`.
          example: 2
        full_bathrooms:
          type: number
          minimum: 0
          maximum: 10
          description: The number of full bathrooms the Unit has. Must be a number in `[0,
            10]` — decimals are allowed.
          example: 2
        half_bathrooms:
          type: integer
          minimum: 0
          maximum: 10
          description: The number of half bathrooms the Unit has. Must be an integer in
            `[0, 10]`.
          example: 1
        team_id:
          type: string
          description: Id of the Team who created or administers the Entity.
          example: 05fDGZRKp4nVmlmEwjsv
        rentvine_sync:
          $ref: "#/components/schemas/rentvine_sync"
    unit_response_model:
      type: object
      required:
        - id
        - property_type
        - address
        - city
        - zip_code
        - bedrooms
        - full_bathrooms
        - half_bathrooms
        - building_id
        - latchel_property_id
        - team
        - created_date
        - updated_date
        - rooms_info
        - rooms
        - room_names
        - last_completed_inspection
        - creation_source
        - created_by
        - permissions
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        x_id:
          type: string
          description: External property ID that maps to RentCheck properties
        address:
          type: string
          description: This is the street address for the Unit.
          example: 2001 Red Gates
          readOnly: true
        address_2:
          type: string
          description: Additional address information for the Unit.
          example: Apt. 4B
        city:
          type: string
          description: This is the city of the Unit.
          example: New Orleans
        region:
          type: string
          description: The broader region or area in which the Unit is located.
          example: Orleans
        zip_code:
          type: string
          description: This is the Postal Code for the Unit.
          example: "70130"
          readOnly: true
        team:
          type: object
          required:
            - id
            - name
            - internal_label
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: The name field is the Team name that is displayed on recipient
                facing reports and communication.
              example: The best company
            internal_label:
              type: string
              description: The internal label is a convenience label to help differentiate
                similar teams a user may belong to. It's displayed to you and
                your team members.
              example: The best company label
          nullable: true
          readOnly: true
          description: Team that owns this unit. May be null when the underlying team has
            been removed.
        created_date:
          type: string
          description: Entity creation date, in ISO format
          example: 2022-12-02T14:15:37.925
          readOnly: true
        updated_date:
          type: string
          description: Entity last update date, in ISO format
          example: 2023-10-04T14:15:37.925
          readOnly: true
        rooms:
          type: array
          items:
            type: string
          deprecated: true
          description: "Deprecated: derived from `rooms_info` on read; use `rooms_info`
            instead. An array of strings representing the room configuration for
            the property. Only rooms that are not bedrooms and bathrooms can be
            set this way."
          example:
            - Living Room
            - Den
            - Balcony
          readOnly: true
        room_names:
          type: array
          deprecated: true
          description: "Deprecated: derived from `rooms_info` on read; use `rooms_info`
            instead. Returned unit room names."
          enum:
            - Front Door
            - Back Door
            - Kitchen
            - Dining Room
            - Living Room
            - Family Room
            - Den
            - Office
            - Bedroom
            - First Bedroom
            - Second Bedroom
            - Third Bedroom
            - Fourth Bedroom
            - Fifth Bedroom
            - Sixth Bedroom
            - Seventh Bedroom
            - Eighth Bedroom
            - Ninth Bedroom
            - Tenth Bedroom
            - Bathroom
            - First Bathroom
            - Second Bathroom
            - Third Bathroom
            - Fourth Bathroom
            - Fifth Bathroom
            - Sixth Bathroom
            - Seventh Bathroom
            - Eighth Bathroom
            - Ninth Bathroom
            - Tenth Bathroom
            - Full Bathroom
            - First Full Bathroom
            - Second Full Bathroom
            - Third Full Bathroom
            - Fourth Full Bathroom
            - Fifth Full Bathroom
            - Sixth Full Bathroom
            - Seventh Full Bathroom
            - Eighth Full Bathroom
            - Ninth Full Bathroom
            - Tenth Full Bathroom
            - Half Bathroom
            - First Half Bathroom
            - Second Half Bathroom
            - Third Half Bathroom
            - Fourth Half Bathroom
            - Fifth Half Bathroom
            - Sixth Half Bathroom
            - Seventh Half Bathroom
            - Eighth Half Bathroom
            - Ninth Half Bathroom
            - Tenth Half Bathroom
            - Laundry Room
            - Stairway
            - Stairs
            - Hallway
            - Basement
            - Loft
            - Attic
            - Storage Room
            - Garage
            - Carport
            - Patio
            - Balcony
            - General
            - Property Exterior & Grounds
            - Multi-Unit Common Areas
          readOnly: true
        rooms_info:
          type: array
          description: Structured per-room metadata and the source of truth for room
            labeling. `rooms` and `room_names` are derived from this. `type` is
            the no-ordinal room kind (e.g. "Bedroom"), `name` is the ordinal
            name (e.g. "First Bedroom"), and `label` is a human-meaningful label
            that defaults to `name`.
          items:
            $ref: "#/components/schemas/room_info"
          readOnly: true
        last_completed_inspection:
          type: object
          readOnly: true
          nullable: true
          required:
            - date
            - id
            - type
          properties:
            date:
              type: string
              description: Inspection completion date, in ISO format
              example: 2022-12-02T14:15:37.925
            type:
              type: string
              description: The inspection template name
            id:
              type: string
              description: Inspection ID
        creation_source:
          type: string
          description: App that perform the entity creation.
          enum:
            - zapier
            - rentmanager
            - flatfile
            - 365connect
            - appfolio
            - yardi
            - rentvine
            - test_app_id
          example: zapier
          readOnly: true
        created_by:
          type: object
          properties:
            name:
              type: string
              description: Name of the user that created the property
          description: Info about the user that created the property.
          readOnly: true
        sync_data:
          type: object
          readOnly: true
          nullable: true
          required:
            - vendor
            - last_sync
          description: Single sync envelope used for whichever integration owns the
            property; null when the property is not synced.
          properties:
            last_sync:
              type: string
              description: Sync date, in ISO format
              example: 2022-12-02T14:15:37.925
            vendor:
              type: string
              description: The third party vendor for this sync
              enum:
                - rentmanager
                - yardi
                - appfolio
                - jenark
                - rentvine
                - buildium
            status:
              type: string
              description: Latest manual-sync status. Only present on integrations that expose
                it (e.g. Buildium).
              enum:
                - scheduled
                - processing
                - success
                - error
            error:
              type: string
              nullable: true
              description: Error message from the most recent sync attempt, if any.
            id:
              oneOf:
                - type: string
                - type: integer
              description: External vendor id for the property/unit (string for most vendors;
                integer for AppFolio).
            x_id:
              type: string
              description: Vendor-native external id echo for the legacy Rentvine / Rent
                Manager envelope. Kept for debugging/audit parity with the
                historic `sync_data`.
            location_id:
              type: string
              description: Rent Manager location (property) id — legacy `sync_data`,
                debugging/audit.
            integration_id:
              type: string
              description: RentCheck integration record id (Rent Manager legacy sync) —
                debugging/audit.
            short_property_id:
              type: string
              description: AppFolio short property id, when synced from AppFolio.
            short_unit_id:
              type: string
              description: AppFolio short unit id, when synced from AppFolio.
            upload_response:
              type: array
              description: AppFolio per-destination inspection-upload outcomes.
              items:
                type: object
                required:
                  - resource
                  - resource_id
                  - uploaded_id
                properties:
                  resource:
                    type: string
                    enum:
                      - units
                      - properties
                      - occupancies
                  resource_id:
                    type: string
                  uploaded_id:
                    type: string
        property_type:
          type: string
          description: The property type designates which type of property the Unit is.
            `""` is allowed as a legacy value for units created before the enum
            was tightened.
          enum:
            - ""
            - Single-Family
            - Multi-Family
            - Apartment or Condo
            - Commercial
          example: Single-Family
        bedrooms:
          type: integer
          minimum: 0
          maximum: 10
          description: The number of bedrooms the Unit has. Must be an integer in `[0, 10]`.
          example: 2
        full_bathrooms:
          type: number
          minimum: 0
          maximum: 10
          description: The number of full bathrooms the Unit has. Must be a number in `[0,
            10]` — decimals are allowed.
          example: 2
        half_bathrooms:
          type: integer
          minimum: 0
          maximum: 10
          description: The number of half bathrooms the Unit has. Must be an integer in
            `[0, 10]`.
          example: 1
        building_id:
          type: string
          description: ID of the parent building. Non-nullable.
          example: 00BRcZPSakXz6w7RYoE
          readOnly: true
        latchel_property_id:
          type: integer
          readOnly: true
          nullable: true
          description: Latchel property id when the unit is synced to Latchel; null
            otherwise.
        permissions:
          type: object
          readOnly: true
          required:
            - can_delete
            - can_create_inspection
            - can_add_to_parent_property
            - can_reassign_team
          description: Permissions available to the current user on this unit (matches the
            shape used by `building_response_model` /
            `community_response_model`).
          properties:
            can_delete:
              type: boolean
            can_create_inspection:
              type: boolean
            can_add_to_parent_property:
              type: boolean
            can_reassign_team:
              type: boolean
    building_create_request_model:
      type: object
      required:
        - team_id
        - name
        - address
        - city
        - zip_code
        - floors
      properties:
        x_id:
          type: string
          description: External property ID that maps to RentCheck properties
        team_id:
          type: string
          description: Id of the Team who created or administers the Entity.
          example: 05fDGZRKp4nVmlmEwjsv
        name:
          type: string
          description: This is the name of the Building.
          example: Empire State Building
        address:
          type: string
          description: This is the street address for the Building.
          example: 2001 Red Gates
        address_2:
          type: string
          description: Additional address information for the Building.
          example: Apt. 4B
        city:
          type: string
          description: This is the city of the Building.
          example: New Orleans
        region:
          type: string
          description: The broader region or area in which the Building is located.
          example: Orleans
        zip_code:
          type: string
          description: This is the Postal Code for the Building.
          example: "70130"
        floors:
          type: number
          maximum: 100
          description: The number of floors contained in the Building. This number allows
            us to customize Building inspections to target features on certain
            floors. Required on create, optional on update. Must be ≤ 100. On
            read, a missing value defaults to `1`.
          example: 7
        additional_team_ids:
          type: array
          description: Building can be accessed by multiple teams. This field lists the
            entity ids of Teams that have access to the Building
          items:
            type: string
            description: Entity ID
            example: 00BRcZPSakXz6w7RYoE
        unit_ids:
          type: array
          description: Building can have 1+ Units associated with them. unit_ids is a list
            of entity ids of the Units that belong to a Building
          items:
            type: string
            description: Entity ID
            example: 00BRcZPSakXz6w7RYoE
        rooms:
          type: array
          items:
            type: string
          deprecated: true
          description: 'Deprecated: on write prefer `rooms_info`, which is the
            authoritative room set. An array of arbitrary room-name strings for
            the building (e.g. "Lobby", "Roof Deck"). Building rooms are
            free-form — unlike units they are not constrained to a fixed set or
            derived from bedroom/bathroom counts. Used only when `rooms_info` is
            omitted.'
          example:
            - Lobby
            - Roof Deck
            - Gym
        rooms_info:
          type: array
          description: 'Structured per-room configuration for the building and, on write,
            the authoritative room set: when present, these entries define the
            building’s rooms and `rooms`/`room_names` are derived from them.
            `name` is the source of truth (required) — for buildings the room
            string itself (e.g. "Roof Deck") — while `type` is recomputed from
            it and `label` (the human-meaningful display name, e.g. "Sky
            Lounge") defaults to `name`, so both are optional. An explicit empty
            array clears the rooms. Mutually exclusive with `rooms` — sending
            both is a validation error; omit both to take the room set from
            `rooms`.'
          items:
            $ref: "#/components/schemas/room_info_request"
        state:
          type: string
          description: Two-letter US state code. Optional alias of `region` for callers
            that prefer the postal-style name.
          example: CA
        rentvine_sync:
          $ref: "#/components/schemas/rentvine_sync"
    building_update_request_model:
      type: object
      properties:
        x_id:
          type: string
          description: External property ID that maps to RentCheck properties
        name:
          type: string
          description: This is the name of the Building.
          example: Empire State Building
        address:
          type: string
          description: This is the street address for the Building.
          example: 2001 Red Gates
        address_2:
          type: string
          description: Additional address information for the Building.
          example: Apt. 4B
        city:
          type: string
          description: This is the city of the Building.
          example: New Orleans
        region:
          type: string
          description: The broader region or area in which the Building is located.
          example: Orleans
        zip_code:
          type: string
          description: This is the Postal Code for the Building.
          example: "70130"
        floors:
          type: number
          maximum: 100
          description: The number of floors contained in the Building. This number allows
            us to customize Building inspections to target features on certain
            floors. Required on create, optional on update. Must be ≤ 100. On
            read, a missing value defaults to `1`.
          example: 7
        rooms:
          type: array
          items:
            type: string
          deprecated: true
          description: 'Deprecated: on write prefer `rooms_info`, which is the
            authoritative room set. An array of arbitrary room-name strings for
            the building (e.g. "Lobby", "Roof Deck"). Building rooms are
            free-form — unlike units they are not constrained to a fixed set or
            derived from bedroom/bathroom counts. Used only when `rooms_info` is
            omitted.'
          example:
            - Lobby
            - Roof Deck
            - Gym
        rooms_info:
          type: array
          description: 'Structured per-room configuration for the building and, on write,
            the authoritative room set: when present, these entries define the
            building’s rooms and `rooms`/`room_names` are derived from them.
            `name` is the source of truth (required) — for buildings the room
            string itself (e.g. "Roof Deck") — while `type` is recomputed from
            it and `label` (the human-meaningful display name, e.g. "Sky
            Lounge") defaults to `name`, so both are optional. An explicit empty
            array clears the rooms. Mutually exclusive with `rooms` — sending
            both is a validation error; omit both to leave the building’s
            existing rooms unchanged.'
          items:
            $ref: "#/components/schemas/room_info_request"
        additional_team_ids:
          type: array
          description: Building can be accessed by multiple teams. This field lists the
            entity ids of Teams that have access to the Building
          items:
            type: string
            description: Entity ID
            example: 00BRcZPSakXz6w7RYoE
        unit_ids:
          type: array
          description: Building can have 1+ Units associated with them. unit_ids is a list
            of entity ids of the Units that belong to a Building
          items:
            type: string
            description: Entity ID
            example: 00BRcZPSakXz6w7RYoE
        team_id:
          type: string
          description: Id of the Team who created or administers the Entity.
          example: 05fDGZRKp4nVmlmEwjsv
        state:
          type: string
          description: Two-letter US state code. Optional alias of `region` for callers
            that prefer the postal-style name.
          example: CA
        rentvine_sync:
          $ref: "#/components/schemas/rentvine_sync"
    building_response_model:
      type: object
      required:
        - id
        - name
        - address
        - city
        - zip_code
        - floors
        - property_type
        - team
        - room_names
        - last_completed_inspection
        - permissions
        - created_date
        - updated_date
        - rooms_info
        - rooms
        - creation_source
        - created_by
        - additional_team_ids
        - unit_ids
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        x_id:
          type: string
          description: External property ID that maps to RentCheck properties
        team:
          type: object
          required:
            - id
            - name
            - internal_label
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: The name field is the Team name that is displayed on recipient
                facing reports and communication.
              example: The best company
            internal_label:
              type: string
              description: The internal label is a convenience label to help differentiate
                similar teams a user may belong to. It's displayed to you and
                your team members.
              example: The best company label
          nullable: true
          readOnly: true
          description: Team that owns this building. May be null when the underlying team
            has been removed.
        property_type:
          type: string
          description: Discriminator value identifying this property as a Building.
          enum:
            - Building
          example: Building
          readOnly: true
        name:
          type: string
          description: This is the name of the Building.
          example: Empire State Building
        address:
          type: string
          description: This is the street address for the Building.
          example: 2001 Red Gates
        address_2:
          type: string
          description: Additional address information for the Building.
          example: Apt. 4B
        city:
          type: string
          description: This is the city of the Building.
          example: New Orleans
        region:
          type: string
          description: The broader region or area in which the Building is located.
          example: Orleans
        zip_code:
          type: string
          description: This is the Postal Code for the Building.
          example: "70130"
        floors:
          type: number
          maximum: 100
          description: The number of floors contained in the Building. This number allows
            us to customize Building inspections to target features on certain
            floors. Required on create, optional on update. Must be ≤ 100. On
            read, a missing value defaults to `1`.
          example: 7
        additional_team_ids:
          type: array
          description: Building can be accessed by multiple teams. This field lists the
            entity ids of Teams that have access to the Building
          items:
            type: string
            description: Entity ID
            example: 00BRcZPSakXz6w7RYoE
        unit_ids:
          type: array
          description: Building can have 1+ Units associated with them. unit_ids is a list
            of entity ids of the Units that belong to a Building
          items:
            type: string
            description: Entity ID
            example: 00BRcZPSakXz6w7RYoE
        creation_source:
          type: string
          description: App that perform the entity creation.
          enum:
            - zapier
            - rentmanager
            - flatfile
            - 365connect
            - appfolio
            - yardi
            - rentvine
            - test_app_id
          example: zapier
          readOnly: true
        created_by:
          type: object
          properties:
            name:
              type: string
              description: Name of the user that created the property
          description: Info about the user that created the property.
          readOnly: true
        created_date:
          type: string
          description: Entity creation date, in ISO format
          example: 2022-12-02T14:15:37.925
          readOnly: true
        updated_date:
          type: string
          description: Entity last update date, in ISO format
          example: 2023-10-04T14:15:37.925
          readOnly: true
        rooms:
          type: array
          items:
            type: string
          deprecated: true
          description: "Deprecated: derived from `rooms_info` on read; use `rooms_info`
            instead. An array of strings representing the room configuration for
            the property. Only rooms that are not bedrooms and bathrooms can be
            set this way."
          example:
            - Living Room
            - Den
            - Balcony
          readOnly: true
        room_names:
          type: array
          deprecated: true
          description: "Deprecated: derived from `rooms_info` on read; use `rooms_info`
            instead. Returned unit room names."
          enum:
            - Front Door
            - Back Door
            - Kitchen
            - Dining Room
            - Living Room
            - Family Room
            - Den
            - Office
            - Bedroom
            - First Bedroom
            - Second Bedroom
            - Third Bedroom
            - Fourth Bedroom
            - Fifth Bedroom
            - Sixth Bedroom
            - Seventh Bedroom
            - Eighth Bedroom
            - Ninth Bedroom
            - Tenth Bedroom
            - Bathroom
            - First Bathroom
            - Second Bathroom
            - Third Bathroom
            - Fourth Bathroom
            - Fifth Bathroom
            - Sixth Bathroom
            - Seventh Bathroom
            - Eighth Bathroom
            - Ninth Bathroom
            - Tenth Bathroom
            - Full Bathroom
            - First Full Bathroom
            - Second Full Bathroom
            - Third Full Bathroom
            - Fourth Full Bathroom
            - Fifth Full Bathroom
            - Sixth Full Bathroom
            - Seventh Full Bathroom
            - Eighth Full Bathroom
            - Ninth Full Bathroom
            - Tenth Full Bathroom
            - Half Bathroom
            - First Half Bathroom
            - Second Half Bathroom
            - Third Half Bathroom
            - Fourth Half Bathroom
            - Fifth Half Bathroom
            - Sixth Half Bathroom
            - Seventh Half Bathroom
            - Eighth Half Bathroom
            - Ninth Half Bathroom
            - Tenth Half Bathroom
            - Laundry Room
            - Stairway
            - Stairs
            - Hallway
            - Basement
            - Loft
            - Attic
            - Storage Room
            - Garage
            - Carport
            - Patio
            - Balcony
            - General
            - Property Exterior & Grounds
            - Multi-Unit Common Areas
          readOnly: true
        rooms_info:
          type: array
          description: Structured per-room metadata and the source of truth for room
            labeling. `rooms` and `room_names` are derived from this. `type` is
            the no-ordinal room kind (e.g. "Bedroom"), `name` is the ordinal
            name (e.g. "First Bedroom"), and `label` is a human-meaningful label
            that defaults to `name`.
          items:
            $ref: "#/components/schemas/room_info"
          readOnly: true
        last_completed_inspection:
          type: object
          readOnly: true
          nullable: true
          required:
            - date
            - id
            - type
          properties:
            date:
              type: string
              description: Inspection completion date, in ISO format
              example: 2022-12-02T14:15:37.925
            type:
              type: string
              description: The inspection template name
            id:
              type: string
              description: Inspection ID
        permissions:
          type: object
          readOnly: true
          required:
            - can_delete
            - can_create_inspection
            - can_add_to_parent_property
            - can_reassign_team
          properties:
            can_delete:
              type: boolean
            can_create_inspection:
              type: boolean
            can_add_to_parent_property:
              type: boolean
            can_reassign_team:
              type: boolean
        sync_data:
          type: object
          readOnly: true
          nullable: true
          required:
            - vendor
            - last_sync
          description: Single sync envelope used for whichever integration owns the
            property; null when the property is not synced.
          properties:
            last_sync:
              type: string
              description: Sync date, in ISO format
              example: 2022-12-02T14:15:37.925
            vendor:
              type: string
              description: The third party vendor for this sync
              enum:
                - rentmanager
                - yardi
                - appfolio
                - jenark
                - rentvine
                - buildium
            status:
              type: string
              description: Latest manual-sync status. Only present on integrations that expose
                it (e.g. Buildium).
              enum:
                - scheduled
                - processing
                - success
                - error
            error:
              type: string
              nullable: true
              description: Error message from the most recent sync attempt, if any.
            id:
              oneOf:
                - type: string
                - type: integer
              description: External vendor id for the property/unit (string for most vendors;
                integer for AppFolio).
            x_id:
              type: string
              description: Vendor-native external id echo for the legacy Rentvine / Rent
                Manager envelope. Kept for debugging/audit parity with the
                historic `sync_data`.
            location_id:
              type: string
              description: Rent Manager location (property) id — legacy `sync_data`,
                debugging/audit.
            integration_id:
              type: string
              description: RentCheck integration record id (Rent Manager legacy sync) —
                debugging/audit.
            short_property_id:
              type: string
              description: AppFolio short property id, when synced from AppFolio.
            short_unit_id:
              type: string
              description: AppFolio short unit id, when synced from AppFolio.
            upload_response:
              type: array
              description: AppFolio per-destination inspection-upload outcomes.
              items:
                type: object
                required:
                  - resource
                  - resource_id
                  - uploaded_id
                properties:
                  resource:
                    type: string
                    enum:
                      - units
                      - properties
                      - occupancies
                  resource_id:
                    type: string
                  uploaded_id:
                    type: string
    community_create_request_model:
      type: object
      required:
        - team_id
        - name
        - address
        - city
        - zip_code
      properties:
        x_id:
          type: string
          description: External property ID that maps to RentCheck properties
        team_id:
          type: string
          description: Id of the Team who created or administers the Entity.
          example: 05fDGZRKp4nVmlmEwjsv
        name:
          type: string
          description: This is the name of the Community.
          example: Whispering Pines
        address:
          type: string
          description: This is the street address for the Community.
          example: 2001 Red Gates
        address_2:
          type: string
          description: Additional address information for the Community.
          example: Apt. 4B
        city:
          type: string
          description: This is the city of the Community.
          example: New Orleans
        region:
          type: string
          description: The broader region or area in which the Community is located.
          example: Orleans
        zip_code:
          type: string
          description: This is the Postal Code for the Community.
          example: "70130"
        additional_team_ids:
          type: array
          description: Community can be accessed by multiple teams. This field lists the
            entity ids of Teams that have access to the Community
          items:
            type: string
            description: Entity ID
            example: 00BRcZPSakXz6w7RYoE
        building_ids:
          type: array
          description: Community can have 1+ Buildings associated with them. building_ids
            is a list of entity ids of the Buildings that belong to a Community
          items:
            type: string
            description: Entity ID
            example: 00BRcZPSakXz6w7RYoE
        state:
          type: string
          description: Two-letter US state code. Optional alias of `region` for callers
            that prefer the postal-style name.
          example: CA
    community_update_request_model:
      type: object
      properties:
        x_id:
          type: string
          description: External property ID that maps to RentCheck properties
        name:
          type: string
          description: This is the name of the Community.
          example: Downtown Community
        address:
          type: string
          description: This is the street address for the Community.
          example: 2001 Red Gates
        address_2:
          type: string
          description: Additional address information for the Community.
          example: Apt. 4B
        city:
          type: string
          description: This is the city of the Community.
          example: New Orleans
        region:
          type: string
          description: The broader region or area in which the Community is located.
          example: Orleans
        zip_code:
          type: string
          description: This is the Postal Code for the Community.
          example: "70130"
        additional_team_ids:
          type: array
          description: Community can be accessed by multiple teams. This field lists the
            entity ids of Teams that have access to the Community
          items:
            type: string
            description: Entity ID
            example: 00BRcZPSakXz6w7RYoE
        building_ids:
          type: array
          description: Community can have 1+ Buildings associated with them. building_ids
            is a list of entity ids of the Buildings that belong to a Community
          items:
            type: string
            description: Entity ID
            example: 00BRcZPSakXz6w7RYoE
        team_id:
          type: string
          description: Id of the Team who created or administers the Entity.
          example: 05fDGZRKp4nVmlmEwjsv
        state:
          type: string
          description: Two-letter US state code. Optional alias of `region` for callers
            that prefer the postal-style name.
          example: CA
    community_response_model:
      type: object
      required:
        - id
        - name
        - address
        - city
        - zip_code
        - property_type
        - team
        - room_names
        - last_completed_inspection
        - permissions
        - created_date
        - updated_date
        - rooms_info
        - rooms
        - creation_source
        - created_by
        - additional_team_ids
        - building_ids
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        x_id:
          type: string
          description: External property ID that maps to RentCheck properties
        team:
          type: object
          required:
            - id
            - name
            - internal_label
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: The name field is the Team name that is displayed on recipient
                facing reports and communication.
              example: The best company
            internal_label:
              type: string
              description: The internal label is a convenience label to help differentiate
                similar teams a user may belong to. It's displayed to you and
                your team members.
              example: The best company label
          nullable: true
          readOnly: true
          description: Team that owns this community. May be null when the underlying team
            has been removed.
        property_type:
          type: string
          description: Discriminator value identifying this property as a Community.
          enum:
            - Community
          example: Community
          readOnly: true
        name:
          type: string
          description: This is the name of the Community.
          example: Whispering Pines
        address:
          type: string
          description: This is the street address for the Community.
          example: 2001 Red Gates
        address_2:
          type: string
          description: Additional address information for the Community.
          example: Apt. 4B
        city:
          type: string
          description: This is the city of the Community.
          example: New Orleans
        region:
          type: string
          description: The broader region or area in which the Community is located.
          example: Orleans
        zip_code:
          type: string
          description: This is the Postal Code for the Community.
          example: "70130"
        additional_team_ids:
          type: array
          description: Community can be accessed by multiple teams. This field lists the
            entity ids of Teams that have access to the Community
          items:
            type: string
            description: Entity ID
            example: 00BRcZPSakXz6w7RYoE
        building_ids:
          type: array
          description: Community can have 1+ Buildings associated with them. building_ids
            is a list of entity ids of the Buildings that belong to a Community
          items:
            type: string
            description: Entity ID
            example: 00BRcZPSakXz6w7RYoE
        creation_source:
          type: string
          description: App that perform the entity creation.
          enum:
            - zapier
            - rentmanager
            - flatfile
            - 365connect
            - appfolio
            - yardi
            - rentvine
            - test_app_id
          example: zapier
          readOnly: true
        created_by:
          type: object
          properties:
            name:
              type: string
              description: Name of the user that created the property
          description: Info about the user that created the property.
          readOnly: true
        created_date:
          type: string
          description: Entity creation date, in ISO format
          example: 2022-12-02T14:15:37.925
          readOnly: true
        updated_date:
          type: string
          description: Entity last update date, in ISO format
          example: 2023-10-04T14:15:37.925
          readOnly: true
        rooms:
          type: array
          items:
            type: string
          deprecated: true
          description: "Deprecated: derived from `rooms_info` on read; use `rooms_info`
            instead. An array of strings representing the room configuration for
            the property. Only rooms that are not bedrooms and bathrooms can be
            set this way."
          example:
            - Living Room
            - Den
            - Balcony
          readOnly: true
        room_names:
          type: array
          deprecated: true
          description: "Deprecated: derived from `rooms_info` on read; use `rooms_info`
            instead. Returned unit room names."
          enum:
            - Front Door
            - Back Door
            - Kitchen
            - Dining Room
            - Living Room
            - Family Room
            - Den
            - Office
            - Bedroom
            - First Bedroom
            - Second Bedroom
            - Third Bedroom
            - Fourth Bedroom
            - Fifth Bedroom
            - Sixth Bedroom
            - Seventh Bedroom
            - Eighth Bedroom
            - Ninth Bedroom
            - Tenth Bedroom
            - Bathroom
            - First Bathroom
            - Second Bathroom
            - Third Bathroom
            - Fourth Bathroom
            - Fifth Bathroom
            - Sixth Bathroom
            - Seventh Bathroom
            - Eighth Bathroom
            - Ninth Bathroom
            - Tenth Bathroom
            - Full Bathroom
            - First Full Bathroom
            - Second Full Bathroom
            - Third Full Bathroom
            - Fourth Full Bathroom
            - Fifth Full Bathroom
            - Sixth Full Bathroom
            - Seventh Full Bathroom
            - Eighth Full Bathroom
            - Ninth Full Bathroom
            - Tenth Full Bathroom
            - Half Bathroom
            - First Half Bathroom
            - Second Half Bathroom
            - Third Half Bathroom
            - Fourth Half Bathroom
            - Fifth Half Bathroom
            - Sixth Half Bathroom
            - Seventh Half Bathroom
            - Eighth Half Bathroom
            - Ninth Half Bathroom
            - Tenth Half Bathroom
            - Laundry Room
            - Stairway
            - Stairs
            - Hallway
            - Basement
            - Loft
            - Attic
            - Storage Room
            - Garage
            - Carport
            - Patio
            - Balcony
            - General
            - Property Exterior & Grounds
            - Multi-Unit Common Areas
          readOnly: true
        rooms_info:
          type: array
          description: Structured per-room metadata and the source of truth for room
            labeling. `rooms` and `room_names` are derived from this. `type` is
            the no-ordinal room kind (e.g. "Bedroom"), `name` is the ordinal
            name (e.g. "First Bedroom"), and `label` is a human-meaningful label
            that defaults to `name`.
          items:
            $ref: "#/components/schemas/room_info"
          readOnly: true
        last_completed_inspection:
          type: object
          readOnly: true
          nullable: true
          required:
            - date
            - id
            - type
          properties:
            date:
              type: string
              description: Inspection completion date, in ISO format
              example: 2022-12-02T14:15:37.925
            type:
              type: string
              description: The inspection template name
            id:
              type: string
              description: Inspection ID
        permissions:
          type: object
          readOnly: true
          required:
            - can_delete
            - can_create_inspection
            - can_add_to_parent_property
            - can_reassign_team
          properties:
            can_delete:
              type: boolean
            can_create_inspection:
              type: boolean
            can_add_to_parent_property:
              type: boolean
            can_reassign_team:
              type: boolean
        sync_data:
          type: object
          readOnly: true
          nullable: true
          required:
            - vendor
            - last_sync
          description: Single sync envelope used for whichever integration owns the
            property; null when the property is not synced.
          properties:
            last_sync:
              type: string
              description: Sync date, in ISO format
              example: 2022-12-02T14:15:37.925
            vendor:
              type: string
              description: The third party vendor for this sync
              enum:
                - rentmanager
                - yardi
                - appfolio
                - jenark
                - rentvine
                - buildium
            status:
              type: string
              description: Latest manual-sync status. Only present on integrations that expose
                it (e.g. Buildium).
              enum:
                - scheduled
                - processing
                - success
                - error
            error:
              type: string
              nullable: true
              description: Error message from the most recent sync attempt, if any.
            id:
              oneOf:
                - type: string
                - type: integer
              description: External vendor id for the property/unit (string for most vendors;
                integer for AppFolio).
            x_id:
              type: string
              description: Vendor-native external id echo for the legacy Rentvine / Rent
                Manager envelope. Kept for debugging/audit parity with the
                historic `sync_data`.
            location_id:
              type: string
              description: Rent Manager location (property) id — legacy `sync_data`,
                debugging/audit.
            integration_id:
              type: string
              description: RentCheck integration record id (Rent Manager legacy sync) —
                debugging/audit.
            short_property_id:
              type: string
              description: AppFolio short property id, when synced from AppFolio.
            short_unit_id:
              type: string
              description: AppFolio short unit id, when synced from AppFolio.
            upload_response:
              type: array
              description: AppFolio per-destination inspection-upload outcomes.
              items:
                type: object
                required:
                  - resource
                  - resource_id
                  - uploaded_id
                properties:
                  resource:
                    type: string
                    enum:
                      - units
                      - properties
                      - occupancies
                  resource_id:
                    type: string
                  uploaded_id:
                    type: string
    resident_create_request_model:
      type: object
      required:
        - current_property_id
        - first_name
        - last_name
        - emails
      properties:
        current_property_id:
          type: string
          description: The entity id of the latest Unit occupied by the Resident.
          example: 00BRcZPSakXz6w7RYoE
        first_name:
          type: string
          description: The Resident's first name.
          example: Resident Name
        last_name:
          type: string
          description: The Resident's last name.
          example: Resident Lastname
        emails:
          type: array
          description: Resident's emails
          items:
            type: string
          example:
            - resident@email.com
        phone_numbers:
          type: array
          items:
            type: string
          description: Resident's phone numbers
          example:
            - "+17035556390"
        current_lease_start:
          type: string
          description: The date the Resident's current lease starts.
          example: 2021-01-15T00:00:00.000
        current_lease_end:
          type: string
          description: The date the Resident's current lease should end.
          example: 2022-01-15T00:00:00.000
        current_rent:
          type: number
          description: The dollar amount of the Resident's current rent. Must be ≥ 0.
          example: 100
        current_security_deposit:
          type: number
          description: The dollar amount of the Resident's security deposit. Must be ≥ 0.
          example: 150
        move_out_date:
          type: string
          description: The date that the Resident is scheduled to move out of the current
            Property.
          example: 2022-01-15T00:00:00.000
        previous_properties:
          type: array
          description: Always emitted as an empty array on v1 resident responses. To
            enumerate the past leases for a resident, list them via `GET
            /v2/residents/{residentId}/leases`. Retained here for backward
            compatibility.
          items:
            type: string
            description: Entity ID
            example: 00BRcZPSakXz6w7RYoE
    resident_update_request_model:
      type: object
      properties:
        first_name:
          type: string
          description: The Resident's first name.
          example: Resident Name
        last_name:
          type: string
          description: The Resident's last name.
          example: Resident Lastname
        emails:
          type: array
          description: Resident's emails
          items:
            type: string
          example:
            - resident@email.com
        phone_numbers:
          type: array
          items:
            type: string
          description: Resident's phone numbers
          example:
            - "+17035556390"
    resident_response_model:
      type: object
      required:
        - id
        - organizationID
        - emails
        - previous_properties
        - created_date
        - updated_date
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        user_id:
          type: string
          description: The id of the linked user account for this resident, when one
            exists. Populated once the resident accepts an invite and signs in.
          example: 00BRcZPSakXz6w7RYoE
        organizationID:
          type: string
          description: The id of the organization that owns the resident record.
          example: 00BRcZPSakXz6w7RYoE
        owner_id:
          type: string
          description: User ID of the resident entity
          example: 00BRcZPSakXz6w7RYoE
        first_name:
          type: string
          description: The Resident's first name.
          example: Resident Name
        last_name:
          type: string
          description: The Resident's last name.
          example: Resident Lastname
        emails:
          type: array
          description: Resident's emails
          items:
            type: string
          example:
            - resident@email.com
        phone_numbers:
          type: array
          items:
            type: string
          description: Resident's phone numbers
          example:
            - "+17035556390"
        current_property_id:
          type: string
          description: The entity id of the latest Unit occupied by the Resident.
          example: 00BRcZPSakXz6w7RYoE
        current_property_address:
          type: string
          description: This field is the street address from the property listed in the
            inspection
          example: 1234 Street name
        current_lease_start:
          type: string
          description: The date the Resident's current lease starts.
          example: 2021-01-15T00:00:00.000
        current_lease_end:
          type: string
          description: The date the Resident's current lease should end.
          example: 2022-01-15T00:00:00.000
        current_rent:
          type: number
          description: The dollar amount of the Resident's current rent. Must be ≥ 0.
          example: 100
        current_security_deposit:
          type: number
          description: The dollar amount of the Resident's security deposit. Must be ≥ 0.
          example: 150
        move_out_date:
          type: string
          description: The date that the Resident is scheduled to move out of the current
            Property.
          example: 2022-01-15T00:00:00.000
        previous_properties:
          type: array
          description: Always emitted as an empty array on v1 resident responses. To
            enumerate the past leases for a resident, list them via `GET
            /v2/residents/{residentId}/leases`. Retained here for backward
            compatibility.
          items:
            type: string
            description: Entity ID
            example: 00BRcZPSakXz6w7RYoE
        creation_source:
          type: string
          description: App that perform the entity creation.
          enum:
            - zapier
            - rentmanager
            - flatfile
            - 365connect
            - appfolio
            - yardi
            - rentvine
            - test_app_id
          example: zapier
        created_date:
          type: string
          description: Entity creation date, in ISO format
          example: 2022-12-02T14:15:37.925
        updated_date:
          type: string
          description: Entity last update date, in ISO format
          example: 2023-10-04T14:15:37.925
    feature_rotate_photo_request_model:
      type: object
      required:
        - photo_url
      properties:
        photo_url:
          type: string
          description: URL for the image that needs to be rotated
        angle:
          type: number
    filter_segment_create_request_model:
      type: object
      required:
        - name
        - color
        - icon
        - filters
      description: Request body for creating a filter segment.
      properties:
        name:
          type: string
          description: Segment name. Must be unique per owner (trimmed, case-insensitive).
        color:
          type: string
          enum:
            - grey
            - blue
            - light_blue
            - orange
            - green
            - red
        icon:
          type: string
          enum:
            - paint-roller
            - traffic-cone
            - inbox
            - file-signature
            - exclamation-triangle
            - user-circle
            - archive
            - hammer
            - truck-container
            - user-helmet-safety
            - house
            - house-tree
            - house-chimney-crack
            - house-flood-water
            - snowflake
            - sun
            - umbrella-simple
            - cloud-rainbow
            - rainbow
            - dog
            - raccoon
            - city
            - roller-coaster
            - house-flag
            - tree-city
            - star-shooting
            - clock
            - repeat
            - pen
            - tag
            - shower
            - bed
            - shield-check
            - circle-check
            - circle-xmark
            - files
          description: FontAwesome icon name from the allowlist.
          example: house
        filters:
          type: object
          description: The inspections-list filter combo. At least one filter must be
            provided.
          minProperties: 1
          properties:
            search:
              type: string
            archived:
              type: boolean
            has_flags:
              type: boolean
            due_date:
              type: object
              properties:
                start_date:
                  type: string
                end_date:
                  type: string
            due_date_legacy:
              type: object
              properties:
                start_date:
                  type: string
                end_date:
                  type: string
            completed_date:
              type: object
              properties:
                start_date:
                  type: string
                end_date:
                  type: string
            assigned_to:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
            team:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
                    - none_of
            inspection_template:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
                    - none_of
            inspection_status:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
                    - none_of
            rating:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
            property:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
                    - none_of
            review_step:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
            created_by:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
                    - none_of
    filter_segment_update_request_model:
      type: object
      required:
        - name
        - color
        - icon
        - filters
      description: Request body for updating a filter segment. Full replace — same
        shape and rules as create.
      properties:
        name:
          type: string
          description: Segment name. Must be unique per owner (trimmed, case-insensitive);
            keeping the segment’s current name is allowed.
        color:
          type: string
          enum:
            - grey
            - blue
            - light_blue
            - orange
            - green
            - red
        icon:
          type: string
          enum:
            - paint-roller
            - traffic-cone
            - inbox
            - file-signature
            - exclamation-triangle
            - user-circle
            - archive
            - hammer
            - truck-container
            - user-helmet-safety
            - house
            - house-tree
            - house-chimney-crack
            - house-flood-water
            - snowflake
            - sun
            - umbrella-simple
            - cloud-rainbow
            - rainbow
            - dog
            - raccoon
            - city
            - roller-coaster
            - house-flag
            - tree-city
            - star-shooting
            - clock
            - repeat
            - pen
            - tag
            - shower
            - bed
            - shield-check
            - circle-check
            - circle-xmark
            - files
          description: FontAwesome icon name from the allowlist.
          example: house
        filters:
          type: object
          description: The inspections-list filter combo. At least one filter must be
            provided.
          minProperties: 1
          properties:
            search:
              type: string
            archived:
              type: boolean
            has_flags:
              type: boolean
            due_date:
              type: object
              properties:
                start_date:
                  type: string
                end_date:
                  type: string
            due_date_legacy:
              type: object
              properties:
                start_date:
                  type: string
                end_date:
                  type: string
            completed_date:
              type: object
              properties:
                start_date:
                  type: string
                end_date:
                  type: string
            assigned_to:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
            team:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
                    - none_of
            inspection_template:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
                    - none_of
            inspection_status:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
                    - none_of
            rating:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
            property:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
                    - none_of
            review_step:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
            created_by:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
                    - none_of
    filter_segment_response_model:
      type: object
      required:
        - id
        - name
        - color
        - icon
        - filters
        - created_by
        - updated_by
        - owned_by
        - created_at
        - updated_at
      description: A filter segment.
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        name:
          type: string
        color:
          type: string
          enum:
            - grey
            - blue
            - light_blue
            - orange
            - green
            - red
        icon:
          type: string
          enum:
            - paint-roller
            - traffic-cone
            - inbox
            - file-signature
            - exclamation-triangle
            - user-circle
            - archive
            - hammer
            - truck-container
            - user-helmet-safety
            - house
            - house-tree
            - house-chimney-crack
            - house-flood-water
            - snowflake
            - sun
            - umbrella-simple
            - cloud-rainbow
            - rainbow
            - dog
            - raccoon
            - city
            - roller-coaster
            - house-flag
            - tree-city
            - star-shooting
            - clock
            - repeat
            - pen
            - tag
            - shower
            - bed
            - shield-check
            - circle-check
            - circle-xmark
            - files
          description: FontAwesome icon name.
        filters:
          type: object
          description: The inspections-list filter combo. At least one filter must be
            provided.
          minProperties: 1
          properties:
            search:
              type: string
            archived:
              type: boolean
            has_flags:
              type: boolean
            due_date:
              type: object
              properties:
                start_date:
                  type: string
                end_date:
                  type: string
            due_date_legacy:
              type: object
              properties:
                start_date:
                  type: string
                end_date:
                  type: string
            completed_date:
              type: object
              properties:
                start_date:
                  type: string
                end_date:
                  type: string
            assigned_to:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
            team:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
                    - none_of
            inspection_template:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
                    - none_of
            inspection_status:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
                    - none_of
            rating:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
            property:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
                    - none_of
            review_step:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
            created_by:
              type: object
              required:
                - value
                - condition
              properties:
                value:
                  type: array
                  items:
                    type: string
                  description: Filter values. Duplicate values are rejected.
                condition:
                  type: string
                  enum:
                    - any_of
                    - none_of
        created_by:
          type: object
          required:
            - id
            - name
            - email
            - type
          description: Basic user identity fields.
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: Name of the user
            email:
              type: string
              description: Email of the user
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
              description: User type discriminator.
        updated_by:
          type: object
          required:
            - id
            - name
            - email
            - type
          description: Basic user identity fields.
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: Name of the user
            email:
              type: string
              description: Email of the user
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
              description: User type discriminator.
        owned_by:
          type: object
          required:
            - id
            - name
            - email
            - type
          description: Basic user identity fields.
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: Name of the user
            email:
              type: string
              description: Email of the user
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
              description: User type discriminator.
        created_at:
          type: string
          format: date-time
          description: ISO timestamp.
        updated_at:
          type: string
          format: date-time
          description: ISO timestamp.
    inspection_v2_create_request_model:
      type: object
      description: Provide either `inspection_template_id` or `inspection_type` — when
        `inspection_type` is supplied the matching template is resolved
        automatically, otherwise `inspection_template_id` is required. Provide
        either `property_ids` or `property_x_ids` — when `property_x_ids` is
        supplied it replaces `property_ids`, and when `x_id` is supplied at most
        one property id is allowed.
      required:
        - due_date
        - recipients
      properties:
        inspection_template_id:
          type: string
          description: The inspection_template_id designates which inspection will be
            created, based on the inspection template. Some inspection templates
            require the user to select which features they would like to include
            in the inspection. These features are designated in the
            `selected_features` field
          example: rc_move_in
        inspection_type:
          type: string
          description: Inspection Type designates which type of inspection will be issued.
            Some inspection types require the user to select which features
            apply to the inspection. These features are designated in the
            'selected_features' field.
          enum:
            - Move-in
            - Move-out
            - Annual
            - Turn
            - Periodic
            - Maintenance Check
            - Exterior
            - Pre-Acquisition
            - RV Lot
            - Maintenance / Safety Check
            - Community Maintenance Check
        x_id:
          type: string
          description: External inspection ID that maps to RentCheck inspections
        label:
          type: string
          description: Inspection label
          example: Inspection Label
        due_date:
          type: string
          description: The inspection due date is the date that the recipient must
            complete the inspection by. If recurrence is other that "None", this
            will be used as the starting date.
          example: 2021-12-02T14:15:37.925
        invite_date:
          type: string
          description: The inspection invite date is the date at which the recipient will
            receive the email invites for the inspection.
          example: 2021-12-02T14:15:37.925
        user_timezone:
          type: string
          description: IANA timezone identifier used to interpret `due_date` /
            `invite_date` / recurrence dates on behalf of the caller.
          example: America/New_York
        fast_track:
          type: boolean
          description: When `true`, the created inspection skips the standard review step
            and is moved straight to a completable state.
        property_ids:
          type: array
          description: List of properties id to create inspections
          items:
            type: string
            description: Selected property's id for the new inspection
          example:
            - property 1 id
            - property 2 id
        property_x_ids:
          type: array
          description: List of properties x_id to create inspections, when provided,
            replaces property_ids
          items:
            type: string
            description: Selected property's x_id for the new inspection
          example:
            - property 1 id
            - property 2 id
        recipients:
          type: object
          required:
            - type
          description: Used to notify about the created inspections.
          properties:
            type:
              type: string
              enum:
                - self-perform
                - emails
                - teammates
                - myself
                - residents
            emails:
              type: array
              description: Only mandatory if type is emails
              items:
                type: string
                description: Recipients' email
              example:
                - email1@domain.com
                - email2@domain.com
            teammate_emails:
              type: object
              description: Only mandatory if type is `teammates`. Keys are the target property
                ids; values are the arrays of teammate emails to assign for that
                property. Any property-id key is accepted, and each value must
                contain at least one email address.
              additionalProperties:
                type: array
                description: Teammate emails assigned to this property id.
                minItems: 1
                items:
                  type: string
                  format: email
              example:
                property123:
                  - email1@domain.com
                  - email2@domain.com
                property456:
                  - email3@domain.com
            resident_type:
              type: string
              enum:
                - current
                - future
        recurrence:
          type: object
          required:
            - type
          description: Used to create several inspection with a recurrence strategy.
            Inspection due_date will be used as the starting point. Daily
            recurrence can not exceed 30 days between due_date and end_date. The
            rest can not exceed 2 years. When `x_id` is supplied, `type` is
            forced to `None`.
          properties:
            type:
              type: string
              description: This value specify the recurrence for an inspection on each
                provided property. "end_date" is mandatory for other that None.
                When the top-level `x_id` is set only `None` is accepted.
              enum:
                - None
                - Daily
                - Weekly
                - Monthly
                - Quarterly
                - Semi-Annually
                - Annually
            end_date:
              type: string
              description: The inspection recurrence end date. If recurrence is not "None",
                this value will be used as a limit.
              example: 2024-12-02T14:15:37.925
        selected_features:
          type: array
          description: This field is an array of the areas and features selected for the
            inspection. Each Area contains a name and a list of the names of the
            applicable Features. This field is required depending on the
            inspection type.
          items:
            type: object
            properties:
              area:
                type: string
                description: Inspection's selected property area name to check
                example: Laundry Room
              features:
                type: array
                description: A specific feature inside the selected area
                items:
                  type: string
                example:
                  - Laundry Room Walls
                  - Laundry Room Floor, Ceiling, Walls
            example:
              - area: Laundry Room
                features:
                  - Laundry Room Walls
                  - Laundry Room Floor, Ceiling, Walls
              - area: Kitchen
                features:
                  - Dishwasher Exterior
                  - Freezer Interior
        notify_residents:
          type: object
          description: Optional resident-notification payload. Only used when
            `recipients.type` is `teammates` — in that flow the current
            residents are emailed to let them know a teammate has been assigned
            to inspect their unit. Ignored for other recipient types.
          properties:
            note:
              type: string
              description: Free-form note added to the resident notification email.
        check_automation_conflict_validation:
          type: boolean
          description: When `true`, the create request first checks for a conflicting
            automation. If a conflict is found, no inspections are created and
            the response includes a `conflict` payload the caller is expected to
            resolve before retrying. When `false` (or omitted) the conflict
            check is skipped.
    inspection_update_request_model:
      type: object
      description: All fields are optional; only the fields supplied are applied. Some
        fields are only writable in specific inspection statuses (e.g.
        `due_date`/`invite_date`/`recipients` are only honored before
        completion).
      properties:
        archived:
          type: boolean
          description: Whether or not this inspection should be archived.
        inspection_label:
          type: string
          description: Inspection label to be displayed.
        x_id:
          type: string
          description: External inspection ID that maps to RentCheck inspections
        due_date:
          type: string
          description: The inspection due date (legacy) is the original date by which the
            recipient must complete the inspection.
          example: 2021-12-02T14:15:37.925
        invite_date:
          type: string
          format: date-time
          description: Invite date for the inspection. Only honored while the inspection
            is in `Scheduled` status.
        recipients:
          type: object
          description: Reassigns who can access the inspection. Only honored before the
            inspection is completed. `type` is optional on update; when omitted
            the existing assignment is left unchanged.
          properties:
            type:
              type: string
              enum:
                - self-perform
                - emails
                - teammates
                - myself
                - residents
            emails:
              type: array
              description: Only mandatory if type is emails
              items:
                type: string
                description: Recipients' email
              example:
                - email1@domain.com
                - email2@domain.com
            teammate_emails:
              type: object
              description: Only mandatory if type is `teammates`. Keys are the target property
                ids; values are the arrays of teammate emails to assign for that
                property. Any property-id key is accepted, and each value must
                contain at least one email address.
              additionalProperties:
                type: array
                description: Teammate emails assigned to this property id.
                minItems: 1
                items:
                  type: string
                  format: email
              example:
                property123:
                  - email1@domain.com
                  - email2@domain.com
                property456:
                  - email3@domain.com
            resident_type:
              type: string
              enum:
                - current
                - future
              default: current
        notify_residents:
          type: object
          description: Optional resident-notification payload. Only used when the update
            sets `recipients.type` to `teammates` — the current residents are
            then emailed to let them know a teammate has been assigned to
            inspect their unit. Ignored for other recipient types.
          properties:
            note:
              type: string
              description: Free-form note added to the resident notification email.
        status:
          type: string
          description: "Manual status transition. The accepted values depend on the
            inspection role: `Continue Inspection` undoes a submission,
            `Inspection Complete` finalizes the inspection, and `Review
            Inspection` triggers an initial review."
          enum:
            - Continue Inspection
            - Inspection Complete
            - Review Inspection
        room_names:
          type: array
          description: Replacement list of room names. Only honored while the inspection
            has not been submitted.
          items:
            type: string
        is_initial_review_complete:
          type: boolean
          description: Mark/unmark the initial review as complete. Reviewers only —
            invalid transitions return `unable to complete initial review` /
            `unable to undo initial review`.
    inspection_update_bulk_request_model:
      type: object
      required:
        - due_date
      properties:
        due_date:
          type: string
          description: The inspection due date (legacy) is the original date by which the
            recipient must complete the inspection.
          example: 2021-12-02T14:15:37.925
        invite_date:
          type: string
          format: date-time
          description: New invite date to apply to every inspection in `id[]`.
    inspection_v1_response_model:
      type: object
      description: Optional properties (`x_id`, `invite_date`, `inspection_label`,
        `fast_track`, `team_id`, `team`, `approval`, `rejection`, `signer`,
        `signature`, `completed_by`, `current_performer`, `edits`,
        `pdf_reports`, `pdf_report_location`, `pdf_images_location`,
        `health_score`, `review`, `sections_progress`) are omitted rather than
        nulled when not applicable.
      required:
        - id
        - archived
        - due_date
        - effective_due_date
        - inspection_template
        - inspection_status
        - role
        - assigned_recipients
        - property
        - created_by
        - created_date
        - invites
        - completed_date
        - selected_features
        - progress_pct
        - rooms
        - room_names
        - is_initial_review_complete
        - inspection_summary
        - features_ratings
        - settings
        - metadata
        - permissions
        - signature_acknowledgement
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        x_id:
          type: string
          description: External inspection ID that maps to RentCheck inspections
        team:
          type: object
          required:
            - id
            - name
            - internal_label
            - display_name
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: The name field is the Team name that is displayed on recipient
                facing reports and communication.
              example: The best company
            internal_label:
              type: string
              description: The internal label is a convenience label to help differentiate
                similar teams a user may belong to. It's displayed to you and
                your team members.
              example: The best company label
            display_name:
              type: string
              description: Public-facing team label resolved for the requesting user.
          nullable: true
          description: Team the inspection belongs to. `null` when the inspection is not
            scoped to a team. `display_name` resolves to `internal_label` for
            team members and to the public-facing `name` otherwise.
        due_date:
          type: string
          description: The inspection due date (legacy) is the original date by which the
            recipient must complete the inspection.
          example: 2021-12-02T14:15:37.925
        effective_due_date:
          type: string
          description: The inspection’s effective due date is whichever due date applies —
            original, revision, or review — whether it’s already passed or still
            upcoming.
          example: 2021-12-02T14:15:37.925
        inspection_template:
          $ref: "#/components/schemas/inspection_template_v1_get_by_id_response_model"
        inspection_status:
          type: string
          description: Inspection status represents the actual status of the current
            inspection.
          enum:
            - Scheduled
            - Not Started
            - Started
            - In Review
            - Completed
            - Awaiting Review
            - Revision Requested
            - Revision Review
            - Approved
        role:
          type: string
          description: Role is a calculated field that designates the level of access to
            the inspection for the requesting user.
          enum:
            - creator
            - self-perform-read
            - self-perform-write
            - invitee-write
            - invitee-readonly
            - viewer
        assigned_recipients:
          type: object
          required:
            - type
          description: Recipients assigned to perform / receive this inspection.
          properties:
            type:
              type: string
              description: The type that defines who has access to the inspection
              enum:
                - residents
                - self-perform
                - emails
                - teammates
                - myself
              example: emails
            emails:
              type: array
              description: Specific emails assigned to this inspection when type is emails
              items:
                type: string
            resident_type:
              type: string
              description: Type of residents assigned to this inspection when type is
                residents
              enum:
                - current
                - future
              example: current
        property:
          type: object
          required:
            - id
            - address
            - city
            - zip_code
            - type
          description: Property snapshot embedded in the inspection. Carries the `type`
            discriminator and optionally `x_id` — distinct from the leaner
            basic-property shape used elsewhere.
          properties:
            id:
              type: string
              description: Property id
              example: 00BRcZPSakXz6w7RYoE
            x_id:
              type: string
              description: External property ID from a synced integration, when set.
            address:
              type: string
              description: Property street address
              example: 2001 Red Gates
            address_2:
              type: string
              description: Property additional address info
              example: Apt. 4B
            city:
              type: string
              description: Property city
              example: New Orleans
            region:
              type: string
              description: Property region
              example: Orleans
            zip_code:
              type: string
              description: Property zip code
              example: "70130"
            type:
              type: string
              description: Property's type discriminator.
              enum:
                - Single-Family
                - Multi-Family
                - Apartment or Condo
                - Commercial
                - Building
                - Community
                - ""
        rooms:
          type: array
          items:
            type: string
          deprecated: true
          description: "Deprecated: derived from `rooms_info` on read; use `rooms_info`
            instead. An array of strings representing the room configuration for
            the property. Only rooms that are not bedrooms and bathrooms can be
            set this way."
          example:
            - Living Room
            - Den
            - Balcony
        room_names:
          type: array
          deprecated: true
          description: "Deprecated: derived from `rooms_info` on read; use `rooms_info`
            instead. Returned unit room names."
          enum:
            - Front Door
            - Back Door
            - Kitchen
            - Dining Room
            - Living Room
            - Family Room
            - Den
            - Office
            - Bedroom
            - First Bedroom
            - Second Bedroom
            - Third Bedroom
            - Fourth Bedroom
            - Fifth Bedroom
            - Sixth Bedroom
            - Seventh Bedroom
            - Eighth Bedroom
            - Ninth Bedroom
            - Tenth Bedroom
            - Bathroom
            - First Bathroom
            - Second Bathroom
            - Third Bathroom
            - Fourth Bathroom
            - Fifth Bathroom
            - Sixth Bathroom
            - Seventh Bathroom
            - Eighth Bathroom
            - Ninth Bathroom
            - Tenth Bathroom
            - Full Bathroom
            - First Full Bathroom
            - Second Full Bathroom
            - Third Full Bathroom
            - Fourth Full Bathroom
            - Fifth Full Bathroom
            - Sixth Full Bathroom
            - Seventh Full Bathroom
            - Eighth Full Bathroom
            - Ninth Full Bathroom
            - Tenth Full Bathroom
            - Half Bathroom
            - First Half Bathroom
            - Second Half Bathroom
            - Third Half Bathroom
            - Fourth Half Bathroom
            - Fifth Half Bathroom
            - Sixth Half Bathroom
            - Seventh Half Bathroom
            - Eighth Half Bathroom
            - Ninth Half Bathroom
            - Tenth Half Bathroom
            - Laundry Room
            - Stairway
            - Stairs
            - Hallway
            - Basement
            - Loft
            - Attic
            - Storage Room
            - Garage
            - Carport
            - Patio
            - Balcony
            - General
            - Property Exterior & Grounds
            - Multi-Unit Common Areas
        archived:
          type: boolean
          description: Whether the inspection is archived.
        inspection_label:
          type: string
          description: Optional user-visible label for the inspection.
        fast_track:
          type: boolean
          description: Whether the inspection was created with the fast-track flow (skips
            standard review).
        invite_date:
          type: string
          format: date-time
          description: Invite date used to schedule the recipient notification.
        created_date:
          type: string
          format: date-time
          description: ISO timestamp of when the inspection was created.
        created_by:
          type: object
          required:
            - id
            - name
            - email
            - type
          description: User that created the inspection.
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: Name of the user
            email:
              type: string
              description: Email of the user
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
              description: User type discriminator.
        invites:
          type: array
          description: Pending invite records that were materialized for the inspection
            (id, email and reference_code per invitee).
          items:
            type: object
            required:
              - id
              - email
              - reference_code
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              email:
                type: string
                format: email
                description: Invited email.
              reference_code:
                type: string
                description: Reference code that can be used by the invitee to claim the
                  inspection (e.g. via the resident invite-accept flow).
        selected_features:
          type: array
          description: This field is an array of the areas and features selected for the
            inspection. Each Area contains a name and a list of the names of the
            applicable Features. This field is required depending on the
            inspection type.
          items:
            type: object
            properties:
              area:
                type: string
                description: Inspection's selected property area name to check
                example: Laundry Room
              features:
                type: array
                description: A specific feature inside the selected area
                items:
                  type: string
                example:
                  - Laundry Room Walls
                  - Laundry Room Floor, Ceiling, Walls
            example:
              - area: Laundry Room
                features:
                  - Laundry Room Walls
                  - Laundry Room Floor, Ceiling, Walls
              - area: Kitchen
                features:
                  - Dishwasher Exterior
                  - Freezer Interior
        team_id:
          type: string
          description: ID of the team the inspection belongs to.
          example: 00BRcZPSakXz6w7RYoE
        is_initial_review_complete:
          type: boolean
          description: Whether the initial review step has been marked complete.
        signer:
          type: object
          required:
            - id
            - name
            - email
            - type
          description: User recorded as the signer on the inspection signature, if any.
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: Name of the user
            email:
              type: string
              description: Email of the user
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
              description: User type discriminator.
        completed_by:
          type: object
          required:
            - id
            - name
            - email
            - type
          description: User that submitted / completed the inspection.
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: Name of the user
            email:
              type: string
              description: Email of the user
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
              description: User type discriminator.
        current_performer:
          type: object
          required:
            - id
            - name
            - email
            - type
          description: User currently assigned to perform the inspection.
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: Name of the user
            email:
              type: string
              description: Email of the user
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
              description: User type discriminator.
        edits:
          type: array
          description: Audit trail of edits applied to the inspection.
          items:
            type: object
            additionalProperties: true
        inspection_summary:
          type: string
          description: Free-form summary written during review.
        pdf_reports:
          type: object
          description: Historical PDF reports generated for the inspection, keyed by
            report id.
          additionalProperties:
            type: object
            required:
              - location
              - name
              - created_at
            properties:
              location:
                type: string
                description: Location of the generated PDF report.
              name:
                type: string
                description: Display name of the report.
              created_at:
                type: string
                format: date-time
                description: ISO timestamp of when the PDF was generated.
        pdf_images_location:
          type: string
          description: Link to the most recent generated zip of inspection images, if any.
        features_ratings:
          type: object
          required:
            - total_quantity
            - good_quantity
            - fair_quantity
            - poor_quantity
          description: Aggregated feature rating counts.
          properties:
            total_quantity:
              type: number
            good_quantity:
              type: number
            fair_quantity:
              type: number
            poor_quantity:
              type: number
        review:
          type: object
          description: When a review has been requested, captures the original recipients
            and the review-step due date.
          required:
            - original_assigned_recipients
            - due_date
          properties:
            original_assigned_recipients:
              type: object
              required:
                - type
              description: Recipients assigned to perform / receive this inspection.
              properties:
                type:
                  type: string
                  description: The type that defines who has access to the inspection
                  enum:
                    - residents
                    - self-perform
                    - emails
                    - teammates
                    - myself
                  example: emails
                emails:
                  type: array
                  description: Specific emails assigned to this inspection when type is emails
                  items:
                    type: string
                resident_type:
                  type: string
                  description: Type of residents assigned to this inspection when type is
                    residents
                  enum:
                    - current
                    - future
                  example: current
            due_date:
              type: string
              description: The inspection due date (legacy) is the original date by which the
                recipient must complete the inspection.
              example: 2021-12-02T14:15:37.925
        completed_date:
          type: string
          description: Completed Date is the date that the user submitted the inspection
            for approval or completed the inspection if it was self-performed.
          example: 2021-12-02T14:15:37.925
        signature:
          type: object
          required:
            - signed_by
          description: Inspection signature details.
          properties:
            signed_by:
              type: object
              required:
                - name
                - resident_id
              properties:
                name:
                  type: string
                  description: Name of the user that signed the inspection.
                resident_id:
                  type: string
                  description: ID of the resident that signed the inspection.
            signature_url:
              type: string
              description: Link to the signature image.
            signed_at:
              type: string
              description: Signature date, ISO format
              example: 2022-01-15T00:00:00.000
        approval:
          type: object
          nullable: true
          required:
            - approved_by_name
            - note
            - notification_sent_to
            - signature_text
            - approved_date
          description: Inspection approval details. Emitted as `null` when the inspection
            has not been approved rather than being omitted.
          properties:
            approved_by_name:
              type: string
              description: User name which approved the inspection
              example: Lucy Warburton
            approved_date:
              type: string
              description: ISO Date when the inspection was approved
              example: 2022-01-15T00:00:00.000
            note:
              type: string
              description: Note to add as feedback for RentCheck
              example: RentCheck inspection was easy and quick!
            notification_sent_to:
              type: string
              description: E-Mail sent to on inspection's approval
              example: lucywarburton@gmail.co
            rating:
              type: number
              description: Overall Inspection report rating (1-5)
              example: 5
            signature_text:
              type: string
              description: Inspection's approval signature text
              example: Lucy Warburton
        rejection:
          type: object
          nullable: true
          required:
            - rejected_by_name
            - rejected_date
            - note
            - notification_sent_to
            - due_date
          description: Inspection rejection details. Emitted as `null` when the inspection
            has not been rejected rather than being omitted.
          properties:
            rejected_by_name:
              type: string
              description: User name that rejected the inspection
              example: Lucy Warburton
            rejected_date:
              type: string
              description: ISO Date when the inspection was rejected
              example: 2022-01-15T00:00:00.000
            note:
              type: string
              description: Note about the inspection's rejection
              example: Test
            notification_sent_to:
              type: string
              description: E-Mail sent to on inspection's rejection
              example: lucywarburton@gmail.co
            due_date:
              type: string
              description: ISO Date of the inspections due date
              example: 2022-01-15T00:00:00.000
        health_score:
          type: object
          required:
            - score
            - details
          description: Score card for the current inspection.
          properties:
            score:
              type: number
              description: Overall health score
            details:
              type: object
              required:
                - completeness
                - condition_rating
                - extensive_notes
                - maintenance_flag
                - question_rating
              description: Breakdown of the different scoring groups.
              properties:
                completeness:
                  type: object
                  required:
                    - enough_data
                  description: Score group covering whether the inspection captured enough data.
                  properties:
                    enough_data:
                      type: number
                condition_rating:
                  type: object
                  required:
                    - fair_rating
                    - poor_rating
                  description: Score group covering the property-condition ratings.
                  properties:
                    fair_rating:
                      type: number
                    poor_rating:
                      type: number
                extensive_notes:
                  type: object
                  required:
                    - rating
                  description: Score group covering the presence of extensive performer notes.
                  properties:
                    rating:
                      type: number
                maintenance_flag:
                  type: object
                  required:
                    - rating
                  description: Score group covering maintenance flag coverage.
                  properties:
                    rating:
                      type: number
                question_rating:
                  type: object
                  required:
                    - rating
                  description: Score group covering question/answer ratings.
                  properties:
                    rating:
                      type: number
            areas_of_concern:
              type: array
              description: Score groups currently flagged as areas of concern, when the
                overall score is low.
              items:
                type: string
                enum:
                  - Completeness
                  - Property Condition
        progress_pct:
          type: number
          description: Percentage of how much the inspection is already completed
        pdf_report_location:
          type: string
          description: Link to the inspections completed PDF report if previously generated.
        settings:
          type: object
          description: Inspection runtime settings snapshot.
          required:
            - is_maintenance_flag_default_photos_enabled
            - is_resident_reported_maintenance_enabled
            - is_two_step_approval_required
            - can_record_video
            - can_take_360_photos
            - custom_maintenance_fields
          properties:
            is_maintenance_flag_default_photos_enabled:
              type: boolean
            is_resident_reported_maintenance_enabled:
              type: boolean
            is_two_step_approval_required:
              type: boolean
            can_record_video:
              type: boolean
            can_take_360_photos:
              type: boolean
            custom_maintenance_fields:
              type: array
              description: Custom maintenance fields enabled for this inspection.
              items:
                type: object
                required:
                  - id
                  - title
                  - selection_type
                  - options
                  - enabled
                properties:
                  id:
                    type: string
                    description: Unique field id chosen by the user.
                  title:
                    type: string
                    description: Field title displayed in the flag modal, maintenance reports and
                      CSV exports.
                  description:
                    type: string
                    description: Optional field description shown as a caption below the title.
                  selection_type:
                    type: string
                    enum:
                      - single
                      - multiple
                    description: Whether the user can pick a single option or multiple.
                  options:
                    type: array
                    items:
                      type: string
                    description: Selectable options for the field.
                  required:
                    type: boolean
                    description: Whether a value for this field is required when raising a flag.
                  enabled:
                    type: boolean
                    description: Whether the field is currently active.
        metadata:
          type: object
          required:
            - ai_damage_scanned
            - has_maintenance_flags
          description: Inspection-level metadata flags.
          properties:
            ai_damage_scanned:
              type: boolean
            ai_damage_detected:
              type: boolean
            has_maintenance_flags:
              type: boolean
        permissions:
          type: object
          required:
            - can_archive
            - can_unarchive
            - can_delete
            - can_edit_due_date
            - can_send_reminder
            - can_edit_label
            - can_copy_inspection_deeplink
            - can_make_unit_edits
            - can_re_open
            - can_remove_non_review_revision_media
          description: Per-user permissions on the inspection. `can_manual_sync` is only
            present on the getById endpoint.
          properties:
            can_archive:
              type: boolean
            can_unarchive:
              type: boolean
            can_delete:
              type: boolean
            can_edit_due_date:
              type: boolean
            can_send_reminder:
              type: boolean
            can_edit_label:
              type: boolean
            can_copy_inspection_deeplink:
              type: boolean
            can_make_unit_edits:
              type: boolean
            can_re_open:
              type: boolean
            can_remove_non_review_revision_media:
              type: boolean
            can_manual_sync:
              type: boolean
        signature_acknowledgement:
          $ref: "#/components/schemas/api_signature_acknowledgement"
        sync_data:
          $ref: "#/components/schemas/api_integration_sync"
        sections_progress:
          type: array
          description: Per-section progress breakdown (only returned on the getById
            endpoint). Each entry pairs the originating section info with
            progress data.
          items:
            type: object
            required:
              - section
              - progress
              - is_na
            properties:
              section:
                type: object
                required:
                  - name
                  - name_with_ordinal
                  - type
                description: Section descriptor.
                properties:
                  name:
                    type: string
                    description: Section name (e.g. "Bedroom", "Kitchen").
                  name_with_ordinal:
                    type: string
                    description: Section name including its ordinal index when the section is
                      repeated on the property (e.g. "First Bedroom").
                  type:
                    type: string
                    enum:
                      - room
                      - section
                    description: Whether the section is a room (auto-generated from the property
                      layout) or a free-form section.
              progress:
                type: number
                description: Progress through the section, as a 0-1 fraction.
              is_na:
                type: boolean
                description: Whether the performer marked the section not applicable.
    inspection_template_v1_get_by_id_response_model:
      type: object
      description: Extends the template digest with a full `sections` list. Optional
        properties (`is_pro`, `new_until`, `subscription_id`,
        `signature_acknowledgement`, `is_resident_template`,
        `resident_template_info`) are omitted rather than nulled when not
        applicable.
      required:
        - id
        - name
        - internal_label
        - display_name
        - description
        - is_rc_template
        - is_fast_track_enabled
        - template_type
        - is_published
        - team_ids
        - property_type
        - created_at
        - contact_info
        - permission_groups
        - is_deleted
        - is_new
        - sections
      properties:
        id:
          type: string
          description: Inspection template ID.
          example: rc_move_in
        name:
          type: string
          description: The inspection template name.
        internal_label:
          type: string
          description: The inspection template internal label.
        display_name:
          type: string
          description: "Resolved display name for the inspection template: the
            `internal_label` for users whose teams have access to the template,
            otherwise the public-facing `name`."
        description:
          type: string
          description: The inspection template description.
        is_rc_template:
          type: boolean
          description: "`true` indicates that the inspection template was created by
            RentCheck. While `false` indicates that the inspection template was
            created by a user."
        is_fast_track_enabled:
          type: boolean
          description: Indicates whether the template supports fast track or not.
        is_pro:
          type: boolean
          description: Whether the template is gated behind a Pro subscription. Defaults
            to `false` when absent.
        new_until:
          type: string
          format: date-time
          description: Optional ISO timestamp until which the template is marked as new in
            the UI.
        is_new:
          type: boolean
          description: Whether the template should be flagged as new (computed from
            `new_until`).
        is_deleted:
          type: boolean
          description: Whether the template has been soft-deleted (every team has lost
            access and it is not an RC template). Only set on the API response —
            not stored on the underlying document.
        template_type:
          type: string
          description: When set to all-rooms the inspection will be created with all the
            rooms in the property. When set to specific-features the inspection
            will require features to be selected when creating the inspection.
          enum:
            - all-rooms
            - specific-features
        is_published:
          type: boolean
          description: Indicates if the template will be available for use on inspection
            creation. For RentCheck templates this is recomputed per
            subscription against the `unpublished_sub_ids` list.
        team_ids:
          type: array
          items:
            type: string
          description: An array of team ids that have access to this template.
        subscription_id:
          type: string
          description: The subscription id for the user that created this template.
        property_type:
          type: string
          description: What property type this template applies to.
          enum:
            - unit
            - building
            - community
        created_at:
          type: string
          description: The inspection template creation date.
          example: 2021-12-02T14:15:37.925
        contact_info:
          type: object
          description: Optional contact email / phone surfaced to performers on the
            inspection.
          properties:
            email:
              type: string
            phone_number:
              type: string
        permission_groups:
          type: array
          description: Permission groups that grant access to this template. Each entry is
            `{ id, name }`.
          items:
            type: object
            required:
              - id
              - name
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              name:
                type: string
                description: Permission group display name.
        signature_acknowledgement:
          $ref: "#/components/schemas/api_signature_acknowledgement"
        is_resident_template:
          type: boolean
          description: Whether the template is a resident-facing template (rendered inside
            the resident app on the home screen).
        resident_template_info:
          type: object
          description: Optional resident-template metadata used to render the entry in the
            resident app. Only populated when `is_resident_template` is `true`.
          properties:
            icon:
              type: object
              required:
                - name
                - color
              properties:
                name:
                  type: string
                  enum:
                    - house
                    - truck
                    - clock
                  description: Icon name displayed for the resident template.
                color:
                  type: string
                  description: Icon color (hex string).
            description:
              type: string
              description: Short description displayed under the template entry.
        sections:
          type: array
          description: Array with the inspection template sections
          items:
            type: object
            properties:
              id:
                type: string
                description: ID of the template section.
              name:
                type: string
                description: Name of the template section.
              type:
                type: string
                description: Sections with type `room` are only added to the inspection if the
                  property has those rooms configured. Sections with type
                  `section` do not depend on the property configuration and are
                  always added to the inspection
                enum:
                  - room
                  - section
              features:
                description: Array with the section features
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      description: ID of the template section feature.
                    name:
                      type: string
                      description: Name of the template section feature.
                    description:
                      type: string
                      description: The instructions text shown to the inspection performer when they
                        are in the camera view.
                    is_rating_required:
                      type: boolean
                      description: When set to true the inspection performer will be required to enter
                        a feature rating. If false the rating options will not
                        be shown.
                    is_video_enabled:
                      type: boolean
                      description: When set to true the inspection performer will be able to record a
                        video wherever they can add media.
                    number_of_photos_required:
                      type: number
                      description: How many photos are required for this feature. When set to 0 the
                        inspection performer will not be required to take any
                        photos for this feature
                    reference_photos:
                      type: array
                      items:
                        type: string
                      description: An array of reference photos for this feature. These photos will be
                        shown to the inspection performer when they are in the
                        camera view
                    questions:
                      description: Array with the feature questions
                      type: array
                      items:
                        type: object
                        properties:
                          id:
                            type: string
                            description: Entity ID
                            example: 00BRcZPSakXz6w7RYoE
                          title:
                            type: string
                            description: Question title
                          type:
                            type: string
                            description: Question type
                            enum:
                              - yes/no
                              - free text
                              - multiple choice
                              - asset capture
                          answer_type:
                            type: string
                            description: Answer type
                            enum:
                              - single
                              - multi
                          asset_category:
                            type: string
                            description: Asset Category
                            enum:
                              - Air Filter
                              - Air Conditioner
                              - Air Handler Unit (AHU)
                              - Barbecue Grill
                              - Bathroom Heater
                              - Cooktop
                              - Condenser
                              - Dishwasher
                              - Dryer
                              - Electric Fireplace
                              - Evaporator
                              - Freezer
                              - Furnace
                              - Furnace (AHU)
                              - Garbage Disposal
                              - Heat Pump
                              - Microwave
                              - Package Unit
                              - Pool Heater
                              - PTAC
                              - Range Hood
                              - Range
                              - Refrigerator
                              - Stackable Laundry
                              - Thermostat
                              - Wall Oven
                              - Washer
                              - Water Heater
                              - Other
                          answers:
                            type: object
                            properties:
                              id:
                                type: string
                                description: Entity ID
                                example: 00BRcZPSakXz6w7RYoE
                              title:
                                type: string
                                description: Answer title
                              requires_photo:
                                type: boolean
                              requires_note:
                                type: boolean
    inspection_template_v1_get_all_response_model:
      type: object
      description: Optional properties (`is_pro`, `new_until`, `subscription_id`,
        `signature_acknowledgement`, `is_resident_template`,
        `resident_template_info`) are omitted rather than nulled when not
        applicable.
      required:
        - id
        - name
        - internal_label
        - display_name
        - description
        - is_rc_template
        - is_fast_track_enabled
        - template_type
        - is_published
        - team_ids
        - property_type
        - created_at
        - contact_info
        - permission_groups
        - is_deleted
        - is_new
      properties:
        id:
          type: string
          description: Inspection template ID.
          example: rc_move_in
        name:
          type: string
          description: The inspection template name.
        internal_label:
          type: string
          description: The inspection template internal label.
        display_name:
          type: string
          description: "Resolved display name for the inspection template: the
            `internal_label` for users whose teams have access to the template,
            otherwise the public-facing `name`."
        description:
          type: string
          description: The inspection template description.
        is_rc_template:
          type: boolean
          description: "`true` indicates that the inspection template was created by
            RentCheck. While `false` indicates that the inspection template was
            created by a user."
        is_fast_track_enabled:
          type: boolean
          description: Indicates whether the template supports fast track or not.
        is_pro:
          type: boolean
          description: Whether the template is gated behind a Pro subscription. Defaults
            to `false` when absent.
        new_until:
          type: string
          format: date-time
          description: Optional ISO timestamp until which the template is marked as new in
            the UI.
        is_new:
          type: boolean
          description: Whether the template should be flagged as new (computed from
            `new_until`).
        is_deleted:
          type: boolean
          description: Whether the template has been soft-deleted (every team has lost
            access and it is not an RC template). Only set on the API response —
            not stored on the underlying document.
        template_type:
          type: string
          description: When set to all-rooms the inspection will be created with all the
            rooms in the property. When set to specific-features the inspection
            will require features to be selected when creating the inspection.
          enum:
            - all-rooms
            - specific-features
        is_published:
          type: boolean
          description: Indicates if the template will be available for use on inspection
            creation. For RentCheck templates this is recomputed per
            subscription against the `unpublished_sub_ids` list.
        team_ids:
          type: array
          items:
            type: string
          description: An array of team ids that have access to this template.
        subscription_id:
          type: string
          description: The subscription id for the user that created this template.
        property_type:
          type: string
          description: What property type this template applies to.
          enum:
            - unit
            - building
            - community
        created_at:
          type: string
          description: The inspection template creation date.
          example: 2021-12-02T14:15:37.925
        contact_info:
          type: object
          description: Optional contact email / phone surfaced to performers on the
            inspection.
          properties:
            email:
              type: string
            phone_number:
              type: string
        permission_groups:
          type: array
          description: Permission groups that grant access to this template. Each entry is
            `{ id, name }`.
          items:
            type: object
            required:
              - id
              - name
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              name:
                type: string
                description: Permission group display name.
        signature_acknowledgement:
          $ref: "#/components/schemas/api_signature_acknowledgement"
        is_resident_template:
          type: boolean
          description: Whether the template is a resident-facing template (rendered inside
            the resident app on the home screen).
        resident_template_info:
          type: object
          description: Optional resident-template metadata used to render the entry in the
            resident app. Only populated when `is_resident_template` is `true`.
          properties:
            icon:
              type: object
              required:
                - name
                - color
              properties:
                name:
                  type: string
                  enum:
                    - house
                    - truck
                    - clock
                  description: Icon name displayed for the resident template.
                color:
                  type: string
                  description: Icon color (hex string).
            description:
              type: string
              description: Short description displayed under the template entry.
    inspection_v2_response_model:
      type: object
      required:
        - success
        - errors
        - warnings
      properties:
        success:
          type: array
          description: List of the created inspections
          items:
            type: object
            required:
              - property
              - inspection
            properties:
              property:
                type: object
                required:
                  - id
                  - address
                  - city
                  - zipcode
                properties:
                  id:
                    type: string
                    description: Entity ID
                    example: 00BRcZPSakXz6w7RYoE
                  x_id:
                    type: string
                    description: External property ID that maps to RentCheck properties
                  address:
                    type: string
                    description: This is the street address for the Unit.
                    example: 2001 Red Gates
                  city:
                    type: string
                    description: This is the city of the Unit.
                    example: New Orleans
                  zipcode:
                    type: string
                    description: This is the Postal Code for the Unit.
                    example: "70130"
              inspection:
                type: object
                required:
                  - id
                  - type
                  - due_date
                properties:
                  id:
                    type: string
                    description: Entity ID
                    example: 00BRcZPSakXz6w7RYoE
                  x_id:
                    type: string
                    description: External inspection ID that maps to RentCheck inspections
                  type:
                    type: string
                    description: Inspection Type designates which type of inspection will be issued.
                      Some inspection types require the user to select which
                      features apply to the inspection. These features are
                      designated in the 'selected_features' field.
                    enum:
                      - Move-in
                      - Move-out
                      - Annual
                      - Turn
                      - Periodic
                      - Maintenance Check
                      - Exterior
                      - Pre-Acquisition
                      - RV Lot
                      - Maintenance / Safety Check
                      - Community Maintenance Check
                  due_date:
                    type: string
                    description: The inspection due date (legacy) is the original date by which the
                      recipient must complete the inspection.
                    example: 2021-12-02T14:15:37.925
        errors:
          type: array
          description: List of the failed inspection creations
          items:
            type: object
            required:
              - inspection
              - reason
            properties:
              property:
                type: object
                properties:
                  id:
                    type: string
                    description: Entity ID
                    example: 00BRcZPSakXz6w7RYoE
                  x_id:
                    type: string
                    description: External property ID that maps to RentCheck properties
                  address:
                    type: string
                    description: This is the street address for the Unit.
                    example: 2001 Red Gates
                  city:
                    type: string
                    description: This is the city of the Unit.
                    example: New Orleans
                  zipcode:
                    type: string
                    description: This is the Postal Code for the Unit.
                    example: "70130"
              inspection:
                type: object
                properties:
                  x_id:
                    type: string
                    description: External inspection ID that maps to RentCheck inspections
                  type:
                    type: string
                    description: Inspection Type designates which type of inspection will be issued.
                      Some inspection types require the user to select which
                      features apply to the inspection. These features are
                      designated in the 'selected_features' field.
                    enum:
                      - Move-in
                      - Move-out
                      - Annual
                      - Turn
                      - Periodic
                      - Maintenance Check
                      - Exterior
                      - Pre-Acquisition
                      - RV Lot
                      - Maintenance / Safety Check
                      - Community Maintenance Check
                  template:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Inspection template identifier
                      name:
                        type: string
                        description: Inspection template name.
                      internal_label:
                        type: string
                        description: Inspection template internal label.
              reason:
                type: string
                description: Detail of the creation error for the inspection
                example: invalid property
              status:
                type: string
                description: Optional machine-readable status code categorizing the error.
        warnings:
          type: array
          description: Inspections that succeeded but produced a non-fatal warning the
            caller should surface (for example, a soft validation hit that did
            not block creation).
          items:
            type: object
            required:
              - property
              - inspection
              - reason
            properties:
              property:
                type: object
                required:
                  - id
                  - address
                  - city
                  - zipcode
                properties:
                  id:
                    type: string
                    description: Entity ID
                    example: 00BRcZPSakXz6w7RYoE
                  x_id:
                    type: string
                    description: External property ID that maps to RentCheck properties
                  address:
                    type: string
                    description: This is the street address for the Unit.
                    example: 2001 Red Gates
                  city:
                    type: string
                    description: This is the city of the Unit.
                    example: New Orleans
                  zipcode:
                    type: string
                    description: This is the Postal Code for the Unit.
                    example: "70130"
              inspection:
                type: object
                required:
                  - id
                  - type
                  - due_date
                properties:
                  id:
                    type: string
                    description: Entity ID
                    example: 00BRcZPSakXz6w7RYoE
                  x_id:
                    type: string
                    description: External inspection ID that maps to RentCheck inspections
                  type:
                    type: string
                    description: Inspection Type designates which type of inspection will be issued.
                      Some inspection types require the user to select which
                      features apply to the inspection. These features are
                      designated in the 'selected_features' field.
                    enum:
                      - Move-in
                      - Move-out
                      - Annual
                      - Turn
                      - Periodic
                      - Maintenance Check
                      - Exterior
                      - Pre-Acquisition
                      - RV Lot
                      - Maintenance / Safety Check
                      - Community Maintenance Check
                  due_date:
                    type: string
                    description: The inspection due date (legacy) is the original date by which the
                      recipient must complete the inspection.
                    example: 2021-12-02T14:15:37.925
              reason:
                type: string
                description: Detail of the warning produced for the inspection
              status:
                type: string
                description: Optional machine-readable status code categorizing the warning.
        conflict:
          type: object
          description: Present when `check_automation_conflict_validation` was set and the
            request matched an existing automation. When present, no inspections
            are created — the caller is expected to resolve the conflict before
            retrying.
          properties:
            type:
              type: string
              enum:
                - automation
              description: Discriminator for the conflicting resource.
            info:
              type: object
              description: Conflicting automation.
              additionalProperties: true
    user_get_by_id_response_model:
      x-internal: true
      type: object
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        name:
          type: string
          description: The users full name
        type:
          type: string
          description: User Type designates whether the user is a Renter, Property
            Manager, or Landlord
          enum:
            - Renter
            - Property Manager
            - Landlord
        email:
          type: string
          description: The user's email. This is used for all email channel notifications
            for the user.
        created_date:
          type: string
          description: Entity creation date, in ISO format
          example: 2022-12-02T14:15:37.925
    user_create_request_model:
      type: object
      required:
        - email
        - first_name
        - last_name
        - password
        - user_type
      properties:
        email:
          type: string
          description: The new user's email address.
        first_name:
          type: string
          description: The new user's first name.
        last_name:
          type: string
          description: The new user's last name.
        password:
          type: string
          description: The new user's password.
        user_type:
          type: string
          enum:
            - Renter
            - Property Manager
            - Landlord
          description: User account type for the new user. Restricted to end-user types;
            `System` is rejected with `400 user type has an invalid value`.
        bypass_email_validation:
          type: boolean
          description: When true, skips spell-checking the email for typos. Internal use
            only.
        total_properties:
          type: integer
          description: Optional total properties count captured at sign-up.
        needs_to_perform_onboarding:
          type: boolean
          description: When true, the new user is flagged to see the post sign-up
            onboarding flow.
    user_create_response_model:
      type: object
      properties:
        user:
          description: The newly created user. Returned when account creation succeeds.
            The full profile document, not just the basic identity fields.
          $ref: "#/components/schemas/api_user"
        email_suggestion:
          type: object
          description: Returned when the supplied email looks like a typo and is not
            actually created.
          properties:
            message:
              type: string
            suggestion:
              type: string
    user_invite_request_model:
      type: object
      required:
        - emails
        - team_ids
        - role
        - permission_group_ids
      properties:
        emails:
          type: array
          items:
            type: string
          minItems: 1
          description: Email addresses to invite.
        team_ids:
          type: array
          items:
            type: string
          minItems: 1
          description: Teams that the invited users should be added to.
        role:
          type: string
          enum:
            - admin
            - manager
            - none
          description: Role granted to the user inside a team membership.
        permission_group_ids:
          type: array
          items:
            type: string
          minItems: 1
          description: Permission groups that the invited users should be added to.
    user_invite_response_model:
      type: object
      required:
        - success
        - failure
      properties:
        success:
          type: object
          required:
            - users
          properties:
            users:
              type: array
              items:
                type: object
                required:
                  - id
                  - name
                  - email
                  - type
                  - teams
                  - permission_groups
                  - status
                properties:
                  id:
                    type: string
                    description: Entity ID
                    example: 00BRcZPSakXz6w7RYoE
                  name:
                    type: string
                    description: The users full name
                  email:
                    type: string
                    description: The user's email.
                  type:
                    type: string
                    description: User type.
                    enum:
                      - Renter
                      - Property Manager
                      - Landlord
                      - System
                  status:
                    type: string
                    description: Status of the user. Status can be "Active" or the date on which the
                      user was invited.
                    enum:
                      - Active
                      - Invite sent ({invite date})
                  teams:
                    type: array
                    description: A list of the teams the user belongs to, including the user's role
                      on each.
                    items:
                      type: object
                      required:
                        - id
                        - name
                        - internal_label
                        - role
                      properties:
                        id:
                          type: string
                          description: Entity ID
                          example: 00BRcZPSakXz6w7RYoE
                        name:
                          type: string
                          description: Team display name.
                        internal_label:
                          type: string
                          description: Internal label used to differentiate similar teams.
                        role:
                          type: string
                          enum:
                            - admin
                            - manager
                            - none
                          description: The user's membership role within this team.
                  permission_groups:
                    type: array
                    description: A list of the permission groups the user belongs to
                    items:
                      type: object
                      required:
                        - id
                        - name
                      properties:
                        id:
                          type: string
                          description: Entity ID
                          example: 00BRcZPSakXz6w7RYoE
                        name:
                          type: string
                          description: Name for the permission group, must be unique in the subscription
                            context
              description: Users that were successfully created/invited.
        failure:
          type: object
          required:
            - errors
          properties:
            errors:
              type: array
              items:
                type: object
                required:
                  - email
                  - error
                properties:
                  email:
                    type: string
                  error:
                    type: string
              description: Per-email failure reasons.
    user_update_access_request_model:
      type: object
      required:
        - team_ids
        - role
        - permission_group_ids
      properties:
        team_ids:
          type: array
          items:
            type: string
          minItems: 1
          description: Teams the target user should belong to after the update. The user's
            team memberships are reconciled to this list.
        role:
          type: string
          enum:
            - admin
            - manager
            - none
          description: Role granted to the user inside a team membership.
        permission_group_ids:
          type: array
          items:
            type: string
          description: Permission groups the target user should belong to after the
            update. The user's permission group memberships are reconciled to
            this list.
    user_bulk_edit_permission_groups_request_model:
      type: object
      required:
        - user_ids
        - permission_group_ids
      properties:
        user_ids:
          type: array
          items:
            type: string
          description: Users to apply the new permission groups to. Each user will be
            reconciled to membership in exactly the supplied permission groups.
        permission_group_ids:
          type: array
          items:
            type: string
          description: Permission groups to assign. Pass an empty array to remove the
            users from every permission group.
    api_user_digest:
      type: object
      required:
        - id
        - name
        - email
        - type
        - teams
        - permission_groups
        - status
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        name:
          type: string
          description: The users full name
        email:
          type: string
          description: The user's email.
        type:
          type: string
          description: User type.
          enum:
            - Renter
            - Property Manager
            - Landlord
            - System
        status:
          type: string
          description: Status of the user. Status can be "Active" or the date on which the
            user was invited.
          enum:
            - Active
            - Invite sent ({invite date})
        teams:
          type: array
          description: A list of the teams the user belongs to, including the user's role
            on each.
          items:
            type: object
            required:
              - id
              - name
              - internal_label
              - role
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              name:
                type: string
                description: Team display name.
              internal_label:
                type: string
                description: Internal label used to differentiate similar teams.
              role:
                type: string
                enum:
                  - admin
                  - manager
                  - none
                description: The user's membership role within this team.
        permission_groups:
          type: array
          description: A list of the permission groups the user belongs to
          items:
            type: object
            required:
              - id
              - name
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              name:
                type: string
                description: Name for the permission group, must be unique in the subscription
                  context
    team_get_by_id_response_model:
      type: object
      required:
        - id
        - name
        - internal_label
        - user_role
        - users
        - pending_users
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        name:
          type: string
          description: The name field is the Team name that is displayed on recipient
            facing reports and communication.
          example: The best company
        internal_label:
          type: string
          description: The internal label is a convenience label to help differentiate
            similar teams a user may belong to. It's displayed to you and your
            team members.
          example: The best company label
        logo:
          type: string
          description: The location of a hosted image file that is used to co-brand
            RentCheck reports and email communication.
        user_role:
          type: string
          description: Team role for the user. Falls back to `'none'` when the caller
            isn't a member of the returned team (e.g. the internal-admin “All
            teams” pseudo-team); any of the three values can be persisted.
          enum:
            - admin
            - manager
            - none
          example: admin
        users:
          type: array
          description: A list of users with their info and role that belong to a team.
          items:
            type: object
            required:
              - id
              - user_name
              - email
              - role
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              user_name:
                type: string
                description: User full name.
              email:
                type: string
                description: User email
                example: user@email.com
              role:
                type: string
                description: Team role for the user. Falls back to `'none'` when the caller
                  isn't a member of the returned team (e.g. the internal-admin
                  “All teams” pseudo-team); any of the three values can be
                  persisted.
                enum:
                  - admin
                  - manager
                  - none
                example: admin
        pending_users:
          type: array
          description: A list of users and their info that have not yet accepted their
            invitation to a team.
          items:
            type: object
            required:
              - id
              - email
              - role
              - created_at
              - created_by
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              email:
                type: string
                description: User email
                example: user@email.com
              role:
                type: string
                description: Team role for the user. Falls back to `'none'` when the caller
                  isn't a member of the returned team (e.g. the internal-admin
                  “All teams” pseudo-team); any of the three values can be
                  persisted.
                enum:
                  - admin
                  - manager
                  - none
                example: admin
              created_at:
                type: string
                description: Entity creation date, in ISO format
                example: 2022-12-02T14:15:37.925
              created_by:
                type: object
                required:
                  - id
                  - name
                properties:
                  id:
                    type: string
                    description: Entity ID
                    example: 00BRcZPSakXz6w7RYoE
                  name:
                    type: string
                    description: User full name
    team_get_all_response_model:
      type: object
      required:
        - id
        - name
        - internal_label
        - user_role
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        name:
          type: string
          description: The name field is the Team name that is displayed on recipient
            facing reports and communication.
          example: The best company
        internal_label:
          type: string
          description: The internal label is a convenience label to help differentiate
            similar teams a user may belong to. It's displayed to you and your
            team members.
          example: The best company label
        logo:
          type: string
          description: The location of a hosted image file that is used to co-brand
            RentCheck reports and email communication.
        user_role:
          type: string
          description: Team role for the user. Falls back to `'none'` when the caller
            isn't a member of the returned team (e.g. the internal-admin “All
            teams” pseudo-team); any of the three values can be persisted.
          enum:
            - admin
            - manager
            - none
          example: admin
    team_create_request_model:
      type: object
      required:
        - name
        - internal_label
      properties:
        name:
          type: string
          description: The name field is the Team name that is displayed on recipient
            facing reports and communication.
          example: The best company
        internal_label:
          type: string
          description: The internal label is a convenience label to help differentiate
            similar teams a user may belong to. It's displayed to you and your
            team members.
          example: The best company label
        logo:
          type: string
          description: A base64 string of the image that is goint to be used to co-brand
            RentCheck reports and email communication.
    team_update_request_model:
      type: object
      required: []
      properties:
        name:
          type: string
          description: The name field is the Team name that is displayed on recipient
            facing reports and communication.
          example: The best company
        internal_label:
          type: string
          description: The internal label is a convenience label to help differentiate
            similar teams a user may belong to. It's displayed to you and your
            team members.
          example: The best company label
        logo:
          type: string
          description: A base64 string of the image that is goint to be used to co-brand
            RentCheck reports and email communication.
    team_integration_latchel_request_model:
      x-internal: true
      type: object
      required:
        - api_key
        - team_ids
      properties:
        api_key:
          type: string
          description: Latchel's API key.
          example: awd30j1894asd098afokasmd8971238asc8ca12c
        team_ids:
          type: array
          description: Id of the Teams who created or administers the Entity.
          items:
            type: string
          example:
            - 05fDGZRKp4nVmlmEwjsv
    team_integration_latchel_response_model:
      type: object
      required:
        - api_key
        - company_name
        - created_by
        - created_date
        - team
      properties:
        api_key:
          type: string
          description: Latchel's API key
        company_name:
          type: string
          description: Company name
        created_by:
          type: object
          required:
            - id
            - name
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: User full name
        created_date:
          type: string
          description: Entity creation date, in ISO format
          example: 2022-12-02T14:15:37.925
        team:
          type: object
          required:
            - id
            - name
            - internal_label
            - user_role
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: The name field is the Team name that is displayed on recipient
                facing reports and communication.
              example: The best company
            internal_label:
              type: string
              description: The internal label is a convenience label to help differentiate
                similar teams a user may belong to. It's displayed to you and
                your team members.
              example: The best company label
            logo:
              type: string
              description: The location of a hosted image file that is used to co-brand
                RentCheck reports and email communication.
            user_role:
              type: string
              description: Team role for the user. Falls back to `'none'` when the caller
                isn't a member of the returned team (e.g. the internal-admin
                “All teams” pseudo-team); any of the three values can be
                persisted.
              enum:
                - admin
                - manager
                - none
              example: admin
    latchel_integration_delete_request_model:
      x-internal: true
      type: object
      required:
        - api_key
        - team_ids
      description: Request body for deleting a Latchel integration.
      properties:
        api_key:
          type: string
          description: Latchel's API key.
          example: awd30j1894asd098afokasmd8971238asc8ca12c
        team_ids:
          type: array
          description: Id of the Teams who created or administers the Entity.
          items:
            type: string
          example:
            - 05fDGZRKp4nVmlmEwjsv
    latchel_integration_delete_response_model:
      x-internal: true
      type: object
      required:
        - teams
      description: The `teams` array contains the teams from which the Latchel
        integration was removed.
      properties:
        teams:
          type: array
          items:
            type: object
            required:
              - id
              - name
              - internal_label
              - user_role
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              name:
                type: string
                description: The name field is the Team name that is displayed on recipient
                  facing reports and communication.
                example: The best company
              internal_label:
                type: string
                description: The internal label is a convenience label to help differentiate
                  similar teams a user may belong to. It's displayed to you and
                  your team members.
                example: The best company label
              logo:
                type: string
                description: The location of a hosted image file that is used to co-brand
                  RentCheck reports and email communication.
              user_role:
                type: string
                description: Team role for the user. Falls back to `'none'` when the caller
                  isn't a member of the returned team (e.g. the internal-admin
                  “All teams” pseudo-team); any of the three values can be
                  persisted.
                enum:
                  - admin
                  - manager
                  - none
                example: admin
    team_invite_create_request_model:
      type: object
      required:
        - emails
      properties:
        emails:
          type: array
          minItems: 1
          description: List of users to be invited to team. Must contain at least one
            valid email; invalid addresses are rejected.
          items:
            type: string
            format: email
          example:
            - pm1@email.com
            - pm2@email.com
        role:
          type: string
          enum:
            - admin
            - manager
            - none
          description: Optional team membership role to assign to the invited users.
            Defaults to `manager` when omitted.
    team_invite_create_response_model:
      type: object
      required:
        - team
        - invited
        - not_invited
        - new_profiles
      properties:
        team:
          type: object
          required:
            - id
            - name
            - internal_label
            - user_role
            - users
            - pending_users
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: The name field is the Team name that is displayed on recipient
                facing reports and communication.
              example: The best company
            internal_label:
              type: string
              description: The internal label is a convenience label to help differentiate
                similar teams a user may belong to. It's displayed to you and
                your team members.
              example: The best company label
            logo:
              type: string
              description: The location of a hosted image file that is used to co-brand
                RentCheck reports and email communication.
            user_role:
              type: string
              description: Team role for the user. Falls back to `'none'` when the caller
                isn't a member of the returned team (e.g. the internal-admin
                “All teams” pseudo-team); any of the three values can be
                persisted.
              enum:
                - admin
                - manager
                - none
              example: admin
            users:
              type: array
              description: A list of users with their info and role that belong to a team.
              items:
                type: object
                required:
                  - id
                  - user_name
                  - email
                  - role
                properties:
                  id:
                    type: string
                    description: Entity ID
                    example: 00BRcZPSakXz6w7RYoE
                  user_name:
                    type: string
                    description: User full name.
                  email:
                    type: string
                    description: User email
                    example: user@email.com
                  role:
                    type: string
                    description: Team role for the user. Falls back to `'none'` when the caller
                      isn't a member of the returned team (e.g. the
                      internal-admin “All teams” pseudo-team); any of the three
                      values can be persisted.
                    enum:
                      - admin
                      - manager
                      - none
                    example: admin
            pending_users:
              type: array
              description: A list of users and their info that have not yet accepted their
                invitation to a team.
              items:
                type: object
                required:
                  - id
                  - email
                  - role
                  - created_at
                  - created_by
                properties:
                  id:
                    type: string
                    description: Entity ID
                    example: 00BRcZPSakXz6w7RYoE
                  email:
                    type: string
                    description: User email
                    example: user@email.com
                  role:
                    type: string
                    description: Team role for the user. Falls back to `'none'` when the caller
                      isn't a member of the returned team (e.g. the
                      internal-admin “All teams” pseudo-team); any of the three
                      values can be persisted.
                    enum:
                      - admin
                      - manager
                      - none
                    example: admin
                  created_at:
                    type: string
                    description: Entity creation date, in ISO format
                    example: 2022-12-02T14:15:37.925
                  created_by:
                    type: object
                    required:
                      - id
                      - name
                    properties:
                      id:
                        type: string
                        description: Entity ID
                        example: 00BRcZPSakXz6w7RYoE
                      name:
                        type: string
                        description: User full name
        invited:
          type: array
          description: List of users that were invited to team.
          items:
            type: string
          example:
            - invited1@email.com
            - invited2@email.com
        not_invited:
          type: array
          description: List of users that were not invited to team. For example, users
            that are already part of the team.
          items:
            type: string
          example:
            - notinvited1@email.com
            - notinvited2@email.com
        new_profiles:
          type: array
          description: Profile digests for users that did not exist before the invite and
            have just been created.
          items:
            type: object
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              email:
                type: string
              name:
                type: string
    team_invite_update_request_model:
      type: object
      properties:
        role:
          type: string
          description: Team role for the user. Falls back to `'none'` when the caller
            isn't a member of the returned team (e.g. the internal-admin “All
            teams” pseudo-team); any of the three values can be persisted.
          enum:
            - admin
            - manager
            - none
          example: admin
    team_invite_update_response_model:
      type: object
      required:
        - id
        - email
        - role
        - created_at
        - created_by
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        email:
          type: string
          description: User email
          example: user@email.com
        role:
          type: string
          description: Team role for the user. Falls back to `'none'` when the caller
            isn't a member of the returned team (e.g. the internal-admin “All
            teams” pseudo-team); any of the three values can be persisted.
          enum:
            - admin
            - manager
            - none
          example: admin
        created_at:
          type: string
          description: Entity creation date, in ISO format
          example: 2022-12-02T14:15:37.925
        created_by:
          type: object
          required:
            - id
            - name
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: User full name
    team_invite_get_all_response_model:
      type: object
      required:
        - id
        - email
        - role
        - created_at
        - created_by
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        email:
          type: string
          description: User email
          example: user@email.com
        role:
          type: string
          description: Team role for the user. Falls back to `'none'` when the caller
            isn't a member of the returned team (e.g. the internal-admin “All
            teams” pseudo-team); any of the three values can be persisted.
          enum:
            - admin
            - manager
            - none
          example: admin
        created_at:
          type: string
          description: Entity creation date, in ISO format
          example: 2022-12-02T14:15:37.925
        created_by:
          type: object
          required:
            - id
            - name
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: User full name
    team_invite_accept_all_response_model:
      type: object
      required:
        - id
        - email
        - role
        - created_at
        - created_by
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        email:
          type: string
          description: User email
          example: user@email.com
        role:
          type: string
          description: Team role for the user. Falls back to `'none'` when the caller
            isn't a member of the returned team (e.g. the internal-admin “All
            teams” pseudo-team); any of the three values can be persisted.
          enum:
            - admin
            - manager
            - none
          example: admin
        created_at:
          type: string
          description: Entity creation date, in ISO format
          example: 2022-12-02T14:15:37.925
        created_by:
          type: object
          required:
            - id
            - name
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: User full name
    team_invite_reminder_response_model:
      type: object
      required:
        - id
        - email
        - role
        - created_at
        - created_by
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        email:
          type: string
          description: User email
          example: user@email.com
        role:
          type: string
          description: Team role for the user. Falls back to `'none'` when the caller
            isn't a member of the returned team (e.g. the internal-admin “All
            teams” pseudo-team); any of the three values can be persisted.
          enum:
            - admin
            - manager
            - none
          example: admin
        created_at:
          type: string
          description: Entity creation date, in ISO format
          example: 2022-12-02T14:15:37.925
        created_by:
          type: object
          required:
            - id
            - name
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: User full name
    team_member_update_request_model:
      type: object
      properties:
        role:
          type: string
          description: Team role for the user. Falls back to `'none'` when the caller
            isn't a member of the returned team (e.g. the internal-admin “All
            teams” pseudo-team); any of the three values can be persisted.
          enum:
            - admin
            - manager
            - none
          example: admin
    file_request_create_request_model:
      x-internal: true
      description: "A file request is the API surface for asynchronous PDF/ZIP/CSV
        report generation. The required fields depend on `document_type`: for
        per-record exports (`INSPECTION_REPORT`, `MAINTENANCE_REPORT`,
        `WORK_ORDER`, `INSPECTION_IMAGES`) `document_id` is required; for list
        exports (`*_LIST_EXPORT`) `document_id` is omitted and the server
        generates one for the resulting file."
      oneOf:
        - type: object
          required:
            - document_type
            - export_type
            - document_id
          properties:
            document_type:
              type: string
              description: Name of the report to be generated.
              enum:
                - INSPECTION_REPORT
                - MAINTENANCE_REPORT
                - WORK_ORDER
                - INSPECTION_IMAGES
              example: INSPECTION_REPORT
            export_type:
              type: string
              description: Format of the exported report.
              enum:
                - PDF
                - ZIP
                - CSV
              example: PDF
            document_id:
              type: string
              description: ID of the document you are trying to export
              example: fbb2fcf6-4bf5-48b5-9283-4d284a11d317
            dont_notify:
              type: boolean
              description: Specify whether to email the requestor a copy of the file
              example: true
            throttled:
              type: boolean
              description: When true, the request is debounced against any other pending
                throttled request for the same `document_id` — the prior pending
                request is deleted before the new one is enqueued.
            filters:
              type: object
              description: Optional per-record export delivery filters. When `send_to` is
                provided the resulting file is emailed to those recipients in
                addition to the requesting user; `note` is included in the email
                body.
              properties:
                send_to:
                  type: array
                  items:
                    type: string
                    format: email
                  description: Email addresses to send the resulting file to once generation
                    completes.
                note:
                  type: string
                  description: Free-form note appended to the recipient email.
        - type: object
          required:
            - document_type
            - export_type
          properties:
            document_type:
              type: string
              description: Name of the report to be generated.
              enum:
                - BUILDINGS_LIST_EXPORT
                - COMMUNITIES_LIST_EXPORT
                - INSPECTION_LIST_EXPORT
                - MAINTENANCE_REPORTS_LIST_EXPORT
                - MAINTENANCE_FLAGS_LIST_EXPORT
                - UNITS_LIST_EXPORT
                - SUBSCRIPTION_REPORT_LIST_EXPORT
              example: INSPECTION_REPORT
            export_type:
              type: string
              description: Format of the exported report.
              enum:
                - PDF
                - ZIP
                - CSV
              example: PDF
            dont_notify:
              type: boolean
              description: Specify whether to email the requestor a copy of the file
              example: true
            throttled:
              type: boolean
              description: When true, the request is debounced against any other pending
                throttled request — the prior pending request is deleted before
                the new one is enqueued.
            filters:
              type: object
              description: Optional list-export filter criteria. Shape varies by
                `document_type` — for `INSPECTION_LIST_EXPORT` it matches the
                inspection-list filter shape (`search`, `archived`,
                `assigned_to`, `inspection_template`, `inspection_status`,
                `team`, `property`, `due_date`, `completed_date`, etc.); for
                `*_LIST_EXPORT` variants it matches the matching list endpoint
                filter shape.
              additionalProperties: true
    file_request_response_model:
      x-internal: true
      type: object
      required:
        - fileRequestId
      properties:
        fileRequestId:
          type: string
          description: Entity ID
          example: fbb2fcf6-4bf5-48b5-9283-4d284a11d317
    work_order_create_latchel_request_model:
      x-internal: true
      type: object
      required:
        - work_order_name
        - work_order_description
        - maintenance_flags
        - latchel_property_id
      properties:
        work_order_name:
          type: string
          description: A name / label for the work order.
          example: "Latchel Job #1"
        work_order_description:
          type: string
          description: A description for the work order.
          example: Faulty socket, no power, needs repair.
        vendor_instructions:
          type: string
          description: Optional vendor instruction for the work order.
          example: Use your biggest plunger, do not use little plunger.
        skill:
          type: string
          description: Vendor name of the repair to be made.
          example: Repair socket
        property_access_instructions:
          type: string
          description: Property access instructions for the work order.
          example: Look under the door mat; there is a secret entrance.
        latchel_property_id:
          type: number
          description: Latchel property id for the work order.
        vendor_id:
          type: number
          nullable: true
          description: Latchel vendor id for the work order.
        resident_id:
          type: string
          description: Resident / Tenant ID that will be associated with work order.
        primary_resident_id:
          type: string
          description: Latchel primary resident id for the work order.
        maintenance_flags:
          type: array
          minItems: 1
          description: A list of maintenance flags. Only each flag's `id` and `feature_id`
            are consumed; additional fields are accepted but ignored.
          items:
            type: object
            required:
              - id
              - feature_id
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              feature_id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
            additionalProperties: true
    work_order_create_rent_manager_request_model:
      x-internal: true
      type: object
      required:
        - work_order_name
        - work_order_description
        - maintenance_flags
        - ok_to_enter
      properties:
        work_order_name:
          type: string
          description: A name / label for the work order.
          example: Leaky faucet repair
        work_order_description:
          type: string
          description: A description for the work order.
          example: Faucet in kitchen is dripping — please replace washer.
        skill:
          type: string
          description: Trade / skill required for the work order.
          example: Plumbing
        priority:
          type: string
          enum:
            - Low
            - Normal
            - High
          description: Rent Manager work-order priority.
        ok_to_enter:
          type: boolean
          description: Whether the vendor is authorized to enter the unit without the
            resident present.
        maintenance_flags:
          type: array
          minItems: 1
          description: A list of maintenance flags. The handler only consumes each flag's
            `id` and `feature_id`; additional fields are accepted but ignored.
          items:
            type: object
            required:
              - id
              - feature_id
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              feature_id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
            additionalProperties: true
    work_order_create_jenark_request_model:
      x-internal: true
      type: object
      required:
        - work_order_name
        - work_order_code
        - maintenance_flags
      properties:
        work_order_name:
          type: string
          description: A name / label for the work order.
          example: Replace HVAC filter
        work_order_description:
          type: string
          description: A description for the work order.
          example: Filter is overdue — replace with 16x20x1 pleated filter.
        work_order_code:
          type: string
          description: Jenark work-order classification code.
          example: HVAC
        skill:
          type: string
          description: Trade / skill required for the work order.
          example: HVAC
        maintenance_flags:
          type: array
          minItems: 1
          description: A list of maintenance flags. The handler only consumes each flag's
            `id` and `feature_id`; additional fields are accepted but ignored.
          items:
            type: object
            required:
              - id
              - feature_id
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              feature_id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
            additionalProperties: true
    work_order_create_appfolio_request_model:
      x-internal: true
      type: object
      description: AppFolio work-order create body. Additional AppFolio-specific
        properties beyond those listed here are accepted.
      required:
        - work_order_name
        - work_order_description
        - maintenance_flags
        - ok_to_enter
      properties:
        work_order_name:
          type: string
          description: A name / label for the work order.
        work_order_description:
          type: string
          description: A description for the work order.
        priority:
          type: string
          enum:
            - Urgent
            - Normal
            - Low
          description: AppFolio priority for the work order.
        ok_to_enter:
          type: boolean
          description: Whether the vendor is authorized to enter the unit without the
            resident present.
        maintenance_flags:
          type: array
          minItems: 1
          items:
            type: object
            required:
              - id
              - feature_id
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              feature_id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
            additionalProperties: true
      additionalProperties: true
    work_order_create_rentvine_request_model:
      x-internal: true
      type: object
      description: Rentvine work-order create body. Additional Rentvine-specific
        properties beyond those listed here are accepted.
      required:
        - work_order_name
        - work_order_description
        - priority
        - ok_to_enter
      properties:
        work_order_name:
          type: string
          description: A name / label for the work order.
        work_order_description:
          type: string
          description: A description for the work order.
        priority:
          type: string
          enum:
            - low
            - medium
            - high
          description: Rentvine priority for the work order.
        owner_approved:
          type: boolean
          default: false
          description: Whether the property owner has approved the work order in Rentvine.
            Server-defaulted to `false` when omitted.
        publish_with_tenant:
          type: boolean
          default: false
          description: Whether the work order should be published to the Rentvine tenant
            portal. Server-defaulted to `false` when omitted.
        publish_with_owner:
          type: boolean
          default: false
          description: Whether the work order should be published to the Rentvine owner
            portal. Server-defaulted to `false` when omitted.
        ok_to_enter:
          type: string
          enum:
            - vendor_may_access
            - resident_presence_required
            - not_applicable
          description: Vendor access mode.
        maintenance_flags:
          type: array
          description: Maintenance flags referenced by the work order. No minimum size is
            enforced by validation, but omitting it or passing an empty array
            surfaces as a 500.
          items:
            type: object
            required:
              - id
              - feature_id
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              feature_id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
            additionalProperties: true
      additionalProperties: true
    work_order_response_model:
      x-internal: true
      type: object
      description: Envelope shared by v1 (`POST /v1/work_orders`) and v2 (`GET
        /v2/work_orders`). Provider-specific keys (`latchel`, `yardi`,
        `appfolio`, `jenark`, `rentmanager`, `rentvine`) remain optional and
        appear only when the work order was synced through that vendor.
        `property` is required on v1 responses; v2 (`GET /v2/work_orders`) drops
        the field when the underlying row has no property snapshot, so it is not
        listed as required here.
      required:
        - id
        - name
        - created_date
        - created_by
        - created_by_name
        - status
        - maintenance_flags
        - organization_id
        - inspection
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        name:
          type: string
          description: The name field is the work order name / label.
          example: "Latchel Job #1"
        created_date:
          type: string
          description: Work order creation date, ISO format.
          example: 2022-01-15T00:00:00.000
        created_by:
          type: string
          description: User id that created the work order.
        created_by_name:
          type: string
          description: Display name of the user that created the work order.
        organization_id:
          type: string
          description: Organization (subscription) the work order belongs to.
        status:
          type: string
          description: Up to date status of the work order.
        skill:
          type: string
          description: Skill / category selected for the work order.
        zip_report_location:
          type: string
          description: Location of the most recent generated zip report for the work
            order. Only present on v2 (`GET /v2/work_orders`) responses; v1
            responses do not emit this field.
        last_sync_date:
          type: string
          description: ISO timestamp of the latest sync attempt to the work order
            provider, when one has run. Only present on v2 (`GET
            /v2/work_orders`) responses; v1 responses do not emit this field.
        inspection:
          type: object
          description: This is the entity id of the inspection that the entity is for.
          properties:
            id:
              type: string
              description: Selected entity's inspection id
              example: 00BRcZPSakXz6w7RYoE
            type:
              type: string
              description: Selected entity's inspection type
              enum:
                - Move-in
                - Move-out
                - Annual
                - Turn
                - Periodic
                - Maintenance Check
                - Exterior
                - Pre-Acquisition
                - RV Lot
                - Maintenance / Safety Check
                - Community Maintenance Check
            date:
              type: string
              description: Selected entity's inspection date, ISO format
              example: 2022-01-15T00:00:00.000
            template:
              type: object
            completed_date:
              type: string
              description: Inspection completed date in ISO format, when set.
        property:
          type: object
          required:
            - id
            - address
            - city
            - zip_code
          description: This is the entity id of the Unit, Building, or Community that the
            entity is for.
          properties:
            id:
              type: string
              description: Selected entity's property id
              example: 00BRcZPSakXz6w7RYoE
            address:
              type: string
              description: Selected entity's property address
              example: 2001 Red Gates
            address_2:
              type: string
              description: Selected entity's property additional address info
              example: Apt. 4B
            city:
              type: string
              description: Selected entity's property city
              example: New Orleans
            region:
              type: string
              description: Selected entity's property region
              example: Orleans
            zip_code:
              type: string
              description: Selected entity's property zip_code
              example: "70130"
        maintenance_flags:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              room:
                type: string
                description: Maintenance flag's room.
              feature:
                type: string
                description: Maintenance flag's feature name.
              feature_id:
                type: string
                description: Maintenance flag's feature ID.
              created_date:
                type: string
                description: Feature creation date, ISO format
                example: 2022-01-15T00:00:00.000
              created_by:
                type: object
                description: User that created the flag.
                required:
                  - id
                  - name
                  - type
                properties:
                  id:
                    type: string
                    description: Entity ID
                    example: 00BRcZPSakXz6w7RYoE
                  name:
                    type: string
                    description: Name of the user that created the flag.
                  type:
                    type: string
                    enum:
                      - Renter
                      - Property Manager
                      - Landlord
                      - System
                    description: User type discriminator.
              responsibility:
                type: string
                description: Maintenance flag's responsibility
                enum:
                  - OWNER
                  - RESIDENT
                  - ""
              note:
                type: string
                description: Maintenance flag's notes
              image_urls:
                type: array
                description: Maintenance flag's images
                items:
                  type: string
                  description: URL links to images related to the maintenance flags
              cost:
                type: number
                description: Maintenance flag's cost
        latchel:
          type: object
          description: Latchel-specific work order fields, present when the provider is
            `latchel`.
          additionalProperties: true
        yardi:
          type: object
          description: Yardi-specific work order fields, present when the provider is
            `yardi`.
          additionalProperties: true
        appfolio:
          type: object
          description: AppFolio-specific work order fields, present when the provider is
            `appfolio`.
          additionalProperties: true
        jenark:
          type: object
          description: Jenark-specific work order fields, present when the provider is
            `jenark`.
          additionalProperties: true
        rentmanager:
          type: object
          description: Rent Manager-specific work order fields, present when the provider
            is `rentmanager`.
          additionalProperties: true
        rentvine:
          type: object
          description: Rentvine-specific work order fields, present when the provider is
            `rentvine`.
          additionalProperties: true
    rentvine_sync:
      type: object
      required:
        - last_sync
      description: Rentvine sync metadata.
      properties:
        id:
          type: string
          description: Vendor id. For tenants this is the Rentvine tenant id. For
            properties this is the Rentvine id with a `unit-` or `building-`
            prefix indicating the basic property type.
        x_id:
          type: string
          description: Rentvine id without the basic-property-type prefix that RentCheck
            adds in `id`.
        x_property_id:
          type: string
          description: Rentvine property id. When the property type is a unit, this points
            at the building id.
        last_sync:
          type: string
          format: date-time
          description: ISO-8601 timestamp of the most recent sync. Marked required for
            schema completeness; in practice the integration engine writes it
            and callers do not set it on create / update requests (the request
            validator only checks that the field is an object).
        status:
          type: string
          enum:
            - scheduled
            - processing
            - success
            - error
          description: Job orchestration and terminal outcome for the most recent sync.
        error:
          type: string
          nullable: true
          description: Top-level error message; prefer the per-destination
            `upload_response[].error` for inspection-upload failures.
        upload_response:
          type: array
          description: Per-destination upload outcomes for inspection PDF uploads.
          items:
            type: object
            additionalProperties: true
        excluded_inspection_upload_destinations:
          type: array
          description: Destinations to skip on the next inspection-report upload retry.
          items:
            type: string
            enum:
              - properties
              - tenants
              - inspections
    room_info:
      type: object
      required:
        - type
        - name
        - label
      description: Structured room entry.
      properties:
        type:
          type: string
          description: The no-ordinal room kind (e.g. "Bedroom"), recomputed from `name`
            on every write.
          example: Bedroom
        name:
          type: string
          description: The ordinal room name (e.g. "First Bedroom") and the source of
            truth for the entry.
          example: First Bedroom
        label:
          type: string
          description: Human-meaningful display label that lets multiple rooms of the same
            kind be differentiated. Defaults to `name`.
          example: First Bedroom
    room_info_request:
      type: object
      required:
        - name
      description: Structured room entry (write shape). Only `name` is required; the
        server normalizes the other fields.
      properties:
        type:
          type: string
          description: "Accepted but ignored: the server recomputes it from `name`."
          example: Bedroom
        name:
          type: string
          description: The ordinal room name (e.g. "First Bedroom") and the source of
            truth for the entry.
          example: First Bedroom
        label:
          type: string
          description: "Human-meaningful display label that lets multiple rooms of the
            same kind be differentiated. Optional: defaults to `name`."
          example: First Bedroom
    api_inspection_upload_response_entry:
      x-internal: true
      type: object
      description: Per-destination upload outcome.
      required:
        - resource_id
        - uploaded_id
      properties:
        resource_id:
          type: string
        uploaded_id:
          type: string
          nullable: true
        error:
          type: object
          required:
            - message
          properties:
            message:
              type: string
            vendor_request_id:
              type: string
            attempted_at:
              type: string
              description: ISO string. Absent on entries mapped up from a legacy per-vendor
                sync row.
        attempts:
          type: integer
        last_sync:
          type: string
          description: ISO string of the last successful upload to this resource.
    api_integration_sync:
      x-internal: true
      type: object
      nullable: true
      description: Canonical API shape for an inspection's integration sync state.
        Every vendor is mapped into this envelope — engine-native AppFolio /
        Buildium pass through, and legacy Rent Manager / Rentvine per-vendor
        sync objects are mapped up — so the client reads one uniform structure
        regardless of source vendor. Returns `null` on the inspection response
        when the inspection has no active integration sync.
      required:
        - vendor
        - vendor_integration_id
        - last_sync
        - status
      properties:
        vendor:
          type: string
          enum:
            - appfolio
            - rentvine
            - rentmanager
            - buildium
        vendor_integration_id:
          type: string
        last_sync:
          type: string
          nullable: true
          description: ISO string of the most recent successful sync; `null` when the
            record has never synced successfully.
        status:
          type: string
          description: "`scheduled` / `processing` mark in-flight manual re-syncs."
          enum:
            - scheduled
            - processing
            - success
            - error
        error:
          type: object
          nullable: true
          required:
            - message
          description: "Failure detail. Populated for `status: 'error'` even when the
            engine envelope only carried the failure per-resource on
            `upload_response` — the builder aggregates the per-resource messages
            up so the client always has a message to show."
          properties:
            message:
              type: string
            vendor_request_id:
              type: string
        upload_response:
          type: object
          description: Per-destination upload outcome, keyed by resource. Any of the keys
            may be absent when the corresponding resource has not been
            attempted.
          properties:
            units:
              $ref: "#/components/schemas/api_inspection_upload_response_entry"
            properties:
              $ref: "#/components/schemas/api_inspection_upload_response_entry"
            occupancies:
              $ref: "#/components/schemas/api_inspection_upload_response_entry"
            work_orders:
              $ref: "#/components/schemas/api_inspection_upload_response_entry"
            inspections:
              $ref: "#/components/schemas/api_inspection_upload_response_entry"
        pending_since:
          type: string
          nullable: true
          description: ISO string set while `status` is `scheduled` / `processing`; used
            by the API to expire stale pending state past a TTL.
    api_maintenance_report:
      type: object
      required:
        - id
        - name
        - organize_by
        - created_by
        - inspection_date
        - created_date
        - inspection
        - property
        - maintenance_flags
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        name:
          type: string
          description: A name for the new report
          example: Move-out maintenance report
        organize_by:
          type: string
          description: Group to display when viewing the report
          example: responsibility
          enum:
            - responsibility
            - room
            - skill
        created_by:
          type: object
          required:
            - name
          properties:
            name:
              type: string
              description: Name of user that created the report
        inspection_date:
          type: string
          description: Inspection creation date, ISO format
          example: 2022-01-15T00:00:00.000
        created_date:
          type: string
          description: Report creation date, ISO format
          example: 2022-01-15T00:00:00.000
        inspection:
          type: object
          description: The inspection info from where the report originates.
          required:
            - id
            - date
            - template
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            date:
              type: string
              description: The inspection due date (legacy) is the original date by which the
                recipient must complete the inspection.
              example: 2021-12-02T14:15:37.925
            template:
              type: object
              required:
                - id
                - name
                - internal_label
                - property_type
                - display_name
              properties:
                id:
                  type: string
                  description: Inspection template identifier
                name:
                  type: string
                  description: Inspection template name.
                internal_label:
                  type: string
                  description: Inspection template internal label.
                property_type:
                  type: string
                  description: Inspection template property type.
                display_name:
                  type: string
                  description: Resolved display name for the inspection template, used by clients
                    to render the template title (mirrors `name` for legacy
                    templates).
              description: Inspection template summary (id, name, plus the resolved
                `display_name`).
        property:
          type: object
          required:
            - id
            - address
            - city
            - zip_code
          description: This is the entity id of the Unit, Building, or Community that the
            entity is for.
          properties:
            id:
              type: string
              description: Selected entity's property id
              example: 00BRcZPSakXz6w7RYoE
            address:
              type: string
              description: Selected entity's property address
              example: 2001 Red Gates
            address_2:
              type: string
              description: Selected entity's property additional address info
              example: Apt. 4B
            city:
              type: string
              description: Selected entity's property city
              example: New Orleans
            region:
              type: string
              description: Selected entity's property region
              example: Orleans
            zip_code:
              type: string
              description: Selected entity's property zip_code
              example: "70130"
        zip_report_location:
          type: string
          description: Link to the inspections completed ZIP report if previously generated.
        pdf_report_location:
          type: string
          description: Link to the inspections completed PDF report if previously generated.
        maintenance_flags:
          type: array
          items:
            type: object
            required:
              - id
              - feature_id
              - feature
              - section
              - note
              - skill
              - created_date
              - created_by
              - inspection
              - image_urls
              - video_urls
              - maintenance_reports
              - work_orders
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              feature_id:
                type: string
                description: Maintenance flag's feature ID.
              feature:
                type: string
                description: Maintenance flag's feature name.
              section:
                type: object
                required:
                  - name
                  - name_with_ordinal
                  - type
                description: Section the feature belongs to.
                properties:
                  name:
                    type: string
                    description: The section name for the feature
                    example: Hallway
                  name_with_ordinal:
                    type: string
                    description: The section name with ordinal for the feature. This is only
                      different from section for rooms like Bedroom, Bathroom,
                      etc
                    example: First Bedroom
                  type:
                    type: string
                    description: The section type for the feature as it was defined in the template
                    enum:
                      - room
                      - section
                    example: room
              note:
                type: string
                description: Maintenance flag's notes
              internal_note:
                type: string
                description: Maintenance flag's internal note.
              skill:
                type: string
                description: Maintenance flag's required skill.
              cost:
                type: number
                description: Maintenance flag's cost
              responsibility:
                type: string
                description: Maintenance flag's responsibility
                enum:
                  - OWNER
                  - RESIDENT
                  - ""
              vendor:
                type: string
                description: Vendor the flag is routed to.
                enum:
                  - Latchel
                  - AppFolio
                  - Yardi
                  - Jenark
                  - RentCheck
                  - Rent Manager
                  - Rentvine
              created_date:
                type: string
                description: Flag creation date, ISO format.
                example: 2022-01-15T00:00:00.000
              created_by:
                type: object
                description: Basic identifying info for the user that created the flag.
                required:
                  - id
                  - name
                  - email
                  - type
                properties:
                  id:
                    type: string
                    description: Entity ID
                    example: 00BRcZPSakXz6w7RYoE
                  name:
                    type: string
                  email:
                    type: string
                  type:
                    type: string
                    enum:
                      - Renter
                      - Property Manager
                      - Landlord
                      - System
                    description: User type discriminator.
              image_urls:
                type: array
                description: URLs of images related to the flag.
                items:
                  type: string
              video_urls:
                type: array
                description: URLs of videos related to the flag.
                items:
                  type: string
              custom_fields:
                type: array
                description: Custom-field values captured for this flag.
                items:
                  type: object
                  required:
                    - id
                    - value
                  properties:
                    id:
                      type: string
                      description: Stable identifier of the custom field (chosen by the user when the
                        field was created).
                    value:
                      type: array
                      description: Selected/entered values. Always an array of strings even for
                        single-select fields.
                      items:
                        type: string
              inspection:
                type: object
                required:
                  - id
                  - label
                  - due_date
                  - inspection_template
                properties:
                  id:
                    type: string
                    description: Entity ID
                    example: 00BRcZPSakXz6w7RYoE
                  label:
                    type: string
                    description: Inspection label.
                  due_date:
                    type: string
                    description: The inspection due date (legacy) is the original date by which the
                      recipient must complete the inspection.
                    example: 2021-12-02T14:15:37.925
                  inspection_template:
                    type: object
                    required:
                      - id
                      - name
                      - internal_label
                      - property_type
                      - display_name
                    properties:
                      id:
                        type: string
                        description: Inspection template identifier
                      name:
                        type: string
                        description: Inspection template name.
                      internal_label:
                        type: string
                        description: Inspection template internal label.
                      property_type:
                        type: string
                        description: Inspection template property type.
                      display_name:
                        type: string
                        description: Resolved display name for the inspection template, used by clients
                          to render the template title (mirrors `name` for
                          legacy templates).
                description: Basic info for the inspection the flag belongs to.
              maintenance_reports:
                type: array
                description: Maintenance reports the flag has been included in. Retained for
                  schema compatibility, but always emitted as `[]`.
                items:
                  type: object
                  required:
                    - id
                    - name
                  properties:
                    id:
                      type: string
                      description: Entity ID
                      example: 00BRcZPSakXz6w7RYoE
                    name:
                      type: string
              work_orders:
                type: array
                description: Work orders the flag has been included in.
                items:
                  type: object
                  required:
                    - id
                    - name
                  properties:
                    id:
                      type: string
                      description: Entity ID
                      example: 00BRcZPSakXz6w7RYoE
                    name:
                      type: string
    api_inspection_creator:
      type: object
      required:
        - id
        - name
        - email
        - status
      properties:
        id:
          type: string
          description: Creator user ID
        name:
          type: string
          description: Creator display name
        email:
          type: string
          description: Creator email address
        status:
          type: string
          enum:
            - active
            - inactive
          description: Whether the creator is currently an active member of the organization
    api_inspection_event:
      x-internal: true
      type: object
      required:
        - id
        - type
        - shows_in_timeline
        - timestamp
        - user
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        type:
          type: string
          enum:
            - rooms_added
            - inspection_reopened
            - inspection_status_changed
            - inspection_shared
            - maintenance_report_shared
            - inspection_signature_requested
            - inspection_signature_request_fulfilled
            - inspection_reminder_sent
            - inspection_reminder_sent_sms
            - inspection_reminder_sent_scheduled
            - inspection_requested
            - inspection_request_accepted
            - maintenance_report_created
            - inspection_edited
            - inspection_notification_sent
            - inspection_marked_as_completed
            - inspection_review_request_sent
            - inspection_invitee_self_removed
          description: Inspection event type discriminator.
        metadata:
          type: object
          description: Free-form metadata. Shape varies by `type`.
          additionalProperties: true
        shows_in_timeline:
          type: boolean
          description: Whether the event should be rendered in the inspection timeline UI.
        timestamp:
          type: string
          format: date-time
          description: ISO-8601 timestamp at which the event occurred.
        user:
          type: object
          description: Basic identifying info for the user who triggered the event.
          required:
            - id
            - name
            - email
            - type
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: Full name of the user.
            email:
              type: string
              description: Email address of the user.
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
              description: User type discriminator.
    inspection_event_create_request_model:
      x-internal: true
      type: object
      required:
        - type
      properties:
        type:
          type: string
          enum:
            - rooms_added
            - inspection_reopened
            - inspection_status_changed
            - inspection_shared
            - maintenance_report_shared
            - inspection_signature_requested
            - inspection_signature_request_fulfilled
            - inspection_reminder_sent
            - inspection_reminder_sent_sms
            - inspection_reminder_sent_scheduled
            - inspection_requested
            - inspection_request_accepted
            - maintenance_report_created
            - inspection_edited
            - inspection_notification_sent
            - inspection_marked_as_completed
            - inspection_review_request_sent
          description: Event discriminator. The `inspection_invitee_self_removed` value is
            generated server-side and is rejected on POST.
        metadata:
          type: object
          description: Free-form metadata. Shape varies by `type`.
          additionalProperties: true
        shows_in_timeline:
          type: boolean
          description: Whether the event should be rendered in the inspection timeline UI.
            Defaults to the server-side default.
    inspection_events_get_all_response_model:
      x-internal: true
      type: array
      items:
        $ref: "#/components/schemas/api_inspection_event"
    api_reminder_recipient:
      x-internal: true
      type: object
      required:
        - email
        - inspectionId
        - inviteId
        - phoneNumbers
        - additionalPhoneNumbers
        - selected
      properties:
        email:
          type: string
          description: Email address to send the reminder to.
        name:
          type: string
          description: Recipient name. Present when a matching tenant record exists;
            absent for invite-only recipients.
        inspectionId:
          type: string
          description: The inspection the reminder is for.
          example: 00BRcZPSakXz6w7RYoE
        tenantId:
          type: string
          description: Tenant id linked to the reminder recipient. Absent when no matching
            tenant record exists for the invite email.
          example: 00BRcZPSakXz6w7RYoE
        inviteId:
          type: string
          description: The invite the reminder is for.
          example: 00BRcZPSakXz6w7RYoE
        phoneNumbers:
          type: array
          items:
            type: string
          description: Tenant phone numbers, normalized to international format. Empty
            array when no tenant is matched or the tenant has none stored.
        additionalPhoneNumbers:
          type: array
          items:
            type: string
          description: Any additional phone numbers that should also receive an SMS
            reminder. Always returned as an empty array by `POST
            /v3/inspections/reminder-recipients`; populated client-side before
            posting to the send-reminders endpoint.
        selected:
          type: boolean
          description: Whether the recipient is currently selected to receive the
            reminder. Defaults to `true` from the recipients endpoint.
    inspection_reminder_recipients_request_model:
      x-internal: true
      type: object
      required:
        - inspectionIds
      properties:
        inspectionIds:
          type: array
          items:
            type: string
            description: Inspection id to resolve reminder recipients for.
            example: 00BRcZPSakXz6w7RYoE
          description: Inspection ids to resolve reminder recipients for. Empty arrays are
            accepted and return an empty `data` list. Duplicates are deduped
            before fetching.
    api_inspection_digest:
      type: object
      required:
        - id
        - property
        - inspection_template
        - inspection_status
        - effective_due_date
        - completed_date
        - archived
        - progress_pct
        - features_ratings
        - permissions
        - assigned_recipients
        - metadata
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        property:
          type: object
          required:
            - id
            - address
            - city
            - zip_code
            - type
          description: Property snapshot embedded in the inspection digest. Carries the
            `type` discriminator (and optionally `x_id`) — distinct from the
            leaner basic-property shape used elsewhere.
          properties:
            id:
              type: string
              description: Property id
              example: 00BRcZPSakXz6w7RYoE
            x_id:
              type: string
              description: External property ID from a synced integration, when set.
            address:
              type: string
              description: Property street address
              example: 2001 Red Gates
            address_2:
              type: string
              description: Property additional address info
              example: Apt. 4B
            city:
              type: string
              description: Property city
              example: New Orleans
            region:
              type: string
              description: Property region
              example: Orleans
            zip_code:
              type: string
              description: Property zip code
              example: "70130"
            type:
              type: string
              description: Property's type discriminator.
              enum:
                - Single-Family
                - Multi-Family
                - Apartment or Condo
                - Commercial
                - Building
                - Community
                - ""
        inspection_template:
          type: object
          required:
            - id
            - name
            - internal_label
            - property_type
            - display_name
          properties:
            id:
              type: string
              description: Inspection template identifier
            name:
              type: string
              description: Inspection template name.
            internal_label:
              type: string
              description: Inspection template internal label.
            property_type:
              type: string
              description: Inspection template property type.
            display_name:
              type: string
              description: Resolved display name for the inspection template, used by clients
                to render the template title (mirrors `name` for legacy
                templates).
        inspection_status:
          type: string
          description: Inspection status represents the actual status of the current
            inspection.
          enum:
            - Scheduled
            - Not Started
            - Started
            - In Review
            - Completed
            - Awaiting Review
            - Revision Requested
            - Revision Review
            - Approved
        inspection_label:
          type: string
        effective_due_date:
          type: string
          description: The inspection’s effective due date is whichever due date applies —
            original, revision, or review — whether it’s already passed or still
            upcoming.
          example: 2021-12-02T14:15:37.925
        completed_date:
          type: string
          description: Completed Date is the date that the user submitted the inspection
            for approval or completed the inspection if it was self-performed.
          example: 2021-12-02T14:15:37.925
        fast_track:
          type: boolean
        archived:
          type: boolean
          description: Whether the inspection is archived
        progress_pct:
          type: number
          description: Percentage of how much the inspection is already completed
        features_ratings:
          type: object
          required:
            - total_quantity
            - good_quantity
            - fair_quantity
            - poor_quantity
          properties:
            total_quantity:
              type: number
            good_quantity:
              type: number
            fair_quantity:
              type: number
            poor_quantity:
              type: number
        permissions:
          type: object
          required:
            - can_archive
            - can_unarchive
            - can_delete
            - can_edit_due_date
            - can_send_reminder
            - can_edit_label
            - can_copy_inspection_deeplink
            - can_make_unit_edits
            - can_re_open
            - can_remove_non_review_revision_media
          description: Permissions available to the current user on this inspection.
            `can_manual_sync` is only present on the getById endpoint.
          properties:
            can_archive:
              type: boolean
            can_unarchive:
              type: boolean
            can_delete:
              type: boolean
            can_edit_due_date:
              type: boolean
            can_send_reminder:
              type: boolean
            can_edit_label:
              type: boolean
            can_copy_inspection_deeplink:
              type: boolean
            can_make_unit_edits:
              type: boolean
            can_re_open:
              type: boolean
            can_remove_non_review_revision_media:
              type: boolean
            can_manual_sync:
              type: boolean
        current_performer:
          type: object
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
            email:
              type: string
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
        assigned_recipients:
          type: object
          required:
            - type
          description: Recipients assigned to perform / receive this inspection.
          properties:
            type:
              type: string
              description: The type that defines who has access to the inspection
              enum:
                - residents
                - self-perform
                - emails
                - teammates
                - myself
              example: emails
            emails:
              type: array
              description: Specific emails assigned to this inspection when type is emails
              items:
                type: string
            resident_type:
              type: string
              description: Type of residents assigned to this inspection when type is
                residents
              enum:
                - current
                - future
              example: current
        metadata:
          type: object
          required:
            - ai_damage_scanned
            - has_maintenance_flags
          properties:
            ai_damage_scanned:
              type: boolean
            ai_damage_detected:
              type: boolean
            has_maintenance_flags:
              type: boolean
    api_inspection_permissions:
      type: object
      required:
        - can_create_unit_inspection
        - can_create_building_inspection
        - can_create_community_inspection
        - can_assign_inspection_to_residents
      description: User-level permissions for creating and assigning inspections.
      properties:
        can_create_unit_inspection:
          type: boolean
        can_create_building_inspection:
          type: boolean
        can_create_community_inspection:
          type: boolean
        can_assign_inspection_to_residents:
          type: boolean
        renter:
          type: object
          required:
            - show_create_inspection_card
            - show_invite_code_card
          description: Renter-specific flags. Only present when the requesting user is a
            renter.
          properties:
            show_create_inspection_card:
              type: boolean
            show_invite_code_card:
              type: boolean
    api_property_digest:
      type: object
      required:
        - id
        - address
        - city
        - zip_code
        - property_type
        - team
        - last_completed_inspection
        - permissions
        - rooms_info
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        address:
          type: string
          description: This is the street address for the <<resource>>.
          example: 2001 Red Gates
        address_2:
          type: string
          description: Additional address information for the <<resource>>.
          example: Apt. 4B
        city:
          type: string
          description: This is the city of the <<resource>>.
          example: New Orleans
        region:
          type: string
          description: The broader region or area in which the <<resource>> is located.
          example: Orleans
        zip_code:
          type: string
          description: This is the Postal Code for the <<resource>>.
          example: "70130"
        name:
          type: string
          description: Human-friendly property name. Optional — when the underlying
            property has no name the field is omitted from the response rather
            than serialised as `null` (commonly the case for units).
        property_type:
          type: string
          description: Discriminator value identifying this property as a Unit, Building,
            or Community. The empty string represents an unset unit subtype.
          enum:
            - Single-Family
            - Multi-Family
            - Apartment or Condo
            - Commercial
            - Building
            - Community
            - ""
        team:
          type: object
          required:
            - id
            - name
            - internal_label
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: The name field is the Team name that is displayed on recipient
                facing reports and communication.
              example: The best company
            internal_label:
              type: string
              description: The internal label is a convenience label to help differentiate
                similar teams a user may belong to. It's displayed to you and
                your team members.
              example: The best company label
          nullable: true
        last_completed_inspection:
          type: object
          nullable: true
          required:
            - date
            - id
            - type
          properties:
            date:
              type: string
              description: Completion date of the last completed inspection
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            type:
              type: string
        rooms_info:
          type: array
          description: Structured per-room metadata and the source of truth for room
            labeling. `rooms` and `room_names` are derived from this. `type` is
            the no-ordinal room kind (e.g. "Bedroom"), `name` is the ordinal
            name (e.g. "First Bedroom"), and `label` is a human-meaningful label
            that defaults to `name`.
          items:
            $ref: "#/components/schemas/room_info"
        permissions:
          type: object
          required:
            - can_delete
            - can_create_inspection
            - can_add_to_parent_property
            - can_reassign_team
          properties:
            can_delete:
              type: boolean
            can_create_inspection:
              type: boolean
            can_add_to_parent_property:
              type: boolean
            can_reassign_team:
              type: boolean
        sync_data:
          type: object
          readOnly: true
          nullable: true
          required:
            - vendor
            - last_sync
          description: Sync data for the property, or `null` when the property is not
            synced from an external integration, with `is_recently_synced` set
            to `true` when `last_sync` is within the last 7 days.
          properties:
            is_recently_synced:
              type: boolean
            last_sync:
              type: string
              description: Sync date, in ISO format
              example: 2022-12-02T14:15:37.925
            vendor:
              type: string
              description: The third party vendor for this sync
              enum:
                - rentmanager
                - yardi
                - appfolio
                - jenark
                - rentvine
                - buildium
            status:
              type: string
              description: Latest manual-sync status. Only present on integrations that expose
                it (e.g. Buildium).
              enum:
                - scheduled
                - processing
                - success
                - error
            error:
              type: string
              nullable: true
              description: Error message from the most recent sync attempt, if any.
            id:
              oneOf:
                - type: string
                - type: integer
              description: External vendor id for the property/unit (string for most vendors;
                integer for AppFolio).
            x_id:
              type: string
              description: Vendor-native external id echo for the legacy Rentvine / Rent
                Manager envelope. Kept for debugging/audit parity with the
                historic `sync_data`.
            location_id:
              type: string
              description: Rent Manager location (property) id — legacy `sync_data`,
                debugging/audit.
            integration_id:
              type: string
              description: RentCheck integration record id (Rent Manager legacy sync) —
                debugging/audit.
            short_property_id:
              type: string
              description: AppFolio short property id, when synced from AppFolio.
            short_unit_id:
              type: string
              description: AppFolio short unit id, when synced from AppFolio.
            upload_response:
              type: array
              description: AppFolio per-destination inspection-upload outcomes.
              items:
                type: object
                required:
                  - resource
                  - resource_id
                  - uploaded_id
                properties:
                  resource:
                    type: string
                    enum:
                      - units
                      - properties
                      - occupancies
                  resource_id:
                    type: string
                  uploaded_id:
                    type: string
    v3_property_list_response_model:
      type: object
      required:
        - status
        - data
        - count
        - total_results
      properties:
        status:
          type: integer
          description: HTTP status code
          example: 200
        data:
          type: array
          items:
            $ref: "#/components/schemas/api_property_digest"
        count:
          type: integer
          description: Amount of elements in `data`.
        total_results:
          type: integer
          description: Amount of total elements according to search criteria.
    api_properties_permissions:
      type: object
      required:
        - can_import_units
      properties:
        can_import_units:
          type: boolean
          description: Whether the current user can import units in bulk
    api_work_order_skill:
      type: object
      required:
        - name
        - latchel
      properties:
        name:
          type: string
          description: RentCheck-facing name of the skill.
        latchel:
          type: object
          description: Latchel category mapping for the skill.
          required:
            - id
            - name
            - category_id
            - category_name
          properties:
            id:
              type: integer
              description: Latchel skill id.
            name:
              type: string
              description: Latchel-facing skill name.
            category_id:
              type: integer
              description: Latchel category id.
            category_name:
              type: string
              description: Latchel category name.
    api_file_request:
      type: object
      required:
        - id
        - document_id
        - document_type
        - export_type
        - status
        - created_date
        - updated_date
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        document_id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        document_type:
          type: string
          description: Name of the report to be generated.
          enum:
            - INSPECTION_REPORT
            - MAINTENANCE_REPORT
            - WORK_ORDER
            - INSPECTION_IMAGES
            - BUILDINGS_LIST_EXPORT
            - COMMUNITIES_LIST_EXPORT
            - INSPECTION_LIST_EXPORT
            - MAINTENANCE_REPORTS_LIST_EXPORT
            - MAINTENANCE_FLAGS_LIST_EXPORT
            - UNITS_LIST_EXPORT
            - SUBSCRIPTION_REPORT_LIST_EXPORT
          example: INSPECTION_REPORT
        export_type:
          type: string
          description: Format of the exported report.
          enum:
            - PDF
            - ZIP
            - CSV
          example: PDF
        status:
          type: string
          description: Current status of the file request.
          enum:
            - CREATED
            - PROCESSING
            - DONE
            - ERROR
            - THROTTLED
          example: DONE
        error_message:
          type: string
          description: Error message describing why the file request failed, when status
            is `ERROR`.
        file_location:
          type: string
          format: uri
          description: URL where the generated document can be downloaded once the request
            completes. Only present once the request has produced a file (i.e.
            the underlying `file_name` is set server-side).
        created_date:
          type: string
          format: date-time
          description: ISO-8601 timestamp at which the file request was created.
        updated_date:
          type: string
          format: date-time
          description: ISO-8601 timestamp of the most recent update to the file request.
    file_request_v1_get_by_id_response_model:
      x-internal: true
      type: object
      required:
        - id
        - document_id
        - document_type
        - export_type
        - user_id
        - org_id
        - status
        - created_date
        - updated_date
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        document_id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        document_type:
          type: string
          description: Name of the report to be generated.
          enum:
            - INSPECTION_REPORT
            - MAINTENANCE_REPORT
            - WORK_ORDER
            - INSPECTION_IMAGES
            - BUILDINGS_LIST_EXPORT
            - COMMUNITIES_LIST_EXPORT
            - INSPECTION_LIST_EXPORT
            - MAINTENANCE_REPORTS_LIST_EXPORT
            - MAINTENANCE_FLAGS_LIST_EXPORT
            - UNITS_LIST_EXPORT
            - SUBSCRIPTION_REPORT_LIST_EXPORT
          example: INSPECTION_REPORT
        export_type:
          type: string
          description: Format of the exported report.
          enum:
            - PDF
            - ZIP
            - CSV
          example: PDF
        user_id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        org_id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        status:
          type: string
          description: Current status of the file request.
          enum:
            - CREATED
            - PROCESSING
            - DONE
            - ERROR
            - THROTTLED
          example: DONE
        error_message:
          type: string
          description: Error message describing why the file request failed, when status
            is `ERROR`.
        created_date:
          type: integer
          format: int64
          description: Epoch-millisecond timestamp at which the file request was created.
            The getById endpoint returns the raw stored document so this is a
            `number`, not the ISO string returned by `GET /v1/fileRequests`.
        updated_date:
          type: integer
          format: int64
          description: Epoch-millisecond timestamp of the most recent update. Same caveat
            as `created_date`.
        file_name:
          type: string
          description: Server-side filename for the generated artifact, used to derive the
            download URL.
        file_location:
          type: string
          format: uri
          description: URL where the generated document can be downloaded once the request
            completes. Only present once `file_name` is set.
        dont_notify:
          type: boolean
          description: When `true`, the system will not notify the requesting user when
            the artifact is ready.
        filters:
          type: object
          additionalProperties: true
          description: Per-document-type filter payload that was used to produce the
            artifact (shape varies by `document_type`).
        params:
          type: object
          description: Additional parameters captured at request time (e.g. share
            recipients for an inspection PDF).
          properties:
            send_to:
              type: array
              items:
                type: string
                format: email
            note:
              type: string
    api_inspection_request:
      type: object
      required:
        - id
        - created_date
        - status
        - type
        - sent_by
        - received_by
        - inspection
        - property
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        created_date:
          type: string
          description: Request creation timestamp, in ISO format.
        status:
          type: string
          enum:
            - REQUEST_SENT
            - REQUEST_ACCEPTED
            - REQUEST_DECLINED
            - REQUEST_FULFILLED
          description: Lifecycle state of the signature request.
        type:
          type: string
          enum:
            - ADDITIONAL_SIGNATURE
          description: Type of inspection request.
        sent_by:
          type: object
          required:
            - id
            - name
            - email
            - type
          description: User that created the request.
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: Name of the user
            email:
              type: string
              description: Email of the user
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
              description: User type discriminator.
        received_by:
          type: object
          required:
            - email
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            email:
              type: string
              description: Email the request was sent to.
            name:
              type: string
              description: Recipient name, when known.
        inspection:
          type: object
          required:
            - id
            - label
            - due_date
            - inspection_template
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            label:
              type: string
              description: Inspection label.
            due_date:
              type: string
              description: The inspection due date (legacy) is the original date by which the
                recipient must complete the inspection.
              example: 2021-12-02T14:15:37.925
            inspection_template:
              type: object
              required:
                - id
                - name
                - internal_label
                - property_type
                - display_name
              properties:
                id:
                  type: string
                  description: Inspection template identifier
                name:
                  type: string
                  description: Inspection template name.
                internal_label:
                  type: string
                  description: Inspection template internal label.
                property_type:
                  type: string
                  description: Inspection template property type.
                display_name:
                  type: string
                  description: Resolved display name for the inspection template, used by clients
                    to render the template title (mirrors `name` for legacy
                    templates).
        property:
          type: object
          required:
            - id
            - address
            - city
            - zip_code
          description: This is the entity id of the Unit, Building, or Community that the
            entity is for.
          properties:
            id:
              type: string
              description: Selected entity's property id
              example: 00BRcZPSakXz6w7RYoE
            address:
              type: string
              description: Selected entity's property address
              example: 2001 Red Gates
            address_2:
              type: string
              description: Selected entity's property additional address info
              example: Apt. 4B
            city:
              type: string
              description: Selected entity's property city
              example: New Orleans
            region:
              type: string
              description: Selected entity's property region
              example: Orleans
            zip_code:
              type: string
              description: Selected entity's property zip_code
              example: "70130"
        team:
          type: object
          required:
            - id
            - name
            - internal_label
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: The name field is the Team name that is displayed on recipient
                facing reports and communication.
              example: The best company
            internal_label:
              type: string
              description: The internal label is a convenience label to help differentiate
                similar teams a user may belong to. It's displayed to you and
                your team members.
              example: The best company label
    signature_request_create_request_model:
      type: object
      required:
        - emails
      properties:
        emails:
          type: array
          minItems: 1
          items:
            type: string
          description: Email address(es) the additional-signature request should be sent to.
        note:
          type: string
          description: Optional note included with the signature request email.
    signature_request_complete_request_model:
      type: object
      required:
        - platform
      anyOf:
        - title: With Signature Image
          required:
            - signature_base64
          properties:
            platform:
              type: string
              enum:
                - WEB
                - MOBILE
              description: Platform used to capture the signature. Drives some downstream
                rendering.
            signature_base64:
              type: string
              description: Base64-encoded image data for a drawn signature. Mutually exclusive
                with `signature_text`.
            note:
              type: string
              description: Optional note attached to the signed request.
        - title: With Signature Text
          required:
            - signature_text
          properties:
            platform:
              type: string
              enum:
                - WEB
                - MOBILE
              description: Platform used to capture the signature. Drives some downstream
                rendering.
            signature_text:
              type: string
              description: Typed signature text. Mutually exclusive with `signature_base64`.
            note:
              type: string
              description: Optional note attached to the signed request.
    inspection_send_reminders_request_model:
      type: object
      required:
        - recipients
        - channels
        - message
      properties:
        recipients:
          type: array
          description: Reminder recipients and the inspections they belong to.
          items:
            $ref: "#/components/schemas/api_reminder_recipient"
        channels:
          type: array
          description: Channels to send the reminder through.
          items:
            type: string
            enum:
              - email
              - sms
        message:
          type: string
          description: Free-form message included with the reminder.
    inspection_send_reminders_response_model:
      type: object
      description: Per-channel success and error breakdown for the reminder send.
      required:
        - success
        - error
      properties:
        success:
          type: array
          items:
            type: object
            required:
              - destination
              - type
              - events
            properties:
              destination:
                type: array
                items:
                  type: string
                description: Destinations (emails or phone numbers) contacted.
              type:
                type: string
                enum:
                  - email
                  - sms
              events:
                type: array
                description: Inspection email events emitted for this delivery — empty for `sms`
                  deliveries.
                items:
                  type: object
                  required:
                    - email
                  properties:
                    email:
                      type: string
                      description: Email address the inspection notification was sent to.
                    email_record:
                      type: string
                      description: Sendgrid message id stored on the inspection event for delivery
                        tracking.
                    reference_code:
                      type: string
                      description: Reference code attached to the email (used by the invitee to claim
                        the inspection).
        error:
          type: array
          items:
            type: object
            required:
              - destination
              - type
              - reason
            properties:
              destination:
                type: array
                items:
                  type: string
              type:
                type: string
                enum:
                  - email
                  - sms
              reason:
                type: string
    inspection_sign_request_model:
      type: object
      required:
        - signature_path
      properties:
        signature_path:
          type: string
          description: Storage path (bucket + key) where the signature image has been
            uploaded.
        note:
          type: string
          description: Optional note to attach to the signature.
    inspection_submit_request_model:
      type: object
      properties:
        note:
          type: string
          description: Optional note from the performer that will be visible to reviewers.
    inspection_share_request_model:
      type: object
      required:
        - send_to
      properties:
        send_to:
          type: array
          minItems: 1
          items:
            type: string
            format: email
          description: Email addresses that should receive the PDF copy of the inspection
            report.
        note:
          type: string
          description: Optional personal message included alongside the shared inspection.
    inspection_rate_request_model:
      type: object
      required:
        - feedback
        - rating
      properties:
        feedback:
          type: string
          minLength: 3
          description: Free-form feedback left with the rating.
        rating:
          type: integer
          minimum: 1
          maximum: 5
          description: Numeric rating (1-5) for the inspection.
    inspection_rate_response_model:
      type: object
      required:
        - created_date
        - feedback
        - rating
        - inspection
        - property
        - user
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        created_date:
          type: string
          description: Entity creation date, in ISO format
          example: 2022-12-02T14:15:37.925
        feedback:
          type: string
        rating:
          type: string
          description: Rating persisted on the inspection. The POST body accepts a numeric
            1-5 rating; the stored value is serialized back as a string.
          example: "5"
        inspection:
          type: object
          required:
            - id
            - label
            - due_date
            - inspection_template
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            label:
              type: string
              description: Inspection label.
            due_date:
              type: string
              description: The inspection due date (legacy) is the original date by which the
                recipient must complete the inspection.
              example: 2021-12-02T14:15:37.925
            inspection_template:
              type: object
              required:
                - id
                - name
                - internal_label
                - property_type
                - display_name
              properties:
                id:
                  type: string
                  description: Inspection template identifier
                name:
                  type: string
                  description: Inspection template name.
                internal_label:
                  type: string
                  description: Inspection template internal label.
                property_type:
                  type: string
                  description: Inspection template property type.
                display_name:
                  type: string
                  description: Resolved display name for the inspection template, used by clients
                    to render the template title (mirrors `name` for legacy
                    templates).
        property:
          type: object
          required:
            - id
            - address
            - city
            - zip_code
          description: This is the entity id of the Unit, Building, or Community that the
            entity is for.
          properties:
            id:
              type: string
              description: Selected entity's property id
              example: 00BRcZPSakXz6w7RYoE
            address:
              type: string
              description: Selected entity's property address
              example: 2001 Red Gates
            address_2:
              type: string
              description: Selected entity's property additional address info
              example: Apt. 4B
            city:
              type: string
              description: Selected entity's property city
              example: New Orleans
            region:
              type: string
              description: Selected entity's property region
              example: Orleans
            zip_code:
              type: string
              description: Selected entity's property zip_code
              example: "70130"
        user:
          type: object
          required:
            - id
            - name
            - email
            - type
          description: Basic user identity fields.
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: Name of the user
            email:
              type: string
              description: Email of the user
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
              description: User type discriminator.
    inspection_reject_request_model:
      type: object
      required:
        - rejection
      properties:
        rejection:
          type: object
          required:
            - note
            - due_date
            - features
          properties:
            note:
              type: string
              description: Note about the inspection's rejection
              example: Test
            due_date:
              type: string
              description: ISO Date of the inspections due date
              example: 2022-01-15T00:00:00.000
            features:
              type: array
              description: Features to include in the rejection (each entry identifies a
                single feature plus the section it lives in).
              items:
                type: object
                required:
                  - id
                  - name
                  - section
                properties:
                  id:
                    type: string
                    description: Entity ID
                    example: 00BRcZPSakXz6w7RYoE
                  name:
                    type: string
                    description: Display name of the feature.
                  section:
                    type: object
                    required:
                      - name
                      - name_with_ordinal
                      - type
                    description: Section the feature belongs to (room or arbitrary section).
                    properties:
                      name:
                        type: string
                        description: Display name of the section.
                      name_with_ordinal:
                        type: string
                        description: Display name of the section including its ordinal index (e.g.
                          "Bedroom (1)").
                      type:
                        type: string
                        enum:
                          - room
                          - section
                        description: Whether the section is a room (auto-generated from the property
                          layout) or a free-form section.
    inspection_reject_response_model:
      type: object
      required:
        - sentEmails
      properties:
        sentEmails:
          type: array
          description: Inspection email events generated for each recipient notified of
            the rejection.
          items:
            type: object
            required:
              - email
            properties:
              email:
                type: string
                description: Email address the inspection notification was sent to.
              email_record:
                type: string
                description: Sendgrid message id stored on the inspection event for delivery
                  tracking.
              reference_code:
                type: string
                description: Reference code attached to the email (used by the invitee to claim
                  the inspection).
    inspection_remove_renter_request_model:
      type: object
      required:
        - reason
      properties:
        reason:
          type: string
          enum:
            - moved-out
            - duplicate
          description: Reason for removing the current performer (renter) from this
            inspection.
    properties_merge_request_model:
      type: object
      properties:
        transfer_rooms:
          type: boolean
          description: When `true`, the rooms of the origin property are merged onto the
            target property.
          default: false
    dashboard_get_all_response_model:
      x-internal: true
      type: object
      properties:
        stats:
          type: object
          properties:
            all_star_status:
              type: string
              enum:
                - Rookie
                - Rising Star
                - RentCheck All Star
              description: Status given by RentCheck of your account, based on completion rate
                and risk value
            total_inspections:
              type: number
              description: Total amount of inspections within the date range
            overall_total_inspections:
              type: number
              description: Total amount of inspections
            total_inspections_on_time:
              type: number
              description: Total amount of inspections completed on time within the date range
            total_rejected_inspections:
              type: number
              description: Total amount of inspections rejected within the date range
            total_inspection_ratings:
              type: number
              description: Total amount of inspections with ratings within the date range
            average_inspection_rating:
              type: number
              description: Average rating of inspections completed within the date range
            total_properties:
              type: number
              description: Total amount of properties
            total_inspections_completed:
              type: number
              description: Total amount of inspections completed within the date range
            total_requested_inspections:
              type: number
              description: Total amount of requested inspections within the date range
            total_approved_inspections:
              type: number
              description: Total amount of inspections approved within the date range
            total_requested_inspections_completed:
              type: number
              description: Total amount of requested inspections completed within the date
                range
            total_internal_inspections_completed:
              type: number
              description: Total amount of internal inspections completed within the date
                range
        todo_list:
          type: object
          properties:
            inspections_awaiting_review_count:
              type: number
              description: Total amount of inspections awaiting review
            overdue_inspections_count:
              type: number
              description: Total amount of overdue inspections
            almost_due_inspections_count:
              type: number
              description: Total amount of almost due inspections
            undelivered_emails_count:
              type: number
              description: Total amount of inspections with undelivered emails
            revision_requested_inspections_count:
              type: number
              description: Total amount of inspections with revision requested
            awaiting_signature_inspections_count:
              type: number
              description: Total amount of inspections awaiting signature
            properties_at_risk_count:
              type: number
              description: Total amount of properties at risk
            unassigned_inspections_count:
              type: number
              description: Total amount of inspections that are not yet assigned to a
                performer.
        last_updated_date:
          type: string
          description: Last dashboard data updated date, in ISO format
          example: 2022-12-02T14:15:37.925
    dashboard_get_resource_response_model:
      x-internal: true
      type: object
      description: Envelope returned from `GET /v1/dashboard/{todoItem}`, with a
        single nested `data` array plus `totalResults`. When `todoItem ===
        "propertiesAtRisk"` the items are properties; for every other
        `todoItem`, the items are inspection digests.
      required:
        - data
        - totalResults
      properties:
        data:
          type: array
          items:
            anyOf:
              - type: object
                properties:
                  id:
                    type: string
                    description: Entity ID
                    example: 00BRcZPSakXz6w7RYoE
                  property_id:
                    type: string
                    description: Entity ID
                    example: 00BRcZPSakXz6w7RYoE
                  property:
                    type: object
                    required:
                      - id
                      - address
                      - city
                      - zip_code
                    description: This is the entity id of the Unit, Building, or Community that the
                      entity is for.
                    properties:
                      id:
                        type: string
                        description: Selected entity's property id
                        example: 00BRcZPSakXz6w7RYoE
                      address:
                        type: string
                        description: Selected entity's property address
                        example: 2001 Red Gates
                      address_2:
                        type: string
                        description: Selected entity's property additional address info
                        example: Apt. 4B
                      city:
                        type: string
                        description: Selected entity's property city
                        example: New Orleans
                      region:
                        type: string
                        description: Selected entity's property region
                        example: Orleans
                      zip_code:
                        type: string
                        description: Selected entity's property zip_code
                        example: "70130"
                  inspection_status:
                    type: string
                    description: Inspection status represents the actual status of the current
                      inspection.
                    enum:
                      - Scheduled
                      - Not Started
                      - Started
                      - In Review
                      - Completed
                      - Awaiting Review
                      - Revision Requested
                      - Revision Review
                      - Approved
                  inspection_type:
                    type: string
                    description: Inspection Type designates which type of inspection will be issued.
                      Some inspection types require the user to select which
                      features apply to the inspection. These features are
                      designated in the 'selected_features' field.
                    enum:
                      - Move-in
                      - Move-out
                      - Annual
                      - Turn
                      - Periodic
                      - Maintenance Check
                      - Exterior
                      - Pre-Acquisition
                      - RV Lot
                      - Maintenance / Safety Check
                      - Community Maintenance Check
                  inspection_label:
                    type: string
                  due_date:
                    type: string
                    description: The inspection due date (legacy) is the original date by which the
                      recipient must complete the inspection.
                    example: 2021-12-02T14:15:37.925
                  completed_date:
                    type: string
                    description: Completed Date is the date that the user submitted the inspection
                      for approval or completed the inspection if it was
                      self-performed.
                    example: 2021-12-02T14:15:37.925
                  approval:
                    type: boolean
                    description: Weather or not this inspection has been approved
                  rejection:
                    type: boolean
                    description: Weather or not this inspection has been rejected
                  fast_track:
                    type: boolean
              - type: object
                description: Property row returned when `todoItem === "propertiesAtRisk"`, with
                  `last_completed_inspection` flattened into `{ id, date }`.
                additionalProperties: true
        totalResults:
          type: integer
          description: Total number of items matching the query, ignoring pagination.
    maintenance_report_create_request_model:
      type: object
      required:
        - name
        - organize_by
        - flags
      properties:
        name:
          type: string
          description: A name for the new report
          example: Move-out maintenance report
        organize_by:
          type: string
          description: Group to display when viewing the report
          example: responsibility
          enum:
            - responsibility
            - room
            - skill
        flags:
          type: array
          description: A list of maintenance flag ids and feature ids. An empty `flags`
            array results in a 500 rather than a 400 — no minimum length is
            enforced.
          items:
            type: object
            required:
              - id
              - feature_id
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              feature_id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
    inspection_feature_response_model:
      type: object
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        inspection_id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        created_date:
          type: string
          description: Entity creation date, in ISO format
          example: 2022-12-02T14:15:37.925
        updated_date:
          type: string
          description: Entity last update date, in ISO format
          example: 2023-10-04T14:15:37.925
        name:
          type: string
          description: The feature name
        section:
          type: object
          properties:
            name:
              type: string
              description: The section name
            name_with_ordinal:
              type: string
              description: The section name with ordinal
            type:
              type: string
              enum:
                - room
                - section
        notes:
          type: string
          description: The notes of the feature
        not_applicable:
          type: boolean
          description: Flag to check if the feature should be applicable or not
        rating:
          type: string
          description: The rating given to the feature
          enum:
            - Poor
            - Good
            - Fair
            - ""
        images:
          type: array
          description: Feature images
          items:
            type: string
            description: URL links to images related to the feature
        property:
          type: object
          description: Property associated with the inspection
          properties:
            id:
              type: string
              description: Selected entity's property id
              example: 00BRcZPSakXz6w7RYoE
            address:
              type: string
              description: Selected entity's property address
              example: 2001 Red Gates
            address_2:
              type: string
              description: Selected entity's property additional address info
              example: Apt. 4B
            city:
              type: string
              description: Selected entity's property city
              example: New Orleans
            region:
              type: string
              description: Selected entity's property region
              example: Orleans
            zip_code:
              type: string
              description: Selected entity's property zip_code
              example: "70130"
        responses:
          type: array
          description: Selected response IDs / free-text answers captured for this
            feature, in selection order.
          items:
            type: string
        maintenance_flags:
          type: array
          description: A list of maintenance flags. Items match
            `maintenance_flag_response_model` — `feature_name` is the feature
            name, `created_by` is the full creator object.
          items:
            $ref: "#/components/schemas/maintenance_flag_response_model"
        questions:
          type: array
          description: List of questions and their responses
          items:
            type: object
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              title:
                type: string
                description: Question title
              type:
                type: string
                description: Question type
                enum:
                  - yes/no
                  - free text
                  - multiple choice
                  - asset capture
              answer_type:
                type: string
                description: Answer type
                enum:
                  - single
                  - multi
              asset_category:
                type: string
                description: Asset Category
                enum:
                  - Air Filter
                  - Air Conditioner
                  - Air Handler Unit (AHU)
                  - Barbecue Grill
                  - Bathroom Heater
                  - Cooktop
                  - Condenser
                  - Dishwasher
                  - Dryer
                  - Electric Fireplace
                  - Evaporator
                  - Freezer
                  - Furnace
                  - Furnace (AHU)
                  - Garbage Disposal
                  - Heat Pump
                  - Microwave
                  - Package Unit
                  - Pool Heater
                  - PTAC
                  - Range Hood
                  - Range
                  - Refrigerator
                  - Stackable Laundry
                  - Thermostat
                  - Wall Oven
                  - Washer
                  - Water Heater
                  - Other
              answers:
                type: object
                properties:
                  id:
                    type: string
                    description: Entity ID
                    example: 00BRcZPSakXz6w7RYoE
                  title:
                    type: string
                    description: Answer title
                  requires_photo:
                    type: boolean
                  requires_note:
                    type: boolean
    maintenance_flag_response_model:
      type: object
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        room:
          type: string
          description: Maintenance flag's room.
        feature_name:
          type: string
          description: Maintenance flag's feature name (`feature_name` on the wire —
            aliased from the stored `feature` field).
        feature_id:
          type: string
          description: Maintenance flag's feature ID.
        created_date:
          type: string
          description: Feature creation date, ISO format
          example: 2022-01-15T00:00:00.000
        created_by:
          type: object
          description: User that created the flag.
          required:
            - id
            - name
            - type
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: Name of the user that created the flag.
            type:
              type: string
              description: User type.
        responsibility:
          type: string
          description: Maintenance flag's responsibility
          enum:
            - OWNER
            - RESIDENT
            - ""
        note:
          type: string
          description: Maintenance flag's notes
        internal_note:
          type: string
          description: Maintenance flag's internal note.
        image_urls:
          type: array
          description: Maintenance flag's images
          items:
            type: string
            description: URL links to images related to the maintenance flags
        cost:
          type: number
          description: Maintenance flag's cost
        skill:
          type: string
          description: Maintenance flag's required skill.
        custom_fields:
          type: array
          description: Custom-field values captured for this flag.
          items:
            type: object
            required:
              - id
              - value
            properties:
              id:
                type: string
                description: Stable identifier of the custom field (chosen by the user when the
                  field was created).
              value:
                type: array
                description: Selected/entered values. Always an array of strings even for
                  single-select fields.
                items:
                  type: string
    resident_v2_create_request_model:
      type: object
      required:
        - first_name
        - last_name
        - emails
      properties:
        organization_id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        first_name:
          type: string
          description: The Resident's first name.
          example: Resident Name
        last_name:
          type: string
          description: The Resident's last name.
          example: Resident Lastname
        emails:
          type: array
          description: Resident's emails
          items:
            type: string
          example:
            - resident@email.com
        phone_numbers:
          type: array
          items:
            type: string
          description: Resident's phone numbers
          example:
            - "+17035556390"
        rentvine_sync:
          $ref: "#/components/schemas/rentvine_sync"
        user_id:
          type: string
          description: Optional RentCheck user id to associate with the resident on
            creation.
          example: 00BRcZPSakXz6w7RYoE
    resident_v2_update_request_model:
      type: object
      properties:
        organization_id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        first_name:
          type: string
          description: The Resident's first name.
          example: Resident Name
        last_name:
          type: string
          description: The Resident's last name.
          example: Resident Lastname
        emails:
          type: array
          description: Resident's emails
          items:
            type: string
          example:
            - resident@email.com
          nullable: true
          minItems: 1
        phone_numbers:
          type: array
          items:
            type: string
          description: Resident's phone numbers
          example:
            - "+17035556390"
        rentvine_sync:
          $ref: "#/components/schemas/rentvine_sync"
    resident_v2_response_model:
      type: object
      required:
        - id
        - first_name
        - last_name
        - phone_numbers
        - emails
        - created_date
        - updated_date
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        user:
          type: object
          required:
            - id
            - name
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: User name
        first_name:
          type: string
          description: The Resident's first name.
          example: Resident Name
        last_name:
          type: string
          description: The Resident's last name.
          example: Resident Lastname
        phone_numbers:
          type: array
          items:
            type: string
          description: Resident's phone numbers
          example:
            - "+17035556390"
        emails:
          type: array
          description: Resident's emails
          items:
            type: string
          example:
            - resident@email.com
        owner_id:
          type: string
          description: User ID of the resident entity
          example: 00BRcZPSakXz6w7RYoE
        organization_id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        creation_source:
          type: string
          description: App that perform the entity creation.
          enum:
            - zapier
            - rentmanager
            - flatfile
            - 365connect
            - appfolio
            - yardi
            - rentvine
            - test_app_id
          example: zapier
        created_date:
          type: string
          description: Entity creation date, in ISO format
          example: 2022-12-02T14:15:37.925
        updated_date:
          type: string
          description: Entity last update date, in ISO format
          example: 2023-10-04T14:15:37.925
        deleted:
          type: boolean
          description: Whether the user is deleted or not.
        primary_occupancy:
          type: object
          required:
            - id
            - tenant_id
            - status
            - tags
            - property
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            tenant_id:
              type: string
              description: Entity id of the resident this lease belongs to.
              example: 00BRcZPSakXz6w7RYoE
            status:
              type: string
              description: Indicates if the tenant is a future, past or current one
              enum:
                - future
                - past
                - current
            tags:
              type: array
              description: Tags associated with the lease if it was evicted or is in notice
              items:
                type: string
                enum:
                  - evict
                  - notice
            property:
              type: object
              required:
                - id
                - address
                - city
                - zip_code
              description: This is the entity id of the Unit, Building, or Community that the
                entity is for.
              properties:
                id:
                  type: string
                  description: Selected entity's property id
                  example: 00BRcZPSakXz6w7RYoE
                address:
                  type: string
                  description: Selected entity's property address
                  example: 2001 Red Gates
                address_2:
                  type: string
                  description: Selected entity's property additional address info
                  example: Apt. 4B
                city:
                  type: string
                  description: Selected entity's property city
                  example: New Orleans
                region:
                  type: string
                  description: Selected entity's property region
                  example: Orleans
                zip_code:
                  type: string
                  description: Selected entity's property zip_code
                  example: "70130"
            lease_start:
              type: string
              description: The date the Resident's current lease starts.
              example: 2021-01-15T00:00:00.000
            lease_end:
              type: string
              description: The date the Resident's current lease should end.
              example: 2022-01-15T00:00:00.000
            rent:
              type: number
              description: The dollar amount of the Resident's current rent. Must be ≥ 0.
              example: 100
            security_deposit:
              type: number
              description: Unit security deposit
              example: 1500
            move_out_date:
              type: string
              description: The date that the Resident is scheduled to move out of the current
                Property.
              example: 2022-01-15T00:00:00.000
            move_in_date:
              type: string
              description: The date that the Resident is scheduled to move in to the current
                Property.
              example: 2022-01-15T00:00:00.000
        sync_data:
          oneOf:
            - title: RentManager Sync
              type: object
              required:
                - vendor
                - last_sync
              properties:
                id:
                  type: string
                  description: Entity ID
                  example: 00BRcZPSakXz6w7RYoE
                vendor:
                  type: string
                  enum:
                    - rentmanager
                last_sync:
                  type: string
                  description: The last time the resident was synced with external sources.
                  example: 2024-12-02T14:15:37.925
                x_id:
                  type: string
                location_id:
                  type: string
                integration_id:
                  type: string
                status:
                  type: string
                  description: Latest manual-sync lifecycle state, when the integration exposes
                    it.
                  enum:
                    - scheduled
                    - processing
                    - success
                    - error
                error:
                  type: string
                  nullable: true
            - title: Yardi Sync
              type: object
              required:
                - vendor
                - last_sync
              properties:
                vendor:
                  type: string
                  enum:
                    - yardi
                last_sync:
                  type: string
                  description: The last time the resident was synced with external sources.
                  example: 2024-12-02T14:15:37.925
                code:
                  type: string
                parent_code:
                  type: string
                status:
                  type: string
                  description: Latest manual-sync lifecycle state, when the integration exposes
                    it.
                  enum:
                    - scheduled
                    - processing
                    - success
                    - error
                error:
                  type: string
                  nullable: true
            - title: AppFolio Sync
              type: object
              required:
                - vendor
                - last_sync
              properties:
                vendor:
                  type: string
                  enum:
                    - appfolio
                id:
                  type: string
                  description: Entity ID
                  example: 00BRcZPSakXz6w7RYoE
                short_property_id:
                  type: string
                short_unit_id:
                  type: string
                last_sync:
                  type: string
                  description: The last time the resident was synced with external sources.
                  example: 2024-12-02T14:15:37.925
                status:
                  type: string
                  description: Latest manual-sync lifecycle state, when the integration exposes
                    it.
                  enum:
                    - scheduled
                    - processing
                    - success
                    - error
                error:
                  type: string
                  nullable: true
                upload_response:
                  type: array
                  items:
                    type: object
                    required:
                      - resource
                      - resource_id
                      - uploaded_id
                    properties:
                      resource:
                        type: string
                        enum:
                          - units
                          - properties
                          - occupancies
                      resource_id:
                        type: string
                      uploaded_id:
                        type: string
            - title: Buildium Sync
              type: object
              required:
                - vendor
                - last_sync
              properties:
                vendor:
                  type: string
                  enum:
                    - buildium
                last_sync:
                  type: string
                  description: The last time the resident was synced with external sources.
                  example: 2024-12-02T14:15:37.925
                status:
                  type: string
                  description: Latest manual-sync lifecycle state, when the integration exposes
                    it.
                  enum:
                    - scheduled
                    - processing
                    - success
                    - error
                error:
                  type: string
                  nullable: true
            - title: Jenark Sync
              type: object
              required:
                - vendor
                - last_sync
              properties:
                vendor:
                  type: string
                  enum:
                    - jenark
                last_sync:
                  type: string
                  description: The last time the resident was synced with external sources.
                  example: 2024-12-02T14:15:37.925
                status:
                  type: string
                  description: Latest manual-sync lifecycle state, when the integration exposes
                    it.
                  enum:
                    - scheduled
                    - processing
                    - success
                    - error
                error:
                  type: string
                  nullable: true
            - title: Rentvine Sync
              type: object
              required:
                - vendor
                - last_sync
              properties:
                vendor:
                  type: string
                  enum:
                    - rentvine
                last_sync:
                  type: string
                  description: The last time the resident was synced with external sources.
                  example: 2024-12-02T14:15:37.925
                id:
                  type: string
                  description: Entity ID
                  example: 00BRcZPSakXz6w7RYoE
                x_id:
                  type: string
                status:
                  type: string
                  description: Latest manual-sync lifecycle state, when the integration exposes
                    it.
                  enum:
                    - scheduled
                    - processing
                    - success
                    - error
                error:
                  type: string
                  nullable: true
    resident_v3_response_model:
      type: object
      required:
        - id
        - first_name
        - last_name
        - phone_numbers
        - emails
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        first_name:
          type: string
          description: The Resident's first name.
          example: Resident Name
        last_name:
          type: string
          description: The Resident's last name.
          example: Resident Lastname
        phone_numbers:
          type: array
          items:
            type: string
          description: Resident's phone numbers
          example:
            - "+17035556390"
        emails:
          type: array
          description: Resident's emails
          items:
            type: string
          example:
            - resident@email.com
        sync_data:
          oneOf:
            - title: RentManager Sync
              type: object
              required:
                - vendor
                - last_sync
              properties:
                id:
                  type: string
                  description: Entity ID
                  example: 00BRcZPSakXz6w7RYoE
                vendor:
                  type: string
                  enum:
                    - rentmanager
                last_sync:
                  type: string
                  description: The last time the resident was synced with external sources.
                  example: 2024-12-02T14:15:37.925
                x_id:
                  type: string
                location_id:
                  type: string
                integration_id:
                  type: string
                status:
                  type: string
                  description: Latest manual-sync lifecycle state, when the integration exposes
                    it.
                  enum:
                    - scheduled
                    - processing
                    - success
                    - error
                error:
                  type: string
                  nullable: true
            - title: Yardi Sync
              type: object
              required:
                - vendor
                - last_sync
              properties:
                vendor:
                  type: string
                  enum:
                    - yardi
                last_sync:
                  type: string
                  description: The last time the resident was synced with external sources.
                  example: 2024-12-02T14:15:37.925
                code:
                  type: string
                parent_code:
                  type: string
                status:
                  type: string
                  description: Latest manual-sync lifecycle state, when the integration exposes
                    it.
                  enum:
                    - scheduled
                    - processing
                    - success
                    - error
                error:
                  type: string
                  nullable: true
            - title: AppFolio Sync
              type: object
              required:
                - vendor
                - last_sync
              properties:
                vendor:
                  type: string
                  enum:
                    - appfolio
                id:
                  type: string
                  description: Entity ID
                  example: 00BRcZPSakXz6w7RYoE
                short_property_id:
                  type: string
                short_unit_id:
                  type: string
                last_sync:
                  type: string
                  description: The last time the resident was synced with external sources.
                  example: 2024-12-02T14:15:37.925
                status:
                  type: string
                  description: Latest manual-sync lifecycle state, when the integration exposes
                    it.
                  enum:
                    - scheduled
                    - processing
                    - success
                    - error
                error:
                  type: string
                  nullable: true
                upload_response:
                  type: array
                  items:
                    type: object
                    required:
                      - resource
                      - resource_id
                      - uploaded_id
                    properties:
                      resource:
                        type: string
                        enum:
                          - units
                          - properties
                          - occupancies
                      resource_id:
                        type: string
                      uploaded_id:
                        type: string
            - title: Buildium Sync
              type: object
              required:
                - vendor
                - last_sync
              properties:
                vendor:
                  type: string
                  enum:
                    - buildium
                last_sync:
                  type: string
                  description: The last time the resident was synced with external sources.
                  example: 2024-12-02T14:15:37.925
                status:
                  type: string
                  description: Latest manual-sync lifecycle state, when the integration exposes
                    it.
                  enum:
                    - scheduled
                    - processing
                    - success
                    - error
                error:
                  type: string
                  nullable: true
            - title: Jenark Sync
              type: object
              required:
                - vendor
                - last_sync
              properties:
                vendor:
                  type: string
                  enum:
                    - jenark
                last_sync:
                  type: string
                  description: The last time the resident was synced with external sources.
                  example: 2024-12-02T14:15:37.925
                status:
                  type: string
                  description: Latest manual-sync lifecycle state, when the integration exposes
                    it.
                  enum:
                    - scheduled
                    - processing
                    - success
                    - error
                error:
                  type: string
                  nullable: true
            - title: Rentvine Sync
              type: object
              required:
                - vendor
                - last_sync
              properties:
                vendor:
                  type: string
                  enum:
                    - rentvine
                last_sync:
                  type: string
                  description: The last time the resident was synced with external sources.
                  example: 2024-12-02T14:15:37.925
                id:
                  type: string
                  description: Entity ID
                  example: 00BRcZPSakXz6w7RYoE
                x_id:
                  type: string
                status:
                  type: string
                  description: Latest manual-sync lifecycle state, when the integration exposes
                    it.
                  enum:
                    - scheduled
                    - processing
                    - success
                    - error
                error:
                  type: string
                  nullable: true
        primary_occupancy:
          type: object
          required:
            - id
            - status
            - lease_end
            - lease_start
          description: Present when the tenant has a primary occupancy. `lease_start` /
            `lease_end` are always emitted keys but are `null` when the value is
            empty, so the property is required but nullable.
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            status:
              type: string
              description: Indicates if the tenant is a future, past or current one
              enum:
                - future
                - past
                - current
            lease_start:
              type: string
              description: The date the Resident's current lease starts.
              example: 2021-01-15T00:00:00.000
              nullable: true
            lease_end:
              type: string
              description: The date the Resident's current lease should end.
              example: 2022-01-15T00:00:00.000
              nullable: true
            property:
              type: object
              required:
                - id
                - address
              description: This is the entity id of the Unit, Building, or Community that the
                entity is for. Optional — legacy occupancies can lack property
                data.
              properties:
                id:
                  type: string
                  description: Selected entity's property id
                  example: 00BRcZPSakXz6w7RYoE
                address:
                  type: string
                  description: Selected entity's property address
                  example: 2001 Red Gates
    lease_v2_create_request_model:
      type: object
      required:
        - status
      properties:
        status:
          type: string
          description: Indicates if the tenant is a future, past or current one
          enum:
            - future
            - past
            - current
        tags:
          type: array
          description: Tags associated with the lease if it was evicted or is in notice
          items:
            type: string
            enum:
              - evict
              - notice
        lease_start:
          type: string
          description: The date the Resident's current lease starts.
          example: 2021-01-15T00:00:00.000
          nullable: true
        lease_end:
          type: string
          description: The date the Resident's current lease should end.
          example: 2022-01-15T00:00:00.000
          nullable: true
        rent:
          type: number
          description: The dollar amount of the Resident's current rent. Must be ≥ 0.
          example: 100
          nullable: true
        security_deposit:
          type: number
          description: Unit security deposit
          example: 1500
          nullable: true
        move_out_date:
          type: string
          description: The date that the Resident is scheduled to move out of the current
            Property.
          example: 2022-01-15T00:00:00.000
          nullable: true
        move_in_date:
          type: string
          description: The date that the Resident is scheduled to move in to the current
            Property.
          example: 2022-01-15T00:00:00.000
          nullable: true
        property_id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        rentvine_sync:
          $ref: "#/components/schemas/rentvine_sync"
    lease_v2_update_request_model:
      type: object
      properties:
        status:
          type: string
          description: Indicates if the tenant is a future, past or current one
          enum:
            - future
            - past
            - current
        tags:
          type: array
          description: Tags associated with the lease if it was evicted or is in notice
          items:
            type: string
            enum:
              - evict
              - notice
        lease_start:
          type: string
          description: The date the Resident's current lease starts.
          example: 2021-01-15T00:00:00.000
          nullable: true
        lease_end:
          type: string
          description: The date the Resident's current lease should end.
          example: 2022-01-15T00:00:00.000
          nullable: true
        rent:
          type: number
          description: The dollar amount of the Resident's current rent. Must be ≥ 0.
          example: 100
          nullable: true
        security_deposit:
          type: number
          description: Unit security deposit
          example: 1500
          nullable: true
        move_out_date:
          type: string
          description: The date that the Resident is scheduled to move out of the current
            Property.
          example: 2022-01-15T00:00:00.000
          nullable: true
        move_in_date:
          type: string
          description: The date that the Resident is scheduled to move in to the current
            Property.
          example: 2022-01-15T00:00:00.000
          nullable: true
        property_id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        rentvine_sync:
          $ref: "#/components/schemas/rentvine_sync"
    lease_v2_response_model:
      type: object
      required:
        - id
        - tenant_id
        - status
        - tags
        - property
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        tenant_id:
          type: string
          description: Entity id of the resident this lease belongs to.
          example: 00BRcZPSakXz6w7RYoE
        status:
          type: string
          description: Indicates if the tenant is a future, past or current one
          enum:
            - future
            - past
            - current
        tags:
          type: array
          description: Tags associated with the lease if it was evicted or is in notice
          items:
            type: string
            enum:
              - evict
              - notice
        property:
          type: object
          required:
            - id
            - address
            - city
            - zip_code
          description: This is the entity id of the Unit, Building, or Community that the
            entity is for.
          properties:
            id:
              type: string
              description: Selected entity's property id
              example: 00BRcZPSakXz6w7RYoE
            address:
              type: string
              description: Selected entity's property address
              example: 2001 Red Gates
            address_2:
              type: string
              description: Selected entity's property additional address info
              example: Apt. 4B
            city:
              type: string
              description: Selected entity's property city
              example: New Orleans
            region:
              type: string
              description: Selected entity's property region
              example: Orleans
            zip_code:
              type: string
              description: Selected entity's property zip_code
              example: "70130"
        lease_start:
          type: string
          description: The date the Resident's current lease starts.
          example: 2021-01-15T00:00:00.000
        lease_end:
          type: string
          description: The date the Resident's current lease should end.
          example: 2022-01-15T00:00:00.000
        rent:
          type: number
          description: The dollar amount of the Resident's current rent. Must be ≥ 0.
          example: 100
        security_deposit:
          type: number
          description: Unit security deposit
          example: 1500
        move_out_date:
          type: string
          description: The date that the Resident is scheduled to move out of the current
            Property.
          example: 2022-01-15T00:00:00.000
        move_in_date:
          type: string
          description: The date that the Resident is scheduled to move in to the current
            Property.
          example: 2022-01-15T00:00:00.000
    inspection_skills_get_all_response_model:
      type: object
      required:
        - title
        - default
        - popular
        - vendor
      properties:
        title:
          type: string
          description: Title of the skill
        default:
          type: boolean
          description: The default skill is automatically selected when the user does not
            inform one during maintenance flag creation. This is the skill
            selected when a maintenance flag is added by a resident.
        popular:
          type: boolean
          description: Skills that have popular equal to true are shown first on the
            listings. For example, they are listed at the top of the mobile
            skills list when flagging items.
        vendor:
          type: string
          description: Indicates where the skill is coming from. Depends on what
            integrations are active for the subscription.
          enum:
            - Latchel
            - AppFolio
            - Yardi
            - Jenark
            - RentCheck
            - Rent Manager
            - Rentvine
    api_email_customization:
      x-internal: true
      type: object
      required:
        - id
        - content
        - message
        - subject
        - template
      description: Email customization.
      properties:
        id:
          type: string
        content:
          type: string
          description: HTML / template body of the email customization.
        message:
          type: object
          required:
            - email_title
            - email_type
            - email_explanatory_text
          properties:
            email_title:
              type: string
            email_type:
              type: string
              enum:
                - inspection_invitation_new_to_rentcheck
                - inspection_invitation_existing_rentcheck_users
                - final_inspection_reminder_3_days_overdue
                - inspection_approval_confirmation
                - inspection_review_request
              description: Allowed email-customization types.
            email_explanatory_text:
              type: string
        subject:
          type: object
          required:
            - title
            - merge_tags
          properties:
            title:
              type: string
            merge_tags:
              type: array
              items:
                type: object
                required:
                  - value
                  - display_name
                properties:
                  value:
                    type: string
                  display_name:
                    type: string
        template:
          type: object
          required:
            - id
            - name
            - internal_label
          properties:
            id:
              type: string
            name:
              type: string
            internal_label:
              type: string
        updated_by:
          type: object
          properties:
            username:
              type: string
            email:
              type: string
            updated_date:
              type: string
              format: date-time
    basic_email_customization:
      x-internal: true
      type: object
      required:
        - id
        - email_type_id
        - email_title
        - email_explanatory_text
        - template
      description: Basic email customization.
      properties:
        id:
          type: string
        email_type_id:
          type: string
          enum:
            - inspection_invitation_new_to_rentcheck
            - inspection_invitation_existing_rentcheck_users
            - final_inspection_reminder_3_days_overdue
            - inspection_approval_confirmation
            - inspection_review_request
          description: Allowed email-customization types.
        email_title:
          type: string
        email_explanatory_text:
          type: string
        template:
          type: object
          required:
            - id
            - name
            - internal_label
          properties:
            id:
              type: string
            name:
              type: string
            internal_label:
              type: string
        updated_by:
          type: object
          properties:
            username:
              type: string
            email:
              type: string
            updated_date:
              type: string
              format: date-time
    email_customization_test_request_model:
      x-internal: true
      type: object
      required:
        - recipients
        - content
        - property_id
        - inspection_template_id
        - email_type_id
      description: Request body for sending a test email customization.
      properties:
        recipients:
          type: array
          items:
            type: string
            format: email
          minItems: 1
        content:
          type: string
        property_id:
          type: string
        inspection_template_id:
          type: string
        email_type_id:
          type: string
          enum:
            - inspection_invitation_new_to_rentcheck
            - inspection_invitation_existing_rentcheck_users
            - final_inspection_reminder_3_days_overdue
            - inspection_approval_confirmation
            - inspection_review_request
          description: Allowed email-customization types.
    email_customization_upsert_request_model:
      x-internal: true
      type: object
      required:
        - content
        - email_type_id
        - inspection_template_ids
      description: Request body for upserting an email customization.
      properties:
        content:
          type: string
        email_type_id:
          type: string
          enum:
            - inspection_invitation_new_to_rentcheck
            - inspection_invitation_existing_rentcheck_users
            - final_inspection_reminder_3_days_overdue
            - inspection_approval_confirmation
            - inspection_review_request
          description: Allowed email-customization types.
        inspection_template_ids:
          type: array
          items:
            type: string
    api_signature_acknowledgement:
      x-internal: true
      type: object
      required:
        - text
        - updated_by
      description: Signature acknowledgement.
      properties:
        text:
          type: string
        updated_by:
          type: object
          required:
            - id
            - username
            - email
            - updated_date
          properties:
            id:
              type: string
            username:
              type: string
            email:
              type: string
            updated_date:
              type: string
              format: date-time
    signature_acknowledgements_response_model:
      x-internal: true
      type: object
      required:
        - default
      description: Signature acknowledgements grouped by inspection template.
      properties:
        default:
          $ref: "#/components/schemas/api_signature_acknowledgement"
        global:
          $ref: "#/components/schemas/api_signature_acknowledgement"
        templates:
          type: array
          items:
            type: object
            required:
              - inspection_template
            properties:
              inspection_template:
                type: object
                required:
                  - id
                  - name
                  - display_name
                  - internal_label
                  - description
                  - is_deleted
                  - is_rc_template
                  - is_published
                  - property_type
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  display_name:
                    type: string
                  internal_label:
                    type: string
                  description:
                    type: string
                  is_deleted:
                    type: boolean
                  is_rc_template:
                    type: boolean
                  is_published:
                    type: boolean
                  property_type:
                    type: string
                    description: The property type designates which type of property the Unit is.
                      `""` is allowed as a legacy value for units created before
                      the enum was tightened.
                    enum:
                      - ""
                      - Single-Family
                      - Multi-Family
                      - Apartment or Condo
                      - Commercial
                    example: Single-Family
              customization:
                $ref: "#/components/schemas/api_signature_acknowledgement"
    signature_acknowledgements_update_request_model:
      x-internal: true
      type: object
      required:
        - signature_acknowledgements
      description: Use the literal id `default` to update the subscription-wide
        acknowledgement, or an inspection template id to scope the change.
      properties:
        signature_acknowledgements:
          type: array
          minItems: 1
          items:
            type: object
            required:
              - id
              - text
            properties:
              id:
                type: string
                description: Either the literal string 'default' or an inspection template id.
              text:
                type: string
    account_settings_delete_rooms_request_model:
      x-internal: true
      type: object
      required:
        - property_type
        - rooms
      description: Request body for deleting rooms.
      properties:
        property_type:
          type: string
          description: The property type designates which type of property the Unit is.
            `""` is allowed as a legacy value for units created before the enum
            was tightened.
          enum:
            - ""
            - Single-Family
            - Multi-Family
            - Apartment or Condo
            - Commercial
          example: Single-Family
        rooms:
          type: array
          items:
            type: object
          description: List of room entries to remove.
    subscription_get_by_user_response_model:
      type: object
      required:
        - id
        - status
        - created_at
        - teams
        - plan
        - addons
        - rentcheck_contact
        - coupons
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        status:
          type: string
          description: Current status of subscription
          enum:
            - active
            - future
            - in_trial
            - non_renewing
            - paused
            - cancelled
        created_at:
          type: string
          description: Entity creation date, in ISO format
          example: 2022-12-02T14:15:37.925
        cancelled_at:
          type: string
          description: Subscription cancellation date, in ISO format
          example: 2022-12-02T14:15:37.925
        next_billing_at:
          type: string
          description: Next billing date for the subscription, in ISO format.
          example: 2022-12-02T14:15:37.925
        teams:
          type: array
          items:
            type: object
            required:
              - id
              - name
              - internal_label
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              name:
                type: string
                description: The name field is the Team name that is displayed on recipient
                  facing reports and communication.
                example: The best company
              internal_label:
                type: string
                description: The internal label is a convenience label to help differentiate
                  similar teams a user may belong to. It's displayed to you and
                  your team members.
                example: The best company label
              logo:
                type: string
                description: The location of a hosted image file that is used to co-brand
                  RentCheck reports and email communication.
        plan:
          type: object
          required:
            - sub_id
            - plan_id
            - plan_name
            - customer_id
            - billing_email
            - plan_unit_price
            - plan_amount
            - plan_qty
            - pricing_model
            - active_units
          properties:
            sub_id:
              type: string
            plan_id:
              type: string
            plan_name:
              type: string
            invoice_name:
              type: string
              description: Optional label used on invoices for this plan.
            customer_id:
              type: string
            billing_email:
              type: string
            plan_unit_price:
              type: number
            plan_amount:
              type: number
            plan_qty:
              type: number
            pricing_model:
              type: string
              enum:
                - flat_fee
                - per_unit
                - tiered
                - volume
                - stairstep
            active_units:
              type: number
              description: Number of billable units currently active on this plan.
        addons:
          type: array
          items:
            type: object
            required:
              - id
              - name
              - amount
              - quantity
              - unit_price
              - pricing_model
            properties:
              id:
                type: string
              name:
                type: string
              amount:
                type: number
              quantity:
                type: number
              unit_price:
                type: number
              pricing_model:
                type: string
                enum:
                  - flat_fee
                  - per_unit
                  - tiered
                  - volume
                  - stairstep
        payment_method:
          type: object
          description: Optional payment method associated with the subscription. The full
            Chargebee payload (id, updated_at, type, reference_id, status, plus
            optional `card` / `bank_account` blocks) is passed through as-is.
          additionalProperties: true
        plan_trial:
          type: object
          description: Details of the most recent plan trial, when the subscription has an
            active or completed plan-trial.
          required:
            - plan_id
            - duration_in_days
          properties:
            plan_id:
              type: string
            duration_in_days:
              type: number
            start:
              type: string
              description: Trial start date, in ISO format.
              example: 2022-12-02T14:15:37.925
            end:
              type: string
              description: Trial end date, in ISO format.
              example: 2022-12-02T14:15:37.925
        plan_free_trials:
          type: array
          description: List of plan free trials the caller has already participated in —
            used server-side to avoid re-offering the same trial. An omitted
            value is normalised to `[]`, so the field is always present on the
            wire.
          items:
            type: object
            required:
              - id
            properties:
              id:
                type: string
        addon_free_trials:
          type: array
          items:
            type: object
            required:
              - status
              - trial_start
              - trial_end
              - paid_addon_id
            properties:
              status:
                type: string
                enum:
                  - active
                  - expired
              trial_start:
                type: string
              trial_end:
                type: string
              paid_addon_id:
                type: string
        rentcheck_contact:
          type: object
          required:
            - id
            - name
            - email
            - company
            - company_id
          properties:
            id:
              type: string
            name:
              type: string
            email:
              type: string
            company:
              type: string
            company_id:
              type: string
        coupons:
          type: array
          items:
            type: object
            required:
              - id
              - name
              - discount_type
            properties:
              id:
                type: string
              name:
                type: string
              discount_type:
                type: string
                enum:
                  - fixed_amount
                  - percentage
              discount_amount:
                type: number
              discount_percentage:
                type: number
    subscription_invoice_response_model:
      x-internal: true
      type: object
      required:
        - id
        - date
        - status
        - amount_due
      description: Subset of the Chargebee invoice payload surfaced to clients.
      properties:
        id:
          type: string
          description: Chargebee invoice id.
        date:
          type: integer
          description: Unix-second timestamp the invoice was issued.
        status:
          type: string
          enum:
            - paid
            - posted
            - payment_due
            - not_paid
            - voided
            - pending
        amount_due:
          type: integer
          description: Amount due on the invoice (in the smallest currency unit).
    subscription_create_trial_request_model:
      x-internal: true
      type: object
      required:
        - user_name
        - company
        - portfolio_size
      description: Request body for creating a subscription trial.
      properties:
        user_name:
          type: string
        company:
          type: string
        portfolio_size:
          type: integer
    subscription_create_request_request_model:
      x-internal: true
      type: object
      required:
        - plan_id
      description: Used to request a trial extension or plan change.
      properties:
        plan_id:
          type: string
          description: Chargebee plan id the user is requesting.
        note:
          type: string
    subscription_update_request_model:
      x-internal: true
      type: object
      description: At least one of `plan` or `addons` should be supplied.
      properties:
        plan:
          type: object
          required:
            - id
          properties:
            id:
              type: string
              description: Chargebee plan id to switch to.
        addons:
          type: array
          items:
            type: object
            required:
              - id
            properties:
              id:
                type: string
                description: Chargebee addon id.
    api_invite_model:
      type: object
      required:
        - id
        - invite_type
        - status
        - created_date
        - reference_code
        - created_by
        - recipient_email
        - inspection
        - property
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        invite_type:
          type: string
          description: The type of the invite
          enum:
            - Perform Inspection
            - Inspection Request
            - Review Inspection
        status:
          type: string
          description: The status reflects if the invite is sent or accepted
          enum:
            - sent
            - accepted
        created_date:
          type: string
          description: Entity creation date, in ISO format
          example: 2022-12-02T14:15:37.925
        reference_code:
          type: string
          description: 4 or 6 character string to inform when accepting the invite
          example: ABC123
        created_by:
          type: object
          required:
            - id
            - name
            - email
            - type
            - display_name
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: Name of the user
            email:
              type: string
              description: Email of the user
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
              description: User type discriminator.
            display_name:
              type: string
              description: Either the name of the organization or the creator name
            organization_id:
              type: string
              description: Id of the Team who created or administers the Entity.
              example: 05fDGZRKp4nVmlmEwjsv
        accepted_by:
          type: object
          required:
            - id
            - name
            - email
            - type
          description: Data of who accepted the invite. This can differ from
            recipient_email because a different user can accept an invite given
            a correct reference_code
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: Name of the user
            email:
              type: string
              description: Email of the user
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
              description: User type discriminator.
        recipient_email:
          type: string
          description: Email that the invite was originally sent to
        inspection:
          type: object
          required:
            - id
            - label
            - due_date
            - inspection_template
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            label:
              type: string
              description: Inspection label.
            due_date:
              type: string
              description: The inspection due date (legacy) is the original date by which the
                recipient must complete the inspection.
              example: 2021-12-02T14:15:37.925
            inspection_template:
              type: object
              required:
                - id
                - name
                - internal_label
                - property_type
                - display_name
              properties:
                id:
                  type: string
                  description: Inspection template identifier
                name:
                  type: string
                  description: Inspection template name.
                internal_label:
                  type: string
                  description: Inspection template internal label.
                property_type:
                  type: string
                  description: Inspection template property type.
                display_name:
                  type: string
                  description: Resolved display name for the inspection template, used by clients
                    to render the template title (mirrors `name` for legacy
                    templates).
        property:
          type: object
          required:
            - id
            - address
            - city
            - zip_code
          description: This is the entity id of the Unit, Building, or Community that the
            entity is for.
          properties:
            id:
              type: string
              description: Selected entity's property id
              example: 00BRcZPSakXz6w7RYoE
            address:
              type: string
              description: Selected entity's property address
              example: 2001 Red Gates
            address_2:
              type: string
              description: Selected entity's property additional address info
              example: Apt. 4B
            city:
              type: string
              description: Selected entity's property city
              example: New Orleans
            region:
              type: string
              description: Selected entity's property region
              example: Orleans
            zip_code:
              type: string
              description: Selected entity's property zip_code
              example: "70130"
    inspection_invite_accept_request_model:
      type: object
      properties:
        reference_code:
          type: string
          description: 6 character string to inform when accepting the invite (also
            accepts legacy 4 character reference code string)
          example: ABC123
    inspection_review_request_model:
      type: object
      required:
        - recipients
        - due_date
      properties:
        recipients:
          type: array
          minItems: 1
          description: Recipients' emails. Each entry is validated as a lowercase email
            address before being saved. When used for review requests, an empty
            array is rejected with `"data.recipients" must contain at least 1
            items`.
          items:
            type: string
            format: email
        due_date:
          type: string
          description: The inspection due date (legacy) is the original date by which the
            recipient must complete the inspection.
          example: 2021-12-02T14:15:37.925
        message:
          type: string
          description: Message to be displayed to the recipient
          example: Missing photos of the kitchen
    api_permission_group:
      type: object
      required:
        - id
        - name
        - subscription_id
        - inspection_templates
        - users
        - allow_billing_plan_editing
        - allow_automation_editing
        - allow_notifications_preferences_editing
        - allow_inspection_messaging_editing
        - allow_ai_manual_scan
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        name:
          type: string
          description: Name for the permission group, must be unique in the subscription
            context
        subscription_id:
          type: string
          description: Id for the subscription the permission group belongs to
        allow_billing_plan_editing:
          type: boolean
          description: Enable or disable billing plan editing for users on this permission
            group
        allow_automation_editing:
          type: boolean
          description: Enable or disable automation editing for users on this permission
            group
        allow_notifications_preferences_editing:
          type: boolean
          description: Enable or disable notification preferences editing for users on
            this permission group
        allow_inspection_messaging_editing:
          type: boolean
          description: Enable or disable inspection messaging editing for users on this
            permission group
        allow_ai_manual_scan:
          type: boolean
          description: Enable or disable manual AI damage scans for users on this
            permission group
        inspection_templates:
          type: array
          description: Which inspection templates users that belong to this permission
            group have access to
          items:
            type: object
            required:
              - id
              - name
              - internal_label
              - property_type
              - display_name
            properties:
              id:
                type: string
                description: Inspection template identifier
              name:
                type: string
                description: Inspection template name.
              internal_label:
                type: string
                description: Inspection template internal label.
              property_type:
                type: string
                description: Inspection template property type.
              display_name:
                type: string
                description: Resolved display name for the inspection template, used by clients
                  to render the template title (mirrors `name` for legacy
                  templates).
        users:
          description: Which users belong to this permission group
          type: array
          items:
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              name:
                type: string
                description: Name of the user
              email:
                type: string
                description: Email of the user
              type:
                type: string
                enum:
                  - Renter
                  - Property Manager
                  - Landlord
                  - System
                description: User type discriminator.
              user_type:
                type: string
                enum:
                  - Renter
                  - Property Manager
                  - Landlord
                example: Property Manager
    permission_group_create_request_model:
      type: object
      required:
        - name
        - inspection_template_ids
        - user_ids
        - allow_billing_plan_editing
      properties:
        name:
          type: string
          description: Name for the permission group, must be unique in the subscription
            context
        allow_billing_plan_editing:
          type: boolean
          description: Enable or disable billing plan editing for users on this permission
            group
        allow_automation_editing:
          type: boolean
          description: Enable or disable automation editing for users on this permission
            group
        allow_notifications_preferences_editing:
          type: boolean
          description: Enable or disable notification preferences editing for users on
            this permission group
        allow_inspection_messaging_editing:
          type: boolean
          description: Enable or disable inspection messaging editing for users on this
            permission group
        allow_ai_manual_scan:
          type: boolean
          description: Enable or disable manual AI damage scans for users on this
            permission group
        inspection_template_ids:
          type: array
          items:
            type: string
          description: IDs of the inspection templates to be associated with the new
            permission group
        user_ids:
          type: array
          items:
            type: string
          description: IDs of the users to be associated with the new permission group
    permission_group_update_request_model:
      type: object
      required:
        - name
        - inspection_template_ids
        - user_ids
        - allow_billing_plan_editing
      properties:
        name:
          type: string
          description: Name for the permission group, must be unique in the subscription
            context
        inspection_template_ids:
          type: array
          items:
            type: string
          description: IDs of the inspection templates to be associated with the
            permission group
        user_ids:
          type: array
          items:
            type: string
          description: IDs of the users to be associated with the permission group
        allow_billing_plan_editing:
          type: boolean
          description: Enable or disable billing plan editing for users on this permission
            group
        allow_automation_editing:
          type: boolean
          description: Enable or disable automation editing for users on this permission
            group
        allow_notifications_preferences_editing:
          type: boolean
          description: Enable or disable notification preferences editing for users on
            this permission group
        allow_inspection_messaging_editing:
          type: boolean
          description: Enable or disable inspection messaging editing for users on this
            permission group
        allow_ai_manual_scan:
          type: boolean
          description: Enable or disable manual AI damage scans for users on this
            permission group
    permission_group_delete_request_model:
      type: object
      required:
        - ids
      properties:
        ids:
          type: array
          minItems: 1
          items:
            type: string
          description: IDs of the permission groups to be deleted. Must contain at least
            one id.
    permission_group_get_all_response_model:
      type: object
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        name:
          type: string
          description: Name of the permission group
        user_count:
          type: number
          description: Total amount of users on the permission group
        allow_billing_plan_editing:
          type: boolean
          description: Whether users on this permission group can edit billing plans.
        allow_automation_editing:
          type: boolean
          description: Whether users on this permission group can edit automations.
        allow_notifications_preferences_editing:
          type: boolean
          description: Whether users on this permission group can edit notification
            preferences.
        allow_inspection_messaging_editing:
          type: boolean
          description: Whether users on this permission group can edit inspection messaging.
        allow_ai_manual_scan:
          type: boolean
          description: Whether users on this permission group can run manual AI damage
            scans.
    api_account_settings:
      type: object
      required:
        - id
        - subscription_id
        - rooms
        - is_maintenance_flag_default_photos_enabled
        - is_resident_reported_maintenance_enabled
        - is_inspection_review_autoapproval_enabled
        - custom_maintenance_fields
        - inspection_report_templates
        - inspection_contact_info
        - two_step_inspection_approval
        - ai_settings
        - beta_access
      description: Account settings. `signature_acknowledgement` uses the API-facing shape.
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        subscription_id:
          type: string
          description: The subscription id for the user that created this template.
        is_maintenance_flag_default_photos_enabled:
          type: boolean
          title: is_maintenance_flag_default_photos_enabled
          default: false
        is_resident_reported_maintenance_enabled:
          type: boolean
          title: is_resident_reported_maintenance_enabled
          default: false
        is_inspection_review_autoapproval_enabled:
          type: boolean
          description: When true, submitted inspections skip manual review and are
            approved automatically.
          default: false
        custom_maintenance_fields:
          type: array
          items:
            type: object
            required:
              - id
              - title
              - selection_type
              - options
              - template_ids
              - enabled
            description: Custom maintenance field setting.
            properties:
              id:
                type: string
                maxLength: 30
              title:
                type: string
                maxLength: 30
              description:
                type: string
                maxLength: 100
              selection_type:
                type: string
                enum:
                  - single
                  - multiple
              options:
                type: array
                items:
                  type: string
                minItems: 2
                description: The options that the user can select. Minimum of two.
              template_ids:
                type: array
                items:
                  type: string
                description: Inspection template ids the field is enabled for. Include `'*'` to
                  apply to every template.
              required:
                type: boolean
              enabled:
                type: boolean
          description: Custom fields the account applies to maintenance flags on
            inspections.
        two_step_inspection_approval:
          type: object
          required:
            - enabled
            - template_ids
          description: "`template_ids` accepts the wildcard `['*']` to opt every template
            in."
          properties:
            enabled:
              type: boolean
            template_ids:
              type: array
              items:
                type: string
              description: Inspection template ids requiring two-step approval. Include `'*'`
                to apply to every template.
        ai_settings:
          type: object
          required:
            - damage_detection
          description: AI settings.
          properties:
            damage_detection:
              type: object
              required:
                - automatic_scanning
                - manual_scanning
                - free_scans_used
                - free_scans_limit
              description: The `free_scans_*` counters and `open_ai_model` are populated
                server-side and not accepted on `PUT /v1/account-settings`.
              properties:
                automatic_scanning:
                  type: object
                  required:
                    - enabled
                    - template_ids
                  properties:
                    enabled:
                      type: boolean
                    template_ids:
                      type: array
                      items:
                        type: string
                      description: Inspection template ids automatic scanning is enabled for. Include
                        `'*'` to apply to every template.
                manual_scanning:
                  type: object
                  required:
                    - enabled
                  properties:
                    enabled:
                      type: boolean
                free_scans_used:
                  type: integer
                  description: Number of complimentary scans already used.
                free_scans_limit:
                  type: integer
                  description: Maximum number of complimentary scans the account may use.
                open_ai_model:
                  type: string
                  description: Model id used for damage detection. `gpt-*` routes through OpenAI,
                    `gemini-*` routes through Gemini. When unset, falls back to
                    the application-wide default model.
        signature_acknowledgement:
          $ref: "#/components/schemas/api_signature_acknowledgement"
        inspection_tab_backsync_modal_shown:
          type: boolean
          description: Whether the user has been shown the backsync confirmation modal
            after enabling the inspection tab sync destination.
        beta_access:
          type: object
          title: beta_access
          description: Early-access ("beta") feature flags the account is enrolled in.
          required:
            - resident_insights
          properties:
            resident_insights:
              type: boolean
              description: Whether the account is enrolled in the Resident Insights (Pioneer)
                beta.
              default: false
        rooms:
          type: object
          title: rooms
          required:
            - unit
            - building
            - community
          properties:
            unit:
              type: array
              items:
                type: object
                required:
                  - category
                  - name
                properties:
                  name:
                    type: string
                  category:
                    type: string
                    enum:
                      - interior
                      - exterior
                      - other
            building:
              type: array
              items:
                type: object
                required:
                  - category
                  - name
                properties:
                  name:
                    type: string
                  category:
                    type: string
                    enum:
                      - building
                      - other
            community:
              type: array
              items:
                type: object
                required:
                  - category
                  - name
                properties:
                  name:
                    type: string
                  category:
                    type: string
                    enum:
                      - community
                      - other
        skills:
          type: array
          items:
            type: object
            title: skills
            required:
              - category
              - is_default
            properties:
              category:
                type: string
              sub_category:
                type: string
              is_default:
                type: boolean
        inspection_report_templates:
          type: object
          title: inspection_report_templates
          properties:
            default:
              type: object
              required:
                - name
                - summary_section
                - inspection_features_section
                - maintenance_flags_section
              properties:
                name:
                  type: string
                summary_section:
                  type: object
                  required:
                    - section_active
                    - resident_info
                    - summary_page
                    - table_of_contents
                    - inspection_label
                    - summary_note
                  properties:
                    section_active:
                      type: boolean
                      default: true
                    resident_info:
                      type: boolean
                      default: false
                    summary_page:
                      type: boolean
                      default: true
                    table_of_contents:
                      type: boolean
                      default: true
                    inspection_label:
                      type: boolean
                      default: true
                    summary_note:
                      type: boolean
                      default: false
                inspection_features_section:
                  type: object
                  required:
                    - section_active
                    - questions_answers
                    - images
                    - notes
                    - ratings
                  properties:
                    section_active:
                      type: boolean
                      default: true
                    questions_answers:
                      type: boolean
                      default: true
                    images:
                      type: boolean
                      default: true
                    notes:
                      type: boolean
                      default: true
                    ratings:
                      type: boolean
                      default: true
                maintenance_flags_section:
                  type: object
                  required:
                    - section_active
                    - owner_flags
                    - uncategorized_flags
                    - resident_flags
                  properties:
                    section_active:
                      type: boolean
                      default: true
                    owner_flags:
                      type: object
                      required:
                        - section_active
                        - cost
                        - notes
                        - internal_notes
                      properties:
                        section_active:
                          type: boolean
                          default: false
                        cost:
                          type: boolean
                          default: false
                        notes:
                          type: boolean
                          default: false
                        internal_notes:
                          type: boolean
                          default: false
                    uncategorized_flags:
                      type: object
                      required:
                        - section_active
                        - cost
                        - notes
                        - internal_notes
                      properties:
                        section_active:
                          type: boolean
                          default: false
                        cost:
                          type: boolean
                          default: false
                        notes:
                          type: boolean
                          default: false
                        internal_notes:
                          type: boolean
                          default: false
                    resident_flags:
                      type: object
                      required:
                        - section_active
                        - cost
                        - notes
                        - internal_notes
                      properties:
                        section_active:
                          type: boolean
                          default: false
                        cost:
                          type: boolean
                          default: false
                        notes:
                          type: boolean
                          default: false
                        internal_notes:
                          type: boolean
                          default: false
        inspection_contact_info:
          type: object
          title: inspection_contact_info
          properties:
            email:
              type: string
            phone_number:
              type: string
    account_settings_update_request_model:
      type: object
      description: Partial update of account settings. All keys optional; the server
        merges supplied values onto the current document.
      properties:
        skills:
          type: array
          items:
            type: object
            title: skills
            required:
              - category
              - is_default
            properties:
              category:
                type: string
              sub_category:
                type: string
              is_default:
                type: boolean
        rooms:
          type: object
          title: rooms
          required:
            - unit
            - building
            - community
          properties:
            unit:
              type: array
              items:
                type: object
                required:
                  - category
                  - name
                properties:
                  name:
                    type: string
                  category:
                    type: string
                    enum:
                      - interior
                      - exterior
                      - other
            building:
              type: array
              items:
                type: object
                required:
                  - category
                  - name
                properties:
                  name:
                    type: string
                  category:
                    type: string
                    enum:
                      - building
                      - other
            community:
              type: array
              items:
                type: object
                required:
                  - category
                  - name
                properties:
                  name:
                    type: string
                  category:
                    type: string
                    enum:
                      - community
                      - other
        inspection_report_templates:
          type: object
          title: inspection_report_templates
          properties:
            default:
              type: object
              required:
                - name
                - summary_section
                - inspection_features_section
                - maintenance_flags_section
              properties:
                name:
                  type: string
                summary_section:
                  type: object
                  required:
                    - section_active
                    - resident_info
                    - summary_page
                    - table_of_contents
                    - inspection_label
                    - summary_note
                  properties:
                    section_active:
                      type: boolean
                      default: true
                    resident_info:
                      type: boolean
                      default: false
                    summary_page:
                      type: boolean
                      default: true
                    table_of_contents:
                      type: boolean
                      default: true
                    inspection_label:
                      type: boolean
                      default: true
                    summary_note:
                      type: boolean
                      default: false
                inspection_features_section:
                  type: object
                  required:
                    - section_active
                    - questions_answers
                    - images
                    - notes
                    - ratings
                  properties:
                    section_active:
                      type: boolean
                      default: true
                    questions_answers:
                      type: boolean
                      default: true
                    images:
                      type: boolean
                      default: true
                    notes:
                      type: boolean
                      default: true
                    ratings:
                      type: boolean
                      default: true
                maintenance_flags_section:
                  type: object
                  required:
                    - section_active
                    - owner_flags
                    - uncategorized_flags
                    - resident_flags
                  properties:
                    section_active:
                      type: boolean
                      default: true
                    owner_flags:
                      type: object
                      required:
                        - section_active
                        - cost
                        - notes
                        - internal_notes
                      properties:
                        section_active:
                          type: boolean
                          default: false
                        cost:
                          type: boolean
                          default: false
                        notes:
                          type: boolean
                          default: false
                        internal_notes:
                          type: boolean
                          default: false
                    uncategorized_flags:
                      type: object
                      required:
                        - section_active
                        - cost
                        - notes
                        - internal_notes
                      properties:
                        section_active:
                          type: boolean
                          default: false
                        cost:
                          type: boolean
                          default: false
                        notes:
                          type: boolean
                          default: false
                        internal_notes:
                          type: boolean
                          default: false
                    resident_flags:
                      type: object
                      required:
                        - section_active
                        - cost
                        - notes
                        - internal_notes
                      properties:
                        section_active:
                          type: boolean
                          default: false
                        cost:
                          type: boolean
                          default: false
                        notes:
                          type: boolean
                          default: false
                        internal_notes:
                          type: boolean
                          default: false
        is_resident_reported_maintenance_enabled:
          type: boolean
          title: is_resident_reported_maintenance_enabled
          default: false
        is_maintenance_flag_default_photos_enabled:
          type: boolean
          title: is_maintenance_flag_default_photos_enabled
          default: false
        is_inspection_review_autoapproval_enabled:
          type: boolean
          description: When true, submitted inspections skip manual review and are
            approved automatically.
          default: false
        custom_maintenance_fields:
          type: array
          items:
            type: object
            required:
              - id
              - title
              - selection_type
              - options
              - template_ids
              - enabled
            description: Custom maintenance field setting.
            properties:
              id:
                type: string
                maxLength: 30
              title:
                type: string
                maxLength: 30
              description:
                type: string
                maxLength: 100
              selection_type:
                type: string
                enum:
                  - single
                  - multiple
              options:
                type: array
                items:
                  type: string
                minItems: 2
                description: The options that the user can select. Minimum of two.
              template_ids:
                type: array
                items:
                  type: string
                description: Inspection template ids the field is enabled for. Include `'*'` to
                  apply to every template.
              required:
                type: boolean
              enabled:
                type: boolean
          description: Custom fields the account applies to maintenance flags on
            inspections.
        two_step_inspection_approval:
          type: object
          required:
            - enabled
            - template_ids
          description: "`template_ids` accepts the wildcard `['*']` to opt every template
            in."
          properties:
            enabled:
              type: boolean
            template_ids:
              type: array
              items:
                type: string
              description: Inspection template ids requiring two-step approval. Include `'*'`
                to apply to every template.
        ai_settings:
          type: object
          additionalProperties: false
          properties:
            damage_detection:
              type: object
              description: Update surface for damage detection. Only `automatic_scanning` and
                `manual_scanning` are accepted; `free_scans_*` and
                `open_ai_model` are server-managed.
              additionalProperties: false
              properties:
                automatic_scanning:
                  type: object
                  required:
                    - enabled
                    - template_ids
                  additionalProperties: false
                  properties:
                    enabled:
                      type: boolean
                    template_ids:
                      type: array
                      items:
                        type: string
                      minItems: 1
                      description: Inspection template ids automatic scanning is enabled for. Include
                        `'*'` to apply to every template.
                manual_scanning:
                  type: object
                  required:
                    - enabled
                  additionalProperties: false
                  properties:
                    enabled:
                      type: boolean
        inspection_tab_backsync_modal_shown:
          type: boolean
          description: Whether the user has been shown the backsync confirmation modal
            after enabling the inspection tab sync destination.
        inspection_contact_info:
          type: object
          title: inspection_contact_info
          properties:
            email:
              type: string
            phone_number:
              type: string
    inspection_approve_request_model:
      type: object
      oneOf:
        - title: With Signature Text
          required:
            - signature_text
          properties:
            signature_text:
              type: string
              description: Inspection's approval signature text
              example: Lucy Warburton
            user_facing_note:
              type: string
              description: Add the inspection approval note displayed to the user who
                completed it.
              example: Inspection approved. Thanks for your quick work!
            rating:
              type: number
              description: Overall Inspection report rating (1-5)
              example: 5
            note:
              type: string
              description: Note to add as feedback for RentCheck
              example: RentCheck inspection was easy and quick!
        - title: With Signature Image Data
          required:
            - signature_image_data
          properties:
            signature_image_data:
              type: string
              description: Base64 encoded image data for the signature. This is used to
                generate the signature image. Use when signature_text is not
                provided.
            user_facing_note:
              type: string
              description: Add the inspection approval note displayed to the user who
                completed it.
              example: Inspection approved. Thanks for your quick work!
            rating:
              type: number
              description: Overall Inspection report rating (1-5)
              example: 5
            note:
              type: string
              description: Note to add as feedback for RentCheck
              example: RentCheck inspection was easy and quick!
    inspection_template_create_request_model:
      x-internal: true
      type: object
      required:
        - name
        - internal_label
        - description
        - team_ids
        - property_type
        - is_fast_track_enabled
        - template_type
        - sections
        - contact_info
        - permission_group_ids
      description: Payload to create a user inspection template. `subscription_id` is
        always derived from the caller (never accepted from the client), and
        `is_rc_template`/`is_published`/`unpublished_sub_ids` are stamped
        server-side. `team_ids` must be a non-empty subset of the caller's
        organizations.
      properties:
        name:
          type: string
          description: The inspection template name.
        internal_label:
          type: string
          description: The inspection template internal label.
        description:
          type: string
          description: The inspection template description.
        team_ids:
          type: array
          items:
            type: string
          description: An array of team ids that have access to this template.
        property_type:
          type: string
          description: What property type this template applies to.
          enum:
            - unit
            - building
            - community
        is_fast_track_enabled:
          type: boolean
          description: Indicates whether the template supports fast track or not.
        template_type:
          type: string
          description: When set to all-rooms the inspection will be created with all the
            rooms in the property. When set to specific-features the inspection
            will require features to be selected when creating the inspection.
          enum:
            - all-rooms
            - specific-features
        sections:
          type: array
          description: Array with the inspection template sections
          items:
            type: object
            properties:
              id:
                type: string
                description: ID of the template section.
              name:
                type: string
                description: Name of the template section.
              type:
                type: string
                description: Sections with type `room` are only added to the inspection if the
                  property has those rooms configured. Sections with type
                  `section` do not depend on the property configuration and are
                  always added to the inspection
                enum:
                  - room
                  - section
              features:
                description: Array with the section features
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      description: ID of the template section feature.
                    name:
                      type: string
                      description: Name of the template section feature.
                    description:
                      type: string
                      description: The instructions text shown to the inspection performer when they
                        are in the camera view.
                    is_rating_required:
                      type: boolean
                      description: When set to true the inspection performer will be required to enter
                        a feature rating. If false the rating options will not
                        be shown.
                    is_video_enabled:
                      type: boolean
                      description: When set to true the inspection performer will be able to record a
                        video wherever they can add media.
                    number_of_photos_required:
                      type: number
                      description: How many photos are required for this feature. When set to 0 the
                        inspection performer will not be required to take any
                        photos for this feature
                    reference_photos:
                      type: array
                      items:
                        type: string
                      description: An array of reference photos for this feature. These photos will be
                        shown to the inspection performer when they are in the
                        camera view
                    questions:
                      description: Array with the feature questions
                      type: array
                      items:
                        type: object
                        properties:
                          id:
                            type: string
                            description: Entity ID
                            example: 00BRcZPSakXz6w7RYoE
                          title:
                            type: string
                            description: Question title
                          type:
                            type: string
                            description: Question type
                            enum:
                              - yes/no
                              - free text
                              - multiple choice
                              - asset capture
                          answer_type:
                            type: string
                            description: Answer type
                            enum:
                              - single
                              - multi
                          asset_category:
                            type: string
                            description: Asset Category
                            enum:
                              - Air Filter
                              - Air Conditioner
                              - Air Handler Unit (AHU)
                              - Barbecue Grill
                              - Bathroom Heater
                              - Cooktop
                              - Condenser
                              - Dishwasher
                              - Dryer
                              - Electric Fireplace
                              - Evaporator
                              - Freezer
                              - Furnace
                              - Furnace (AHU)
                              - Garbage Disposal
                              - Heat Pump
                              - Microwave
                              - Package Unit
                              - Pool Heater
                              - PTAC
                              - Range Hood
                              - Range
                              - Refrigerator
                              - Stackable Laundry
                              - Thermostat
                              - Wall Oven
                              - Washer
                              - Water Heater
                              - Other
                          answers:
                            type: object
                            properties:
                              id:
                                type: string
                                description: Entity ID
                                example: 00BRcZPSakXz6w7RYoE
                              title:
                                type: string
                                description: Answer title
                              requires_photo:
                                type: boolean
                              requires_note:
                                type: boolean
        contact_info:
          type: object
          description: Optional contact email / phone surfaced to performers on the
            inspection.
          properties:
            email:
              type: string
            phone_number:
              type: string
        permission_group_ids:
          type: array
          items:
            type: string
          description: IDs of the permission groups that should govern access to the new
            template.
    inspection_template_update_request_model:
      x-internal: true
      type: object
      minProperties: 1
      description: Whitelist of editable inspection-template fields. All optional —
        only the provided fields are updated. `id`, `subscription_id` and
        `is_rc_template` are immutable and stripped server-side. When `team_ids`
        is provided it must be a non-empty subset of the caller's organizations.
      properties:
        is_published:
          type: boolean
          description: Whether the inspection template should be marked as published.
        name:
          type: string
          description: The inspection template name.
        internal_label:
          type: string
          description: The inspection template internal label.
        description:
          type: string
          description: The inspection template description.
        team_ids:
          type: array
          items:
            type: string
          description: An array of team ids that have access to this template.
        property_type:
          type: string
          description: What property type this template applies to.
          enum:
            - unit
            - building
            - community
        is_fast_track_enabled:
          type: boolean
          description: Indicates whether the template supports fast track or not.
        template_type:
          type: string
          description: When set to all-rooms the inspection will be created with all the
            rooms in the property. When set to specific-features the inspection
            will require features to be selected when creating the inspection.
          enum:
            - all-rooms
            - specific-features
        contact_info:
          type: object
          description: Optional contact email / phone surfaced to performers on the
            inspection.
          properties:
            email:
              type: string
            phone_number:
              type: string
        sections:
          type: array
          description: Array with the inspection template sections
          items:
            type: object
            properties:
              id:
                type: string
                description: ID of the template section.
              name:
                type: string
                description: Name of the template section.
              type:
                type: string
                description: Sections with type `room` are only added to the inspection if the
                  property has those rooms configured. Sections with type
                  `section` do not depend on the property configuration and are
                  always added to the inspection
                enum:
                  - room
                  - section
              features:
                description: Array with the section features
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      description: ID of the template section feature.
                    name:
                      type: string
                      description: Name of the template section feature.
                    description:
                      type: string
                      description: The instructions text shown to the inspection performer when they
                        are in the camera view.
                    is_rating_required:
                      type: boolean
                      description: When set to true the inspection performer will be required to enter
                        a feature rating. If false the rating options will not
                        be shown.
                    is_video_enabled:
                      type: boolean
                      description: When set to true the inspection performer will be able to record a
                        video wherever they can add media.
                    number_of_photos_required:
                      type: number
                      description: How many photos are required for this feature. When set to 0 the
                        inspection performer will not be required to take any
                        photos for this feature
                    reference_photos:
                      type: array
                      items:
                        type: string
                      description: An array of reference photos for this feature. These photos will be
                        shown to the inspection performer when they are in the
                        camera view
                    questions:
                      description: Array with the feature questions
                      type: array
                      items:
                        type: object
                        properties:
                          id:
                            type: string
                            description: Entity ID
                            example: 00BRcZPSakXz6w7RYoE
                          title:
                            type: string
                            description: Question title
                          type:
                            type: string
                            description: Question type
                            enum:
                              - yes/no
                              - free text
                              - multiple choice
                              - asset capture
                          answer_type:
                            type: string
                            description: Answer type
                            enum:
                              - single
                              - multi
                          asset_category:
                            type: string
                            description: Asset Category
                            enum:
                              - Air Filter
                              - Air Conditioner
                              - Air Handler Unit (AHU)
                              - Barbecue Grill
                              - Bathroom Heater
                              - Cooktop
                              - Condenser
                              - Dishwasher
                              - Dryer
                              - Electric Fireplace
                              - Evaporator
                              - Freezer
                              - Furnace
                              - Furnace (AHU)
                              - Garbage Disposal
                              - Heat Pump
                              - Microwave
                              - Package Unit
                              - Pool Heater
                              - PTAC
                              - Range Hood
                              - Range
                              - Refrigerator
                              - Stackable Laundry
                              - Thermostat
                              - Wall Oven
                              - Washer
                              - Water Heater
                              - Other
                          answers:
                            type: object
                            properties:
                              id:
                                type: string
                                description: Entity ID
                                example: 00BRcZPSakXz6w7RYoE
                              title:
                                type: string
                                description: Answer title
                              requires_photo:
                                type: boolean
                              requires_note:
                                type: boolean
        signature_acknowledgement:
          $ref: "#/components/schemas/api_signature_acknowledgement"
    inspection_template_update_response_model:
      x-internal: true
      type: object
      properties:
        template:
          $ref: "#/components/schemas/inspection_template_v1_get_by_id_response_model"
        status:
          type: string
          enum:
            - success
            - error
          description: Outcome of the update.
        error:
          type: object
          description: Populated when `status` is `error` (e.g. the template is referenced
            by an automation that prevents the change).
          properties:
            reason:
              type: string
              enum:
                - in-use
              description: Reason the update could not be applied.
            automation_names:
              type: array
              items:
                type: string
              description: Names of automations referencing this template, when applicable.
    inspection_template_delete_response_model:
      x-internal: true
      type: object
      properties:
        status:
          type: string
          enum:
            - success
            - error
          description: Outcome of the delete.
        error:
          type: object
          properties:
            reason:
              type: string
              enum:
                - in-use
            automation_names:
              type: array
              items:
                type: string
    inspection_template_edit_permission_groups_request_model:
      x-internal: true
      type: object
      required:
        - permission_group_ids
      properties:
        permission_group_ids:
          type: array
          items:
            type: string
          description: IDs of the permission groups that should govern access to this
            template.
    inspection_template_edit_automations_request_model:
      x-internal: true
      type: object
      required:
        - is_fast_track_enabled
      properties:
        is_fast_track_enabled:
          type: boolean
          description: Whether fast-track automation should be enabled for this template.
    api_tenant_occupancy:
      type: object
      required:
        - id
        - status
        - lease_type
        - created_date
        - property
        - permissions
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        status:
          type: string
          enum:
            - current
            - past
          description: Whether the tenant currently occupies the property or has moved out.
        lease_type:
          type: string
          enum:
            - renter
            - pm
          description: "`renter` for a resident-managed lease; `pm` when the lease is
            owned and synced by the property manager / integration."
        pm_occupancy_id:
          type: string
          description: Reference to the underlying RentCheck occupancy row that this
            tenant-occupancy wraps. Set for both `renter`- and `pm`-typed leases
            when the row was created from an existing occupancy.
        created_date:
          type: string
          description: Entity creation date, in ISO format
          example: 2022-12-02T14:15:37.925
        move_in_date:
          type: string
          description: Move-in date for the lease, in ISO format. Optional — emitted as
            `undefined` when unset, never `null`.
        lease_end_date:
          type: string
          description: Lease end date, in ISO format. Optional — emitted as `undefined`
            when unset, never `null`.
        property:
          type: object
          required:
            - id
            - address
            - city
            - zip_code
          description: This is the entity id of the Unit, Building, or Community that the
            entity is for.
          properties:
            id:
              type: string
              description: Selected entity's property id
              example: 00BRcZPSakXz6w7RYoE
            address:
              type: string
              description: Selected entity's property address
              example: 2001 Red Gates
            address_2:
              type: string
              description: Selected entity's property additional address info
              example: Apt. 4B
            city:
              type: string
              description: Selected entity's property city
              example: New Orleans
            region:
              type: string
              description: Selected entity's property region
              example: Orleans
            zip_code:
              type: string
              description: Selected entity's property zip_code
              example: "70130"
        team:
          type: object
          required:
            - id
            - name
            - internal_label
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: The name field is the Team name that is displayed on recipient
                facing reports and communication.
              example: The best company
            internal_label:
              type: string
              description: The internal label is a convenience label to help differentiate
                similar teams a user may belong to. It's displayed to you and
                your team members.
              example: The best company label
        permissions:
          type: object
          required:
            - can_update_status
            - can_update_dates
            - can_delete
          properties:
            can_update_status:
              type: boolean
            can_update_dates:
              type: boolean
            can_delete:
              type: boolean
    tenant_occupancy_create_request_model:
      type: object
      required:
        - status
      properties:
        status:
          type: string
          enum:
            - current
            - past
          description: Initial status for the new lease.
        move_in_date:
          type: string
          nullable: true
          description: Optional move-in date, in ISO format.
        lease_end_date:
          type: string
          nullable: true
          description: Optional lease end date, in ISO format.
    tenant_occupancy_update_request_model:
      type: object
      properties:
        status:
          type: string
          enum:
            - current
            - past
          description: New lease status.
        move_in_date:
          type: string
          nullable: true
          description: Move-in date, ISO format.
        lease_end_date:
          type: string
          nullable: true
          description: Lease end date, ISO format.
    resident_moved_out_response_model:
      type: object
      required:
        - leases
        - inspections
      properties:
        leases:
          type: array
          items:
            $ref: "#/components/schemas/api_tenant_occupancy"
          description: The leases for the property after the move-out.
        inspections:
          type: array
          items:
            type: object
            additionalProperties: true
          description: Inspection documents affected by the move-out.
    inspection_manual_scan_response_model:
      x-internal: true
      type: object
      required:
        - id
        - document_id
        - document_type
        - status
      description: Handle to the queued AI-damage scan throttle record.
      properties:
        id:
          type: string
          description: Auto-generated throttle id.
          example: 00BRcZPSakXz6w7RYoE
        document_id:
          type: string
          description: Id of the document (inspection) the scan applies to.
          example: 00BRcZPSakXz6w7RYoE
        document_type:
          type: string
          enum:
            - INSPECTION_AI_DAMAGE
          description: Discriminator for the throttled document type.
        status:
          type: string
          enum:
            - THROTTLED
            - CREATED
            - PROCESSING
            - DONE
            - ERROR
          description: Current status of the scan.
    inspection_media_upload_urls_request_model:
      x-internal: true
      type: object
      required:
        - items
      properties:
        items:
          type: array
          minItems: 1
          maxItems: 50
          description: Media items to generate pre-signed PUT URLs for. This list must
            contain between 1 and 50 items; empty or oversize batches are
            rejected.
          items:
            oneOf:
              - type: object
                required:
                  - filename
                  - type
                  - sectionNameWithOrdinal
                  - featureName
                  - contentType
                properties:
                  filename:
                    type: string
                    maxLength: 255
                    pattern: ^[a-zA-Z0-9._-]+$
                    description: Original filename. Restricted to alphanumerics, dots, underscores
                      and dashes; capped at 255 characters.
                    example: kitchen-1.jpg
                  type:
                    type: string
                    enum:
                      - image
                      - 360-image
                    description: Media type for the upload (image or 360-image).
                  sectionNameWithOrdinal:
                    type: string
                    description: Section the media belongs to, including its ordinal (e.g. 'Bedroom
                      1').
                  featureName:
                    type: string
                    description: Feature within the section the media is attached to.
                  contentType:
                    type: string
                    enum:
                      - image/jpeg
                      - image/jpg
                      - image/png
                      - image/webp
                      - image/heic
                      - image/heif
                    description: MIME type for image and 360-image uploads.
                    example: image/jpeg
              - type: object
                required:
                  - filename
                  - type
                  - sectionNameWithOrdinal
                  - featureName
                  - contentType
                properties:
                  filename:
                    type: string
                    maxLength: 255
                    pattern: ^[a-zA-Z0-9._-]+$
                    description: Original filename. Restricted to alphanumerics, dots, underscores
                      and dashes; capped at 255 characters.
                    example: walkthrough.mp4
                  type:
                    type: string
                    enum:
                      - video
                    description: Media type for the upload (video).
                  sectionNameWithOrdinal:
                    type: string
                    description: Section the media belongs to, including its ordinal (e.g. 'Bedroom
                      1').
                  featureName:
                    type: string
                    description: Feature within the section the media is attached to.
                  contentType:
                    type: string
                    enum:
                      - video/mp4
                      - video/quicktime
                      - video/mpeg
                      - video/webm
                    description: MIME type for video uploads.
                    example: video/mp4
    inspection_media_upload_urls_response_model:
      x-internal: true
      type: array
      description: One pre-signed URL entry per item in the request, in the same order.
      items:
        type: object
        required:
          - key
          - uploadUrl
          - expiresAt
        properties:
          key:
            type: string
            description: Logical storage key reference
              (`s3://<inspectionId>/<section>/<feature>/<uuid>-<filename>`). Use
              this to record the uploaded media against the inspection feature.
            example: s3://insp_123/Bedroom-1/Walls/3f6e1a-kitchen-1.jpg
          uploadUrl:
            type: string
            description: Pre-signed PUT URL the caller uploads the file to. Use the same
              `Content-Type` declared in the request.
          expiresAt:
            type: string
            format: date-time
            description: ISO-8601 timestamp at which every URL in the response expires (7
              days after generation).
    api_inspection_feature_media:
      x-internal: true
      type: object
      required:
        - path
        - type
        - flags
        - links
        - operation
      properties:
        path:
          type: string
          description: Storage path or URL for the media item.
        type:
          type: string
          enum:
            - image
            - video
            - 360-image
          description: Media type.
        flags:
          type: array
          description: Media role suffixes.
          items:
            type: string
            enum:
              - -revision
              - -review
              - -camera-roll
              - -360-image
              - -dualfisheye
              - -equirectangular
        image_metadata:
          type: object
          description: Optional image metadata extracted by the AI pipeline.
          additionalProperties: true
        links:
          type: array
          description: Cross-references to other inspection objects this media is linked to.
          items:
            type: object
            required:
              - type
              - linked_object_id
            properties:
              type:
                type: string
                enum:
                  - no_answer
              linked_object_id:
                type: string
                description: Id of the linked object.
        operation:
          type: string
          enum:
            - add
            - remove
            - keep
          description: Collaborative-edit operation for the media item. On submit
            endpoints `operation` is optional and an omitted value is treated as
            `add` for legacy clients.
    api_maintenance_flag_with_media:
      x-internal: true
      type: object
      description: Returns full user / inspection / work-order / maintenance-report
        snapshots alongside the flag. `operation` is omitted on responses (it is
        a client-supplied hint on submit — see the
        `api_maintenance_flag_with_media_input` variant).
      required:
        - id
        - feature_id
        - feature
        - section
        - note
        - internal_note
        - vendor
        - skill
        - cost
        - responsibility
        - custom_fields
        - media
        - created_by
        - created_date
        - inspection
        - maintenance_reports
        - work_orders
        - linked_damage_item_ids
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        feature_id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        feature:
          type: string
          description: Maintenance flag's feature name (copied from the parent feature).
        section:
          type: object
          required:
            - name
            - name_with_ordinal
            - type
          description: Section the feature belongs to.
          properties:
            name:
              type: string
              description: The section name for the feature
              example: Hallway
            name_with_ordinal:
              type: string
              description: The section name with ordinal for the feature. This is only
                different from section for rooms like Bedroom, Bathroom, etc
              example: First Bedroom
            type:
              type: string
              description: The section type for the feature as it was defined in the template
              enum:
                - room
                - section
              example: room
        note:
          type: string
          description: Maintenance flag's note.
        internal_note:
          type: string
          description: Maintenance flag's internal note.
        skill:
          type: string
          description: Required skill to address this flag.
        vendor:
          type: string
          enum:
            - Latchel
            - AppFolio
            - Yardi
            - Jenark
            - RentCheck
            - Rent Manager
            - Rentvine
          description: Vendor assigned to address the flag, when set.
        cost:
          type: number
          description: Estimated or actual cost.
        responsibility:
          type: string
          enum:
            - OWNER
            - RESIDENT
            - ""
          description: Party responsible for the flag.
        custom_fields:
          type: array
          description: Caller-defined custom fields keyed by id, each holding a list of
            selected values.
          items:
            type: object
            required:
              - id
              - value
            properties:
              id:
                type: string
              value:
                type: array
                items:
                  type: string
        created_date:
          type: string
          description: Flag creation date, ISO format.
          example: 2022-01-15T00:00:00.000
        created_by:
          type: object
          description: Basic identifying info for the user that created the flag.
          required:
            - id
            - name
            - email
            - type
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
            email:
              type: string
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
              description: User type discriminator.
        inspection:
          type: object
          required:
            - id
            - label
            - due_date
            - inspection_template
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            label:
              type: string
              description: Inspection label.
            due_date:
              type: string
              description: The inspection due date (legacy) is the original date by which the
                recipient must complete the inspection.
              example: 2021-12-02T14:15:37.925
            inspection_template:
              type: object
              required:
                - id
                - name
                - internal_label
                - property_type
                - display_name
              properties:
                id:
                  type: string
                  description: Inspection template identifier
                name:
                  type: string
                  description: Inspection template name.
                internal_label:
                  type: string
                  description: Inspection template internal label.
                property_type:
                  type: string
                  description: Inspection template property type.
                display_name:
                  type: string
                  description: Resolved display name for the inspection template, used by clients
                    to render the template title (mirrors `name` for legacy
                    templates).
          description: Basic info for the inspection the flag belongs to.
        maintenance_reports:
          type: array
          description: Maintenance reports the flag has been included in. Retained for
            schema compatibility, but always emitted as `[]`.
          items:
            type: object
            required:
              - id
              - name
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              name:
                type: string
        work_orders:
          type: array
          description: Work orders the flag has been included in.
          items:
            type: object
            required:
              - id
              - name
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              name:
                type: string
        linked_damage_item_ids:
          type: array
          items:
            type: string
          description: Ids of damage items this flag is linked to (when the AI damage
            pipeline produced any).
        media:
          type: array
          description: Replaces the legacy `image_urls`/`video_urls` lists with structured
            media entries.
          items:
            $ref: "#/components/schemas/api_inspection_feature_media"
        operation:
          type: string
          enum:
            - add
            - remove
            - keep
          description: Collaborative-edit operation for the flag itself. Optional —
            omitted on API responses; set by clients during submit. When omitted
            on submit, the entry is treated as an implicit `add` for legacy
            clients.
    api_maintenance_flag_with_media_input:
      x-internal: true
      type: object
      required:
        - id
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        feature_id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        note:
          type: string
          description: Maintenance flag's note.
        internal_note:
          type: string
          description: Maintenance flag's internal note.
        skill:
          type: string
          description: Required skill to address this flag.
        vendor:
          type: string
          enum:
            - Latchel
            - AppFolio
            - Yardi
            - Jenark
            - RentCheck
            - Rent Manager
            - Rentvine
          description: Vendor assigned to address the flag, when set.
        cost:
          type: number
          description: Estimated or actual cost.
        responsibility:
          type: string
          enum:
            - OWNER
            - RESIDENT
            - ""
          description: Party responsible for the flag.
        custom_fields:
          type: array
          description: Caller-defined custom fields keyed by id, each holding a list of
            selected values.
          items:
            type: object
            required:
              - id
              - value
            properties:
              id:
                type: string
              value:
                type: array
                items:
                  type: string
        linked_damage_item_ids:
          type: array
          items:
            type: string
          description: Ids of damage items this flag is linked to (when the AI damage
            pipeline produced any).
        media:
          type: array
          description: Structured media entries. Items use the input variant so
            `operation` may be omitted (treated as an implicit `add` for legacy
            clients).
          items:
            $ref: "#/components/schemas/inspection_feature_media_input_model"
        operation:
          type: string
          enum:
            - add
            - remove
            - keep
          description: Collaborative-edit operation for the flag. Optional — when omitted
            on submit the entry is treated as an implicit `add` for legacy
            clients.
    api_inspection_feature:
      x-internal: true
      type: object
      description: "`created_by`/`updated_by`/`review.reviewed_by` are full user
        snapshots, and `inspection` is a basic inspection snapshot (not a custom
        truncated shape). `review` and `revision` are always emitted (nullable
        when absent)."
      required:
        - id
        - name
        - description
        - section
        - created_by
        - created_at
        - updated_by
        - updated_at
        - inspection
        - rating
        - notes
        - not_applicable
        - incomplete
        - review
        - revision
        - template_settings
        - maintenance_flags
        - media
        - questions
        - responses
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        name:
          type: string
          description: The feature name.
        description:
          type: string
          description: Long-form description of the feature.
        section:
          type: object
          required:
            - name
            - name_with_ordinal
            - type
          properties:
            name:
              type: string
              description: Section name.
            name_with_ordinal:
              type: string
              description: Section name with its ordinal (e.g. `Bedroom 1`).
            type:
              type: string
              enum:
                - room
                - section
        created_by:
          type: object
          required:
            - id
            - name
            - email
            - type
          description: Basic user identity fields.
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: Name of the user
            email:
              type: string
              description: Email of the user
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
              description: User type discriminator.
        created_at:
          type: string
          format: date-time
        updated_by:
          type: object
          required:
            - id
            - name
            - email
            - type
          description: Basic user identity fields.
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: Name of the user
            email:
              type: string
              description: Email of the user
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
              description: User type discriminator.
        updated_at:
          type: string
          format: date-time
        inspection:
          type: object
          required:
            - id
            - label
            - due_date
            - inspection_template
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            label:
              type: string
              description: Inspection label.
            due_date:
              type: string
              description: The inspection due date (legacy) is the original date by which the
                recipient must complete the inspection.
              example: 2021-12-02T14:15:37.925
            inspection_template:
              type: object
              required:
                - id
                - name
                - internal_label
                - property_type
                - display_name
              properties:
                id:
                  type: string
                  description: Inspection template identifier
                name:
                  type: string
                  description: Inspection template name.
                internal_label:
                  type: string
                  description: Inspection template internal label.
                property_type:
                  type: string
                  description: Inspection template property type.
                display_name:
                  type: string
                  description: Resolved display name for the inspection template, used by clients
                    to render the template title (mirrors `name` for legacy
                    templates).
          description: Basic info for the inspection the feature belongs to.
        rating:
          type: string
          nullable: true
          enum:
            - Poor
            - Good
            - Fair
            - ""
          description: Feature rating, or null when not rated.
        notes:
          type: string
          nullable: true
        not_applicable:
          type: boolean
        incomplete:
          type: boolean
        review:
          type: object
          nullable: true
          properties:
            approved:
              type: boolean
            reviewed_by:
              type: object
              required:
                - id
                - name
                - email
                - type
              description: Basic user identity fields.
              properties:
                id:
                  type: string
                  description: Entity ID
                  example: 00BRcZPSakXz6w7RYoE
                name:
                  type: string
                  description: Name of the user
                email:
                  type: string
                  description: Email of the user
                type:
                  type: string
                  enum:
                    - Renter
                    - Property Manager
                    - Landlord
                    - System
                  description: User type discriminator.
            reviewed_at:
              type: string
              format: date-time
            notes:
              type: string
              nullable: true
        revision:
          type: object
          nullable: true
          properties:
            notes:
              type: string
              nullable: true
            updated:
              type: boolean
              description: True when the performer has updated the feature since the revision
                was requested.
        template_settings:
          type: object
          properties:
            is_rating_required:
              type: boolean
            is_video_enabled:
              type: boolean
            number_of_photos_required:
              type: integer
            reference_photos:
              type: array
              items:
                type: string
        maintenance_flags:
          type: array
          items:
            $ref: "#/components/schemas/api_maintenance_flag_with_media"
        media:
          type: array
          items:
            $ref: "#/components/schemas/api_inspection_feature_media"
        questions:
          type: array
          items:
            type: object
            additionalProperties: true
          description: Template questions for this feature.
        responses:
          type: array
          items:
            type: string
          description: Answers given to the template questions.
    inspection_feature_media_input_model:
      x-internal: true
      type: object
      required:
        - path
        - type
      properties:
        path:
          type: string
        type:
          type: string
          enum:
            - image
            - video
            - 360-image
        flags:
          type: array
          items:
            type: string
            enum:
              - -revision
              - -review
              - -camera-roll
              - -360-image
              - -dualfisheye
              - -equirectangular
        image_metadata:
          type: object
          additionalProperties: true
        links:
          type: array
          items:
            type: object
            required:
              - type
              - linked_object_id
            properties:
              type:
                type: string
                enum:
                  - no_answer
              linked_object_id:
                type: string
        operation:
          type: string
          enum:
            - add
            - remove
            - keep
          description: Optional on submit; when omitted the entry is treated as an
            implicit `add` for legacy clients.
    inspection_feature_edit_request_model:
      x-internal: true
      type: object
      description: Partial update to an existing inspection feature.
      properties:
        rating:
          type: string
          enum:
            - Poor
            - Good
            - Fair
            - ""
        responses:
          type: array
          items:
            type: string
        notes:
          type: string
        media:
          type: array
          items:
            $ref: "#/components/schemas/inspection_feature_media_input_model"
    inspection_feature_submit_request_model:
      x-internal: true
      type: object
      required:
        - name
        - section
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        name:
          type: string
        section:
          type: object
          required:
            - name
            - name_with_ordinal
            - type
          properties:
            name:
              type: string
            name_with_ordinal:
              type: string
            type:
              type: string
              enum:
                - room
                - section
        rating:
          type: string
          nullable: true
          enum:
            - Poor
            - Good
            - Fair
            - ""
        notes:
          type: string
          nullable: true
        not_applicable:
          type: boolean
        responses:
          type: array
          items:
            type: string
        media:
          type: array
          items:
            $ref: "#/components/schemas/inspection_feature_media_input_model"
        maintenance_flags:
          type: array
          description: Maintenance flags to upsert alongside the feature. Each entry uses
            the input variant so inner `media.operation` may be omitted (legacy
            compatibility).
          items:
            $ref: "#/components/schemas/api_maintenance_flag_with_media_input"
        review:
          type: object
          properties:
            notes:
              type: string
              nullable: true
        revision:
          type: object
          properties:
            notes:
              type: string
              nullable: true
    inspection_feature_revision_request_model:
      x-internal: true
      type: object
      description: Partial revision payload.
      properties:
        notes:
          type: string
          nullable: true
        updated:
          type: boolean
    maintenance_flag_create_request_model:
      x-internal: true
      type: object
      required:
        - note
        - skill
      properties:
        note:
          type: string
        internal_note:
          type: string
        responsibility:
          type: string
          enum:
            - OWNER
            - RESIDENT
            - ""
        cost:
          type: number
        skill:
          type: string
        image_urls:
          type: array
          items:
            type: string
        video_urls:
          type: array
          items:
            type: string
        vendor:
          type: string
          enum:
            - Latchel
            - AppFolio
            - Yardi
            - Jenark
            - RentCheck
            - Rent Manager
            - Rentvine
        custom_fields:
          type: array
          items:
            type: object
            required:
              - id
              - value
            properties:
              id:
                type: string
              value:
                type: array
                items:
                  type: string
        damage_item_ids:
          type: array
          items:
            type: string
    maintenance_flag_upsert_request_model:
      x-internal: true
      type: object
      description: Partial update to an existing maintenance flag. All fields from
        `maintenance_flag_create_request_model` are accepted as optional.
      properties:
        note:
          type: string
        internal_note:
          type: string
        responsibility:
          type: string
          enum:
            - OWNER
            - RESIDENT
            - ""
        cost:
          type: number
        skill:
          type: string
        image_urls:
          type: array
          items:
            type: string
        video_urls:
          type: array
          items:
            type: string
        vendor:
          type: string
          enum:
            - Latchel
            - AppFolio
            - Yardi
            - Jenark
            - RentCheck
            - Rent Manager
            - Rentvine
        custom_fields:
          type: array
          items:
            type: object
            required:
              - id
              - value
            properties:
              id:
                type: string
              value:
                type: array
                items:
                  type: string
        damage_item_ids:
          type: array
          items:
            type: string
    notifications_preferences_response_model:
      type: object
      required:
        - id
        - inspections_awaiting_review
        - inspections_in_revision_review
        - inspections_completed
        - automation_failures
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        user_id:
          type: string
          description: Set when the preferences belong to a registered RentCheck user.
        user_email:
          type: string
          description: Set when the preferences belong to an unregistered recipient
            managing settings via an emailed token.
        subscription_id:
          type: string
          description: Set when the preferences are the subscription-level defaults
            applied when no per-user preferences exist.
        inspections_awaiting_review:
          $ref: "#/components/schemas/notification_preference"
        inspections_in_revision_review:
          $ref: "#/components/schemas/notification_preference"
        inspections_completed:
          $ref: "#/components/schemas/notification_preference"
        automation_failures:
          $ref: "#/components/schemas/notification_preference_no_email"
    notifications_preferences_update_request_model:
      type: object
      description: Partial update — any combination of the fields on the preferences
        document may be provided. Server merges the supplied values onto the
        current preferences.
      properties:
        inspections_awaiting_review:
          $ref: "#/components/schemas/notification_preference"
        inspections_in_revision_review:
          $ref: "#/components/schemas/notification_preference"
        inspections_completed:
          $ref: "#/components/schemas/notification_preference"
        automation_failures:
          $ref: "#/components/schemas/notification_preference_no_email"
    notification_preference:
      type: object
      required:
        - enabled
      properties:
        enabled:
          type: boolean
          description: Whether notifications for this category are enabled at all.
        email:
          type: string
          enum:
            - all
            - created_by_me
          description: Optional email-scoping rule when enabled. Defaults to all matching
            events when omitted.
    notification_preference_no_email:
      type: object
      required:
        - enabled
      properties:
        enabled:
          type: boolean
          description: Whether notifications for this category are enabled at all.
    api_image_metadata:
      type: object
      required:
        - id
        - status
        - inspection_feature
        - damage_items
        - associated_flags
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        status:
          type: string
          enum:
            - THROTTLED
            - CREATED
            - PROCESSING
            - PROCESSED
            - FAILED
          description: Lifecycle status of the AI image-analysis run for this metadata
            document.
        inspection_feature:
          type: object
          required:
            - id
            - image_key
            - inspection_id
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            image_key:
              type: string
              description: Storage key for the analyzed image.
            inspection_id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
        damage_items:
          type: array
          items:
            $ref: "#/components/schemas/api_image_metadata_damage_item"
        associated_flags:
          type: array
          items:
            type: object
            additionalProperties: true
          description: Maintenance flags on the underlying inspection feature that this
            image is currently associated with.
    api_image_metadata_damage_item:
      type: object
      required:
        - id
        - damage_type
        - damage_description
        - damage_severity
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        damage_type:
          type: string
          description: The type of damage incurred.
        damage_description:
          type: string
          description: A description providing details about the damage.
        damage_severity:
          type: number
          minimum: 1
          maximum: 10
          description: Severity rating from 1 to 10, where 10 is the most severe.
        skill_guess:
          type: string
          description: Skill guess inferred by the image-analysis pipeline that maps to
            the damage item.
        user_feedback:
          type: string
          enum:
            - correct
            - incorrect
          description: Optional user feedback acknowledging or correcting the inferred
            damage.
        confidence:
          type: number
          minimum: 0
          maximum: 1
          description: Model-reported confidence (0-1) that this is real damage rather
            than normal wear, a reflection, a shadow, or dust. Populated by both
            the Gemini-backed and OpenAI damage-detection paths. Absent on
            damage items persisted before this field was added.
        polygon:
          type: array
          description: Optional polygon outlining the damage region. Each vertex is an
            `{x, y}` object in 0-1 normalized image coordinates (top-left
            origin). Populated by the Gemini-backed damage-detection path; the
            OpenAI path leaves this field undefined. Typical polygons have 12-24
            vertices.
          items:
            type: object
            required:
              - x
              - y
            properties:
              x:
                type: number
                minimum: 0
                maximum: 1
                description: Horizontal coordinate of the vertex, normalized to the image width
                  (0 = left edge, 1 = right edge).
              y:
                type: number
                minimum: 0
                maximum: 1
                description: Vertical coordinate of the vertex, normalized to the image height
                  (0 = top edge, 1 = bottom edge).
    image_metadata_get_all_response_model:
      type: array
      items:
        $ref: "#/components/schemas/api_image_metadata"
    image_metadata_update_request_model:
      type: object
      description: Partial update for a single image metadata document. Currently only
        `damage_items` is editable, and entries are matched by their `id`.
      properties:
        damage_items:
          type: array
          description: Subset of damage items to update. Each entry must include the
            existing `id` of the damage item being patched.
          items:
            type: object
            required:
              - id
            properties:
              id:
                type: string
                description: Entity ID
                example: 00BRcZPSakXz6w7RYoE
              user_feedback:
                type: string
                enum:
                  - correct
                  - incorrect
                description: User feedback acknowledging or correcting the inferred damage.
    rev_share_prospect_create_request_model:
      type: object
      required:
        - prospect_email
        - total_properties
        - chargebee_plan_id
      properties:
        prospect_email:
          type: string
          format: email
          description: Email address of the prospect being onboarded.
        user_name:
          type: string
          description: Optional display name to assign to the newly-created prospect user.
        total_properties:
          type: string
          description: Self-reported portfolio size for the prospect, as a numeric string
            parsed by the backend.
        chargebee_plan_id:
          type: string
          description: Chargebee plan id the prospect should be associated with.
        chargebee_addon_ids:
          type: array
          items:
            type: string
          description: Chargebee addon ids to attach to the prospect alongside the
            selected plan.
    zapier_resident_create_request_model:
      x-internal: true
      type: object
      required:
        - address
        - postal_code
        - first_name
        - last_name
        - email
      properties:
        address:
          type: string
          description: Street address of the resident's current property. Combined with
            `postal_code` to locate the property owned by the caller's user or
            organizations.
        postal_code:
          type: string
          description: Postal code (ZIP) matching the property being looked up.
        first_name:
          type: string
          description: Resident's first name.
        last_name:
          type: string
          description: Resident's last name.
        email:
          type: string
          format: email
          description: Resident's email address. Used both to check for an existing
            resident record on the property and as the primary contact email on
            the created resident.
        phone_number:
          type: string
          description: Optional single phone number, added as the sole entry in the
            resident's `phone_numbers` array.
        lease_start_date:
          type: string
          description: Optional ISO-8601 lease start date.
        lease_end_date:
          type: string
          description: Optional ISO-8601 lease end date.
        monthly_rent:
          type: number
          description: Optional monthly rent amount for the lease.
        security_deposit:
          type: number
          description: Optional security deposit amount for the lease.
    zapier_inspection_create_request_model:
      x-internal: true
      type: object
      required:
        - inspection_template_id
        - address
        - postal_code
        - due_date
      properties:
        inspection_template_id:
          type: string
          description: ID of the inspection template to use. Resolved server-side; the
            resolved template's `property_type` scopes the property lookup
            below.
        address:
          type: string
          description: Street address of the target property. Combined with `postal_code`
            to locate the property owned by the caller's user or organizations.
        postal_code:
          type: string
          description: Postal code (ZIP) matching the property being looked up.
        due_date:
          type: string
          description: ISO-8601 due date. Forwarded to the v2 create flow, which validates
            the date is not in the past.
        invite_date:
          type: string
          description: Optional ISO-8601 invite date. Forwarded to the v2 create flow.
        inspection_label:
          type: string
          description: Optional inspection label, mapped onto `data.label` for the v2
            create flow.
        is_fast_track:
          type: boolean
          description: When `true`, the inspection is created with the fast-track flow
            (skips standard review). Mapped onto `data.fast_track` for the v2
            create flow.
        resident_email_1:
          type: string
          format: email
          description: "Optional resident email #1. Any of `resident_email_1..5` that are
            provided are collected into the v2 `recipients.emails` array; when
            none are provided the inspection is created as `self-perform`."
        resident_email_2:
          type: string
          format: email
          description: "Optional resident email #2 (see `resident_email_1`)."
        resident_email_3:
          type: string
          format: email
          description: "Optional resident email #3 (see `resident_email_1`)."
        resident_email_4:
          type: string
          format: email
          description: "Optional resident email #4 (see `resident_email_1`)."
        resident_email_5:
          type: string
          format: email
          description: "Optional resident email #5 (see `resident_email_1`)."
    zapier_property_create_request_model:
      x-internal: true
      type: object
      required:
        - address
        - city
        - state
        - postal_code
        - property_type
        - bedrooms
        - bathrooms
      properties:
        address:
          type: string
          description: Street address of the unit being created.
        city:
          type: string
          description: City of the unit.
        state:
          type: string
          description: US state code. Used to derive the persisted `region` value.
        postal_code:
          type: string
          description: Postal code (ZIP) of the unit.
        property_type:
          type: string
          description: Unit property type.
          enum:
            - ""
            - Single-Family
            - Multi-Family
            - Apartment or Condo
            - Commercial
        bedrooms:
          type: integer
          minimum: 0
          maximum: 10
          description: Number of bedrooms. Enforced to `[0, 10]`.
        bathrooms:
          type: number
          minimum: 0
          description: Total number of bathrooms. Split into `full_bathrooms` /
            `half_bathrooms` before delegating to the v1 unit create flow.
    api_user_team_digest:
      type: object
      required:
        - id
        - name
        - internal_label
        - role
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        name:
          type: string
          description: Team display name.
        internal_label:
          type: string
          description: Internal label used to differentiate similar teams.
        role:
          type: string
          enum:
            - admin
            - manager
            - none
          description: The user's membership role within this team.
    api_user_metadata:
      type: object
      description: Per-user metadata document tracking onboarding dismissals,
        view-mode preferences and other UI state.
      required:
        - needs_to_perform_onboard_survey
        - needs_to_complete_subscription
      properties:
        import_residents_integration_appfolio_dismissed:
          type: boolean
          description: Whether the AppFolio import-residents integration banner has been
            dismissed.
        import_residents_integration_rentmanager_dismissed:
          type: boolean
          description: Whether the Rent Manager import-residents integration banner has
            been dismissed.
        import_units_integration_appfolio_dismissed:
          type: boolean
          description: Whether the AppFolio import-units integration banner has been
            dismissed.
        import_units_integration_rentmanager_dismissed:
          type: boolean
          description: Whether the Rent Manager import-units integration banner has been
            dismissed.
        inspection_onboarding_shown_count:
          type: integer
          description: Number of times the inspection-details onboarding has been shown to
            the user.
        onboarding_shown_count:
          type: integer
          description: Number of times the general onboarding has been shown.
        rating_shown_count:
          type: integer
          description: Number of times the in-app rating prompt has been shown.
        try_pro_plan_inspection_templates_card_shown_count:
          type: integer
          description: Number of times the "Try Pro plan templates" upsell card has been
            shown. Card is hidden once count reaches 30.
        feature_editing_dismissed:
          type: boolean
          description: Whether the feature-editing "don't show again" modal has been
            dismissed.
        inspection_view_mode:
          type: string
          enum:
            - list
            - review
          description: Preferred view mode on the inspection-details screen.
        sign_up_reason:
          type: string
          enum:
            - pm-signup
            - self-serve
          description: The reason the user signed up; used to tailor the onboarding flow.
        needs_to_perform_onboard_survey:
          type: boolean
          description: When true, the user must complete the onboarding survey before
            proceeding.
        needs_to_complete_subscription:
          type: boolean
          description: When true, the user must complete the subscription setup before
            proceeding.
        total_properties:
          type: integer
          description: Self-reported portfolio size captured at sign-up.
    api_user_external_identities:
      type: object
      description: Third-party identity verification payloads. Keys are provider-specific.
      properties:
        unlayer:
          type: object
          description: Unlayer email-builder identity verification (HMAC signed user id).
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            signature:
              type: string
              description: HMAC-SHA256 signature of the user id.
    api_user:
      type: object
      description: Full user document for the authenticated caller.
      required:
        - id
        - email
        - name
        - type
        - created_date
        - metadata
        - internal_admin
        - internal_sales
        - template_admin
        - teams
        - permissions
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        email:
          type: string
          description: The user's email.
        name:
          type: string
          description: The user's full name.
        type:
          type: string
          enum:
            - Renter
            - Property Manager
            - Landlord
            - System
          description: User type.
        created_date:
          type: string
          description: ISO timestamp the user was created.
        fcm_tokens:
          type: array
          items:
            type: string
          description: Registered FCM device tokens for push notifications.
        temporary_password:
          type: string
          description: Temporary password assigned when the account was created on behalf
            of the user; cleared on first password update.
        internal_admin:
          type: boolean
          description: When true, the user has access to the internal admin panel.
        internal_sales:
          type: boolean
          description: When true, the user has access to the internal sales panel.
        template_admin:
          type: boolean
          description: When true, the user can edit shared inspection templates.
        prospect_data:
          type: object
          additionalProperties: true
          description: Sales prospect payload captured during the demo follow-up flow.
            Only set when the user has no active subscription.
        teams:
          type: array
          items:
            $ref: "#/components/schemas/api_user_team_digest"
          description: Teams the user belongs to and their role on each.
        current_occupancy:
          type: object
          additionalProperties: true
          description: When the caller is a renter, the occupancy currently flagged
            `current`.
        metadata:
          $ref: "#/components/schemas/api_user_metadata"
        permissions:
          type: object
          required:
            - allow_billing_plan_editing
            - allow_automation_creation
            - allow_automation_editing
            - allow_notifications_preferences_editing
            - allow_inspection_messaging_editing
            - allow_ai_manual_scan
          properties:
            allow_billing_plan_editing:
              type: boolean
            allow_automation_creation:
              type: boolean
            allow_automation_editing:
              type: boolean
            allow_notifications_preferences_editing:
              type: boolean
            allow_inspection_messaging_editing:
              type: boolean
            allow_ai_manual_scan:
              type: boolean
          description: Per-feature permissions resolved from the user's permission groups
            and subscription.
        external_identities:
          $ref: "#/components/schemas/api_user_external_identities"
    me_update_request_model:
      type: object
      description: Partial update for the authenticated user. Email or password
        changes require `current_password`.
      properties:
        name:
          type: string
          minLength: 1
          description: New display name.
        email:
          type: string
          format: email
          description: New email address. Requires `current_password` for re-authentication.
        password:
          type: string
          minLength: 8
          description: New password. Requires `current_password` for re-authentication.
        current_password:
          type: string
          description: Current password, required when changing `email` or `password`.
        feature_editing_dismissed:
          type: boolean
          description: Dismiss the feature-editing onboarding modal.
        rating_shown_count:
          type: integer
          description: Update the in-app rating prompt counter.
        inspection_onboarding_shown_count:
          type: integer
          description: Update the inspection onboarding shown counter.
        payment_info_provided:
          type: boolean
          description: Marks that the user has completed payment-method entry. Persisted
            on the user profile.
        try_pro_plan_inspection_templates_card_shown_count:
          type: integer
          description: Update the "Try Pro plan templates" upsell card shown counter.
    me_update_settings_request_model:
      type: object
      description: Partial update for the authenticated user's metadata / preferences.
      properties:
        inspection_view_mode:
          type: string
          enum:
            - list
            - review
          description: Preferred view mode on the inspection-details screen.
        import_units_integration_appfolio_dismissed:
          type: boolean
        import_units_integration_rentmanager_dismissed:
          type: boolean
        import_residents_integration_appfolio_dismissed:
          type: boolean
        import_residents_integration_rentmanager_dismissed:
          type: boolean
        needs_to_perform_onboard_survey:
          type: boolean
        needs_to_complete_subscription:
          type: boolean
        total_properties:
          type: integer
          minimum: 0
    me_link_with_tenant_request_model:
      type: object
      required:
        - invite_id
      description: Request body for linking the caller to a tenant record via an invite.
      properties:
        invite_id:
          type: string
          description: ID of the inspection invite whose tenant record should be linked to
            the caller.
          example: 00BRcZPSakXz6w7RYoE
    me_update_fcm_token_request_model:
      type: object
      required:
        - fcm_token
      description: Request body for registering an FCM device token.
      properties:
        fcm_token:
          type: string
          minLength: 1
          description: FCM device token to register for the caller.
    me_get_integration_token_response_model:
      type: object
      required:
        - token
      properties:
        token:
          type: string
          description: Long-lived integration token the caller can use for
            service-to-service API calls.
    me_get_firebase_token_response_model:
      type: object
      required:
        - token
      properties:
        token:
          type: string
          description: Short-lived Firebase custom auth token minted for the caller.
    me_get_metabase_embed_token_response_model:
      x-internal: true
      type: object
      description: Envelope for `GET /v1/me/metabase-embed-token`. Returns `{ token }`
        wrapped in the standard `{ data }` shape.
      required:
        - status
        - data
      properties:
        status:
          type: integer
          description: HTTP status code
          example: 200
        data:
          type: object
          required:
            - token
          properties:
            token:
              type: string
              description: Signed Metabase static-embedding JWT, scoped to the caller's
                subscription and locked to the Resident Insights dashboard.
                Signed with `HS256`, with a server-controlled TTL.
    oauth2_token_request_model:
      x-internal: true
      type: object
      description: Body for `POST /v1/oAuth2/token`. Used to complete the RentCheck
        OAuth2 authorization-code exchange (primary consumer is the Zapier
        integration).
      required:
        - client_id
        - client_secret
        - code
      properties:
        client_id:
          type: string
          description: Integration application id issued by RentCheck (aka `x-app-id`).
        client_secret:
          type: string
          description: Integration application secret issued by RentCheck (aka
            `x-app-secret`).
        code:
          type: string
          description: One-time authorization code returned by the RentCheck OAuth2 login
            flow. Consumed on success (the same code cannot be exchanged twice).
    oauth2_token_response_model:
      x-internal: true
      type: object
      description: Successful token-exchange response for `POST /v1/oAuth2/token`.
      required:
        - access_token
        - refresh_token
        - org_id
      properties:
        access_token:
          type: string
          description: "Short-lived bearer token to use as `Authorization: Bearer
            <access_token>` on subsequent RentCheck API calls."
        refresh_token:
          type: string
          description: Long-lived refresh token accepted by `POST /v1/oAuth2/refresh_token`.
        org_id:
          type: string
          description: Organization id the tokens are scoped to (the caller's
            Zapier-configured `org_id`).
    oauth2_me_response_model:
      x-internal: true
      type: object
      description: Response body for `POST /v1/oAuth2/me`. Returns a synthetic
        identity payload the OAuth2 client can display back to the user.
      required:
        - email
        - timestamp
        - userId
        - orgId
        - appId
      properties:
        email:
          type: string
          format: email
          description: The authenticated user's email.
        timestamp:
          type: string
          description: Server-side localised timestamp string captured at the time the
            endpoint served the request. Purely informational.
        userId:
          type: string
          description: The authenticated user's RentCheck id.
        orgId:
          type: string
          description: Organization id decoded from the caller's access token.
        appId:
          type: string
          description: Integration application id decoded from the caller's access token.
    me_get_pm_contact_info_response_model:
      type: object
      description: Property manager contact info exposed to a linked renter.
      required:
        - company
        - email
        - phone_number
      properties:
        company:
          type: string
          description: Property management company name.
        email:
          type: string
          description: Primary contact email.
        phone_number:
          type: string
          description: Primary contact phone number.
    me_get_user_requests_response_model:
      type: array
      description: Inspection requests pending action from the caller.
      items:
        $ref: "#/components/schemas/api_inspection_request"
    api_chargebee_addon:
      x-internal: true
      type: object
      required:
        - id
        - name
        - invoice_name
        - description
        - pricing_model
        - price
      properties:
        id:
          type: string
          description: Chargebee addon id.
        name:
          type: string
          description: Addon name.
        invoice_name:
          type: string
          description: Name shown on the invoice line item.
        description:
          type: string
          description: Addon description.
        pricing_model:
          type: string
          enum:
            - flat_fee
            - per_unit
            - tiered
            - volume
            - stairstep
          description: Chargebee pricing model for the addon.
        price:
          type: integer
          description: Price in cents (Chargebee stores money as integer cents).
    api_chargebee_plan:
      x-internal: true
      type: object
      required:
        - id
        - name
        - invoice_name
        - description
        - charge_model
        - price
      properties:
        id:
          type: string
          description: Chargebee plan id.
        name:
          type: string
          description: Plan name.
        invoice_name:
          type: string
          description: Name shown on the invoice line item.
        description:
          type: string
          description: Plan description.
        charge_model:
          type: string
          enum:
            - flat_fee
            - per_unit
            - tiered
            - volume
            - stairstep
          description: Chargebee charge model for the plan.
        trial_period:
          type: integer
          description: Length of the trial period, if any.
        trial_period_unit:
          type: string
          enum:
            - day
            - month
          description: Unit for `trial_period`.
        price:
          type: integer
          description: Price in cents (Chargebee stores money as integer cents).
    api_chargebee_subscription:
      x-internal: true
      type: object
      required:
        - id
        - plan_id
        - plan_quantity
        - plan_unit_price
        - customer_id
        - plan_amount
        - plan_free_quantity
        - status
        - current_term_start
        - current_term_end
        - created_at
        - started_at
        - updated_at
        - currency_code
        - due_invoices_count
        - cf_Minimum_Units
      properties:
        id:
          type: string
          description: Chargebee subscription id.
        plan_id:
          type: string
          description: Chargebee plan id the subscription is on.
        plan_quantity:
          type: integer
          description: Plan quantity.
        plan_unit_price:
          type: integer
          description: Plan unit price in cents.
        customer_id:
          type: string
          description: Chargebee customer id that owns the subscription.
        plan_amount:
          type: integer
          description: Plan amount in cents.
        plan_free_quantity:
          type: integer
          description: Quantity included free with the plan.
        status:
          type: string
          enum:
            - active
            - future
            - in_trial
            - non_renewing
            - paused
            - cancelled
          description: Subscription status.
        current_term_start:
          type: integer
          format: int64
          description: Start of the current term, in seconds since epoch.
        current_term_end:
          type: integer
          format: int64
          description: End of the current term, in seconds since epoch.
        next_billing_at:
          type: integer
          format: int64
          description: Next billing timestamp, in seconds since epoch.
        created_at:
          type: integer
          format: int64
          description: Creation timestamp, in seconds since epoch.
        pause_date:
          type: integer
          format: int64
          description: Pause timestamp, in seconds since epoch.
        started_at:
          type: integer
          format: int64
          description: Start timestamp, in seconds since epoch.
        updated_at:
          type: integer
          format: int64
          description: Last update timestamp, in seconds since epoch.
        currency_code:
          type: string
          enum:
            - USD
          description: Subscription currency.
        addons:
          type: array
          description: Addons attached to the subscription.
          items:
            type: object
            required:
              - id
              - quantity
              - unit_price
              - amount
            properties:
              id:
                type: string
                description: Addon id.
              quantity:
                type: integer
                description: Addon quantity.
              unit_price:
                type: integer
                description: Unit price in cents.
              amount:
                type: integer
                description: Total amount in cents.
        coupons:
          type: array
          description: Coupons applied to the subscription.
          items:
            type: object
            required:
              - coupon_id
              - applied_count
            properties:
              coupon_id:
                type: string
                description: Coupon id.
              applied_count:
                type: integer
                description: Number of times the coupon has been applied.
        due_invoices_count:
          type: integer
          description: Number of due invoices.
        cf_Minimum_Units:
          type: integer
          description: Minimum number of units enforced by the subscription.
        trial_start:
          type: integer
          format: int64
          description: Trial start timestamp, in seconds since epoch.
        trial_end:
          type: integer
          format: int64
          description: Trial end timestamp, in seconds since epoch.
    chargebee_get_all_addons_response_model:
      x-internal: true
      type: array
      description: List of Chargebee addons available to the caller.
      items:
        $ref: "#/components/schemas/api_chargebee_addon"
    chargebee_get_all_plans_response_model:
      x-internal: true
      type: array
      description: List of Chargebee plans available to the caller.
      items:
        $ref: "#/components/schemas/api_chargebee_plan"
    api_automation:
      x-internal: true
      type: object
      description: Inspection automation record.
      required:
        - id
        - name
        - creator
        - subscription_id
        - active
        - is_rc_automation
        - fast_track
        - selection_type
        - inspection_template
        - creation_date
        - trigger_type
        - assignment_type
        - recurrence
        - created_at
      properties:
        id:
          type: string
          description: Entity ID
          example: 00BRcZPSakXz6w7RYoE
        name:
          type: string
          description: Automation name.
        creator:
          type: object
          required:
            - id
            - name
            - email
            - type
          description: Basic user identity fields.
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
              description: Name of the user
            email:
              type: string
              description: Email of the user
            type:
              type: string
              enum:
                - Renter
                - Property Manager
                - Landlord
                - System
              description: User type discriminator.
        subscription_id:
          type: string
          description: Subscription that owns the automation.
        active:
          type: boolean
          description: Whether the automation is currently active. Newly created
            automations are inactive until manually enabled.
        is_rc_automation:
          type: boolean
          description: "`true` when the automation was created by default by RentCheck;
            `false` for user-created automations."
        fast_track:
          type: boolean
          description: When true, generated inspections are created in fast-track mode.
        selection_type:
          type: object
          required:
            - type
          description: Determines which properties the automation targets.
          properties:
            type:
              type: string
              enum:
                - all_units
                - specific_teams
                - specific_units
            ids:
              type: array
              items:
                type: string
              description: Required when `type` is `specific_teams` or `specific_units`; must
                contain at least one id.
        inspection_template:
          type: object
          required:
            - id
            - name
            - internal_label
            - is_fast_track_enabled
            - property_type
            - is_rc_template
          description: Snapshot of the inspection template fields needed by the automation
            runtime.
          properties:
            id:
              type: string
              description: Entity ID
              example: 00BRcZPSakXz6w7RYoE
            name:
              type: string
            internal_label:
              type: string
            is_fast_track_enabled:
              type: boolean
            property_type:
              type: string
              enum:
                - unit
                - building
                - community
            is_rc_template:
              type: boolean
        creation_date:
          type: object
          required:
            - unit
            - value
            - condition
            - due_date_after_days
          description: Window relative to the trigger that determines when inspections are
            scheduled.
          properties:
            unit:
              type: string
              enum:
                - days
                - weeks
            value:
              type: integer
              minimum: 0
              maximum: 999
              description: Amount of `unit`. Validated 0–999 inclusive.
            condition:
              type: string
              enum:
                - before
                - after
            due_date_after_days:
              type: integer
              minimum: 0
              maximum: 99
              description: Days between the inspection creation date and its due date (0–99).
        trigger_type:
          type: string
          enum:
            - tenant-move-in
            - tenant-move-out
            - lease-end
            - lease-start
          description: Event that triggers the automation.
        assignment_type:
          type: object
          required:
            - type
          description: Who the generated inspections are assigned to. The required
            follow-up fields depend on `type`.
          properties:
            type:
              type: string
              enum:
                - residents
                - self-perform
                - emails
                - teammates
            emails:
              oneOf:
                - type: array
                  items:
                    type: string
                    format: email
                  minItems: 1
                - type: "null"
              description: Required (min 1) when `type` is `emails`; must be null otherwise.
            teammate_emails:
              oneOf:
                - type: object
                  additionalProperties:
                    type: array
                    items:
                      type: string
                      format: email
                    minItems: 1
                - type: "null"
              description: Required when `type` is `teammates`. Maps team id → non-empty array
                of teammate emails. Must be null otherwise.
            resident_type:
              oneOf:
                - type: string
                  enum:
                    - current
                    - future
                - type: "null"
              description: Required when `type` is `residents`; must be null otherwise.
        recurrence:
          type: object
          required:
            - frequency
          description: How often the automation repeats after its initial inspection. When
            `trigger_type` is `tenant-move-out` or `lease-end`, `frequency` must
            be `does_not_repeat`.
          properties:
            frequency:
              type: string
              enum:
                - does_not_repeat
                - monthly
                - quarterly
                - semi_annually
                - annually
                - custom
            custom_interval:
              type: integer
              minimum: 1
              description: Required when `frequency` is `custom`.
            custom_unit:
              type: string
              enum:
                - days
                - weeks
                - months
              description: Required when `frequency` is `custom`.
            end_trigger:
              type: string
              enum:
                - move_out
                - lease_end
              description: Required when `frequency` is anything other than `does_not_repeat`.
        created_at:
          type: string
          format: date-time
          description: ISO timestamp when the automation was created.
        updated_at:
          type: string
          format: date-time
          description: ISO timestamp of the last automation update.
    automation_create_request_model:
      x-internal: true
      type: object
      description: Request body for creating an automation.
      required:
        - name
        - selection_type
        - inspection_template_id
        - creation_date
        - trigger_type
        - assignment_type
      properties:
        name:
          type: string
          description: Automation name. Must be unique within the subscription —
            duplicates throw 409.
        selection_type:
          type: object
          required:
            - type
          properties:
            type:
              type: string
              enum:
                - all_units
                - specific_teams
                - specific_units
            ids:
              type: array
              items:
                type: string
              minItems: 1
              description: Required when `type` is `specific_teams` or `specific_units`.
        fast_track:
          type: boolean
          description: "Optional. Requires the selected template to have
            `is_fast_track_enabled: true`."
        inspection_template_id:
          type: string
          description: Id of the inspection template to clone for each run.
        creation_date:
          type: object
          required:
            - unit
            - value
            - condition
            - due_date_after_days
          properties:
            unit:
              type: string
              enum:
                - days
                - weeks
            value:
              type: integer
              minimum: 0
              maximum: 999
            condition:
              type: string
              enum:
                - before
                - after
            due_date_after_days:
              type: integer
              minimum: 0
              maximum: 99
        trigger_type:
          type: string
          enum:
            - tenant-move-in
            - tenant-move-out
            - lease-end
            - lease-start
        assignment_type:
          type: object
          required:
            - type
          properties:
            type:
              type: string
              enum:
                - residents
                - self-perform
                - emails
                - teammates
            emails:
              type: array
              items:
                type: string
                format: email
              minItems: 1
            teammate_emails:
              type: object
              additionalProperties:
                type: array
                items:
                  type: string
                  format: email
                minItems: 1
            resident_type:
              type: string
              enum:
                - current
                - future
        recurrence:
          type: object
          description: 'Optional. Defaults to `{ frequency: "does_not_repeat" }` when
            omitted. Must be `does_not_repeat` when `trigger_type` is
            `tenant-move-out` or `lease-end`.'
          required:
            - frequency
          properties:
            frequency:
              type: string
              enum:
                - does_not_repeat
                - monthly
                - quarterly
                - semi_annually
                - annually
                - custom
            custom_interval:
              type: integer
              minimum: 1
            custom_unit:
              type: string
              enum:
                - days
                - weeks
                - months
            end_trigger:
              type: string
              enum:
                - move_out
                - lease_end
    automation_update_request_model:
      x-internal: true
      type: object
      description: All fields are optional, but if `recurrence` is supplied then
        `trigger_type` must also be supplied.
      properties:
        name:
          type: string
        active:
          type: boolean
          description: Toggle the automation on or off.
        selection_type:
          type: object
          required:
            - type
          properties:
            type:
              type: string
              enum:
                - all_units
                - specific_teams
                - specific_units
            ids:
              type: array
              items:
                type: string
              minItems: 1
        fast_track:
          type: boolean
        inspection_template_id:
          type: string
        creation_date:
          type: object
          required:
            - unit
            - value
            - condition
            - due_date_after_days
          properties:
            unit:
              type: string
              enum:
                - days
                - weeks
            value:
              type: integer
              minimum: 0
              maximum: 999
            condition:
              type: string
              enum:
                - before
                - after
            due_date_after_days:
              type: integer
              minimum: 0
              maximum: 99
        trigger_type:
          type: string
          enum:
            - tenant-move-in
            - tenant-move-out
            - lease-end
            - lease-start
        assignment_type:
          type: object
          required:
            - type
          properties:
            type:
              type: string
              enum:
                - residents
                - self-perform
                - emails
                - teammates
            emails:
              type: array
              items:
                type: string
                format: email
              minItems: 1
            teammate_emails:
              type: object
              additionalProperties:
                type: array
                items:
                  type: string
                  format: email
                minItems: 1
            resident_type:
              type: string
              enum:
                - current
                - future
        recurrence:
          type: object
          required:
            - frequency
          properties:
            frequency:
              type: string
              enum:
                - does_not_repeat
                - monthly
                - quarterly
                - semi_annually
                - annually
                - custom
            custom_interval:
              type: integer
              minimum: 1
            custom_unit:
              type: string
              enum:
                - days
                - weeks
                - months
            end_trigger:
              type: string
              enum:
                - move_out
                - lease_end
    automation_get_all_response_model:
      x-internal: true
      type: array
      description: Paginated list of automations.
      items:
        $ref: "#/components/schemas/api_automation"
    intercom_messenger_jwt_response_model:
      x-internal: true
      type: object
      required:
        - token
      description: Short-lived signed JWT used by the Intercom Messenger client to
        authenticate the caller.
      properties:
        token:
          type: string
          description: Signed Intercom Messenger JWT (HS256). Refresh on every Messenger
            boot — the token TTL is 10 minutes.
    intercom_update_companies_request_model:
      x-internal: true
      type: object
      required:
        - companyId
      description: Attaches the caller to the given Intercom company.
      properties:
        companyId:
          type: string
          description: Intercom company id the authenticated user should be attached to.
    intercom_update_companies_response_model:
      x-internal: true
      type: object
      description: Result of attaching the caller to the requested Intercom company
        and refreshing their custom attributes. Both fields proxy the Intercom
        REST API response and are intentionally untyped.
      properties:
        company:
          description: Intercom company-attach response (the contact-to-company link).
            `null` / absent when the caller has an internal email or no email
            recorded — in that case the attach short-circuits without calling
            Intercom.
          nullable: true
        user:
          description: Intercom user-update response after refreshing the caller's custom
            attributes (rentcheck/chargebee subscription ids, company id).
    api_intercom_article:
      x-internal: true
      type: object
      description: Intercom Help Center article record — a direct pass-through of the
        Intercom `articles` REST resource, cached by the RentCheck backend.
      required:
        - id
        - type
        - workspace_id
        - parent_type
        - title
        - description
        - body
        - author_id
        - state
        - created_at
        - updated_at
        - url
      properties:
        id:
          type: string
          description: Intercom article id.
        type:
          type: string
          description: Intercom resource type — always `"article"` for this endpoint.
        workspace_id:
          type: string
          description: Intercom workspace the article belongs to.
        parent_id:
          type: integer
          description: Optional Intercom collection/section id the article is nested under
            (the immediate parent).
        parent_ids:
          type: array
          items:
            type: integer
          description: Optional Intercom parent chain (collection → section → …) for the
            article.
        parent_type:
          type: string
          enum:
            - collection
            - section
          description: Kind of parent the article lives under.
        title:
          type: string
          description: Article title.
        description:
          type: string
          description: Short article summary.
        body:
          type: string
          description: Full HTML article body as authored in Intercom.
        author_id:
          type: integer
          description: Intercom user id of the article author.
        state:
          type: string
          enum:
            - published
            - draft
          description: Publication state. The handler filters to `published` by default.
        created_at:
          type: integer
          format: int64
          description: Unix epoch seconds when the article was created (Intercom native
            format).
        updated_at:
          type: integer
          format: int64
          description: Unix epoch seconds when the article was last updated (Intercom
            native format).
        url:
          type: string
          description: Public Help Center URL for the article.
    api_integration_rentcheck:
      x-internal: true
      type: object
      description: RentCheck-API integration credentials — the `app_id`/`app_secret`
        pair used to sign requests against the public API for a given
        subscription.
      required:
        - id
        - app_id
        - app_secret
        - subscription_id
      properties:
        id:
          type: string
          description: Document id for the integration record.
        app_id:
          type: string
          description: Public API application id — sent as the `x-app-id` header.
        app_secret:
          type: string
          description: Public API application secret — sent as the `x-app-secret` header.
        subscription_id:
          type: string
          description: RentCheck subscription id the integration is scoped to.
security:
  - bearerAuth: []
    x-app-id: []
    x-app-secret: []
tags:
  - oAuth2
  - Users
  - Teams
  - Buildings
  - Communities
  - Units
  - Residents V2
  - Residents V3
  - Residents (deprecated)
  - File Requests
  - Leases
  - Inspections
  - Inspections V2
  - Inspection Features
  - Inspections Templates
  - Permission Groups
  - Properties V2
  - Properties V3
  - Maintenance Flags
  - Maintenance Reports
  - Maintenance Reports V2
  - Work Orders
  - Work Orders V2
  - Filter Segments
  - Dashboard
  - Subscriptions
  - Account Settings
  - Automations
  - Chargebee
  - Integrations
paths:
  "/v1/account-settings/email-customizations/{customizationId}":
    delete:
      x-internal: true
      summary: Delete an email customization
      tags:
        - Account Settings
      description: >
        Removes the supplied email customization. After deletion the affected
        template + email type

        falls back to the platform default content.
      parameters:
        - name: customizationId
          in: path
          description: Email customization ID.
          required: true
          schema:
            type: string
      responses:
        "204":
          description: The email customization has been removed.
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `subscription not found` or `email customization not
            found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - subscription not found
                      - email customization not found
                    example: email customization not found
    get:
      x-internal: true
      summary: Get an email customization by ID
      tags:
        - Account Settings
      description: |
        Returns a single email customization, including the rendered subject
        merge tags available for the template / email type.
      parameters:
        - name: customizationId
          in: path
          description: Email customization ID.
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested email customization.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_email_customization"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `subscription not found` or `email customization not
            found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - subscription not found
                      - email customization not found
                    example: email customization not found
  /v1/account-settings/email-customizations:
    get:
      x-internal: true
      summary: List email customizations
      tags:
        - Account Settings
      description: >
        Returns every email customization configured on the caller's
        subscription, one entry per

        inspection template / email type combination.
      responses:
        "200":
          description: Returns the email customizations.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/basic_email_customization"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `subscription not found` for the caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: subscription not found
    post:
      x-internal: true
      summary: Upsert email customizations
      tags:
        - Account Settings
      description: >
        Creates or updates the email customization for a given `email_type_id`
        across one or more

        inspection templates. Returns the resulting set of customizations.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/email_customization_upsert_request_model"
      responses:
        "200":
          description: Returns the upserted email customizations.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/basic_email_customization"
        "400":
          description: Bad request — request validation rejected the body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.email_type_id" is required'
        "401":
          $ref: "#/components/responses/401"
  /v1/account-settings/email-customizations/test-email:
    post:
      x-internal: true
      summary: Send a test email customization
      tags:
        - Account Settings
      description: >
        Renders the supplied email customization content using the supplied
        property + inspection

        template context and sends it to the listed recipients. Used by the email builder UI to

        preview the final email.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/email_customization_test_request_model"
      responses:
        "200":
          description: Test email accepted for delivery.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
        "400":
          description: Bad request — request validation rejected the body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.recipients" is required'
        "401":
          $ref: "#/components/responses/401"
  /v1/account-settings:
    get:
      summary: Get account settings
      tags:
        - Account Settings
      description: Get account settings for user's subscription.
      responses:
        "200":
          description: |
            Returns the account settings for the caller's subscription. When the
            caller has no subscription or no settings record yet, the server
            falls back to `defaultAccountSettings` so this endpoint never 4xx's
            on a successfully authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_account_settings"
        "401":
          $ref: "#/components/responses/401"
    put:
      summary: Update account settings
      tags:
        - Account Settings
      description: Update parameters of the subscription's account settings.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/account_settings_update_request_model"
      responses:
        "200":
          description: Returns the updated account settings
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_account_settings"
        "400":
          description: >
            Bad request — request body validation failed, an unknown room name
            was

            supplied, two-step inspection approval / custom maintenance

            fields / AI damage-detection settings did not pass the

            subscription-aware validations, or one of the validated values is

            otherwise invalid.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — `subscription not found` (caller has no subscription) or
            `account setting not found` (subscription has no settings record).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: subscription not found
  /v1/account-settings/rooms:
    delete:
      x-internal: true
      summary: Delete custom rooms from account settings
      tags:
        - Account Settings
      description: >
        Removes one or more custom rooms from the supplied `property_type`
        bucket on the caller's

        account settings.

        Unlike most DELETE endpoints, this one accepts a JSON request body — required because the

        removal is scoped both by `property_type` and by individual room entry.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/account_settings_delete_rooms_request_model"
      responses:
        "200":
          description: Returns the updated account settings.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_account_settings"
        "400":
          description: Bad request — request validation rejected the body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.property_type" is required'
        "401":
          $ref: "#/components/responses/401"
  "/v1/account-settings/signature-acknowledgements/{templateId}":
    delete:
      x-internal: true
      summary: Delete a signature acknowledgement override
      tags:
        - Account Settings
      description: >
        Removes the signature acknowledgement override for the supplied
        inspection template. After

        deletion the template falls back to the subscription-wide acknowledgement (or to the platform

        default if no subscription-wide override is set). Returns the post-delete state in the same

        shape as `GET /v1/account-settings/signature-acknowledgements`.
      parameters:
        - name: templateId
          in: path
          description: Inspection template ID whose acknowledgement override should be
            removed.
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the post-delete state of the signature acknowledgements.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/signature_acknowledgements_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          $ref: "#/components/responses/404"
  /v1/account-settings/signature-acknowledgements:
    get:
      x-internal: true
      summary: Get signature acknowledgements
      tags:
        - Account Settings
      description: >
        Returns the platform-default signature acknowledgement text, the
        subscription-wide override

        (if any), and the per-inspection-template overrides (if any).
      responses:
        "200":
          description: Returns the signature acknowledgements.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/signature_acknowledgements_response_model"
        "401":
          $ref: "#/components/responses/401"
    put:
      x-internal: true
      summary: Update signature acknowledgements
      tags:
        - Account Settings
      description: >
        Upserts one or more signature acknowledgements. Each entry's `id` is
        either the literal string

        `default` (to update the subscription-wide acknowledgement) or an inspection template id

        (to scope the change to a single template). Returns the post-update state in the same shape as

        `GET /v1/account-settings/signature-acknowledgements`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/signature_acknowledgements_update_request_model"
      responses:
        "200":
          description: Returns the updated signature acknowledgements.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/signature_acknowledgements_response_model"
        "400":
          description: Bad request — request validation rejected the body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.signature_acknowledgements" is required'
        "401":
          $ref: "#/components/responses/401"
  /v1/oAuth2/access:
    post:
      x-internal: true
      summary: Grant OAuth2 access and redirect back to the integration
      tags:
        - oAuth2
      description: |
        Fourth (and final) hop of the OAuth2 authorization-code flow used by
        integration apps (currently only the Zapier connector). The grant-access
        form rendered by the earlier steps posts here once the user confirms.
        The one-time authorization `code` is persisted against the
        user's profile so that a subsequent
        `POST /v1/oAuth2/token` call from the integration can exchange it for
        an access + refresh token pair, and then issues a 302 redirect back to
        the integration's `redirectUrl`.

        This endpoint does not require a bearer token.
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - redirectUrl
                - code
                - uid
              properties:
                redirectUrl:
                  type: string
                  format: uri
                  description: The `redirect_uri` originally supplied to `GET
                    /v1/oAuth2/authentication`; used as the 302 `Location`
                    target with `code` and `state` appended.
                code:
                  type: string
                  description: The one-time authorization code generated by `GET
                    /v1/oAuth2/authentication`, persisted on the user's profile
                    so `POST /v1/oAuth2/token` can exchange it for a token pair.
                uid:
                  type: string
                  description: The RentCheck user id resolved earlier in the flow.
                orgId:
                  type: string
                  description: Optional RentCheck organisation id when the caller belongs to more
                    than one org (Zapier only).
      responses:
        "302":
          description: |
            Redirects back to the integration's `redirectUrl`, with the OAuth
            `code` (and `state`) appended as query-string parameters, so the
            integration can complete the authorization-code exchange.
          headers:
            Location:
              description: The `redirectUrl` supplied by the caller.
              schema:
                type: string
                format: uri
  /v1/oAuth2/authentication:
    get:
      x-internal: true
      summary: Render the RentCheck OAuth2 login form
      tags:
        - oAuth2
      description: |
        First hop of the OAuth2 authorization-code flow used by integration apps
        (currently only the Zapier connector). Returns an HTML login form the
        end user submits to prove their identity — the form's action target is
        `POST /v1/oAuth2/validateUser`. The response `Content-Type` is
        `text/html`; nothing about this endpoint is JSON.

        This endpoint does not require a bearer token because it *is* the point
        where the user authenticates.
      parameters:
        - in: query
          name: client_id
          required: true
          description: |
            The RentCheck integration-app id. If it does not resolve to a
            registered app the endpoint responds with 401 `App not authorized
            or missing client_id`.
          schema:
            type: string
        - in: query
          name: state
          required: true
          description: Opaque OAuth2 `state` value; echoed back verbatim on the redirect
            the login form will fire.
          schema:
            type: string
        - in: query
          name: redirect_uri
          required: true
          description: The callback URL to redirect to once the user has authenticated.
            Appended to the redirect URL as
            `<redirect_uri>?state=<state>&code=<code>`.
          schema:
            type: string
            format: uri
      responses:
        "200":
          description: Returns the login HTML page.
          content:
            text/html:
              schema:
                type: string
                description: The rendered login HTML page.
        "400":
          description: >
            Bad request — one of the required query-string parameters is
            missing.

            The exact strings are listed below.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - parameters are missing
                      - parameter state is required
                      - parameter redirect_uri is required
                    example: parameter state is required
        "401":
          description: |
            Unauthorized — the `client_id` does not resolve to a registered
            integration app.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 401
                  error:
                    type: string
                    enum:
                      - App not authorized or missing client_id
                    example: App not authorized or missing client_id
  /v1/oAuth2/emailPasswordAuth:
    post:
      summary: Creates an access/refresh token pair
      tags:
        - oAuth2
      security:
        - x-app-id: []
          x-app-secret: []
      description: >
        Exchanges an email + password for a fresh access/refresh token pair. The
        body must

        supply the caller's `email` together with EITHER `password` (RentCheck account password)

        OR `integration_password` (the value obtained from the RentCheck API integrations page).

        Exactly one of the two credentials must be supplied.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/oauth2_email_password_auth_request_model"
      responses:
        "200":
          description: Returns the new tokens pair
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/oauth2_email_password_auth_response_model"
        "400":
          description: >
            Bad request — the request body failed validation. The validation
            error message

            is returned as `error`. Common cases:
              - `"data.email" is required` — the `email` field is missing.
              - `"data" must contain at least one of [password, integration_password]` —
                neither credential was supplied.
              - `"data" contains a conflict between exclusive peers [password, integration_password]` —
                both credentials were supplied at once.
              - `"data.email" must be a valid email` — malformed email address.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.email" is required'
        "401":
          description: >
            Unauthorized — the supplied credentials did not resolve to a
            RentCheck user.

            Both authentication paths (RentCheck account password and integration password)

            collapse every failure mode into a single generic error to avoid account

            enumeration.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 401
                  error:
                    type: string
                    enum:
                      - invalid credentials
                    example: invalid credentials
  /v1/oAuth2/me:
    post:
      x-internal: true
      summary: Identify the caller by their OAuth2 access token
      tags:
        - oAuth2
      description: |
        Companion to `POST /v1/oAuth2/token`: given the access token issued by
        the token endpoint, returns the RentCheck user id, org id, and app id
        the token was scoped to, along with the current server time. Primarily
        consumed by the Zapier integration to render the connected account's
        email in the UI.

        The endpoint reads the caller's identity from the decoded access token;
        the request body is ignored.
      responses:
        "200":
          description: Returns the caller's identity payload.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/oauth2_me_response_model"
        "401":
          description: >
            Unauthorized — either a standard auth-token failure, or the userId

            decoded from the token no longer resolves to an active user, in which

            case the error is `Code not valid`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 401
                  error:
                    type: string
                    enum:
                      - token is not valid
                      - authorization header is empty
                      - authorization header is not valid
                      - "format for Authorization must be: Bearer [token]"
                      - refresh token is not valid for resources
                      - not well-formed token
                      - Code not valid
                    example: Code not valid
  /v1/oAuth2/refreshToken:
    post:
      summary: Refresh tokens
      tags:
        - oAuth2
      security: []
      description: >
        Exchange a refresh token for a new access/refresh token pair. The
        endpoint is unauthenticated

        (no bearer token, app id, or app secret headers required); the supplied refresh token JWT is

        the only credential.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/oauth2_refresh_token_request_model"
      responses:
        "200":
          description: Returns the new tokens pair
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/oauth2_refresh_token_response_model"
        "401":
          description: Unauthorized — supplied refresh token failed validation.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 401
                  error:
                    type: string
                    enum:
                      - Invalid token
                    example: Invalid token
  /v1/oAuth2/token:
    post:
      x-internal: true
      security: []
      summary: Exchange an OAuth2 authorization code for access + refresh tokens
      tags:
        - oAuth2
      description: >
        Third leg of the RentCheck OAuth2 authorization-code flow — primarily

        used by the Zapier integration. The caller presents its integration

        `client_id` / `client_secret` alongside the one-time `code` issued to the

        RentCheck user during login, and receives a short-lived access token +

        long-lived refresh token in return.


        The `code` is single-use: it is cleared as soon as the exchange succeeds.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/oauth2_token_request_model"
      responses:
        "200":
          description: Returns the caller's fresh access + refresh tokens and their scoped
            org id.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/oauth2_token_response_model"
        "401":
          description: >
            Unauthorized — either the `client_id` / `client_secret` failed to

            validate against the RentCheck integration settings, or the supplied `code`

            did not resolve to a user (already-consumed / never-issued).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 401
                  error:
                    type: string
                    enum:
                      - App not valid
                      - Code not valid
                    example: Code not valid
  /v1/oAuth2/validateOrg:
    post:
      x-internal: true
      summary: Confirm the organisation the OAuth2 code should be scoped to
      tags:
        - oAuth2
      description: |
        Third hop of the OAuth2 authorization-code flow used by integration
        apps whose users belong to more than one RentCheck organisation
        (currently only Zapier). The org-selection form rendered by
        `POST /v1/oAuth2/validateUser` submits the selected org here. Response
        is HTML — nothing about this endpoint is JSON.

        This endpoint does not require a bearer token.

        Response flow:
        - `orgId` missing → re-renders the org-selection form with an inline
          `Please select a Team from the dropdown.` error (`HTTP 409`).
        - `orgId` present → renders the grant-access form (`HTTP 200`).
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - redirectUrl
                - code
                - uid
              properties:
                redirectUrl:
                  type: string
                  format: uri
                  description: The `redirect_uri` originally supplied to `GET
                    /v1/oAuth2/authentication`.
                code:
                  type: string
                  description: The one-time authorization code generated by `GET
                    /v1/oAuth2/authentication`.
                uid:
                  type: string
                  description: The RentCheck user id resolved by `POST /v1/oAuth2/validateUser`.
                orgId:
                  type: string
                  description: The RentCheck organisation id selected by the end user. Omitting or
                    leaving this blank re-renders the selection form with an
                    inline error.
                label:
                  type: string
                  description: Optional label displayed on the rendered form; forwarded verbatim
                    from the caller.
      responses:
        "200":
          description: The user selected an org; renders the grant-access form.
          content:
            text/html:
              schema:
                type: string
                description: The rendered grant-access HTML form.
        "409":
          description: |
            The org-selection form was submitted without an `orgId`. The form is
            re-rendered with an inline `Please select a Team from the dropdown.`
            error banner.
          content:
            text/html:
              schema:
                type: string
                description: The rendered org-selection HTML form with the inline error banner.
  /v1/oAuth2/validateUser:
    post:
      x-internal: true
      summary: Validate user credentials during the OAuth2 login form submit
      tags:
        - oAuth2
      description: |
        Second hop of the OAuth2 authorization-code flow used by integration
        apps (currently only the Zapier connector). The RentCheck login form
        rendered by `GET /v1/oAuth2/authentication` POSTs the user's email and
        password here. Depending on the outcome the endpoint re-renders one of
        three HTML forms and returns HTML — nothing about this endpoint is JSON.

        This endpoint does not require a bearer token because it *is* the point
        where the user authenticates.

        Response flow:
        - Missing or invalid email/password → re-renders the login form with
          an inline error banner and `HTTP 409` status.
        - Valid credentials for a non-Zapier app, or a Zapier app whose user
          belongs to a single organisation → stores the OAuth code against
          the user and renders the grant-access form (`HTTP 200`).
        - Valid credentials for a Zapier app whose user belongs to multiple
          organisations → renders the org-selection form (`HTTP 200`).
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - redirectUrl
                - code
                - appId
                - email
                - password
              properties:
                redirectUrl:
                  type: string
                  format: uri
                  description: The `redirect_uri` originally supplied to `GET
                    /v1/oAuth2/authentication`.
                code:
                  type: string
                  description: The one-time authorization code generated by `GET
                    /v1/oAuth2/authentication`.
                appId:
                  type: string
                  description: RentCheck integration-app id (matches the `client_id` supplied to
                    `GET /v1/oAuth2/authentication`).
                email:
                  type: string
                  format: email
                  description: End-user's RentCheck email address.
                password:
                  type: string
                  description: End-user's RentCheck integration password.
                label:
                  type: string
                  description: Optional label displayed on the rendered form; forwarded verbatim
                    from the auth form.
      responses:
        "200":
          description: Renders either the grant-access form (single org / non-Zapier) or
            the org-selection form (multi-org Zapier caller).
          content:
            text/html:
              schema:
                type: string
                description: The rendered grant-access or org-selection HTML form.
        "409":
          description: |
            Credentials failed validation. The login form is re-rendered with an
            inline error banner (`E-mail and password are required`, `Invalid
            e-mail`, or `Login failed`).
          content:
            text/html:
              schema:
                type: string
                description: The rendered login HTML form with the inline error banner.
  /v1/automations:
    post:
      x-internal: true
      summary: Create an automation
      tags:
        - Automations
      description: >
        Creates a new automation for the caller's active subscription. New

        automations are created in the deactivated state (`active: false`) and

        must be enabled via a follow-up `PUT` call.


        The following cross-field validation rules apply:
          * `selection_type.ids` is required when `selection_type.type` is
            `specific_units` or `specific_teams`.
          * `assignment_type.emails` is required when `assignment_type.type` is
            `emails`; `assignment_type.teammate_emails` is required when type is
            `teammates`; `assignment_type.resident_type` is required when type
            is `residents`.
          * When `trigger_type` is `tenant-move-out` or `lease-end`,
            `recurrence.frequency` must be `does_not_repeat`. The schema applies
            a default of `{ frequency: 'does_not_repeat' }` when `recurrence`
            is omitted.
          * Automation names must be unique within the subscription — a duplicate
            throws 409.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/automation_create_request_model"
      responses:
        "201":
          description: Returns the newly created automation.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 201
                  data:
                    $ref: "#/components/schemas/api_automation"
        "400":
          description: >
            Bad request — body validation failed, or one of the automation

            checks rejected the request. Examples:
              - Validation messages such as `"data.name" is required`,
                `"data.selection_type.type" must be one of [all_units, specific_teams, specific_units]`,
                `"data.creation_date.value" must be less than or equal to 999`.
              - `inspection template not published` (target template is
                unpublished).
              - `fast track not enabled for this inspection template`.
              - `a teammate must be provided for all teams` (teammate
                assignment is missing teams from the selection).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
        "401":
          description: |
            Unauthorized — standard auth failures, plus
            `User does not have permissions to create automation` when the
            caller lacks permission to create automations.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 401
                  error:
                    type: string
                    example: User does not have permissions to create automation
        "404":
          description: |
            Not Found — one of `user`, `subscription`, `inspection template`,
            `unit`, `team`, or `teammate` referenced by the request could not be
            resolved.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection template not found
        "409":
          description: |
            Conflict — `An automation with this name already exists on the
            subscription`, or `too many subscriptions` when more than one
            active subscription is associated with the caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 409
                  error:
                    type: string
                    enum:
                      - An automation with this name already exists on the
                        subscription
                      - too many subscriptions
                    example: An automation with this name already exists on the subscription
    get:
      x-internal: true
      summary: List automations for the caller's subscription
      tags:
        - Automations
      description: |
        Returns automations belonging to the caller's active subscription, with
        pagination and sorting controlled via `options[pagination]` /
        `options[sort]` query parameters. Defaults are
        `page_size=20`, `page_number=0`, `sort_by=created_at`,
        `sort_type=DESC`. Allowed `sort_by` values are listed below.
      parameters:
        - in: query
          name: options[pagination][page_size]
          schema:
            type: integer
            default: 20
          description: Page size for pagination.
        - in: query
          name: options[pagination][page_number]
          schema:
            type: integer
            default: 0
          description: Zero-indexed page number.
        - in: query
          name: options[sort][sort_by]
          schema:
            type: string
            default: created_at
            enum:
              - name
              - assignment_type
              - selection_type
              - trigger_type
              - created_at
          description: Field to sort by.
        - in: query
          name: options[sort][sort_type]
          schema:
            type: string
            default: DESC
            enum:
              - ASC
              - DESC
              - asc
              - desc
          description: |
            Sort direction. The value is uppercased before use, so both upper-
            and lower-case forms are accepted.
      responses:
        "200":
          description: Paginated list of automations for the caller's subscription.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/automation_get_all_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Total number of automations matching the query.
        "400":
          description: |
            Bad request — `options[sort][sort_by]` is not one of the allowed
            values, or `options[pagination]` failed validation.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `subscription not found` (the caller has no active
            subscription).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: subscription not found
  "/v1/automations/{automationId}":
    delete:
      x-internal: true
      summary: Delete an automation
      tags:
        - Automations
      description: |
        Hard-removes the automation. RC default automations
        (`is_rc_automation: true`) cannot be removed, and the caller must hold
        the `canEditAutomation` permission **and** the subscription must have
        the inspection automation addon. Returns 204 on success.
      parameters:
        - name: automationId
          in: path
          description: Automation ID.
          required: true
          schema:
            type: string
            format: uuid
            minLength: 1
      responses:
        "204":
          description: Automation successfully deleted.
        "400":
          description: |
            Bad request — `automationId` failed validation
            (`"automationId" must be a valid GUID`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: '"automationId" must be a valid GUID'
        "401":
          description: |
            Unauthorized — standard auth failures, plus
            `User does not have permissions to delete automations` (caller is
            missing the user record, lacks the inspection automation addon, or
            can't edit automations), or
            `User does not have permissions to delete automation` (target is
            an RC default automation), or
            `User does not have permissions to delete default automation`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 401
                  error:
                    type: string
                    example: User does not have permissions to delete automations
        "404":
          description: Not Found — `automation not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: automation not found
    get:
      x-internal: true
      summary: Get an automation by ID
      tags:
        - Automations
      description: |
        Returns the requested automation. The automation must exist and belong
        to the caller's active subscription; otherwise `automation not found` is
        returned.
      parameters:
        - name: automationId
          in: path
          description: Automation ID.
          required: true
          schema:
            type: string
            format: uuid
            minLength: 1
      responses:
        "200":
          description: Returns the requested automation.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_automation"
        "400":
          description: |
            Bad request — `automationId` failed validation
            (`"automationId" must be a valid GUID`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: '"automationId" must be a valid GUID'
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `automation not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: automation not found
    put:
      x-internal: true
      summary: Update an automation
      tags:
        - Automations
      description: |
        Patch an existing automation. All body fields are optional; if
        `recurrence` is supplied, `trigger_type` must be supplied as well.

        Side-effects worth noting:
          * If the resolved template has `is_fast_track_enabled: false`,
            `fast_track` is forced to `false` on the stored automation
            regardless of what the request body says.
          * If the resulting `trigger_type` is `tenant-move-out` or `lease-end`,
            `recurrence` is reset to `does_not_repeat` (with all
            custom fields cleared).
          * If after applying the update the automation has an invalid
            assignment (e.g. `emails` type with no emails, or `teammates` type
            with no teammates), the automation is automatically deactivated
            (`active: false`).

        RC default automations (`is_rc_automation: true`) cannot be edited.
      parameters:
        - name: automationId
          in: path
          description: Automation ID.
          required: true
          schema:
            type: string
            format: uuid
            minLength: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/automation_update_request_model"
      responses:
        "200":
          description: Returns the updated automation.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_automation"
        "400":
          description: >
            Bad request — request validation failed, or one of the cross-field

            rules rejected the change. Examples:
              - Validation messages such as `"data.creation_date.value" must be less than or equal to 999`.
              - `inspection template not published`.
              - `fast track not enabled for this inspection template`.
              - `a teammate must be provided for all teams`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
        "401":
          description: |
            Unauthorized — standard auth failures, plus
            `User does not have permissions to update automations` when the
            caller can't edit automations (or lacks the inspection automation
            addon for custom automations).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 401
                  error:
                    type: string
                    example: User does not have permissions to update automations
        "404":
          description: |
            Not Found — `automation not found`, `inspection template not
            found`, `unit not found`, `team not found`, `teammate not found`,
            `user not found`, or `subscription not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: automation not found
        "409":
          description: |
            Conflict — `An automation with this name already exists on the
            subscription` (when renaming), or `too many subscriptions`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 409
                  error:
                    type: string
                    enum:
                      - An automation with this name already exists on the
                        subscription
                      - too many subscriptions
                    example: An automation with this name already exists on the subscription
  /v1/buildings:
    post:
      summary: Create a building
      tags:
        - Buildings
      description: This resource allows you to create a Building. Buildings are
        properties that can contain a number of individual Units.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/building_create_request_model"
      responses:
        "200":
          description: Returns the created building
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/building_response_model"
        "400":
          description: >
            Bad request — request body validation failed, or the caller is not

            authorized to assign the building to the requested team. The `floors` field is

            capped at 100. Examples:
              - `"team_id" is required`
              - `"address" is required`
              - `"city" is required`
              - `"zip_code" is required`
              - `"floors" is required`
              - `"floors" must be less than or equal to 100`
              - `"name" is required`
              - `team_id has an invalid value` (raised for both `team_id` and any
                value in `additional_team_ids` that is not one of the caller's
                organizations — both cases share the same error string)
              - `"value" contains a conflict between optional exclusive peers [rooms, rooms_info]` —
                supplying both `rooms` and `rooms_info` at the same time is
                rejected.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"address" is required'
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: >
            Not Found — one of the supplied `unit_ids` is not accessible, is
            itself a

            Building/Community, or does not exist. The message carries the

            offending id, i.e. `unit not found (&lt;id&gt;)`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: unit not found
    delete:
      summary: Delete buildings
      tags:
        - Buildings
      description: This resource allows you to delete multiple buildings at once.
      parameters:
        - in: query
          name: id[]
          required: true
          style: form
          explode: true
          schema:
            type: array
            minItems: 1
            items:
              type: string
          description: |
            The ids of the buildings to be deleted. Repeat the parameter to send
            multiple values (e.g. `?id[]=abc&id[]=def`). The query string must
            contain `id[]`, and a missing array
            surfaces as a 500. Individual entries are not validated; ids the
            caller cannot access are silently skipped.
      responses:
        "200":
          description: Returns the count of deleted buildings.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: object
                    required:
                      - totalRemoved
                    properties:
                      totalRemoved:
                        type: integer
                        description: Number of buildings that were removed.
                        example: 1
        "401":
          $ref: "#/components/responses/401"
        "403":
          $ref: "#/components/responses/synced_property_delete_forbidden"
  "/v1/buildings/{buildingId}":
    get:
      summary: Get a building by id
      tags:
        - Buildings
      description: This resource allows you to get a Building. Buildings are
        properties that can contain a number of individual Units.
      parameters:
        - name: buildingId
          in: path
          description: Building ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested building
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/building_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `building not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: building not found
    put:
      summary: Update a building
      tags:
        - Buildings
      description: Update parameters of a building
      parameters:
        - name: buildingId
          in: path
          description: Building ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/building_update_request_model"
      responses:
        "200":
          description: Returns the updated building
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/building_response_model"
        "400":
          description: >
            Bad request — request body validation failed (the `floors` field is

            capped at 100, the same constraint as on create) or the supplied

            `team_id` / `additional_team_ids` are not ones the caller can access.

            Common error strings:
              - `"floors" must be less than or equal to 100`
              - `team_id has an invalid value`
              - `"value" contains a conflict between optional exclusive peers [rooms, rooms_info]` —
                supplying both `rooms` and `rooms_info` at the same time is
                rejected.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: team_id has an invalid value
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: >
            Not Found — `building not found`, or one of the supplied `unit_ids`
            is not

            accessible. Inaccessible-unit failures carry the offending id in the

            message, i.e. `unit not found (&lt;id&gt;)`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: building not found
  /v1/chargebee/addons:
    get:
      x-internal: true
      summary: List Chargebee addons
      tags:
        - Chargebee
      description: |
        Returns the list of Chargebee addons currently configured for the
        RentCheck account. The response is the raw Chargebee addon payload.
      responses:
        "200":
          description: List of Chargebee addons.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/chargebee_get_all_addons_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "401":
          $ref: "#/components/responses/401"
  /v1/chargebee/plans:
    get:
      x-internal: true
      summary: List Chargebee plans
      tags:
        - Chargebee
      description: |
        Returns the list of Chargebee plans currently configured for the
        RentCheck account. The response is the raw Chargebee plan payload.
      responses:
        "200":
          description: List of Chargebee plans.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/chargebee_get_all_plans_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "401":
          $ref: "#/components/responses/401"
  "/v1/chargebee/subscriptions/{subscriptionId}":
    get:
      x-internal: true
      summary: Get a Chargebee subscription by ID
      tags:
        - Chargebee
      description: |
        Returns the Chargebee subscription for the given subscription
        id. Returns `Subscription not found` when no matching subscription
        exists.
      parameters:
        - name: subscriptionId
          in: path
          description: Chargebee subscription ID.
          required: true
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: The requested Chargebee subscription.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_chargebee_subscription"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `Subscription not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: Subscription not found
  /v1/communities:
    post:
      summary: Create a community
      tags:
        - Communities
      description: This resource allows you to create a Community. Communities are
        properties that contain one or more individual Units.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/community_create_request_model"
      responses:
        "200":
          description: Returns the created community
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/community_response_model"
        "400":
          description: >
            Bad request — request body validation failed, or the caller is not

            authorized to assign the community to the requested team. Examples:
              - `"team_id" is required`
              - `"address" is required`
              - `"city" is required`
              - `"zip_code" is required`
              - `"name" is required`
              - `team_id has an invalid value` (raised for both `team_id` and any
                value in `additional_team_ids` that is not one of the caller's
                organizations — both cases share the same error string)
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"address" is required'
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: >
            Not Found — one of the supplied `building_ids` is not accessible, is
            not

            actually a Building, or does not exist. The message carries the

            offending id, i.e. `building not found (&lt;id&gt;)`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: building not found
    delete:
      summary: Delete communities
      tags:
        - Communities
      description: This resource allows you to delete multiple communities at once.
      parameters:
        - in: query
          name: id[]
          required: true
          style: form
          explode: true
          schema:
            type: array
            minItems: 1
            items:
              type: string
          description: |
            The ids of the communities to be deleted. Repeat the parameter to
            send multiple values (e.g. `?id[]=abc&id[]=def`). The query string
            must contain `id[]`, and a missing array
            surfaces as a 500. Individual entries are not validated; ids the
            caller cannot access are silently skipped.
      responses:
        "200":
          description: Returns the count of deleted communities.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: object
                    required:
                      - totalRemoved
                    properties:
                      totalRemoved:
                        type: integer
                        description: Number of communities that were removed.
                        example: 1
        "401":
          $ref: "#/components/responses/401"
        "403":
          $ref: "#/components/responses/synced_property_delete_forbidden"
  "/v1/communities/{communityId}":
    get:
      summary: Get a community by id
      tags:
        - Communities
      description: This resource allows you to get a Community. Communities are
        properties that contain one or more individual Units.
      parameters:
        - name: communityId
          in: path
          description: Community ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested community
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/community_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `community not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: community not found
    put:
      summary: Update a community
      tags:
        - Communities
      description: Update parameters of a community.
      parameters:
        - name: communityId
          in: path
          description: Community ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/community_update_request_model"
      responses:
        "200":
          description: Returns the updated community
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/community_response_model"
        "400":
          description: >
            Bad request — request body validation failed or the supplied
            `team_id` /

            `additional_team_ids` are not ones the caller can access.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: team_id has an invalid value
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: >
            Not Found — `community not found`, or one of the supplied
            `building_ids`

            is not accessible. Inaccessible-building failures carry the offending

            id in the message, i.e. `building not found (&lt;id&gt;)`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: community not found
  /v1/dashboard:
    get:
      x-internal: true
      summary: Get dashboard data
      tags:
        - Dashboard
      description: Get all dashboard data related to stats and to do list
      parameters:
        - in: query
          name: date_range_start
          schema:
            type: string
          description: Initial value to filter stats by inspection creation date.
        - in: query
          name: date_range_end
          schema:
            type: string
          description: Final value to filter stats by inspection creation date.
        - in: query
          name: property_type
          schema:
            type: string
            enum:
              - Building
              - Community
              - Unit
          description: |
            Filter stats by property type. The value is accepted
            case-insensitively, so `building` / `unit` / `community` are also
            accepted.
        - in: query
          name: property_id[]
          schema:
            type: array
            items:
              type: string
          description: Array of property ids to filter stats by.
        - in: query
          name: team_id[]
          schema:
            type: array
            items:
              type: string
          description: Array of team ids to filter stats by.
        - in: query
          name: inspection_type[]
          schema:
            type: array
            items:
              type: string
          description: Array of inspection types to filter stats by.
      responses:
        "200":
          description: Returns all data to support the dashboard
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/dashboard_get_all_response_model"
        "400":
          description: |
            Bad request — the query string failed validation.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - date_range_start can not be empty
                      - date_range_end can not be empty
                      - date_range_start has an invalid value
                      - date_range_end has an invalid value
                      - property_type has an invalid value
                    example: date_range_start can not be empty
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: |
            Forbidden — at least one entry in `team_id[]` is not one of the
            caller's authorized teams (`invalid team informed`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    enum:
                      - invalid team informed
                    example: invalid team informed
  "/v1/dashboard/{todoItem}":
    get:
      x-internal: true
      summary: Get dashboard resource
      tags:
        - Dashboard
      description: Get a dashboard resource based on the passed identifier
      parameters:
        - in: path
          name: todoItem
          required: true
          schema:
            type: string
            enum:
              - inspectionsAwaitingReview
              - overdueInspections
              - almostDueInspections
              - undeliveredEmails
              - revisionRequestedInspections
              - awaitingSignatureInspections
              - propertiesAtRisk
              - unassignedInspections
          description: Resource name
        - in: query
          name: page_number
          schema:
            type: integer
            default: 0
          description: Page number (zero-indexed)
        - in: query
          name: page_size
          schema:
            type: integer
            default: 20
          description: Page size
        - in: query
          name: sort_by
          schema:
            type: string
          description: Attribute to sort by
        - in: query
          name: sort_type
          schema:
            type: string
            enum:
              - DESC
              - ASC
          description: Sort direction (ascending or descending)
      responses:
        "200":
          description: |
            Returns the requested to-do list. The outer `data` is a response
            envelope (a `{ data, totalResults }` object); the inner `data` array
            is the paginated rows.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/dashboard_get_resource_response_model"
        "400":
          description: |
            Bad request — the `{todoItem}` path parameter is not one of the
            supported resources. The error message reads
            `todoItem query parameter has an invalid value` — the field is a
            path parameter despite what the message string says.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - todoItem query parameter has an invalid value
                    example: todoItem query parameter has an invalid value
        "401":
          $ref: "#/components/responses/401"
  "/v1/inspections/{inspectionId}/features":
    get:
      summary: Get all features
      tags:
        - Inspection Features
      description: Get all features for a given inspection ID
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns all features
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/inspection_feature_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `inspection not found`, `property not found`, or `user
            not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
  "/v1/inspections/{inspectionId}/features/{featureId}":
    get:
      summary: Get a feature by ID for a given inspection
      tags:
        - Inspection Features
      description: Get a feature by ID and inspection ID
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
        - name: featureId
          in: path
          description: Feature ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested feature
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_feature_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection feature not found
                    enum:
                      - inspection not found
                      - property not found
                      - inspection feature not found
  "/v1/features/{featureId}/rotatePhoto":
    put:
      x-internal: true
      summary: Rotate feature photo
      tags:
        - Inspection Features
      description: Update photo orientation in a feature
      parameters:
        - name: featureId
          in: path
          description: Inspection feature ID.
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/feature_rotate_photo_request_model"
      responses:
        "200":
          description: Returns the status code
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: >
            Forbidden — the caller is not on the inspection's
            `authorized_user_ids`.

            Surfaced as `User doesn't have permissions`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: User doesn't have permissions
        "404":
          description: |
            Not Found — the feature, the referenced photo on the feature, the
            owning inspection, or the underlying image data could not be
            located.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - feature not found
                      - photoURL not found
                      - inspection not found
                      - image data not found
                    example: feature not found
  /v1/fileRequests:
    post:
      x-internal: true
      summary: Create a file request
      tags:
        - File Requests
      description: Create a file request
      parameters:
        - in: header
          name: x-app-id
          required: false
          schema:
            type: string
          description: >
            Optional caller application identifier. When set, the value is
            persisted as `app_id` on the file request. Not validated further —
            accepted as any string.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/file_request_create_request_model"
      responses:
        "201":
          description: Returns the created file request ID
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 201
                  data:
                    $ref: "#/components/schemas/file_request_response_model"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - document_type is missing
                      - document_type has an invalid value
                      - export_type is missing
                      - export_type has an invalid value
                      - document_id is missing
                      - document_id has an invalid value
                    example: document_id is missing
        "401":
          $ref: "#/components/responses/401"
    get:
      x-internal: true
      summary: Get all file requests
      tags:
        - File Requests
      description: >
        Returns the list of file requests created by the authenticated user,
        optionally filtered by

        status. File requests capture the state of async PDF/ZIP report generation jobs.
      parameters:
        - in: query
          name: filters[status][value]
          schema:
            type: string
            pattern: ^(CREATED|PROCESSING|DONE|ERROR|THROTTLED)(\|(CREATED|PROCESSING|DONE|ERROR|THROTTLED))*$
          description: |
            Pipe-separated list of status values to filter by
            (e.g. `CREATED|DONE`). When either
            `filters[status][value]` or `filters[status][condition]` is
            supplied, both are required.
        - in: query
          name: filters[status][condition]
          schema:
            type: string
            enum:
              - any_of
              - none_of
          description: Condition to apply to the status filter. Must be paired with
            `filters[status][value]` (see above).
        - in: query
          name: page_size
          schema:
            type: integer
            default: 20
          description: The number of elements in the data collection.
        - in: query
          name: last_evaluated_hash_key
          schema:
            type: string
          description: |
            Cursor for keyset pagination. Pass the value returned by
            the previous page to fetch the next one.
        - in: query
          name: last_evaluated_range_key
          schema:
            type: integer
            format: int64
          description: |
            Range-key cursor (updated_date timestamp in milliseconds)
            for keyset pagination.
      responses:
        "200":
          description: Returns the paginated list of file requests.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/api_file_request"
                      lastEvaluatedKey:
                        type: object
                        description: |
                          Cursor for the next page. Both fields are
                          absent when there are no more pages.
                        properties:
                          last_evaluated_hash_key:
                            type: string
                            description: >
                              File request id of the last item on this page.

                              Pass back as `last_evaluated_hash_key` to fetch the

                              next page.
                          last_evaluated_range_key:
                            type: integer
                            format: int64
                            description: |
                              `updated_date` timestamp (ms) of the last item on
                              this page. Pass back as `last_evaluated_range_key`
                              to fetch the next page.
        "400":
          description: >
            The query parameters failed validation. Common causes: an unknown

            `filters[status][condition]` value, a malformed `filters[status][value]`

            string, or an empty status array.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: '"filters.status.condition" must be one of [any_of, none_of]'
        "401":
          $ref: "#/components/responses/401"
  "/v1/fileRequests/{fileRequestId}":
    get:
      x-internal: true
      summary: Get a file request by ID
      tags:
        - File Requests
      description: >
        Returns a single file request. Used by the apps to poll for the
        completion of long-running

        PDF / ZIP report generation jobs and to retrieve the resulting download URL.
      parameters:
        - name: fileRequestId
          in: path
          description: File request ID.
          required: true
          schema:
            type: string
      responses:
        "200":
          description: |
            Returns the raw stored file request document for the requested
            file request. Note: unlike `GET /v1/fileRequests` (which returns a
            projected shape), this endpoint returns the underlying entity
            unchanged — dates are epoch-millisecond numbers and additional
            persisted fields (`user_id`, `org_id`, `file_name`, `dont_notify`,
            `filters`, `params`) are echoed back.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/file_request_v1_get_by_id_response_model"
        "400":
          description: |
            Bad request — the requested file request does not exist or is not
            owned by the authenticated user. The handler intentionally surfaces
            this case as 400 (not 404) to avoid leaking the existence of
            another user's file request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - id has an invalid value
                    example: id has an invalid value
        "401":
          $ref: "#/components/responses/401"
  /v2/filter-segments:
    post:
      summary: Create a filter segment
      x-internal: true
      tags:
        - Filter Segments
      description: |
        Creates a saved filter segment — a named, colored, and iconed
        inspections-list filter combo — owned by the requesting user.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/filter_segment_create_request_model"
      responses:
        "201":
          description: Returns the created filter segment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 201
                  data:
                    $ref: "#/components/schemas/filter_segment_response_model"
        "400":
          description: >
            Bad request — the request body failed validation. The raw message

            identifies the invalid field, e.g. `"data.name" is required`,

            `"data.color" must be one of [grey, blue, light_blue, orange, green, red]`,

            `"data.filters" must have at least 1 key`, or — when the same value

            appears twice in a multi-value filter —

            `"data.filters.assigned_to.value[1]" contains a duplicate value`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.name" is required'
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `user not found` (the requesting user does not exist).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: user not found
        "409":
          description: >
            Conflict — a filter segment with this name already exists for the
            owner

            (names are unique per owner, trimmed and case-insensitive).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 409
                  error:
                    type: string
                    example: A filter segment with this name already exists
    get:
      summary: Get all filter segments
      x-internal: true
      tags:
        - Filter Segments
      description: >
        Returns the requesting user's saved filter segments grouped with the

        RentCheck default segments. The response is an object with two keys, each

        an array of segments (same shape as the create/update response):


        - `my_segments`: segments the user owns, sorted alphabetically ascending
          by name.
        - `rentcheck_default_segments`: the fixed RentCheck defaults — Needs
          Review, Almost Due, Overdue, Assigned to me, Archived — always present
          in that order. These are not persisted: they carry stable slug ids
          (e.g. `rc-default-needs-review`), a synthetic RentCheck `System`
          identity for `created_by` / `updated_by` / `owned_by`, and epoch
          timestamps. The Almost Due / Overdue date windows are relative to the
          current day, and Assigned to me is scoped to the requesting user.
      responses:
        "200":
          description: Returns the user's segments and the RentCheck default segments.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: object
                    required:
                      - my_segments
                      - rentcheck_default_segments
                    properties:
                      my_segments:
                        type: array
                        description: Segments owned by the user, sorted alphabetically ascending by
                          name.
                        items:
                          $ref: "#/components/schemas/filter_segment_response_model"
                      rentcheck_default_segments:
                        type: array
                        description: The fixed RentCheck default segments, always in the order Needs
                          Review, Almost Due, Overdue, Assigned to me, Archived.
                        items:
                          $ref: "#/components/schemas/filter_segment_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `user not found` (the requesting user does not exist).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: user not found
  "/v2/filter-segments/{filterSegmentId}":
    delete:
      summary: Delete a filter segment
      x-internal: true
      tags:
        - Filter Segments
      description: |
        Hard-deletes a saved filter segment. Only the segment owner can delete
        it.
      parameters:
        - name: filterSegmentId
          in: path
          description: Filter segment ID (UUID)
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Successful deletion of the filter segment. Empty body.
        "400":
          description: |
            Bad request — the path parameter failed validation, e.g.
            `"filterSegmentId" must be a valid GUID`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"filterSegmentId" must be a valid GUID'
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: |
            Forbidden — the requesting user is not the owner of the segment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: only the segment owner can delete it
        "404":
          description: Not Found — `filter segment not found (<id>)`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: filter segment not found (3f8a2c1e-1111-2222-3333-444455556666)
    put:
      summary: Update a filter segment
      x-internal: true
      tags:
        - Filter Segments
      description: |
        Updates a saved filter segment owned by the requesting user. This is a
        full replace — all fields are required. Only the segment owner can edit
        it; a segment owned by another user is reported as not found.
      parameters:
        - name: filterSegmentId
          in: path
          required: true
          description: Id of the filter segment to update.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/filter_segment_update_request_model"
      responses:
        "200":
          description: Returns the updated filter segment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/filter_segment_response_model"
        "400":
          description: >
            Bad request — the request body failed validation. The raw message

            identifies the invalid field, e.g. `"data.name" is required`,

            `"data.color" must be one of [grey, blue, light_blue, orange, green, red]`,

            `"data.filters" must have at least 1 key`, or — when the same value

            appears twice in a multi-value filter —

            `"data.filters.assigned_to.value[1]" contains a duplicate value`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.name" is required'
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — `user not found` (the requesting user does not exist) or
            `filter segment not found` (no segment with this id, or the segment
            is owned by another user).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: filter segment not found (2f9d0f3e-8c1a-4b8b-9d2e-2b5f6a4d5e7c)
        "409":
          description: >
            Conflict — another filter segment with this name already exists for

            the owner (names are unique per owner, trimmed and case-insensitive).

            Keeping the segment's current name is not a conflict.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 409
                  error:
                    type: string
                    example: A filter segment with this name already exists
  "/v1/inspections/{inspectionId}/events":
    post:
      x-internal: true
      summary: Record an event against an inspection
      tags:
        - Inspections
      description: |
        Appends a single event to an inspection's timeline. Used by internal
        surfaces (web/mobile) to record activity such as reminder sends,
        signature requests, reviewer notes, etc.

        The request body accepts every inspection event type except
        `inspection_invitee_self_removed`, which is generated server-side.
      parameters:
        - name: inspectionId
          in: path
          required: true
          description: Inspection ID
          schema:
            type: string
            minLength: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_event_create_request_model"
      responses:
        "200":
          description: Returns the created event.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_inspection_event"
        "400":
          description: Bad request — `type` is missing or not a valid inspection event type.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `inspection not found`, or `user not found` when the
            authenticated userId no longer resolves.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
    get:
      x-internal: true
      summary: Get all events for an inspection
      tags:
        - Inspections
      description: |
        Returns the timeline of events recorded against an inspection (status
        changes, reminders, signature requests, reviewer activity, etc.).

        The caller must already have access to the inspection — the same access
        check used by `GET /v1/inspections/{inspectionId}` runs first and
        surfaces the standard 404 / permission errors.
      parameters:
        - name: inspectionId
          in: path
          required: true
          description: Inspection ID
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Returns the inspection's events ordered by timestamp.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_events_get_all_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `inspection not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
  "/v1/inspections/{inspectionId}/features/{featureId}/metadata":
    get:
      x-internal: true
      summary: Get image metadata for an inspection feature
      tags:
        - Inspection Features
      description: >
        Returns the AI-generated image metadata documents for every photo on a
        single

        inspection feature. Each entry surfaces the analysis status, the inferred

        damage items, and the maintenance flags currently associated with the

        underlying feature. Available only on subscriptions whose account settings

        have AI image analysis enabled.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
            minLength: 1
        - name: featureId
          in: path
          description: Inspection feature ID (must belong to the supplied inspection)
          required: true
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Returns the list of image metadata documents for the feature.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/image_metadata_get_all_response_model"
                  count:
                    type: integer
                    description: Number of metadata documents in the response.
                    example: 3
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: >
            Forbidden — `damage detection not enabled` (the caller's account
            settings do

            not have AI image analysis enabled).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: damage detection not enabled
        "404":
          description: >
            Not Found — `inspection not found` or `user not found` (the
            inspection access

            check fails), `inspection feature not found` (no feature with that id, or it

            does not belong to the supplied inspection), or `subscription not found`

            (the caller has no active subscription).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection feature not found
  "/v1/inspections/{inspectionId}/features/{featureId}/metadata/{metadataId}":
    put:
      x-internal: true
      summary: Update image metadata for an inspection feature
      tags:
        - Inspection Features
      description: >
        Patch the AI-generated image metadata document for a single photo on an

        inspection feature. Currently only `damage_items[].user_feedback` is editable

        — the supplied damage items are matched by `id` and merged onto the existing

        document. Used by the reviewer experience to ack/correct AI-inferred damage.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
            minLength: 1
        - name: featureId
          in: path
          description: Inspection feature ID (must belong to the supplied inspection)
          required: true
          schema:
            type: string
            minLength: 1
        - name: metadataId
          in: path
          description: Image metadata ID (must belong to the supplied feature)
          required: true
          schema:
            type: string
            minLength: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/image_metadata_update_request_model"
      responses:
        "200":
          description: Returns the updated image metadata document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_image_metadata"
        "400":
          description: >
            Bad request — request body validation failed. Examples:
              - `"data.damage_items[0].id" is required`
              - `"data.damage_items[0].user_feedback" must be one of [correct, incorrect]`
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.damage_items[0].id" is required'
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: >
            Not Found — `inspection not found` or `user not found` (the
            inspection access

            check fails), `inspection feature not found` (the feature does not exist or

            does not belong to the supplied inspection), `subscription not found`, or

            `image metadata not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: image metadata not found
  "/v2/inspections/{inspectionId}/features/{featureId}/review/approve":
    post:
      x-internal: true
      summary: Approve a feature review
      tags:
        - Inspection Features
      description: Approves a previously requested review on a feature. Returns the
        updated feature.
      parameters:
        - name: inspectionId
          in: path
          required: true
          description: Inspection ID
          schema:
            type: string
        - name: featureId
          in: path
          required: true
          description: Feature ID
          schema:
            type: string
      responses:
        "200":
          description: Returns the updated feature.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_inspection_feature"
        "400":
          description: >
            Bad request — the caller's role is not permitted to approve a
            feature review

            (`can't approve a feature review as this role`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: can't approve a feature review as this role
        "401":
          $ref: "#/components/responses/401"
        "403":
          $ref: "#/components/responses/403"
        "404":
          description: >
            Not Found — `feature not found` when the referenced feature cannot
            be

            resolved. `inspection not found` can also be returned when the parent

            inspection is missing.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - feature not found
                      - inspection not found
                    example: feature not found
  "/v2/inspections/{inspectionId}/features/{featureId}/flags":
    post:
      x-internal: true
      summary: Create a maintenance flag on a feature
      tags:
        - Inspection Features
      description: Adds a maintenance flag to the given feature. Returns the updated
        feature.
      parameters:
        - name: inspectionId
          in: path
          required: true
          description: Inspection ID
          schema:
            type: string
        - name: featureId
          in: path
          required: true
          description: Feature ID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/maintenance_flag_create_request_model"
      responses:
        "200":
          description: Returns the feature with the new flag attached.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_inspection_feature"
        "400":
          description: >
            Bad request — typical messages: `Invalid skill provided`, plus
            validation

            failures on the request body fields (`note`, `skill`, `responsibility`, etc.

            — see `maintenance_flag_create_request_model`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: Invalid skill provided
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: Forbidden — the caller does not have permission to add flags to
            this feature.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
        "404":
          description: |
            Not Found — `inspection feature not found` / `inspection not found`
            when the referenced feature or inspection cannot be resolved.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection feature not found
  "/v2/inspections/{inspectionId}/features/{featureId}/flags/{flagId}":
    delete:
      x-internal: true
      summary: Delete a maintenance flag from a feature
      tags:
        - Inspection Features
      description: Removes the named maintenance flag from a feature. Returns the
        updated feature.
      parameters:
        - name: inspectionId
          in: path
          required: true
          description: Inspection ID
          schema:
            type: string
        - name: featureId
          in: path
          required: true
          description: Feature ID
          schema:
            type: string
        - name: flagId
          in: path
          required: true
          description: Maintenance flag ID
          schema:
            type: string
      responses:
        "200":
          description: Returns the updated feature.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_inspection_feature"
        "401":
          $ref: "#/components/responses/401"
        "403":
          $ref: "#/components/responses/403"
        "404":
          description: |
            Not Found — `maintenance flag not found` when the supplied `flagId`
            is not present on the feature; `inspection not found` can also be
            returned when the parent inspection is missing.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: maintenance flag not found
    put:
      x-internal: true
      summary: Upsert a maintenance flag on a feature
      tags:
        - Inspection Features
      description: |
        Upserts the named maintenance flag on a feature. If a flag with the
        supplied `flagId` already exists on the feature it is updated in place;
        otherwise a new flag with that id is created and merged with the request
        body. Returns the updated feature. A nonexistent `flagId` is never a
        source of 404s — once the route has matched, an unknown id triggers the
        create branch instead. (Omitting the `{flagId}` path segment entirely
        still fails because the segment is required.)
      parameters:
        - name: inspectionId
          in: path
          required: true
          description: Inspection ID
          schema:
            type: string
        - name: featureId
          in: path
          required: true
          description: Feature ID
          schema:
            type: string
        - name: flagId
          in: path
          required: true
          description: Maintenance flag ID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/maintenance_flag_upsert_request_model"
      responses:
        "200":
          description: Returns the updated feature.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_inspection_feature"
        "400":
          description: >
            Bad request — typical messages: `Invalid skill provided`, plus
            validation

            failures on the request body fields (`note`, `skill`, `responsibility`, etc.

            — see `maintenance_flag_upsert_request_model`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: Invalid skill provided
        "401":
          $ref: "#/components/responses/401"
        "403":
          $ref: "#/components/responses/403"
        "404":
          description: >
            Not Found — `feature not found` or `user not found`, or `inspection

            feature not found` / `inspection not found` when the referenced feature

            or inspection cannot be resolved. A missing `flagId` is NOT a 404 —

            the endpoint creates the flag on demand.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - feature not found
                      - user not found
                      - inspection feature not found
                      - inspection not found
                    example: feature not found
  "/v2/inspections/{inspectionId}/features/{featureId}/revision":
    delete:
      x-internal: true
      summary: Delete a revision request on a feature
      tags:
        - Inspection Features
      description: Cancels an outstanding revision request on a feature. Returns the
        updated feature.
      parameters:
        - name: inspectionId
          in: path
          required: true
          description: Inspection ID
          schema:
            type: string
        - name: featureId
          in: path
          required: true
          description: Feature ID
          schema:
            type: string
      responses:
        "200":
          description: Returns the updated feature.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_inspection_feature"
        "400":
          description: >
            Bad request — the caller's role is not permitted to delete a
            revision

            (`can't delete a revision as this role`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: can't delete a revision as this role
        "401":
          $ref: "#/components/responses/401"
        "403":
          $ref: "#/components/responses/403"
        "404":
          description: >
            Not Found — `inspection feature not found` / `user not found` when
            the

            referenced resource cannot be resolved. `inspection not found` can also

            be returned when the parent inspection is missing.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - inspection feature not found
                      - user not found
                      - inspection not found
                    example: inspection feature not found
    put:
      x-internal: true
      summary: Upsert a revision request on a feature
      tags:
        - Inspection Features
      description: >
        Creates or updates the revision request on a feature. Reviewers use this
        to ask the performer to redo a feature; the performer's subsequent edits
        set `revision.updated` to true.
      parameters:
        - name: inspectionId
          in: path
          required: true
          description: Inspection ID
          schema:
            type: string
        - name: featureId
          in: path
          required: true
          description: Feature ID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_feature_revision_request_model"
      responses:
        "200":
          description: Returns the updated feature.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_inspection_feature"
        "400":
          description: >
            Bad request — request body validation failed, or the caller's role
            is

            not permitted to request a revision (`can't request a revision as this role`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: can't request a revision as this role
        "401":
          $ref: "#/components/responses/401"
        "403":
          $ref: "#/components/responses/403"
        "404":
          description: >
            Not Found — `inspection feature not found` / `user not found` when
            the

            referenced resource cannot be resolved. `inspection not found` can also

            be returned when the parent inspection is missing.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - inspection feature not found
                      - user not found
                      - inspection not found
                    example: inspection feature not found
  "/v2/inspections/{inspectionId}/features/{featureId}/edit":
    put:
      x-internal: true
      summary: Edit an inspection feature
      tags:
        - Inspection Features
      description: >
        Applies a partial update to an existing inspection feature (rating,
        responses, notes, media). Returns the updated feature.
      parameters:
        - name: inspectionId
          in: path
          required: true
          description: Inspection ID
          schema:
            type: string
        - name: featureId
          in: path
          required: true
          description: Feature ID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_feature_edit_request_model"
      responses:
        "200":
          description: Returns the updated feature.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_inspection_feature"
        "400":
          description: |
            Bad request — request body validation failed (e.g. invalid `rating`,
            unknown response key, malformed media operation).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "403":
          $ref: "#/components/responses/403"
        "404":
          description: >
            Not Found — `inspection feature not found` / `inspection not found`
            /

            `user not found` when the referenced resource cannot be resolved.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection feature not found
  "/v2/inspections/{inspectionId}/features":
    get:
      x-internal: true
      summary: Get all features for an inspection (v2)
      tags:
        - Inspection Features
      description: >
        Returns the list of features that belong to a single inspection.
        Supports two mutually-exclusive paging modes:


        - Cursor-based: pass `cursor` (the id of the last feature returned in the previous page; an empty string requests the first page). The handler iterates ordered by id ASC with a 5 MB soft response-size budget per page (returns early once a feature would push the running total past 5 MB).

        - Legacy: pass `pagination[page_number]` and `pagination[page_size]`.


        Combining `cursor` with either `pagination[page_*]` parameter is rejected.
      parameters:
        - name: inspectionId
          in: path
          required: true
          description: Inspection ID
          schema:
            type: string
        - in: query
          name: cursor
          schema:
            type: string
          description: >
            Id of the last feature returned by a previous request. Pass an empty
            string to request the first cursor-based page. Mutually exclusive
            with `pagination[page_number]` and `pagination[page_size]`.
        - in: query
          name: pagination[page_number]
          schema:
            type: integer
          description: Legacy pagination — zero-indexed page number. Mutually exclusive
            with `cursor`.
        - in: query
          name: pagination[page_size]
          schema:
            type: integer
          description: Legacy pagination — page size. Mutually exclusive with `cursor`.
      responses:
        "200":
          description: Returns the features for the inspection.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/api_inspection_feature"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "400":
          description: Bad request — `cursor` was combined with legacy pagination params.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: "Invalid pagination parameters: cursor cannot be combined with
                      page_number/page_size. Use either cursor pagination or
                      page_number/page_size."
        "401":
          $ref: "#/components/responses/401"
        "404":
          $ref: "#/components/responses/404"
        "413":
          description: Payload Too Large — a single feature exceeds the hard 6 MB cap.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 413
                  error:
                    type: string
                    example: A single inspection feature exceeds the maximum response size limit and
                      cannot be returned via this endpoint.
  "/v2/inspections/{inspectionId}/features/{featureId}":
    get:
      x-internal: true
      summary: Get an inspection feature by id
      tags:
        - Inspection Features
      description: Returns a single inspection feature, including its maintenance
        flags, media, and template settings.
      parameters:
        - name: inspectionId
          in: path
          required: true
          description: Inspection ID
          schema:
            type: string
        - name: featureId
          in: path
          required: true
          description: Feature ID
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested feature.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_inspection_feature"
        "401":
          $ref: "#/components/responses/401"
        "403":
          $ref: "#/components/responses/403"
        "404":
          description: |
            Not Found — `inspection feature not found` when the `featureId` does
            not resolve. `inspection not found` can also be returned when the
            parent inspection is missing.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection feature not found
  "/v2/inspections/{inspectionId}/features/submit":
    post:
      x-internal: true
      summary: Submit an inspection feature
      tags:
        - Inspection Features
      description: >
        Idempotent submit of an inspection feature. Used by the mobile app to
        commit a feature's rating, notes, media, and (optionally)
        review/revision payload. When `id` is omitted on first submit, the API
        treats this as a create; subsequent submits with the same `id` update
        the existing feature.
      parameters:
        - name: inspectionId
          in: path
          required: true
          description: Inspection ID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_feature_submit_request_model"
      responses:
        "200":
          description: Returns the submitted feature.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_inspection_feature"
        "400":
          description: >
            Bad request — request body validation failed, or the

            feature payload references a section / feature-name that does not

            match the inspection's template. The latter class of errors carry the

            following message shapes:
              - `feature name "<name>" for feature id "<id>" does not match existing feature`
              - `section name "<section>" for feature id "<id>" does not match existing feature`
              - `section name with ordinal "<name_with_ordinal>" for feature id "<id>" does not match existing feature`
              - `section name "<name>" is not a valid section name`
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: feature name "Front Door" for feature id "abc123" does not match
                      existing feature
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: >
            Forbidden — emitted in two cases:
              - The caller does not have permission to submit features on this
                inspection.
              - The inspection is not in a status that allows feature submits
                (valid statuses are `Not Started`, `Started`, `In Review`,
                `Revision Requested`). The error string surfaces the inspection's
                current status and the valid list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: "inspection is not in a valid status to submit inspection features.
                      Current status: Completed. Valid statuses: Not Started,
                      Started, In Review, Revision Requested."
        "404":
          description: |
            Not Found — `inspection not found`, `inspection template not found`,
            `section not found`, or `user not found` when a
            referenced resource cannot be resolved.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
  "/v1/inspections/{inspectionId}/rate":
    post:
      x-internal: true
      summary: Rate an inspection
      tags:
        - Inspections
      description: >
        Records a numeric rating and free-form feedback left by the performer
        about the inspection

        experience. Displayed alongside the inspection in the dashboard.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_rate_request_model"
      responses:
        "200":
          description: Returns the stored rating.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_rate_response_model"
        "400":
          description: >
            Bad request — request body validation failed (e.g. `data.rating` is
            not an integer

            between 1 and 5, `data.feedback` is shorter than 3 characters).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — `inspection not found` or `user not found`. The
            underlying access check maps missing-user and permission failures to
            these strings.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
  "/v1/inspections/{inspectionId}/reject":
    post:
      x-internal: true
      summary: Reject an inspection
      tags:
        - Inspections
      description: >
        Rejects a submitted inspection with a note and a new due date,
        optionally flagging specific

        features that require revision. Sends notifications to the original performer(s). The response

        body lists the email events emitted as part of the rejection notification.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_reject_request_model"
      responses:
        "200":
          description: Returns the list of email events emitted by the rejection.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_reject_response_model"
        "400":
          description: >
            Bad request — request body validation failed. Messages include

            `"data.rejection.note" is required`, `"data.rejection.due_date" is required`,

            and `"data.rejection.features" is required`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `inspection not found` or `user not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
  /v1/inspections/reminders:
    post:
      x-internal: true
      summary: Send reminders for one or more inspections
      tags:
        - Inspections
      description: >
        Sends an email and/or sms reminder about one or more inspection invites.
        The request body

        groups recipients by invite; the response surfaces per-channel success and error breakdowns

        so the caller can report partial failures.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_send_reminders_request_model"
      responses:
        "200":
          description: Returns the per-channel reminder status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_send_reminders_response_model"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - recipients is missing
                      - message is missing
                      - channels is missing
                    example: recipients is missing
        "401":
          $ref: "#/components/responses/inspection_access_unauthorized"
  "/v1/inspections/{inspectionId}/scan":
    post:
      x-internal: true
      summary: Queue a manual AI damage scan for an inspection
      tags:
        - Inspections
      description: >
        Enqueues a manual AI damage-detection scan against a completed
        inspection

        and returns the throttle record clients poll for progress.


        Guards enforced before enqueue:
          - Caller must be on an active Pro Plan (returns 404 `subscription not found`
            when no subscription record exists, otherwise 403 with the
            Pro-Plan-required message).
          - The account must have manual AI damage detection enabled.
          - The inspection must exist, be completed, and have inspection metadata.
          - The inspection must not already have a previous scan.
          - The caller must have manual-scan permissions and access to the
            inspection.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID.
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the queued throttle record.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_manual_scan_response_model"
        "400":
          description: |
            Bad request — the inspection exists but is not yet completed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - Inspection is not completed
                    example: Inspection is not completed
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: >
            Forbidden — the caller is on a non-Pro or inactive plan, the org has

            manual scanning disabled (`Manual sync setting is false for subscription`),

            the caller lacks the manual-scan permission (`invalid permissions`), or

            the caller lacks access to the inspection team

            (`User must be a member of the team`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    enum:
                      - This feature is only available on the Professional plan.
                      - Manual sync setting is false for subscription
                      - invalid permissions
                      - User must be a member of the team
                    example: invalid permissions
        "404":
          description: >
            Not Found — the caller has no subscription (`subscription not
            found`),

            the target inspection does not exist (`inspection not found`), or its

            inspection metadata is missing (`inspection metadata not found`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - subscription not found
                      - inspection not found
                      - inspection metadata not found
                    example: inspection not found
        "409":
          description: |
            Conflict — a previous scan already exists for this inspection.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 409
                  error:
                    type: string
                    enum:
                      - Inspection has already had a manual scan
                    example: Inspection has already had a manual scan
        "500":
          description: |
            Internal server error — the throttle queue rejected the enqueue.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 500
                  error:
                    type: string
                    enum:
                      - Unable to queue inspection scan
                    example: Unable to queue inspection scan
  "/v1/inspections/{inspectionId}/share":
    post:
      x-internal: true
      summary: Share an inspection PDF
      tags:
        - Inspections
      description: >
        Queues a PDF copy of the inspection report and emails it to the provided
        recipients.

        PDF generation and email delivery happen asynchronously — the response contains a

        `fileRequestId` that can be polled via `GET /v1/fileRequests/{fileRequestId}` for status.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_share_request_model"
      responses:
        "201":
          description: Returns the created file request ID.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 201
                  data:
                    $ref: "#/components/schemas/file_request_response_model"
        "400":
          description: >
            Bad request — returned when:

            - `send_to` is missing, empty, or contains an invalid email address

            - The inspection at `{inspectionId}` cannot be resolved or the caller
              lacks access to it (surfaced as `document_id has an invalid value`,
              not a 404)
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: document_id has an invalid value
        "401":
          $ref: "#/components/responses/401"
  "/v1/inspections/{inspectionId}/sign":
    post:
      x-internal: true
      summary: Sign an inspection
      tags:
        - Inspections
      description: >
        Attaches a performer signature to the inspection. The `signature_path`
        is the storage key of an

        image previously uploaded to the RentCheck image bucket. Used by the mobile inspection flow.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_sign_request_model"
      responses:
        "200":
          description: Returns the inspection updated with the signature.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_v1_response_model"
        "400":
          description: >
            Bad request — request body validation failed. `signature_path` is
            required, and

            `note`, when supplied, must be a string.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"signature_path" is required'
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `inspection not found` (the underlying access check
            fails for a missing inspection or user).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
        "500":
          description: |
            Internal server error — surfaces when the inspection signing guard
            rejects the sign attempt; the guard's reason string is returned as a
            500 instead of a 403.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 500
                  error:
                    type: string
  "/v1/inspections/{inspectionId}/start":
    post:
      x-internal: true
      summary: Start an inspection
      tags:
        - Inspections
      description: >
        Marks an inspection as started by the authenticated user. Used by the
        mobile performer flow. The

        response body is the inspection as returned by `GET /v1/inspections/{inspectionId}`.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        description: |
          The body is required by request validation but takes no fields today —
          send an empty JSON object `{}`.
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: Returns the updated inspection.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_v1_response_model"
        "400":
          description: >
            Bad request — the request body must be a JSON object; non-object
            bodies (string, array,

            primitive) are rejected by request validation.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data" must be an object'
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `inspection not found` or `user not found` (the
            underlying access check fails).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
  "/v1/inspections/{inspectionId}/submit":
    post:
      x-internal: true
      summary: Submit an inspection
      tags:
        - Inspections
      description: >
        Submits a performed inspection for review. Moves the inspection out of
        `In Progress` and

        notifies reviewers. Used by the mobile performer flow after features have been filled in.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_submit_request_model"
      responses:
        "200":
          description: Returns the updated inspection.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_v1_response_model"
        "400":
          description: >
            Bad request — request body validation failed. `note`, when supplied,
            must be a

            string; a non-object body is rejected.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"note" must be a string'
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: Forbidden — the caller doesn't have permission to submit this
            inspection.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
        "404":
          description: >
            Not Found — `user not found` or `inspection not found`. The

            access guard returns `user not found` when the caller's profile cannot

            be loaded and `inspection not found` when the inspection does not exist

            or the caller cannot read it. `inspection not found` can also be

            returned post-update if the update does not persist.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - user not found
                      - inspection not found
                    example: inspection not found
  "/v1/inspections/{inspectionId}/approve":
    post:
      summary: Approve inspection
      tags:
        - Inspections
      description: >
        Approve an inspection with the given ID. Approval requires a signature,
        provided either as text

        (`signature_text`) or as a base64-encoded image (`signature_image_data`). An optional numeric

        `rating`, free-form `note`, and `user_facing_note` (displayed to the performer) can also be

        supplied.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
            minLength: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_approve_request_model"
      responses:
        "200":
          description: Returns the approved inspection
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_v1_response_model"
        "400":
          description: >
            Bad request — request body validation failed. Typical messages
            mention an

            xor conflict between `data.signature_text` and `data.signature_image_data` (exactly one

            must be supplied), or a malformed `data.rating` / `data.note` / `data.user_facing_note`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: >
            Forbidden — the caller can't approve this inspection. The `error`
            message states why: the inspection is not in an approvable state
            (e.g. already approved), the caller lacks permission, or a required
            first-step review hasn't been completed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: You don't have permission to approve this inspection.
        "404":
          description: |
            Not Found — `inspection not found`, `user not found`,
            `performer not found`, or
            `signature text or image data not found`. The underlying access
            check maps missing-user lookups to `user not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
  /v1/inspections/invites/accept:
    patch:
      summary: Accept invite
      tags:
        - Inspections
      description: Accept an inspection invite. When `reference_code` is passed it
        will search for that invite and accept it, otherwise it will search all
        invites for that user and accept them. A JSON body is always required —
        omitting the body surfaces as a 500.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_invite_accept_request_model"
      responses:
        "200":
          description: Returns the updated inspection
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/api_invite_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "400":
          description: |
            Bad request — `reference_code`, when supplied, must be either 4 or 6
            characters long. Any other length is rejected with an "alternatives"
            match failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.reference_code" does not match any of the allowed types'
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `invite not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: invite not found
        "409":
          description: |
            Conflict — the invite has already been claimed by a different user.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 409
                  error:
                    type: string
                    enum:
                      - This invite code has already been claimed by another
                        user.
                    example: This invite code has already been claimed by another user.
  /v1/inspections/invites:
    get:
      summary: Get all inspection invites
      tags:
        - Inspections
      description: This resource retrieves all invites available for a given user.
      responses:
        "200":
          description: Returns all invites
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/api_invite_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "401":
          $ref: "#/components/responses/401"
  "/v1/inspections/{inspectionId}/review":
    delete:
      summary: Cancel inspection review
      tags:
        - Inspections
      description: >
        Cancel inspection review. The caller must have permission to access the

        inspection — the inspection owner qualifies, plus any user granted access

        via the inspection's permission group.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the updated inspection
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_v1_response_model"
        "400":
          description: >
            Bad request — examples include `inspection.review is missing`,

            `inspection status does not support cancelling review request`,

            `inspection assignment type does not support cancelling review request`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/inspection_access_unauthorized"
        "404":
          description: Not Found — `inspection not found` or `user not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
    post:
      summary: Review an inspection
      tags:
        - Inspections
      description: >
        Review an inspection. The caller must have permission to access the

        inspection — the inspection owner qualifies, plus any user granted access

        via the inspection's permission group.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_review_request_model"
      responses:
        "200":
          description: Returns the updated inspection
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_v1_response_model"
        "400":
          description: >
            Bad request — examples include `Cannot send an inspection review
            request to yourself. Please choose another recipient.`,

            `assigned_recipients.type has an invalid value: <type>`, `inspection_status has an invalid value: <status>`,

            plus request body validation failures (e.g. `"data.recipients" must contain at least 1 items`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/inspection_access_unauthorized"
        "404":
          description: Not Found — `inspection not found` or `user not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
  "/v1/inspections/{inspectionId}/skills":
    get:
      summary: Get all inspection skills
      tags:
        - Inspections
      description: This resource retrieves all skills for creating maintenance flags
        available for a given inspection.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns all skills
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/inspection_skills_get_all_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `inspection not found` or `user not found` (the
            underlying access check fails).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
  /v2/inspections:
    post:
      summary: Create inspections
      tags:
        - Inspections V2
      description: Create inspections
      parameters:
        - in: header
          name: x-app-id
          required: true
          description: |
            Creation-source identifier. Persisted as `creation_source` on each
            created inspection. Required — a missing value yields a 400.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_v2_create_request_model"
      responses:
        "200":
          description: Returns the status of the operation
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_v2_response_model"
        "400":
          description: >
            Bad request — top-level body validation rejected the create.
            Per-property

            validation failures (template / property checks) do not surface as 4xx —

            they are caught and returned as 200 with the offending property recorded

            in the response's `errors[].reason` field. Top-level 400 messages (each

            carries `<field> has an invalid value: <reason>` when a reason is supplied):
              - `due_date has an invalid value` /
                `due_date has an invalid value: cannot be in the past` /
                `invite_date has an invalid value` /
                `invite_date has an invalid value: cannot be in the past`
              - `recurrence type has an invalid value` /
                `recurrence end_date is missing` /
                `recurrence end_date has an invalid value` /
                `recurrence end_date has an invalid value: cannot be in the past` /
                `recurrence.end_date has an invalid value: exceeds maximum (Daily exceeds 1 month)` /
                `recurrence.end_date has an invalid value: exceeds maximum (2 years)` /
                `recurrence.end_date has an invalid value: date is before inspection due date`
              - `selected_features must be an array`
              - `self-perform inspection does not allow invite_date`
              - `invitation date should not be after due date`
              - `No <type> assigned to perform this inspection`
              - `Cannot include your own email when inviting others.`
            Feature validation also runs before the per-property checks, so the

            following throw as top-level 400 rather than being caught into

            `errors[].reason`:
              - `inspection template does not accept selected features`
              - `<template> requires at least one feature`
              - `area has an invalid value: <name>`
              - `feature has an invalid value: <area> for section <feature>`
              - `invalid area name, <area> requires an ordinal to be valid. e.g. First <area>, Second <area>, etc.`
            Per-property reasons surfaced in `errors[].reason` include:
              - `<propertyType> does not support <inspectionType> inspections`
              - `<template> requires the property to have at least one room`
              - `Unit does not have access to selected inspection template`
              - `this template id is not published for subscription <name>`
              - `inspection template does not support fast track`
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `user not found` (the caller's user could not be
            loaded; per-property `property not found` does not surface as 404,
            it is recorded in `errors[].reason`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: user not found
    get:
      summary: Get all inspections
      tags:
        - Inspections V2
      description: Get all inspections
      parameters:
        - in: query
          name: search
          schema:
            type: string
          description: Search string to match in several inspection's fields
        - in: query
          name: page_number
          schema:
            type: integer
            default: 0
          description: The page number of the collection. Zero-indexed.
        - in: query
          name: page_size
          schema:
            type: integer
            default: 20
          description: The number of elements in the data collection.
        - in: query
          name: sort_by
          schema:
            type: string
            default: due_date
            enum:
              - address
              - due_date
              - inspection_status
              - completed_date
              - approved_date
          description: >
            Sort results by one of the available fields. `approved_date` sorts
            by

            the approved date, falling back to the completed date when there is no

            approval.
        - in: query
          name: sort_type
          schema:
            type: string
            default: DESC
            pattern: ^(?:[Aa][Ss][Cc]|[Dd][Ee][Ss][Cc])$
            example: DESC
          description: |
            Sort direction. Accepted case-insensitively — any casing of `asc` /
            `desc` is silently normalised to `ASC` / `DESC`.
        - in: query
          name: inspection_status[]
          deprecated: true
          schema:
            type: array
            items:
              type: string
              enum:
                - Scheduled
                - Not Started
                - Started
                - In Review
                - Completed
                - Awaiting Review
                - Revision Requested
                - Revision Review
                - Approved
          description: List of inspection statuses to filter inspections. Prefer
            `filters[inspection_status][value]`.
        - in: query
          name: inspection_template[]
          deprecated: true
          schema:
            type: array
            items:
              type: string
          description: List of inspection template ids to filter inspections. Prefer
            `filters[inspection_template][value]`.
        - in: query
          name: team_id[]
          deprecated: true
          schema:
            type: array
            items:
              type: string
          description: List of teams id to filter inspections. Prefer
            `filters[team][value]`.
        - in: query
          name: assigned_to[]
          deprecated: true
          schema:
            type: array
            items:
              type: string
          description: List of user ids to filter inspections. Prefer
            `filters[assigned_to][value]`.
        - in: query
          name: ratings[]
          deprecated: true
          schema:
            type: array
            items:
              type: string
              enum:
                - Good
                - Fair
                - Poor
          description: List of feature ratings to filter inspections by. Prefer
            `filters[rating][value]`.
        - in: query
          name: property_id[]
          deprecated: true
          schema:
            type: array
            items:
              type: string
          description: List of property ids to filter inspections by. Prefer
            `filters[property][value]`.
        - in: query
          name: archived
          deprecated: true
          schema:
            type: string
            enum:
              - "true"
              - "false"
          description: Filter inspections by their archived status. Prefer
            `filters[archived]`.
        - in: query
          name: start_due_date
          deprecated: true
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[012])-(0[1-9]|[12][0-9]|3[01])$
          description: Value to filter inspection date by a starting date. `yyyy-mm-dd`
            format. The legacy parameter is forwarded onto
            `filters[due_date][start_date]`. Prefer
            `filters[due_date][start_date]`.
        - in: query
          name: end_due_date
          deprecated: true
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[012])-(0[1-9]|[12][0-9]|3[01])$
          description: Value to filter inspection date by a ending date. `yyyy-mm-dd`
            format. Prefer `filters[due_date][end_date]`.
        - in: query
          name: start_completed_date
          deprecated: true
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[012])-(0[1-9]|[12][0-9]|3[01])$
          description: Value to filter completed inspection date by a starting date.
            `yyyy-mm-dd` format. Prefer `filters[completed_date][start_date]`.
        - in: query
          name: end_completed_date
          deprecated: true
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[012])-(0[1-9]|[12][0-9]|3[01])$
          description: Value to filter completed inspection date by a ending date.
            `yyyy-mm-dd` format. Prefer `filters[completed_date][end_date]`.
        - in: query
          name: filters[search]
          schema:
            type: string
          description: Search string to match in several inspection's fields. Preferred
            over the legacy `search` parameter.
        - in: query
          name: filters[archived]
          schema:
            type: string
            enum:
              - "true"
              - "false"
          description: Filter inspections by its archived status.
        - in: query
          name: filters[has_flags]
          schema:
            type: string
            enum:
              - "true"
              - "false"
          description: Filter inspections by whether they have one or more maintenance
            flags. `true` returns only inspections with flags; `false` returns
            only inspections with none.
        - in: query
          name: filters[inspection_template][value]
          schema:
            type: string
          description: List of inspection templates to filter inspections. This parameter
            accepts multiple ids concatenated with a `|`.
        - in: query
          name: filters[inspection_template][condition]
          schema:
            type: string
            enum:
              - any_of
              - none_of
          description: Condition to be used to filter by inspection templates.
        - in: query
          name: filters[inspection_status][value]
          schema:
            type: string
            pattern: ^(Scheduled|Not Started|Started|In Review|Completed|Awaiting
              Review|Revision Requested|Revision
              Review|Approved)(\|(Scheduled|Not Started|Started|In
              Review|Completed|Awaiting Review|Revision Requested|Revision
              Review|Approved))*$
          description: Pipe-separated list of inspection statuses to filter by (e.g. `Not
            Started|In Review`).
        - in: query
          name: filters[inspection_status][condition]
          schema:
            type: string
            enum:
              - any_of
              - none_of
          description: Condition to be used to filter by inspection status.
        - in: query
          name: filters[team][value]
          schema:
            type: string
          description: List of teams id to filter inspections. This parameter accepts
            multiple ids concatenated with a `|`.
        - in: query
          name: filters[team][condition]
          schema:
            type: string
            enum:
              - any_of
              - none_of
          description: Condition to be used to filter by teams id.
        - in: query
          name: filters[property][value]
          schema:
            type: string
          description: List of properties id to filter inspections. This parameter accepts
            multiple ids concatenated with a `|`.
        - in: query
          name: filters[property][condition]
          schema:
            type: string
            enum:
              - any_of
              - none_of
          description: Condition to be used to filter by properties id.
        - in: query
          name: filters[rating][value]
          schema:
            type: string
            pattern: ^(Good|Fair|Poor)(\|(Good|Fair|Poor))*$
          description: List of inspection ratings to filter by. Multiple values are
            concatenated with `|` (e.g. `Good|Fair`). Allowed values, `Good`,
            `Fair`, `Poor`.
        - in: query
          name: filters[rating][condition]
          schema:
            type: string
            enum:
              - any_of
          description: Condition to be used to filter by ratings.
        - in: query
          name: filters[assigned_to][value]
          schema:
            type: string
          description: List of user ids to filter inspections by. This parameter accepts
            multiple ids concatenated with a `|`.
        - in: query
          name: filters[assigned_to][condition]
          schema:
            type: string
            enum:
              - any_of
          description: Condition to be used to filter by assigned to.
        - in: query
          name: filters[review_step][value]
          schema:
            type: string
            pattern: ^(Initial Review|Secondary Review)(\|(Initial Review|Secondary
              Review))*$
          description: List of review steps to filter inspections by. Multiple values are
            concatenated with `|` (e.g. `Initial Review|Secondary Review`).
            Allowed values, `Initial Review`, `Secondary Review`.
        - in: query
          name: filters[review_step][condition]
          schema:
            type: string
            enum:
              - any_of
          description: Condition to apply to the review step filter. The validator only
            accepts `any_of`.
        - in: query
          name: filters[created_by][value]
          schema:
            type: string
          description: List of creator ids to filter inspections by. This parameter
            accepts multiple values concatenated with a `|`.
        - in: query
          name: filters[created_by][condition]
          schema:
            type: string
            enum:
              - any_of
              - none_of
          description: Condition to be used to filter by created by.
        - in: query
          name: filters[due_date][start_date]
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[012])-(0[1-9]|[12][0-9]|3[01])$
          description: |
            Value to filter inspection effective due date by a starting date.
            `yyyy-mm-dd` format.
        - in: query
          name: filters[due_date][end_date]
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[012])-(0[1-9]|[12][0-9]|3[01])$
          description: >
            Value to filter inspection effective due date by a ending date.

            `yyyy-mm-dd` format. `end_date` must be on or after `start_date`,

            otherwise the response is `end date must be equal or after start date`.
        - in: query
          name: filters[due_date_legacy][start_date]
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[012])-(0[1-9]|[12][0-9]|3[01])$
          description: >
            Value to filter inspection due date (pre-effective-due-date) by a
            starting date.

            `yyyy-mm-dd` format. Cannot be combined with

            `filters[due_date]`; that combination is rejected with

            `You cannot use both due_date and due_date_legacy filters at the same time`.
        - in: query
          name: filters[due_date_legacy][end_date]
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[012])-(0[1-9]|[12][0-9]|3[01])$
          description: >
            Value to filter inspection due date (pre-effective-due-date) by a
            ending date.

            `yyyy-mm-dd` format. `end_date` must be on or after `start_date`, otherwise

            the response is `end date must be equal or after start date`. See `start_date`

            for the mutually-exclusive constraint with `filters[due_date]`.
        - in: query
          name: filters[completed_date][start_date]
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[012])-(0[1-9]|[12][0-9]|3[01])$
          description: |
            Value to filter completed inspection date by a starting date.
            `yyyy-mm-dd` format.
        - in: query
          name: filters[completed_date][end_date]
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[012])-(0[1-9]|[12][0-9]|3[01])$
          description: >
            Value to filter completed inspection date by a ending date.

            `yyyy-mm-dd` format. `end_date` must be on or after `start_date`, otherwise

            the response is `end date must be equal or after start date`.
        - in: query
          name: fields[]
          x-internal: true
          schema:
            type: array
            items:
              type: string
          description: Optional list of inspection fields to include in the response. When
            omitted, the default projection is returned. Internal use only.
        - in: query
          name: include_full_template_info
          x-internal: true
          schema:
            type: string
            enum:
              - "true"
              - "false"
            default: "false"
          description: When `true`, each inspection's `template` field is returned as the
            full template document instead of the default digest. Internal use
            only.
      responses:
        "200":
          description: >
            Returns the paginated list of inspections. Each item is a (possibly
            partial)

            `inspection_v1_response_model` — when `fields[]` is supplied, only the requested

            fields are included on each item.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/inspection_v1_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "400":
          description: >
            Bad request — query-string filters failed validation. Examples:
              - `You cannot use both due_date and due_date_legacy filters at the same time`
              - `end date must be equal or after start date`
              - Messages such as `"due_date.start_date" with value "..." fails to match the required pattern`
                when the supplied date string is not an ISO date.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: end date must be equal or after start date
        "401":
          $ref: "#/components/responses/401"
  /v2/inspections/creators:
    get:
      x-internal: true
      summary: Get all inspection creators
      tags:
        - Inspections V2
      description: Returns the list of users that have ever created an inspection the
        current user can see. Used to populate the `filters[created_by]` picker
        on the inspections list.
      responses:
        "200":
          description: Returns the list of inspection creators
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/api_inspection_creator"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "401":
          $ref: "#/components/responses/401"
  "/v1/inspections/{inspectionId}":
    delete:
      x-internal: true
      summary: Remove the current renter from an inspection
      tags:
        - Inspections
      description: >
        Removes the current performer (renter) from the inspection and records a
        removal reason. This is

        used by the mobile flow when a resident is moving out or a duplicate invite needs to be

        cleaned up. Returns 204 with no content on success.


        Note: this `DELETE /v1/inspections/{inspectionId}` route is distinct from the bulk

        `DELETE /v1/inspections?id[]=...` endpoint, which deletes inspections by id.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_remove_renter_request_model"
      responses:
        "204":
          description: Renter successfully removed.
        "400":
          description: >
            Bad request — request body validation failed or the caller is not a

            renter / the inspection is in a non-removable status. Example error

            strings:
              - `"reason" must be one of [moved-out, duplicate]`
              - `"reason" is required`
              - `only renters are allowed to perform this action`
              - `only inspections with the following statuses can be deleted: Not Started, Started, In Review, Revision Requested.`
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `inspection not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
    get:
      summary: Get an inspection by ID
      tags:
        - Inspections
      description: Get an inspection by ID. Returns the full inspection document with
        template, health score, feature ratings, permissions, and associated
        sync data.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Returns the requested inspection
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_v1_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `inspection not found` or `user not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
    put:
      summary: Update an inspection
      tags:
        - Inspections
      description: >
        Update parameters of an inspection. The list of fields that can be
        updated depends on the current

        status of the inspection: `x_id`, `inspection_label`, `archived`, `status`, and

        `is_initial_review_complete` can always be updated, while other fields (e.g. `due_date`,

        `recipients`) can only be updated while the inspection is in the Scheduled or In-Progress states.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
            minLength: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_update_request_model"
      responses:
        "200":
          description: Returns the updated inspection
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_v1_response_model"
        "400":
          description: >
            Bad request — request body validation failed, or one of the
            inspection

            state-machine checks rejected the update. Examples:
              - `due_date has an invalid value: must be a valid date in the future`
              - `invite_date has an invalid value: must be a valid date in the future`
              - `inspection status does not support invite date update`
              - `invitation date should not be after due date`
              - `unable to complete initial review`
              - `unable to undo initial review`
              - `can not update "<field>" on completed inspection`
              - validation messages such as `"archived" must be a boolean`, `"inspection_label" must be a string` — the raw body is validated, so paths are not prefixed with `data.`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/inspection_access_unauthorized"
        "404":
          description: Not Found — `inspection not found` or `user not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
        "500":
          description: |
            Internal server error — surfaces when the underlying state-machine
            guard rejects the requested transition. These currently surface as
            `500` rather than `400`:
              - `cannot mark as completed`
              - `cannot reopen`
              - `cannot cancel revision request`
              - `invalid status change operation`
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 500
                  error:
                    type: string
                    example: invalid status change operation
  "/v1/inspections/{inspectionId}/revision":
    delete:
      x-internal: true
      summary: Cancel inspection revision
      tags:
        - Inspections
      description: >
        Cancels an active revision request for an inspection. Only the
        inspection owner (or a user with

        the appropriate permission on their team) can cancel the revision. The response body is the

        inspection as returned by `GET /v1/inspections/{inspectionId}`.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the updated inspection.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_v1_response_model"
        "400":
          description: >
            Bad request — `inspection.rejection is missing` (the inspection is
            not currently in a

            revision-requested state).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: inspection.rejection is missing
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `inspection not found` or `user not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
        "500":
          description: >
            Internal Server Error — `cannot cancel revision request`. Surfaces
            as a 500 when the

            revision request cannot be cancelled in the inspection's current state.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 500
                  error:
                    type: string
                    example: cannot cancel revision request
  /v1/inspections:
    delete:
      summary: Delete inspections
      tags:
        - Inspections
      description: Delete selected inspections by ids
      parameters:
        - in: query
          name: id[]
          required: true
          style: form
          explode: true
          schema:
            type: array
            minItems: 1
            items:
              type: string
              minLength: 1
          description: >
            List of inspection ids to delete. At least one id is required.
            Repeat the

            parameter to send multiple values (e.g. `?id[]=abc&id[]=def`).
      responses:
        "200":
          description: Returns the number of inspections that were actually removed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: object
                    required:
                      - totalRemoved
                    properties:
                      totalRemoved:
                        type: integer
                        description: Number of inspections removed.
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - inspection_ids is missing
                    example: inspection_ids is missing
        "401":
          $ref: "#/components/responses/401"
    put:
      summary: Update multiple inspections at once
      tags:
        - Inspections
      description: Update due date for multiple inspections at once
      parameters:
        - in: query
          name: id[]
          required: true
          style: form
          explode: true
          schema:
            type: array
            minItems: 1
            items:
              type: string
              minLength: 1
          description: >
            List of inspection ids to update. At least one id is required.
            Repeat the

            parameter to send multiple values (e.g. `?id[]=abc&id[]=def`).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_update_bulk_request_model"
      responses:
        "200":
          description: >
            Returns updated inspections. The `inspection_template` field on each

            returned inspection is context-dependent: when the request targets a

            single id, the full `inspection_template_v1_get_by_id_response_model`

            (including `sections`) is returned; when the request targets more

            than one id, the bulk handler strips the template down to

            `{id, name, internal_label, property_type, display_name}` for

            response-size reasons.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/inspection_v1_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - "due_date has an invalid value: must be a valid date in
                        the future"
                      - "invite_date has an invalid value: must be a valid date
                        in the future"
                      - inspection status does not support invite date update
                      - invitation date should not be after due date
                      - ids must be an array
                      - "ids has an invalid value: must be a non empty array"
                      - none inspection ids are in progress
                    example: "ids has an invalid value: must be a non empty array"
        "401":
          $ref: "#/components/responses/inspection_access_unauthorized"
        "404":
          description: Not Found — `inspection not found` or `user not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
  /v3/inspections:
    get:
      x-internal: true
      summary: Get all inspections (v3)
      tags:
        - Inspections
      description: >
        Returns a paginated list of inspection digests.

        V3 is intended for list views that don't need the full inspection document; it returns `api_inspection_digest` entries (a trimmed projection of `api_inspection`) and uses the nested `filters[...]` query-param syntax with flat `page_size` / `page_number` / `sort_by` / `sort_type` options.
      parameters:
        - in: query
          name: page_size
          schema:
            type: integer
            default: 20
          description: The number of elements in the data collection.
        - in: query
          name: page_number
          schema:
            type: integer
            default: 0
          description: The page number of the collection. Zero-indexed.
        - in: query
          name: sort_by
          schema:
            type: string
            default: due_date
            enum:
              - address
              - due_date
              - inspection_status
              - completed_date
              - approved_date
          description: |
            Sort results by one of the available fields. `approved_date` sorts
            by the approved date, falling back to the completed date when an
            inspection has no approved date.
        - in: query
          name: sort_type
          schema:
            type: string
            default: desc
            enum:
              - asc
              - desc
              - ASC
              - DESC
          description: |
            Sort direction. Case-insensitive: `asc`/`ASC`
            and `desc`/`DESC` are all accepted.
        - in: query
          name: filters[archived]
          schema:
            type: string
            enum:
              - "true"
              - "false"
          description: |
            Filter inspections by their archived status. Ignored when the caller
            is a renter.
        - in: query
          name: archived
          deprecated: true
          schema:
            type: string
            enum:
              - "true"
              - "false"
          description: |
            Deprecated. Prefer `filters[archived]`. Legacy key still accepted.
        - in: query
          name: filters[has_flags]
          schema:
            type: string
            enum:
              - "true"
              - "false"
          description: Filter inspections by whether they have one or more maintenance
            flags. `true` returns only inspections with flags; `false` returns
            only inspections with none.
        - in: query
          name: has_flags
          deprecated: true
          schema:
            type: string
            enum:
              - "true"
              - "false"
          description: |
            Deprecated. Prefer `filters[has_flags]`. Legacy key still accepted.
        - in: query
          name: filters[search]
          schema:
            type: string
          description: Search value matched across inspection properties.
        - in: query
          name: search
          deprecated: true
          schema:
            type: string
          description: |
            Deprecated. Prefer `filters[search]`. Legacy key still accepted.
        - in: query
          name: filters[inspection_status][value]
          schema:
            type: string
            pattern: ^(Scheduled|Not Started|Started|In Review|Completed|Awaiting
              Review|Revision Requested|Revision
              Review|Approved)(\|(Scheduled|Not Started|Started|In
              Review|Completed|Awaiting Review|Revision Requested|Revision
              Review|Approved))*$
          description: >
            Pipe-separated list of inspection status values to filter by (e.g.
            `Not Started|Started`). Multiple values are concatenated with `|`.
            Allowed values, `Scheduled`, `Not Started`, `Started`, `In Review`,
            `Completed`, `Awaiting Review`, `Revision Requested`, `Revision
            Review`, `Approved`.
        - in: query
          name: filters[inspection_status][condition]
          schema:
            type: string
            enum:
              - any_of
              - none_of
          description: Condition to apply to the inspection status filter.
        - in: query
          name: inspection_status[]
          deprecated: true
          style: form
          explode: true
          schema:
            type: array
            items:
              type: string
          description: |
            Deprecated. Prefer `filters[inspection_status][value]`. Legacy
            multi-value array key still accepted.
        - in: query
          name: filters[inspection_template][value]
          schema:
            type: string
          description: List of inspection template ids to filter by. Multiple values are
            concatenated with `|`.
        - in: query
          name: filters[inspection_template][condition]
          schema:
            type: string
            enum:
              - any_of
              - none_of
          description: Condition to apply to the inspection template filter.
        - in: query
          name: inspection_template[]
          deprecated: true
          style: form
          explode: true
          schema:
            type: array
            items:
              type: string
          description: |
            Deprecated. Prefer `filters[inspection_template][value]`. Legacy
            multi-value array key still accepted.
        - in: query
          name: filters[team][value]
          schema:
            type: string
          description: List of team ids to filter by. Multiple values are concatenated
            with `|`.
        - in: query
          name: filters[team][condition]
          schema:
            type: string
            enum:
              - any_of
              - none_of
          description: Condition to apply to the team filter.
        - in: query
          name: team_id[]
          deprecated: true
          style: form
          explode: true
          schema:
            type: array
            items:
              type: string
          description: |
            Deprecated. Prefer `filters[team][value]`. Legacy multi-value array
            key still accepted.
        - in: query
          name: filters[property][value]
          schema:
            type: string
          description: List of property ids to filter by. Multiple values are concatenated
            with `|`.
        - in: query
          name: filters[property][condition]
          schema:
            type: string
            enum:
              - any_of
              - none_of
          description: Condition to apply to the property filter.
        - in: query
          name: property_id[]
          deprecated: true
          style: form
          explode: true
          schema:
            type: array
            items:
              type: string
          description: |
            Deprecated. Prefer `filters[property][value]`. Legacy multi-value
            array key still accepted.
        - in: query
          name: filters[rating][value]
          schema:
            type: string
            pattern: ^(Good|Fair|Poor)(\|(Good|Fair|Poor))*$
          description: List of inspection ratings to filter by. Multiple values are
            concatenated with `|` (e.g. `Good|Fair`). Allowed values, `Good`,
            `Fair`, `Poor`.
        - in: query
          name: filters[rating][condition]
          schema:
            type: string
            enum:
              - any_of
          description: Condition to apply to the rating filter.
        - in: query
          name: ratings[]
          deprecated: true
          style: form
          explode: true
          schema:
            type: array
            items:
              type: string
              enum:
                - Good
                - Fair
                - Poor
          description: |
            Deprecated. Prefer `filters[rating][value]`. Legacy multi-value
            array key still accepted.
        - in: query
          name: filters[assigned_to][value]
          schema:
            type: string
          description: List of user ids to filter inspections by. Multiple values are
            concatenated with `|`.
        - in: query
          name: filters[assigned_to][condition]
          schema:
            type: string
            enum:
              - any_of
          description: Condition to apply to the assigned_to filter.
        - in: query
          name: assigned_to[]
          deprecated: true
          style: form
          explode: true
          schema:
            type: array
            items:
              type: string
          description: |
            Deprecated. Prefer `filters[assigned_to][value]`. Legacy multi-value
            array key still accepted.
        - in: query
          name: filters[review_step][value]
          schema:
            type: string
            pattern: ^(Initial Review|Secondary Review)(\|(Initial Review|Secondary
              Review))*$
          description: List of review steps to filter by. Multiple values are concatenated
            with `|` (e.g. `Initial Review|Secondary Review`). Allowed values,
            `Initial Review`, `Secondary Review`.
        - in: query
          name: filters[review_step][condition]
          schema:
            type: string
            enum:
              - any_of
          description: Condition to apply to the review step filter. The validator only
            accepts `any_of`.
        - in: query
          name: filters[created_by][value]
          schema:
            type: string
          description: List of creator user ids to filter by. Multiple values are
            concatenated with `|`.
        - in: query
          name: filters[created_by][condition]
          schema:
            type: string
            enum:
              - any_of
              - none_of
          description: Condition to apply to the created_by filter.
        - in: query
          name: filters[due_date][start_date]
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[012])-(0[1-9]|[12][0-9]|3[01])$
          description: |
            Value to filter inspection effective due date by a starting date.
            Must be in `yyyy-mm-dd` format.
        - in: query
          name: filters[due_date][end_date]
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[012])-(0[1-9]|[12][0-9]|3[01])$
          description: |
            Value to filter inspection effective due date by an ending date.
            Must be in `yyyy-mm-dd` format. `end_date`
            must be on or after `start_date`, otherwise the response returns
            `end date must be equal or after start date`.
        - in: query
          name: filters[due_date_legacy][start_date]
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[012])-(0[1-9]|[12][0-9]|3[01])$
          description: >
            Value to filter inspection due date (pre-effective-due-date) by a
            starting date.

            Must be in `yyyy-mm-dd` format. Cannot be combined with

            `filters[due_date]`; that combination is rejected with

            `You cannot use both due_date and due_date_legacy filters at the same time`.
        - in: query
          name: filters[due_date_legacy][end_date]
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[012])-(0[1-9]|[12][0-9]|3[01])$
          description: >
            Value to filter inspection due date (pre-effective-due-date) by an
            ending date.

            Must be in `yyyy-mm-dd` format. `end_date`

            must be on or after `start_date`, otherwise the response returns

            `end date must be equal or after start date`. See `start_date` for the

            mutually-exclusive constraint with `filters[due_date]`.
        - in: query
          name: filters[completed_date][start_date]
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[012])-(0[1-9]|[12][0-9]|3[01])$
          description: |
            Value to filter completed inspection date by a starting date.
            Must be in `yyyy-mm-dd` format.
        - in: query
          name: filters[completed_date][end_date]
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[012])-(0[1-9]|[12][0-9]|3[01])$
          description: |
            Value to filter completed inspection date by an ending date.
            Must be in `yyyy-mm-dd` format. `end_date`
            must be on or after `start_date`, otherwise the response returns
            `end date must be equal or after start date`.
      responses:
        "200":
          description: Returns the paginated list of inspection digests.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/api_inspection_digest"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "400":
          description: >
            Bad request — query-string filters failed validation. Examples:
              - `You cannot use both due_date and due_date_legacy filters at the same time`
              - `end date must be equal or after start date`
              - date-pattern messages when the supplied date is not ISO.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: end date must be equal or after start date
        "401":
          $ref: "#/components/responses/401"
  /v3/inspections/permissions:
    get:
      x-internal: true
      summary: Get inspection permissions for the current user
      tags:
        - Inspections
      description: >
        Returns the per-user permissions that drive the inspection creation and
        assignment UI

        (e.g. whether the user can create inspections on units / buildings / communities, whether

        they can assign to residents, and renter-specific flags). Used internally by the mobile

        and web apps.
      responses:
        "200":
          description: Returns the inspection permissions for the current user.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_inspection_permissions"
        "401":
          $ref: "#/components/responses/401"
  "/v1/inspections/{inspectionId}/media/upload-urls":
    post:
      x-internal: true
      summary: Request pre-signed media upload URLs
      tags:
        - Inspections
      description: >
        Generates a batch of pre-signed S3 PUT URLs that the inspection
        performer or invitee can use to upload images, 360 images, or videos for
        an inspection feature. Each URL is valid for 7 days.


        Only callers whose inspection role is `invitee-write` or `self-perform-write` may request URLs — owners and reviewers receive a permissions error.


        The response order matches the request order one-to-one.
      parameters:
        - name: inspectionId
          in: path
          required: true
          description: Inspection ID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_media_upload_urls_request_model"
      responses:
        "200":
          description: Returns a list of pre-signed S3 PUT URLs.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_media_upload_urls_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "400":
          description: Bad request — request body failed validation (missing/invalid
            fields, empty `items`, more than 50 items, unsupported
            `contentType`, filename containing disallowed characters, etc.).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/inspection_access_unauthorized"
        "404":
          $ref: "#/components/responses/404"
  /v3/inspections/reminder-recipients:
    post:
      x-internal: true
      summary: Resolve reminder recipients for inspections
      tags:
        - Inspections
      description: |
        Resolves the set of reminder recipients across a batch of inspections.
        Invites are batch-fetched once for the supplied inspection ids,
        tenants are batch-fetched once for the unique recipient emails, and
        the result is deduped by email.

        Access is validated the same way the send-reminders path is, so the
        caller can't read invite emails for inspections it can't already access.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_reminder_recipients_request_model"
      responses:
        "200":
          description: |
            Deduped recipients across the supplied inspections. Each item is
            a reminder recipient suitable for posting back to
            `POST /v1/inspections/reminders`. Returns an empty `data` array
            when `inspectionIds` is empty after filtering / deduping.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/api_reminder_recipient"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "400":
          description: |
            Bad request — `inspectionIds` was supplied but is not an array.
            Returns a 400 with the message `inspectionIds must be an array`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - inspectionIds must be an array
                    example: inspectionIds must be an array
        "401":
          $ref: "#/components/responses/inspection_access_unauthorized"
  /v1/inspection-templates:
    post:
      x-internal: true
      summary: Create an inspection template
      tags:
        - Inspections Templates
      description: |
        Create a user inspection template. `subscription_id` is always derived
        from the caller (never accepted from the client), and
        `is_rc_template` / `is_published` / `unpublished_sub_ids` are stamped
        server-side. `team_ids` must be a non-empty subset of the caller's
        organizations. The supplied `permission_group_ids` are wired to the new
        template, and the created template is returned in the same shape as
        `GET /v1/inspection-templates/{inspectionTemplateId}`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_template_create_request_model"
      responses:
        "200":
          description: Returns the created inspection template.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_template_v1_get_by_id_response_model"
        "400":
          description: |
            Bad request — request body validation failed. The following fields
            are required: `name`, `internal_label`, `description`, `team_ids`
            (min 1), `property_type`, `is_fast_track_enabled`, `template_type`,
            `sections`, `contact_info`, and `permission_group_ids`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.name" is required'
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: Forbidden — `invalid permissions` or `User does not have access to
            the provided team ids`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: User does not have access to the provided team ids
        "409":
          description: |
            Conflict — the caller has more than one active subscription so the
            target subscription to wire the template to cannot be inferred.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 409
                  error:
                    type: string
                    enum:
                      - user has more than one active subscription
                    example: user has more than one active subscription
        "500":
          description: Internal Server Error — `Failed to create inspection template`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 500
                  error:
                    type: string
                    example: Failed to create inspection template
    get:
      summary: Get all inspection templates
      tags:
        - Inspections Templates
      description: >
        Returns the list of inspection templates available to the caller. The
        optional boolean

        `include_*` / `*_only` filters (passed as the strings `true`/`false`) and the multi-value

        `property_types[]` filter narrow the result set. Templates that have no permission groups

        shared with the caller are filtered out unless `include_unavailable=true`.
      parameters:
        - in: query
          name: include_unavailable
          schema:
            type: string
            enum:
              - "true"
              - "false"
          description: When `true`, also include templates that have no permission groups
            shared with the caller (which would otherwise be excluded as
            unavailable).
        - in: query
          name: include_deleted
          schema:
            type: string
            enum:
              - "true"
              - "false"
          description: When `true`, include templates that have been soft-deleted.
        - in: query
          name: active_only
          schema:
            type: string
            enum:
              - "true"
              - "false"
          description: When `true`, only return templates that have been used to create at
            least one inspection.
        - in: query
          name: published_only
          schema:
            type: string
            enum:
              - "true"
              - "false"
          description: When `true`, only return templates marked `published`.
        - in: query
          name: exclude_rc_templates
          schema:
            type: string
            enum:
              - "true"
              - "false"
          description: When `true`, exclude RentCheck-managed templates.
        - in: query
          name: resident_templates_only
          schema:
            type: string
            enum:
              - "true"
              - "false"
          description: When `true`, only return templates intended for resident
            self-perform inspections.
        - in: query
          name: validate_pro_templates
          schema:
            type: string
            enum:
              - "true"
              - "false"
          description: When `true`, run subscription/permission validation against pro
            templates and surface only the ones the caller can use.
        - in: query
          name: property_types[]
          schema:
            type: array
            items:
              type: string
              enum:
                - unit
                - building
                - community
          description: Optional list of property types to restrict templates to.
      responses:
        "200":
          description: Returns all inspection templates
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/inspection_template_v1_get_all_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — the authenticated user's profile could not be resolved
            (`user not found`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: user not found
  "/v1/inspection-templates/{inspectionTemplateId}":
    delete:
      x-internal: true
      summary: Delete an inspection template
      tags:
        - Inspections Templates
      description: |
        Delete an inspection template. The server decides the outcome: when no
        inspection references the template it is permanently removed (the
        document itself and its permission-group references); when inspections
        reference it the template is archived (soft delete) instead, preserving
        historical inspections. Email customizations tied to the template are
        removed in both outcomes. When the template is referenced by an active
        automation that prevents the delete, the response carries
        `status: error` with `error.reason: in-use` and the offending
        automation names instead of removing the record.
      parameters:
        - name: inspectionTemplateId
          in: path
          description: Inspection Template ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Delete outcome envelope.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_template_delete_response_model"
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: >
            Forbidden — `cannot delete default inspection templates`, `invalid
            permissions`,

            or `User does not have access to the inspection template`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: cannot delete default inspection templates
        "404":
          description: Not Found — `inspection template not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection template not found
    get:
      summary: Get an inspection template by ID
      tags:
        - Inspections Templates
      description: Get an inspection template by ID
      parameters:
        - name: inspectionTemplateId
          in: path
          description: Inspection Template ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested inspection template
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_template_v1_get_by_id_response_model"
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: Forbidden — `invalid permissions` or `User does not have access to
            the inspection template`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: User does not have access to the inspection template
        "404":
          description: Not Found — `inspection template not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection template not found
    put:
      x-internal: true
      summary: Update an inspection template
      tags:
        - Inspections Templates
      description: >
        Update a whitelist of editable inspection-template fields

        (`is_published`, `sections`, `name`, `internal_label`, `description`,

        `team_ids`, `property_type`, `is_fast_track_enabled`, `template_type`,

        `contact_info`, `signature_acknowledgement`). All fields are optional; only the provided ones are

        updated. `id`, `subscription_id` and `is_rc_template` are immutable and

        stripped server-side. When `team_ids` is provided it must be a non-empty

        subset of the caller's organizations. When the template is referenced by

        an automation that prevents unpublishing, the response carries

        `status: error` with `error.reason: in-use` and the offending automation

        names.
      parameters:
        - name: inspectionTemplateId
          in: path
          description: Inspection Template ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_template_update_request_model"
      responses:
        "200":
          description: Returns the updated inspection template (or an error envelope when
            blocked).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_template_update_response_model"
        "400":
          description: >
            Bad request — request body validation failed. The `data` object must

            have at least one whitelisted field, and each supplied optional field

            is type-checked (e.g. `is_fast_track_enabled` boolean, `team_ids`

            array with min 1 item, `property_type` one of `unit|building|community`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data" must have at least 1 key'
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: >
            Forbidden — `invalid permissions`, `User does not have access to the
            inspection template`,

            or `User does not have access to the provided team ids` (returned when

            reassigning `team_ids` to teams the caller does not belong to).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    enum:
                      - invalid permissions
                      - User does not have access to the inspection template
                      - User does not have access to the provided team ids
                    example: invalid permissions
        "404":
          description: Not Found — `inspection template not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection template not found
  "/v1/inspection-templates/{inspectionTemplateId}/automations":
    put:
      x-internal: true
      summary: Edit automation flags on an inspection template
      tags:
        - Inspections Templates
      description: |
        Toggle automation flags on an inspection template (currently
        `is_fast_track_enabled`). Returns the updated template.
      parameters:
        - name: inspectionTemplateId
          in: path
          description: Inspection Template ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_template_edit_automations_request_model"
      responses:
        "200":
          description: Returns the inspection template with the new automation flags
            applied.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_template_v1_get_by_id_response_model"
        "400":
          description: >
            Bad request — request body validation failed.
            `data.is_fast_track_enabled`

            (boolean) is required.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.is_fast_track_enabled" is required'
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: Forbidden — `invalid permissions`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: invalid permissions
        "404":
          description: Not Found — `inspection template not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection template not found
        "409":
          description: Conflict — the caller has more than one active subscription so the
            target subscription cannot be inferred.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 409
                  error:
                    type: string
                    example: user has more than one active subscription
  "/v1/inspection-templates/{inspectionTemplateId}/permission-groups":
    put:
      x-internal: true
      summary: Replace the permission groups attached to an inspection template
      tags:
        - Inspections Templates
      description: |
        Replace the set of permission groups that govern access to the
        inspection template. Returns the updated template.
      parameters:
        - name: inspectionTemplateId
          in: path
          description: Inspection Template ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/inspection_template_edit_permission_groups_request_\
                model"
      responses:
        "200":
          description: Returns the inspection template with the new permission groups
            applied.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_template_v1_get_by_id_response_model"
        "400":
          description: >
            Bad request — request body validation failed.
            `data.permission_group_ids`

            (array of strings) is required; an empty array is accepted and clears

            all groups.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.permission_group_ids" is required'
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: |
            Forbidden — the caller has no active subscription, or the resolved
            subscription is `cancelled`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    enum:
                      - invalid permissions
                    example: invalid permissions
        "404":
          description: >
            Not Found — the inspection template id in the path does not resolve
            to a

            template the caller can access.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection template not found
        "409":
          description: Conflict — the caller has more than one active subscription so the
            target subscription cannot be inferred.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 409
                  error:
                    type: string
                    example: user has more than one active subscription
  /v1/integrations/rentcheck:
    post:
      x-internal: true
      summary: Rotate RentCheck-API integration credentials
      tags:
        - Integrations
      description: >
        Creates a fresh `app_id` / `app_secret` pair for the caller's active

        RentCheck subscription and provisions the matching AWS API Gateway API

        key + usage-plan entry so the credentials can immediately sign public API

        requests. Any existing integration record for the subscription is removed

        first, so calling this endpoint effectively rotates the credentials.


        If the AWS API-key provisioning step fails after the record is created,

        the just-created integration record is removed and the failure surfaces

        as a 500 `Error creating AWS API key`.
      responses:
        "200":
          description: Newly-created RentCheck-API integration credentials.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_integration_rentcheck"
        "400":
          description: |
            Bad Request — the caller's active subscription is missing the
            `rentcheck_contact.company` field, which is required before the
            AWS API key can be labelled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - subscription.rentcheck_contact.company is missing
                    example: subscription.rentcheck_contact.company is missing
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — the caller has no active subscription.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - subscription not found
                    example: subscription not found
        "500":
          description: |
            Internal Server Error — the AWS API-key / usage-plan provisioning
            call failed. The just-created integration record is removed and the
            failure surfaces as `Error creating AWS API key`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 500
                  error:
                    type: string
                    example: Error creating AWS API key
    get:
      x-internal: true
      summary: Get RentCheck-API integration credentials
      tags:
        - Integrations
      description: >
        Returns the `x-app-id` / `x-app-secret` pair for the caller's active

        RentCheck subscription. Used by the `Account → Integrations → RentCheck

        API` panel to display the credentials integrators need to sign public API

        requests.
      responses:
        "200":
          description: RentCheck-API integration for the caller's active subscription.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_integration_rentcheck"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — either the caller has no active subscription, or the
            subscription has no RentCheck-API integration record yet.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - subscription not found
                      - integration not found
                    example: integration not found
  /v1/intercom/articles:
    get:
      x-internal: true
      summary: List Intercom help-center articles under a parent collection or section
      tags:
        - Users
      description: >
        Returns Intercom Help Center articles that live under the given
        `parent_id`

        collection or section, optionally filtered by publication state and

        capped at a maximum number of results. Results are read from a cache

        backed by the Intercom Help Center API.
      parameters:
        - in: query
          name: parent_id
          required: true
          schema:
            type: string
          description: |
            Intercom collection or section id to list articles for. Required —
            a 400 is returned when this is missing.
        - in: query
          name: state
          schema:
            type: string
            enum:
              - published
              - draft
            default: published
          description: |
            Filter articles by publication state. Defaults to `published` when
            the parameter is omitted.
        - in: query
          name: quantity
          schema:
            type: integer
            minimum: 1
          description: |
            Optional cap on the number of articles returned after filtering.
            When omitted, all matching articles are returned.
      responses:
        "200":
          description: Returns the matching Intercom articles.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/api_intercom_article"
                  count:
                    type: integer
                    description: Number of articles returned in `data`.
        "400":
          description: |
            Bad request — the required `parent_id` query parameter is missing.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - parent_id
                    example: parent_id
        "401":
          $ref: "#/components/responses/401"
  /v1/intercom/messenger-jwt:
    get:
      x-internal: true
      summary: Mint an Intercom Messenger JWT for the caller
      tags:
        - Users
      description: >
        Returns a short-lived signed Intercom Messenger JWT for the
        authenticated

        user. The token is signed with the platform-specific Messenger secret

        (Intercom issues a distinct secret per web / iOS / Android client), so

        the caller must declare which platform is booting via the `platform`

        query parameter.


        The token TTL is 10 minutes — refresh on every Messenger boot.
      parameters:
        - in: query
          name: platform
          required: true
          schema:
            type: string
            enum:
              - web
              - ios
              - android
          description: >
            Which Messenger client is booting. Determines which
            platform-specific

            Messenger secret the JWT is signed with — `INTERCOM_MESSENGER_SECRET_WEB`,

            `INTERCOM_MESSENGER_SECRET_IOS`, or `INTERCOM_MESSENGER_SECRET_ANDROID`.
      responses:
        "200":
          description: The minted Intercom Messenger JWT.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/intercom_messenger_jwt_response_model"
        "400":
          description: >
            Bad request — missing/invalid inputs are rejected. `userId`

            fails when the caller's auth token has no decoded user id; `platform`

            fails when the query parameter is absent or not one of `web` / `ios`

            / `android`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - userId has an invalid value
                      - platform has an invalid value
                    example: platform has an invalid value
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — the caller's `userId` did not resolve to a user record.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - user not found
                    example: user not found
        "500":
          description: |
            Internal Server Error — the platform-specific Messenger secret env
            var (`INTERCOM_MESSENGER_SECRET_WEB` / `..._IOS` / `..._ANDROID`)
            is not configured on the server.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 500
                  error:
                    type: string
                    example: INTERCOM_MESSENGER_SECRET_WEB is not configured
  /v1/intercom/users/companies:
    put:
      x-internal: true
      summary: Attach the caller to an Intercom company
      tags:
        - Users
      description: |
        Attaches the authenticated user to the given Intercom company and then
        refreshes their Intercom custom attributes (rentcheck/chargebee
        subscription ids, company id). Both the company-attach and the user
        refresh proxy through the Intercom REST API; the response payload
        mirrors Intercom's responses verbatim and is intentionally untyped.

        The attach step is a no-op when the caller has an internal RentCheck
        email, in which case `company` is omitted / null while `user` is still
        refreshed.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/intercom_update_companies_request_model"
      responses:
        "200":
          description: The Intercom attach + user-refresh responses.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/intercom_update_companies_response_model"
        "400":
          description: |
            Bad request — missing/invalid inputs are rejected.
            `userId` fails when the caller's auth token has no decoded user id;
            `companyId` fails when the request body omits it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - userId has an invalid value
                      - companyId has an invalid value
                    example: companyId has an invalid value
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: >
            Not Found — one of three lookups failed. `user not found` and

            `company not found` occur when the caller's `userId` does not resolve

            to an Intercom contact or the requested `companyId` does not exist in

            Intercom. `subscription not found` occurs when the caller has no

            active RentCheck subscription, which is looked up before the Intercom

            refresh.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - user not found
                      - subscription not found
                      - company not found
                    example: company not found
  "/v1/latchel/{orgId}/properties":
    get:
      x-internal: true
      summary: Get all latchel properties
      tags:
        - LatchelProxy
      description: Get all latchel properties
      responses:
        "200":
          description: Latchel response for get all properties, ref
            https://papi.latchel.com/api-docs/papi/#operation/properties_index
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/401"
  "/v1/latchel/{orgId}/tenants":
    get:
      x-internal: true
      summary: Get all latchel tenants
      tags:
        - LatchelProxy
      description: Get all latchel tenants
      responses:
        "200":
          description: Latchel response for get all tenants, ref
            https://papi.latchel.com/api-docs/papi/#operation/tenants_index
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/401"
  "/v1/latchel/{orgId}/vendor-companies":
    get:
      x-internal: true
      summary: Get all latchel vendor companies
      tags:
        - LatchelProxy
      description: Get all latchel vendor companies
      responses:
        "200":
          description: Latchel response for get all vendor companies, ref
            https://papi.latchel.com/api-docs/papi/#operation/vendor_company_index
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/401"
  "/v1/latchel/{orgId}/vendor-companies/prioritized":
    get:
      x-internal: true
      summary: Get all latchel vendor companies prioritized
      tags:
        - LatchelProxy
      description: Get all latchel vendor companies prioritized
      responses:
        "200":
          description: Latchel response for get all vendor companies prioritized, ref
            https://papi.latchel.com/api-docs/papi/#operation/vendor_company_prioritized_index
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/401"
  "/v1/latchel/{orgId}/properties/{id}":
    get:
      x-internal: true
      summary: Get a latchel property
      tags:
        - LatchelProxy
      description: Get a latchel property
      responses:
        "200":
          description: Latchel response for get a property, ref
            https://papi.latchel.com/api-docs/papi/#operation/properties_get
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/401"
  "/v1/latchel/{orgId}/properties/{id}/vendor-companies":
    get:
      x-internal: true
      summary: Get a latchel property's prioritized vendors
      tags:
        - LatchelProxy
      description: Get a latchel property's prioritized vendors
      responses:
        "200":
          description: Latchel response for get a property's prioritized vendors, ref
            https://papi.latchel.com/api-docs/papi/#tag/Properties/paths/~1properties~1{property_id}~1vendor-companies/get
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/401"
  "/v2/residents/{residentId}/leases":
    post:
      summary: Create a lease for a resident
      tags:
        - Leases
      description: This resource allows you to create a Lease for a Resident in RentCheck.
      parameters:
        - name: residentId
          in: path
          description: Resident ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/lease_v2_create_request_model"
      responses:
        "200":
          description: Returns the created lease
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/lease_v2_response_model"
        "400":
          description: >
            Bad request — request body failed validation. The raw

            message identifies the invalid field, e.g. `"data.status" is required`,

            `"data.status" must be one of [future, past, current]`,

            `"data.tags" must be an array`, or

            `"data.tags[0]" must be one of [evict, notice]`. A missing or unknown

            `property_id` surfaces as `404 unit not found`, not a 400.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.status" is required'
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — `resident not found` (the resident at `residentId`
            does not exist or is inaccessible) or `unit not found` (the unit
            identified by `property_id` does not exist or is inaccessible).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: resident not found
    get:
      summary: Get all leases for resident
      tags:
        - Leases
      description: This resource allows you to get all Leases for a Resident.
      parameters:
        - name: residentId
          in: path
          description: Resident ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns all leases for a resident
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/lease_v2_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `resident not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: resident not found
  "/v2/residents/{residentId}/leases/{leaseId}":
    delete:
      summary: Delete lease
      tags:
        - Leases
      description: Delete selected lease by ID for a resident.
      parameters:
        - name: residentId
          in: path
          description: Resident ID
          required: true
          schema:
            type: string
        - name: leaseId
          in: path
          description: Lease ID
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Successful deletion of lease
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `resident not found` or `lease not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: lease not found
                    enum:
                      - resident not found
                      - lease not found
    get:
      summary: Get a lease by ID for a resident
      tags:
        - Leases
      description: This resource allows you to get a lease for a resident Resident in
        RentCheck.
      parameters:
        - name: residentId
          in: path
          description: Resident ID
          required: true
          schema:
            type: string
        - name: leaseId
          in: path
          description: Lease ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested lease
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/lease_v2_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `resident not found` or `lease not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: lease not found
                    enum:
                      - resident not found
                      - lease not found
    put:
      summary: Update a lease for a resident
      tags:
        - Leases
      description: This resource allows you to update a Lease for a Resident in RentCheck.
      parameters:
        - name: residentId
          in: path
          description: Resident ID
          required: true
          schema:
            type: string
        - name: leaseId
          in: path
          description: Lease ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/lease_v2_update_request_model"
      responses:
        "200":
          description: Returns the updated lease
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/lease_v2_response_model"
        "400":
          description: |
            Bad request — request body failed validation. All body fields are
            optional; the raw message identifies the invalid field, e.g.
            `"data.status" must be one of [future, past, current]`,
            `"data.tags" must be an array`, or
            `"data.tags[0]" must be one of [evict, notice]`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.status" must be one of [future, past, current]'
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `lease not found`, `resident not found`, or `unit not
            found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: lease not found
                    enum:
                      - lease not found
                      - resident not found
                      - unit not found
  "/v2/residents/{residentId}/leases/{leaseId}/move-in":
    patch:
      summary: Update a lease move in date and status
      tags:
        - Leases
      description: This resource sets a Lease move_in_date to the current date and the
        status to "current".
      parameters:
        - name: residentId
          in: path
          description: Resident ID
          required: true
          schema:
            type: string
        - name: leaseId
          in: path
          description: Lease ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the updated lease
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/lease_v2_response_model"
        "400":
          description: Bad request — the lease's current status does not allow a move-in
            transition.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - lease status has an invalid value
                    example: lease status has an invalid value
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `lease not found` or `resident not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: lease not found
                    enum:
                      - lease not found
                      - resident not found
  "/v2/residents/{residentId}/leases/{leaseId}/move-out":
    patch:
      summary: Update a lease move out date and status
      tags:
        - Leases
      description: This resource sets a Lease move_out_date to the current date and
        the status to "past".
      parameters:
        - name: residentId
          in: path
          description: Resident ID
          required: true
          schema:
            type: string
        - name: leaseId
          in: path
          description: Lease ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the updated lease
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/lease_v2_response_model"
        "400":
          description: Bad request — the lease's current status does not allow a move-out
            transition.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - lease status has an invalid value
                    example: lease status has an invalid value
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `lease not found` or `resident not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: lease not found
                    enum:
                      - lease not found
                      - resident not found
  /v1/maintenance_reports:
    post:
      summary: Create a maintenance report
      tags:
        - Maintenance Reports
      description: This resource allows you to create a Maintenance Report.
      parameters:
        - in: header
          name: x-app-id
          required: true
          description: >
            Creation-source identifier. Read from the request headers and
            required

            by request validation — omitting it returns a 400 `"appId" is required`.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/maintenance_report_create_request_model"
      responses:
        "201":
          description: Returns the created maintenance report
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 201
                  data:
                    $ref: "#/components/schemas/api_maintenance_report"
        "400":
          description: >
            Bad request — request body validation failed, or one of

            the cross-flag checks rejected the create. The request is wrapped

            under `data`, and the `x-app-id` header is additionally required to

            populate `appId`. Examples:
              - `"data.name" is required`
              - `"data.organize_by" is required`
              - `"data.organize_by" must be one of [responsibility, room, skill]`
              - `"data.flags" is required`
              - `"data.flags[0].id" is required`
              - `"data.flags[0].feature_id" is required`
              - `"appId" is required` — surfaces when the `x-app-id` header is
                missing.
              - `All features should belong to the same inspection`
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: >
            Forbidden — the caller does not have access to this maintenance
            report. Access is

            delegated to the underlying inspection that the requested flags belong to; if the

            caller cannot access that inspection, the create is rejected with the error string below.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: User does not have access to this maintenance report
    delete:
      summary: Delete a maintenance report
      tags:
        - Maintenance Reports
      description: This resource allows you to delete a Maintenance Report.
      parameters:
        - name: id[]
          in: query
          description: |
            Maintenance Report IDs. Repeat the parameter to send multiple values
            (e.g. `?id[]=abc&id[]=def`).
          required: true
          style: form
          explode: true
          schema:
            type: array
            minItems: 1
            items:
              type: string
              minLength: 1
      responses:
        "204":
          description: |
            Confirmation of deletion. Returned even when none of the supplied
            ids matched — the request short-circuits when the ID set resolves
            to an empty list of reports, so this endpoint does not emit a 404
            for unknown ids.
        "400":
          description: |
            Bad request — query string validation failed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - '"data.ids" is required'
                      - '"data.ids" must contain at least 1 items'
                    example: '"data.ids" must contain at least 1 items'
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: |
            Forbidden — the caller does not have access to at least one of the
            requested maintenance reports.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    enum:
                      - User does not have access to this maintenance report
                    example: User does not have access to this maintenance report
  /v2/maintenance_reports:
    get:
      summary: Get all maintenance reports
      tags:
        - Maintenance Reports V2
      description: Get all maintenance reports
      parameters:
        - in: query
          name: filters[search]
          description: Search value among maintenance report's name, inspection label,
            property address, city and zip code.
          schema:
            type: string
        - in: query
          name: filters[created_date][start]
          description: Value to filter maintenance report creation date by a starting date.
          schema:
            type: string
        - in: query
          name: filters[created_date][end]
          description: Value to filter maintenance report creation date by an ending date.
          schema:
            type: string
        - in: query
          name: filters[property_id]
          description: Get all maintenance reports for this property.
          schema:
            type: string
        - in: query
          name: filters[inspection_id]
          description: Get all maintenance reports for this inspection.
          schema:
            type: string
        - in: query
          name: filters[inspection_templates]
          description: Get all maintenance reports for the inspection templates informed.
            This parameter accepts multiple ids concatenated with a `,` (comma).
          schema:
            type: string
            example: inspection_template_id_1,inspection_template_id_2,inspection_template_id_3
        - in: query
          name: options[pagination][page_size]
          description: How many items to be returned for each page.
          schema:
            type: integer
            default: 20
        - in: query
          name: options[pagination][page_number]
          description: Zero-indexed page number.
          schema:
            type: integer
            default: 0
        - in: query
          name: options[sort][sort_type]
          description: |
            Sort list in ascending or descending order. Accepted
            case-insensitively.
          schema:
            type: string
            default: DESC
            enum:
              - ASC
              - DESC
              - asc
              - desc
        - in: query
          name: options[sort][sort_by]
          description: Sort list by a specific property.
          schema:
            type: string
            default: created_date
            enum:
              - property_address
              - property_city
              - property_zip_code
              - name
              - inspection_template
              - created_date
      responses:
        "200":
          description: Returns all maintenance reports
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/api_maintenance_report"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "400":
          description: |
            Bad request — request failed validation. Most
            commonly an unsupported `sort_by` value, a malformed
            `created_date` range, or a non-string filter value.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
  "/v1/maintenance_reports/{maintenanceReportId}":
    get:
      summary: Get a maintenance report by ID
      tags:
        - Maintenance Reports
      description: Get a maintenance report by ID
      parameters:
        - name: maintenanceReportId
          in: path
          description: Maintenance Report ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested maintenance report
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_maintenance_report"
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: Forbidden — the caller does not have access to this maintenance
            report.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: User does not have access to this maintenance report
        "404":
          description: Not Found — `maintenance report not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: maintenance report not found
  "/v1/inspections/{inspectionId}/features/{featureId}/maintenanceFlags":
    get:
      x-internal: true
      summary: Get all maintenance flags for a given inspection and feature
      tags:
        - Maintenance Flags
      description: Get all maintenance flags for a given inspection ID and feature ID
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
        - name: featureId
          in: path
          description: Feature ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns all maintenance flags for the inspection feature
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/maintenance_flag_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — `user not found`, `inspection not found`,
            `property not found`, or `inspection feature not found`. The
            inspection-access guard maps permission failures to
            `inspection not found` instead of 401, so a 404 here can also
            indicate that the inspection exists but the caller cannot read it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection feature not found
  "/v1/inspections/{inspectionId}/maintenanceFlags":
    get:
      x-internal: true
      summary: Get all maintenance flags for an inspection
      tags:
        - Maintenance Flags
      description: Get all maintenance flags for a given inspection ID
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns all maintenance flags for the inspection
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/maintenance_flag_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — `user not found`, `inspection not found`, or
            `property not found`. The inspection-access guard maps permission
            failures to `inspection not found` instead of 401, so a 404 here
            can also indicate that the inspection exists but the caller cannot
            read it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
  "/v1/inspections/{inspectionId}/features/{featureId}/maintenanceFlags/{maintenanceFlagId}":
    get:
      x-internal: true
      summary: Get a maintenance flag by ID for a given inspection and feature
      tags:
        - Maintenance Flags
      description: Get a maintenance flag by ID for a given inspection and feature.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
        - name: featureId
          in: path
          description: Feature ID
          required: true
          schema:
            type: string
        - name: maintenanceFlagId
          in: path
          description: Maintenance flag ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested maintenance flag
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/maintenance_flag_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found. The inspection-access guard maps permission failures to
            `inspection not found` instead of 401, so a 404 here can also
            indicate that the inspection exists but the caller cannot read it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: maintenance flag not found
                    enum:
                      - user not found
                      - inspection not found
                      - property not found
                      - inspection feature not found
                      - maintenance flag not found
  /v1/me/fcm-token:
    delete:
      x-internal: true
      summary: Clear the caller's FCM device tokens
      tags:
        - Users
      description: |
        Removes every FCM device token registered for the caller. Used by the
        mobile clients on sign-out / push unregistration.
      responses:
        "200":
          description: The updated user document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_user"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — the caller's profile could not be located.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: user not found
    put:
      x-internal: true
      summary: Register an FCM device token for the caller
      tags:
        - Users
      description: |
        Replaces all of the caller's registered FCM device tokens with the
        supplied `fcm_token`. Used by the mobile clients on app start / push
        registration.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/me_update_fcm_token_request_model"
      responses:
        "200":
          description: The updated user document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_user"
        "400":
          description: Bad request — `fcm_token` was empty or otherwise invalid.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — the caller's profile could not be located.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: user not found
  /v1/me:
    delete:
      x-internal: true
      summary: Disable the authenticated user
      tags:
        - Users
      description: |
        Disables the authenticated caller's account. The profile is retained
        for audit; the user can no longer sign in.
      responses:
        "204":
          description: User disabled. No content.
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — the caller's profile could not be located.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: user not found
    get:
      x-internal: true
      summary: Get the authenticated user
      tags:
        - Users
      description: >
        Returns the full profile document for the authenticated caller,
        including teams,

        resolved permissions, metadata and external identity verification payloads.
      responses:
        "200":
          description: The authenticated user.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_user"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — the caller's profile could not be located.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: user not found
    put:
      x-internal: true
      summary: Update the authenticated user
      tags:
        - Users
      description: >
        Partial update for the authenticated caller. Changing `email` or
        `password`

        requires `current_password` for re-authentication. Other fields update the

        caller's profile metadata in place.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/me_update_request_model"
      responses:
        "200":
          description: The updated user.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_user"
        "400":
          description: >
            Bad request — validation failed (e.g. `password` shorter than 8

            characters, malformed `email`), or `current_password` was omitted while

            changing `email`/`password`, or the supplied `current_password` did not

            authenticate.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
  /v1/me/firebase-token:
    get:
      x-internal: true
      summary: Mint a Firebase custom auth token for the caller
      tags:
        - Users
      description: |
        Returns a short-lived Firebase custom auth token the caller can exchange
        for a Firebase ID token via the Firebase Auth client SDKs. Used by
        embedded surfaces that need to re-authenticate the caller against
        Firebase.
      responses:
        "200":
          description: The minted custom auth token.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/me_get_firebase_token_response_model"
        "401":
          $ref: "#/components/responses/401"
  /v1/me/notifications-preferences:
    get:
      x-internal: true
      summary: Get the caller's personal notifications preferences
      tags:
        - Users
      description: >
        Returns the per-user notifications preferences document for the

        authenticated caller. Used to drive the per-user notification settings UI.
      responses:
        "200":
          description: The caller's notifications preferences.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/notifications_preferences_response_model"
        "401":
          $ref: "#/components/responses/401"
    put:
      x-internal: true
      summary: Update the caller's personal notifications preferences
      tags:
        - Users
      description: |
        Partial update for the caller's per-user notifications preferences. Any
        combination of `inspections_awaiting_review`,
        `inspections_in_revision_review`, `inspections_completed` and
        `automation_failures` may be supplied; the server merges the values onto
        the current document.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/notifications_preferences_update_request_model"
      responses:
        "200":
          description: The updated notifications preferences.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/notifications_preferences_response_model"
        "400":
          description: Bad request — validation failed on one of the preference items.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
  /v1/me/settings:
    get:
      x-internal: true
      summary: Get the authenticated user's settings
      tags:
        - Users
      description: |
        Returns the metadata document for the authenticated caller — onboarding
        counters, dismiss flags, and view-mode preferences. Defaults are merged
        onto any persisted values.
      responses:
        "200":
          description: The caller's settings.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_user_metadata"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — the caller's profile could not be located.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: user not found
    put:
      x-internal: true
      summary: Update the authenticated user's settings
      tags:
        - Users
      description: |
        Partial update for the caller's metadata. Defaults are merged onto any
        currently-persisted values before the supplied fields are written.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/me_update_settings_request_model"
      responses:
        "200":
          description: The updated settings.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_user_metadata"
        "400":
          description: Bad request — validation failed on one of the metadata fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — the caller's profile could not be located.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: user not found
  /v1/me/subscriptions/notifications-preferences:
    get:
      x-internal: true
      summary: Get the subscription-level notifications preferences for the caller
      tags:
        - Users
      description: >
        Returns the subscription-level defaults that apply to every member of
        the

        caller's subscription when no per-user override exists. Resolved from the

        subscription owned by (or shared with) the caller.
      responses:
        "200":
          description: The subscription-level notifications preferences.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/notifications_preferences_response_model"
        "401":
          $ref: "#/components/responses/401"
    put:
      x-internal: true
      summary: Update the subscription-level notifications preferences for the caller
      tags:
        - Users
      description: |
        Partial update for the subscription-level notifications preferences
        shared across the caller's subscription. Requires the caller to have
        `allow_notifications_preferences_editing` permission on the
        subscription.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/notifications_preferences_update_request_model"
      responses:
        "200":
          description: The updated subscription-level notifications preferences.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/notifications_preferences_response_model"
        "400":
          description: Bad request — validation failed on one of the preference items.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: >
            Forbidden — the caller does not have permission to edit
            notifications

            preferences for the subscription.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
  /v1/me/integration-token:
    get:
      x-internal: true
      summary: Get the caller's long-lived integration token
      tags:
        - Users
      description: |
        Returns the caller's integration token, used for service-to-service API
        calls (e.g. external integrations that authenticate as the caller). The
        token is rotated by the backend when integration credentials change.
      responses:
        "200":
          description: The caller's integration token.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/me_get_integration_token_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — the caller's profile could not be located.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: user not found
  /v1/me/link-with-tenant:
    post:
      x-internal: true
      summary: Link the authenticated user to a tenant record
      tags:
        - Users
      description: |
        Looks up the tenant record on the invite's inspection (or owner) that
        matches the caller's email and links it to the authenticated user. If
        no matching tenant exists, a new tenant + current occupancy is created
        under the appropriate owner so the caller can immediately participate
        in the invited inspection.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/me_link_with_tenant_request_model"
      responses:
        "204":
          description: Tenant linked. No content.
        "400":
          description: Bad request — `invite_id` was missing or otherwise invalid.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — the invite, the invite's inspection, or a suitable owner
            user for the inspection's team could not be located.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - invite not found
                      - inspection not found
                      - user not found
  /v1/me/metabase-embed-token:
    get:
      x-internal: true
      summary: Mint a Metabase static-embedding token for Resident Insights
      tags:
        - Users
      description: |
        Returns a signed Metabase static-embedding JWT scoped to the caller's
        subscription so the Resident Experience Insights dashboard can be
        embedded in web / mobile without exposing another customer's data. The
        `subscription_id` is resolved server-side and signed into the token's
        locked `params`.

        Access is gated to accounts enrolled in the Resident Insights beta —
        non-enrolled callers get 403.
      responses:
        "200":
          description: The minted embed token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/me_get_metabase_embed_token_response_model"
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: |
            Forbidden — the caller's subscription is not enrolled in the
            Resident Insights beta.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: resident insights is not enabled for this account
        "404":
          description: |
            Not Found — `subscription not found`. Returned when the caller has
            no subscription attached.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: subscription not found
  /v1/me/pm-contact-info:
    get:
      x-internal: true
      summary: Get the caller's property manager contact info
      tags:
        - Users
      description: |
        Returns the contact info for the property management company that owns
        the caller's tenant record. Used by renter surfaces to expose a way to
        contact the PM directly.
      responses:
        "200":
          description: The caller's property manager contact info.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/me_get_pm_contact_info_response_model"
        "400":
          description: |
            Bad request — the caller's user record is not a Renter.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - user_type
                    example: user_type
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — the caller is not linked to any tenant record, or the
            owning property manager has no contact info on file.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
  /v1/me/requests:
    get:
      x-internal: true
      summary: Get the caller's pending inspection requests
      tags:
        - Users
      description: >
        Returns the list of inspection requests sent to the authenticated caller

        that are awaiting action (e.g. additional signature requests). Used by

        the apps' notification bell to surface requests the caller still needs to

        respond to.
      responses:
        "200":
          description: The caller's pending inspection requests.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/me_get_user_requests_response_model"
                  count:
                    type: integer
                    description: Number of elements in `data`.
        "401":
          $ref: "#/components/responses/401"
  "/v1/notifications-preferences/{preferencesId}":
    get:
      x-internal: true
      security: []
      summary: Get notification preferences by token
      tags:
        - Account Settings
      description: >
        Resolve a notifications-preferences document from the encrypted token
        included

        in RentCheck-issued notification emails. The token decrypts to the recipient's

        email; if no preferences document exists for that email yet, the default

        preferences shape is returned with `user_email` populated. This endpoint is

        reachable without an authenticated session — clients land on it from

        "manage your notification preferences" links.


        For the equivalent authenticated endpoint, use

        `GET /v1/me/notifications-preferences`.
      parameters:
        - name: preferencesId
          in: path
          description: The encrypted token identifying the recipient. Issued by RentCheck
            and embedded in notification email links.
          required: true
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Returns the notification preferences for the resolved recipient.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/notifications_preferences_response_model"
        "401":
          description: >
            Unauthorized — the token failed to decrypt. Example: `invalid
            notifications preferences id`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 401
                  error:
                    type: string
                    example: invalid notifications preferences id
    put:
      x-internal: true
      security: []
      summary: Update notification preferences via token
      tags:
        - Account Settings
      description: >
        Update a notifications-preferences document for the recipient identified
        by

        the encrypted token. Used by the "manage your notification preferences"

        experience opened from RentCheck notification emails — no authenticated

        session is required. Supplied fields are merged onto the current preferences;

        if no preferences document exists yet, one is created and then patched.


        For the equivalent authenticated endpoint, use

        `PUT /v1/me/notifications-preferences`.
      parameters:
        - name: preferencesId
          in: path
          description: The encrypted token identifying the recipient. Issued by RentCheck
            and embedded in notification email links.
          required: true
          schema:
            type: string
            minLength: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/notifications_preferences_update_request_model"
      responses:
        "200":
          description: Returns the updated notification preferences document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/notifications_preferences_response_model"
        "400":
          description: >
            Bad request — validation on the request body failed. `enabled` is

            optional on every preference, so the only messages actually emitted

            come from the `email` enum check on the three preferences that expose

            it (`inspections_awaiting_review`, `inspections_in_revision_review`,

            `inspections_completed`). Example:

            `"inspections_awaiting_review.email" must be one of [all, created_by_me]`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"inspections_awaiting_review.email" must be one of [all,
                      created_by_me]'
        "401":
          description: >
            Unauthorized — the token failed to decrypt or did not resolve to a

            well-formed email address. Example: `invalid notifications preferences id`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 401
                  error:
                    type: string
                    example: invalid notifications preferences id
  /v1/permission-groups:
    post:
      summary: Create permission group
      tags:
        - Permission Groups
      description: This resource allows you to create a Permission Group in RentCheck.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/permission_group_create_request_model"
      responses:
        "201":
          description: Returns the created permission group
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 201
                  data:
                    $ref: "#/components/schemas/api_permission_group"
        "400":
          description: Bad request — request body validation failed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - '"data.name" is required'
                      - '"data.inspection_template_ids" is required'
                      - '"data.allow_billing_plan_editing" is required'
                      - '"data.user_ids" is required'
                    example: '"data.name" is required'
        "401":
          description: >
            Unauthorized — either a standard auth-token failure, or the caller's

            role does not allow creating permission groups. When the role is

            insufficient, the request surfaces as 401 with the error string below.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 401
                  error:
                    type: string
                    enum:
                      - token is not valid
                      - authorization header is empty
                      - authorization header is not valid
                      - "format for Authorization must be: Bearer [token]"
                      - refresh token is not valid for resources
                      - not well-formed token
                      - User does not have permissions to create permission group
                    example: User does not have permissions to create permission group
        "404":
          description: |
            Not Found — `subscription not found` (caller has no active
            subscription), `inspection template not found (<id>)`, or
            `user not found (<id>)` when one of the supplied `user_ids` /
            `inspection_template_ids` does not resolve.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - subscription not found
                      - inspection template not found (<id>)
                      - user not found (<id>)
                    example: subscription not found
        "409":
          description: >
            Conflict — a permission group with the requested `name` already
            exists

            on the caller's subscription (names are unique per subscription).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 409
                  error:
                    type: string
                    example: A permission group with this name already exists on the subscription
    delete:
      summary: Delete permission groups
      tags:
        - Permission Groups
      description: >
        Bulk-delete permission groups belonging to the caller's subscription.
        Group ids are passed in the

        request body (not the URL); at least one permission group must remain on the subscription after

        the operation, so attempting to delete every remaining group is rejected with a 403.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/permission_group_delete_request_model"
      responses:
        "204":
          description: Successful deletion of permission groups.
        "400":
          description: |
            Bad request — validation rejected the body. `data.ids` must be a
            non-empty string array, so both a missing field and an empty array
            surface here.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - '"data.ids" is required'
                      - '"data.ids" must contain at least 1 items'
                    example: '"data.ids" is required'
        "401":
          description: |
            Unauthorized — either a standard auth-token failure, or the caller's
            role does not allow deleting permission groups.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 401
                  error:
                    type: string
                    enum:
                      - token is not valid
                      - authorization header is empty
                      - authorization header is not valid
                      - "format for Authorization must be: Bearer [token]"
                      - refresh token is not valid for resources
                      - not well-formed token
                      - User does not have permissions to delete permission group
                    example: User does not have permissions to delete permission group
        "403":
          description: Forbidden — the delete would leave the subscription with no
            remaining permission groups.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    enum:
                      - At least one permission group must remain on the
                        subscription
                    example: At least one permission group must remain on the subscription
        "404":
          description: >
            Not Found — `subscription not found`, or `permission group not found
            (<id>)`.

            The permission-group message includes the offending id in parentheses,

            i.e. `<name> not found (<id>)`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: permission group not found (abc123)
    get:
      summary: Get all permission groups
      tags:
        - Permission Groups
      description: This resource fetches all Permission Groups associated with the
        user's subscription.
      parameters:
        - in: query
          name: filters[search]
          description: Search by permission group name or id.
          schema:
            type: string
        - in: query
          name: filters[user_id]
          description: Restrict results to permission groups that include the given user id.
          schema:
            type: string
        - in: query
          name: options[pagination][page_size]
          description: How many items to be returned for each page.
          schema:
            type: integer
            default: 20
        - in: query
          name: options[pagination][page_number]
          description: Page number (zero-indexed).
          schema:
            type: integer
            default: 0
        - in: query
          name: options[sort][sort_type]
          description: Sort list in ascending or descending order.
          schema:
            type: string
            default: ASC
            enum:
              - ASC
              - DESC
        - in: query
          name: options[sort][sort_by]
          description: Sort list by a specific property.
          schema:
            type: string
            default: name
            enum:
              - name
              - user_count
      responses:
        "200":
          description: Returns all permission groups
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/permission_group_get_all_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data` on the current page.
                  total_results:
                    type: integer
                    description: Total amount of permission groups matching the filter criteria
                      (irrespective of paging).
        "401":
          $ref: "#/components/responses/401"
  "/v1/permission-groups/{id}":
    get:
      summary: Get permission group by ID
      tags:
        - Permission Groups
      description: This resource allows you to get a Permission Group in RentCheck.
      parameters:
        - name: id
          in: path
          description: Permission group ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested permission group
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_permission_group"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `subscription not found` (when the caller has no
            subscription) or `permission group not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - subscription not found
                      - permission group not found
                    example: permission group not found
    put:
      summary: Update permission group
      tags:
        - Permission Groups
      description: This resource allows you to update a Permission Group in RentCheck.
      parameters:
        - name: id
          in: path
          description: Permission group ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/permission_group_update_request_model"
      responses:
        "200":
          description: Returns the updated permission group
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_permission_group"
        "400":
          description: Bad request — validation rejected the request body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - '"data.name" is required'
                      - '"data.inspection_template_ids" is required'
                      - '"data.user_ids" is required'
                    example: '"data.name" is required'
        "401":
          description: >
            Unauthorized — either a standard auth-token failure, or the caller's

            role does not allow updating permission groups. When the role is

            insufficient, the request surfaces as 401 with the error string below.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 401
                  error:
                    type: string
                    enum:
                      - token is not valid
                      - authorization header is empty
                      - authorization header is not valid
                      - "format for Authorization must be: Bearer [token]"
                      - refresh token is not valid for resources
                      - not well-formed token
                      - User does not have permissions to update permission group
                    example: User does not have permissions to update permission group
        "404":
          description: |
            Not Found — `subscription not found`, `permission group not found`,
            `user not found (<id>)`, or `inspection template not found (<id>)`.
            The `user` and `inspection template` messages include the offending
            id in parentheses, i.e. `<name> not found (<id>)`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: user not found (abc123)
        "409":
          description: |
            Conflict — a different permission group already owns the requested
            `name` on the caller's subscription (names are unique per
            subscription). Renaming to the current name is not a conflict.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 409
                  error:
                    type: string
                    example: A permission group with this name already exists on the subscription
  /v2/properties/buildings:
    get:
      summary: Get buildings
      tags:
        - Properties V2
      description: This resource allows you to get all Buildings. Buildings are
        properties that can contain a number of individual Units.
      parameters:
        - in: query
          name: page_number
          schema:
            type: integer
            default: 0
          description: The page number of the collection. Zero-indexed.
        - in: query
          name: page_size
          schema:
            type: integer
            default: 20
          description: The number of elements in the data collection.
        - in: query
          name: search
          schema:
            type: string
          description: Search string to match building addresses
        - $ref: "#/components/parameters/V3PropertyListTeamValue"
        - $ref: "#/components/parameters/V3PropertyListTeamCondition"
        - $ref: "#/components/parameters/V3PropertyListTeamIdLegacy"
        - $ref: "#/components/parameters/V3PropertyListLastCompletedStart"
        - $ref: "#/components/parameters/V3PropertyListLastCompletedEnd"
        - in: query
          name: synced
          schema:
            type: string
            enum:
              - synced
              - not synced
          description: Value to filter by one of the available integration sync status
        - in: query
          name: sort_by
          schema:
            type: string
            default: address
            enum:
              - name
              - address
              - last_completed_inspection
          description: Sort results by one of the available fields.
        - $ref: "#/components/parameters/V3PropertyListSortType"
      responses:
        "200":
          description: Returns buildings
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - data
                  - count
                  - total_results
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/building_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "400":
          description: |
            One of the `filters` values failed validation. Only the `filters`
            object is server-side validated; invalid
            `sort_by` / `sort_type` values do NOT surface as 400.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: '"filters.synced" must be one of [synced, not synced]'
        "401":
          $ref: "#/components/responses/401"
  /v2/properties/communities:
    get:
      summary: Get communities
      tags:
        - Properties V2
      description: This resource allows you to get all Communities. Communities are
        properties that contain one or more individual Units.
      parameters:
        - in: query
          name: page_number
          schema:
            type: integer
            default: 0
          description: The page number of the collection. Zero-indexed.
        - in: query
          name: page_size
          schema:
            type: integer
            default: 20
          description: The number of elements in the data collection.
        - in: query
          name: search
          schema:
            type: string
          description: Search string to match community addresses
        - $ref: "#/components/parameters/V3PropertyListTeamValue"
        - $ref: "#/components/parameters/V3PropertyListTeamCondition"
        - $ref: "#/components/parameters/V3PropertyListTeamIdLegacy"
        - $ref: "#/components/parameters/V3PropertyListLastCompletedStart"
        - $ref: "#/components/parameters/V3PropertyListLastCompletedEnd"
        - in: query
          name: synced
          schema:
            type: string
            enum:
              - synced
              - not synced
          description: Value to filter by one of the available integration sync status
        - in: query
          name: sort_by
          schema:
            type: string
            default: address
            enum:
              - name
              - address
              - last_completed_inspection
          description: Sort results by one of the available fields.
        - $ref: "#/components/parameters/V3PropertyListSortType"
      responses:
        "200":
          description: Returns communities
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - data
                  - count
                  - total_results
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/community_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "400":
          description: |
            One of the `filters` values failed validation. Only the `filters`
            object is server-side validated; invalid
            `sort_by` / `sort_type` values do NOT surface as 400.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: '"filters.synced" must be one of [synced, not synced]'
        "401":
          $ref: "#/components/responses/401"
  /v3/properties/buildings:
    get:
      x-internal: true
      summary: Get all buildings (v3 digest)
      tags:
        - Properties V3
      description: >
        Returns a paginated list of building property digests. V3 returns a
        trimmed `api_property_digest`

        projection and uses the `filters[team][value|condition]` syntax (with the legacy `team_id[]` parameter

        still accepted).
      parameters:
        - $ref: "#/components/parameters/V3PropertyListPageSize"
        - $ref: "#/components/parameters/V3PropertyListPageNumber"
        - $ref: "#/components/parameters/V3PropertyListSortBy"
        - $ref: "#/components/parameters/V3PropertyListSortType"
        - in: query
          name: search
          schema:
            type: string
          description: Search string to match building addresses.
        - $ref: "#/components/parameters/V3PropertyListTeamValue"
        - $ref: "#/components/parameters/V3PropertyListTeamCondition"
        - $ref: "#/components/parameters/V3PropertyListTeamIdLegacy"
        - $ref: "#/components/parameters/V3PropertyListLastCompletedStart"
        - $ref: "#/components/parameters/V3PropertyListLastCompletedEnd"
        - $ref: "#/components/parameters/V3PropertyListSynced"
        - $ref: "#/components/parameters/V3PropertyListExtraFields"
      responses:
        "200":
          description: Returns the paginated list of building digests.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/v3_property_list_response_model"
        "400":
          description: |
            One of the `filters` values failed validation. Only the `filters`
            object is server-side validated;
            invalid `sort_by` / `sort_type` values do NOT surface as 400.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: '"filters.synced" must be one of [synced, not synced]'
        "401":
          $ref: "#/components/responses/401"
  /v3/properties/communities:
    get:
      x-internal: true
      summary: Get all communities (v3 digest)
      tags:
        - Properties V3
      description: >
        Returns a paginated list of community property digests. Uses the same
        filter / pagination shape as

        `/v3/properties/buildings` and `/v3/properties/units`.
      parameters:
        - $ref: "#/components/parameters/V3PropertyListPageSize"
        - $ref: "#/components/parameters/V3PropertyListPageNumber"
        - $ref: "#/components/parameters/V3PropertyListSortBy"
        - $ref: "#/components/parameters/V3PropertyListSortType"
        - in: query
          name: search
          schema:
            type: string
          description: Search string to match community addresses.
        - $ref: "#/components/parameters/V3PropertyListTeamValue"
        - $ref: "#/components/parameters/V3PropertyListTeamCondition"
        - $ref: "#/components/parameters/V3PropertyListTeamIdLegacy"
        - $ref: "#/components/parameters/V3PropertyListLastCompletedStart"
        - $ref: "#/components/parameters/V3PropertyListLastCompletedEnd"
        - $ref: "#/components/parameters/V3PropertyListSynced"
        - $ref: "#/components/parameters/V3PropertyListExtraFields"
      responses:
        "200":
          description: Returns the paginated list of community digests.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/v3_property_list_response_model"
        "400":
          description: |
            One of the `filters` values failed validation. Only the `filters`
            object is server-side validated;
            invalid `sort_by` / `sort_type` values do NOT surface as 400.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: '"filters.synced" must be one of [synced, not synced]'
        "401":
          $ref: "#/components/responses/401"
  /v3/properties/units:
    get:
      x-internal: true
      summary: Get all units (v3 digest)
      tags:
        - Properties V3
      description: >
        Returns a paginated list of unit property digests. Uses the same filter
        / pagination shape as

        `/v3/properties/buildings` and `/v3/properties/communities`.
      parameters:
        - $ref: "#/components/parameters/V3PropertyListPageSize"
        - $ref: "#/components/parameters/V3PropertyListPageNumber"
        - $ref: "#/components/parameters/V3PropertyListSortBy"
        - $ref: "#/components/parameters/V3PropertyListSortType"
        - in: query
          name: search
          schema:
            type: string
          description: Search string to match unit addresses.
        - $ref: "#/components/parameters/V3PropertyListTeamValue"
        - $ref: "#/components/parameters/V3PropertyListTeamCondition"
        - $ref: "#/components/parameters/V3PropertyListTeamIdLegacy"
        - $ref: "#/components/parameters/V3PropertyListLastCompletedStart"
        - $ref: "#/components/parameters/V3PropertyListLastCompletedEnd"
        - $ref: "#/components/parameters/V3PropertyListSynced"
        - $ref: "#/components/parameters/V3PropertyListExtraFields"
      responses:
        "200":
          description: Returns the paginated list of unit digests.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/v3_property_list_response_model"
        "400":
          description: |
            One of the `filters` values failed validation. Only the `filters`
            object is server-side validated;
            invalid `sort_by` / `sort_type` values do NOT surface as 400.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: '"filters.synced" must be one of [synced, not synced]'
        "401":
          $ref: "#/components/responses/401"
  /v3/properties/permissions:
    get:
      x-internal: true
      summary: Get property permissions for the current user
      tags:
        - Properties V3
      description: >
        Returns the property-level permissions that drive the RentCheck app UI
        (e.g. whether the user can

        trigger bulk unit imports). Used internally by the mobile and web apps.
      responses:
        "200":
          description: Returns the property permissions for the current user.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_properties_permissions"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `user not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: user not found
  "/v2/properties/{propertyId}":
    get:
      x-internal: true
      summary: Get a property by ID
      tags:
        - Properties V2
      description: Returns a single property (unit, building, or community) by its ID.
      parameters:
        - name: propertyId
          in: path
          description: Property ID. Matches the `id` of a unit, building, or community.
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested property
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    oneOf:
                      - $ref: "#/components/schemas/unit_response_model"
                      - $ref: "#/components/schemas/building_response_model"
                      - $ref: "#/components/schemas/community_response_model"
                    discriminator:
                      propertyName: property_type
                      mapping:
                        "": "#/components/schemas/unit_response_model"
                        Single-Family: "#/components/schemas/unit_response_model"
                        Multi-Family: "#/components/schemas/unit_response_model"
                        Apartment or Condo: "#/components/schemas/unit_response_model"
                        Commercial: "#/components/schemas/unit_response_model"
                        Building: "#/components/schemas/building_response_model"
                        Community: "#/components/schemas/community_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `property not found` or `users not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: property not found
  "/v1/properties/{propertyId}/merge/{targetPropertyId}":
    put:
      x-internal: true
      summary: Merge two properties
      tags:
        - Buildings
        - Communities
        - Units
      description: >
        Merges the origin property (`propertyId`) into the target property
        (`targetPropertyId`). The

        origin is soft-deleted and its occupancies / residents are re-parented to the target. When

        `transfer_rooms` is `true`, the origin's room configuration is merged onto the target.
      parameters:
        - name: propertyId
          in: path
          description: Origin property ID (the one being merged away).
          required: true
          schema:
            type: string
        - name: targetPropertyId
          in: path
          description: Target property ID (the one that will be kept).
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/properties_merge_request_model"
      responses:
        "200":
          description: Returns the merged target property.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    oneOf:
                      - $ref: "#/components/schemas/unit_response_model"
                      - $ref: "#/components/schemas/building_response_model"
                      - $ref: "#/components/schemas/community_response_model"
                    discriminator:
                      propertyName: property_type
                      mapping:
                        "": "#/components/schemas/unit_response_model"
                        Single-Family: "#/components/schemas/unit_response_model"
                        Multi-Family: "#/components/schemas/unit_response_model"
                        Apartment or Condo: "#/components/schemas/unit_response_model"
                        Commercial: "#/components/schemas/unit_response_model"
                        Building: "#/components/schemas/building_response_model"
                        Community: "#/components/schemas/community_response_model"
        "401":
          $ref: "#/components/responses/401"
        "403":
          $ref: "#/components/responses/synced_property_merge_forbidden"
        "404":
          description: >
            Not Found — `property not found` or `user not found`. Both the
            origin

            and target property lookups surface as `property not found` when a

            property is missing or inaccessible.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: property not found
  /v2/properties/units:
    get:
      summary: Get units
      tags:
        - Properties V2
      description: Get units according to logged in user
      parameters:
        - in: query
          name: page_number
          schema:
            type: integer
            default: 0
          description: The page number of the collection. Zero-indexed.
        - in: query
          name: page_size
          schema:
            type: integer
            default: 20
          description: The number of elements in the data collection.
        - in: query
          name: search
          schema:
            type: string
          description: Search string to match unit addresses
        - $ref: "#/components/parameters/V3PropertyListTeamValue"
        - $ref: "#/components/parameters/V3PropertyListTeamCondition"
        - $ref: "#/components/parameters/V3PropertyListTeamIdLegacy"
        - $ref: "#/components/parameters/V3PropertyListLastCompletedStart"
        - $ref: "#/components/parameters/V3PropertyListLastCompletedEnd"
        - in: query
          name: synced
          schema:
            type: string
            enum:
              - synced
              - not synced
          description: Value to filter by one of the available integration sync status
        - in: query
          name: sort_by
          schema:
            type: string
            default: address
            enum:
              - name
              - address
              - last_completed_inspection
          description: >
            Sort results by one of the available fields. `name` has no effect

            for units (units have no `name` field); it is preserved in the enum for

            shared use with buildings/communities.
        - $ref: "#/components/parameters/V3PropertyListSortType"
      responses:
        "200":
          description: Returns units
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - data
                  - count
                  - total_results
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/unit_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "400":
          description: |
            One of the `filters` values failed validation. Only the `filters`
            object is server-side validated; invalid
            `sort_by` / `sort_type` values do NOT surface as 400.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: '"filters.synced" must be one of [synced, not synced]'
        "401":
          $ref: "#/components/responses/401"
  "/v1/properties/{propertyId}/leases":
    get:
      x-internal: true
      summary: List every lease on a property
      tags:
        - Leases
      description: |
        Returns every lease (occupancy) associated with the unit identified by
        `propertyId`. The property is resolved through the unit fetcher — so
        building and community IDs are rejected.

        Not to be confused with `GET /v1/properties/resident-leases`, which
        returns only the leases the authenticated renter has access to.
      parameters:
        - name: propertyId
          in: path
          description: Property (unit) id.
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns every lease on the property.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/lease_v2_response_model"
                  count:
                    type: integer
                    description: Number of leases returned.
                  total_results:
                    type: integer
                    description: Total number of leases on the property.
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: >
            Not Found — `unit not found` when the property does not exist, is a

            building/community, or the caller is not on its authorized user list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: unit not found
  "/v1/properties/{propertyId}/resident-leases":
    post:
      x-internal: true
      summary: Create a resident lease
      tags:
        - Leases
      description: Creates a new resident lease for the given property.
      parameters:
        - name: propertyId
          in: path
          required: true
          description: ID of the property the new lease belongs to.
          schema:
            type: string
            minLength: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/tenant_occupancy_create_request_model"
      responses:
        "200":
          description: Returns the newly created resident lease.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_tenant_occupancy"
        "400":
          description: |
            Bad request — request body validation failed. The raw message
            identifies the invalid field, e.g.
            `"data.status" is required`,
            `"data.status" must be one of [past, current]`, or
            `"data.move_in_date" must be in one of the allowed formats`
            (the field also allows `null`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.status" is required'
        "401":
          $ref: "#/components/responses/resident_lease_unauthorized"
        "403":
          description: |
            Forbidden — only renters can call this endpoint.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    enum:
                      - Only renters can create resident leases
                    example: Only renters can create resident leases
        "404":
          description: Not Found — `property not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - property not found
                    example: property not found
  "/v1/properties/{propertyId}/resident-leases/{residentLeaseId}":
    delete:
      x-internal: true
      summary: Delete a resident lease
      tags:
        - Leases
      description: Deletes a resident lease for the given property.
      parameters:
        - name: propertyId
          in: path
          required: true
          description: ID of the property the lease belongs to.
          schema:
            type: string
            minLength: 1
        - name: residentLeaseId
          in: path
          required: true
          description: |
            ID of the resident lease. Must be a valid UUID — a non-UUID id is
            interpreted as a property-manager lease id and rejected with a 400.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Returns the deleted resident lease.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_tenant_occupancy"
        "400":
          description: >
            Bad request — the `residentLeaseId` is not a valid UUID, which is
            interpreted by the

            handler as a property-manager lease id that cannot be soft-deleted via this endpoint.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: A PM lease can't be deleted
        "401":
          $ref: "#/components/responses/resident_lease_unauthorized"
        "403":
          description: >
            Forbidden — only `Renter` users can delete resident leases.
            Property-manager and admin

            accounts get a `403` with the error string below.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: Only renters can delete resident leases
        "404":
          description: >
            Not Found — either `property not found` (the property does not
            exist) or

            `lease not found` (the lease does not exist or has already been soft-deleted).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - property not found
                      - lease not found
    get:
      x-internal: true
      summary: Get a resident lease by id
      tags:
        - Leases
      description: Returns a single resident lease for the given property.
      parameters:
        - name: propertyId
          in: path
          required: true
          description: ID of the property the lease belongs to.
          schema:
            type: string
            minLength: 1
        - name: residentLeaseId
          in: path
          required: true
          description: ID of the resident lease.
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Returns the resident lease.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_tenant_occupancy"
        "401":
          $ref: "#/components/responses/resident_lease_unauthorized"
        "403":
          description: >
            Forbidden — either the caller is not a `Renter` user, or the lease
            being looked up is

            a property-manager lease (non-UUID id) whose underlying tenant record does not match

            the authenticated user.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    enum:
                      - Only renters can get resident leases
                      - Lease does not belong to user
                    example: Only renters can get resident leases
        "404":
          description: >
            Not Found — the resource referenced in the path (or a related
            resource looked up while

            building the response) could not be found. For PM-lease ids (non-UUID), `tenant not found`

            is also possible when the authenticated user has no tenant record linked to the lease.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - property not found
                      - lease not found
                      - tenant not found
                    example: lease not found
    put:
      x-internal: true
      summary: Update a resident lease
      tags:
        - Leases
      description: Updates a resident lease for the given property.
      parameters:
        - name: propertyId
          in: path
          required: true
          description: ID of the property the lease belongs to.
          schema:
            type: string
            minLength: 1
        - name: residentLeaseId
          in: path
          required: true
          description: ID of the resident lease.
          schema:
            type: string
            minLength: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/tenant_occupancy_update_request_model"
      responses:
        "200":
          description: Returns the updated resident lease.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_tenant_occupancy"
        "400":
          description: |
            Bad request — request body validation failed. The raw message
            identifies the invalid field, e.g.
            `"data.status" must be one of [past, current]` or
            `"data.move_in_date" must be in one of the allowed formats`
            (the field also allows `null`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.status" must be one of [past, current]'
        "401":
          $ref: "#/components/responses/resident_lease_unauthorized"
        "403":
          description: >
            Forbidden — either the caller is not a `Renter` user, or the lease
            being updated does

            not belong to the authenticated user.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    enum:
                      - Only renters can update resident leases
                      - Lease does not belong to user
                    example: Only renters can update resident leases
        "404":
          description: >
            Not Found — the property the lease belongs to does not exist, or the
            lease itself does

            not exist (or has already been soft-deleted).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - property not found
                      - lease not found
                    example: lease not found
  /v1/properties/resident-leases:
    get:
      x-internal: true
      summary: Get all resident leases
      tags:
        - Leases
      description: >
        Returns every resident lease the caller has access to, optionally
        filtered by status, lease

        type, and a free-form search string. Results are returned without pagination.
      parameters:
        - in: query
          name: search
          schema:
            type: string
          description: Free-form search string matched across resident and property fields.
        - in: query
          name: filters[status][value]
          schema:
            type: string
            pattern: ^(past|current)(\|(past|current))*$
          description: >
            List of lease statuses to filter by. Multiple values are
            concatenated with `|`. When either `filters[status][value]` or
            `filters[status][condition]` is supplied, both are required.
        - in: query
          name: filters[status][condition]
          schema:
            type: string
            enum:
              - any_of
          description: Condition to apply to the status filter. The validator only accepts
            `any_of`. Must be paired with `filters[status][value]` (see above).
        - in: query
          name: filters[lease_type][value]
          schema:
            type: string
            pattern: ^(renter|pm)(\|(renter|pm))*$
          description: >
            List of lease types to filter by. Multiple values are concatenated
            with `|`. When either `filters[lease_type][value]` or
            `filters[lease_type][condition]` is supplied, both are required
            (same paired-required rule as `filters[status]` above).
        - in: query
          name: filters[lease_type][condition]
          schema:
            type: string
            enum:
              - any_of
          description: Condition to apply to the lease_type filter. The validator only
            accepts `any_of`. Must be paired with `filters[lease_type][value]`
            (see above).
        - in: query
          name: sort_by
          required: true
          schema:
            type: string
            enum:
              - created_date
          description: >
            Field to sort by. Only `created_date` is currently supported.
            Required —

            omitting it yields `400 "options.sort.sort_by" is required`.
        - in: query
          name: sort_type
          schema:
            type: string
            enum:
              - ASC
              - DESC
          description: >
            Sort direction. `sort_type` is technically required, but a default
            of

            `ASC` is injected when the query string omits it — so callers never see

            the 400 for a missing value in practice.
      responses:
        "200":
          description: Returns the list of resident leases.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/api_tenant_occupancy"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "400":
          description: |
            The query parameters failed validation.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: '"options.sort.sort_by" is required'
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: >
            Forbidden — only `Renter` users can read resident leases.
            Property-manager and admin

            accounts get a `403` with the error string below.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: Only renters can read resident leases
  "/v1/properties/{propertyId}/resident-leases/move-out":
    patch:
      x-internal: true
      summary: Move out the current resident from a property
      tags:
        - Leases
      description: >
        Marks the current resident lease(s) for the property as moved-out, and
        updates any associated

        inspections accordingly. Returns the updated leases and inspections impacted by the move-out.
      parameters:
        - name: propertyId
          in: path
          required: true
          description: ID of the property the lease(s) belong to.
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Returns the leases and inspections affected by the move-out.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/resident_moved_out_response_model"
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: >
            Forbidden — only `Renter` users can perform the resident move-out
            action.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: Only renters can perform this action
        "404":
          description: >
            Not Found — either `property not found` (the property does not
            exist) or

            `occupancy not found` (the caller has no current occupancy to move out of on this property).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - property not found
                      - occupancy not found
  /v2/residents:
    post:
      summary: Create a resident
      tags:
        - Residents V2
      description: This resource allows you to create a Resident in RentCheck.
        Residents are used to provide contact information for inspections.
      parameters:
        - in: header
          name: x-app-id
          required: false
          description: |
            Optional creation-source identifier. Persisted on the resident's
            `creation_source` field. Used as a fallback when
            no `appId` is available on the decoded bearer token.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/resident_v2_create_request_model"
      responses:
        "200":
          description: Returns the created resident
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/resident_v2_response_model"
        "400":
          description: |
            Bad request — request body failed validation. The raw message
            identifies the invalid field, e.g.:
              - `"data.first_name" is required`
              - `"data.last_name" is required`
              - `"data.emails" must contain at least 1 items`
              - `"data.emails[0]" must be a valid email`

            Invalid phone numbers are rejected by a custom validator.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.first_name" is required'
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: >
            Forbidden — the caller is not a member of the requested
            `organization_id`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: invalid organization
        "404":
          description: Not Found — `organization not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: organization not found
    get:
      x-internal: true
      summary: Get all residents
      tags:
        - Residents V2
      description: This resource allows you to list Residents in RentCheck. Supports
        both the new `filters[...]` / `options[...]` query schema and the legacy
        `occupancy_status[]` multi-value syntax.
      parameters:
        - in: query
          name: filters[search]
          schema:
            type: string
          description: Search string to match against resident name, email, and phone
            number.
        - in: query
          name: filters[deleted]
          schema:
            type: string
            enum:
              - "true"
              - "false"
            default: "false"
          description: When `true`, returns soft-deleted residents. Defaults to `false`.
        - in: query
          name: filters[active]
          schema:
            type: string
            enum:
              - "true"
              - "false"
          description: When provided, filters residents by whether they currently have an
            active occupancy.
        - in: query
          name: filters[emails]
          schema:
            type: string
          description: Email(s) to filter residents by. Multiple values may be
            comma-separated — the string is split on `,` and matched by
            array-overlap. Single-value requests remain a comma-free string.
        - in: query
          name: filters[property_id]
          schema:
            type: string
          description: Property ID to filter residents by. Only residents with an
            occupancy on this property are returned.
        - in: query
          name: filters[occupancy_status][value]
          schema:
            type: string
            pattern: ^(future|past|current)(\|(future|past|current))*$
          description: >
            Occupancy status(es) to filter residents by. Multiple values should
            be concatenated with a `|`. Allowed individual values are `future`,
            `past`, and `current`. When either
            `filters[occupancy_status][value]` or
            `filters[occupancy_status][condition]` is supplied, both are
            required.
        - in: query
          name: filters[occupancy_status][condition]
          schema:
            type: string
            enum:
              - any_of
          description: Condition to be used to filter by occupancy status. The validator
            only accepts `any_of`. Must be paired with
            `filters[occupancy_status][value]` (see above). Note also that
            `filters[active]` and `filters[occupancy_status]` are mutually
            exclusive — supplying both is rejected by the request validator.
        - in: query
          name: occupancy_status[]
          deprecated: true
          schema:
            type: array
            items:
              type: string
          description: Legacy multi-value occupancy status filter. Prefer
            `filters[occupancy_status][value]`.
        - in: query
          name: options[pagination][page_size]
          schema:
            type: integer
            default: 20
          description: The number of elements in the data collection.
        - in: query
          name: options[pagination][page_number]
          schema:
            type: integer
            default: 0
          description: The page number of the collection. This value is based on the page
            size.
        - in: query
          name: options[sort][sort_by]
          schema:
            type: string
            default: first_name
            enum:
              - first_name
              - last_name
              - emails
              - property_address
              - lease_start
              - lease_end
              - status
          description: Sort results by one of the available fields.
        - in: query
          name: options[sort][sort_type]
          schema:
            type: string
            enum:
              - ASC
              - DESC
              - asc
              - desc
            default: ASC
          description: Sort direction. Case-insensitive.
      responses:
        "200":
          description: Returns the matching residents
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/resident_v2_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "400":
          description: >
            The query parameters failed validation. Common causes:
            `filters[active]` and

            `filters[occupancy_status]` supplied together (rejected as mutually exclusive), an

            `options[sort][sort_by]` value outside the enum, or a malformed

            `filters[occupancy_status][value]` string.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: '"options.sort.sort_by" must be one of [first_name, last_name, emails,
                      property_address, lease_start, lease_end, status]'
        "401":
          $ref: "#/components/responses/401"
  /v1/residents:
    post:
      x-internal: true
      deprecated: true
      summary: Create a resident (v1, deprecated)
      tags:
        - Residents (deprecated)
      description: >
        Creates a v1 resident document. Prefer `POST /v2/residents`, which uses
        the richer

        tenant model and a structured request body. The v1 request uses the resident shape

        `first_name`, `last_name`, `emails`, and `current_property_id` (see

        `resident_create_request_model`).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/resident_create_request_model"
      responses:
        "200":
          description: Returns the created resident.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/resident_response_model"
        "400":
          description: >
            Bad request — request body failed validation. Possible messages:
              - `first_name is missing`, `last_name is missing`, `emails is missing`,
                `current_property_id is missing` (required fields)
              - `first_name can not be empty`, `last_name can not be empty`,
                `current_property_id can not be empty` (whitespace-only values)
              - `phone_numbers must be an array` (`phone_numbers` is supplied but
                isn't an array)
              - `emails has an invalid value: empty` (`emails` is supplied as a
                non-array or empty array)
              - `email has an invalid value: <value>` (an email fails format
                validation)
              - `phone number has an invalid value: <value>` (a phone number fails
                format validation)
              - `current_lease_start has an invalid value` /
                `current_lease_end has an invalid value` /
                `move_out_date has an invalid value`
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: first_name is missing
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `user not found` or `property not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - user not found
                      - property not found
                    example: property not found
  "/v2/residents/{id}":
    delete:
      summary: Delete resident
      tags:
        - Residents V2
      description: Delete selected resident by id
      parameters:
        - name: id
          in: path
          description: Resident ID
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Successful deletion of resident
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `resident not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: resident not found
    get:
      summary: Get a resident by ID
      tags:
        - Residents V2
      description: This resource allows you to get a Resident in RentCheck. Residents
        are used to provide contact information for inspections.
      parameters:
        - name: id
          in: path
          description: Resident ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested resident
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/resident_v2_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `resident not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: resident not found
    put:
      summary: Update a resident
      tags:
        - Residents V2
      description: This resource allows you to update a Resident in RentCheck.
        Residents are used to provide contact information for inspections.
      parameters:
        - name: id
          in: path
          description: Resident ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/resident_v2_update_request_model"
      responses:
        "200":
          description: Returns the updated resident
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/resident_v2_response_model"
        "400":
          description: >
            Bad request — request body failed validation. All body fields are
            optional;

            the raw message identifies the invalid field, e.g.

            `"data.emails" must contain at least 1 items` or

            `"data.emails[0]" must be a valid email`. Invalid phone numbers are rejected by a

            custom validator.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.emails[0]" must be a valid email'
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: >
            Forbidden — the caller is not a member of the requested
            `organization_id`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: invalid organization
        "404":
          description: Not Found — `resident not found` or `organization not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: resident not found
  /v3/residents:
    get:
      x-internal: true
      summary: Get all residents (v3)
      tags:
        - Residents V3
      description: >
        Returns a paginated list of resident digests. V3 returns a trimmed
        `resident_v3_response_model`

        projection (basic identity + `primary_occupancy`) and uses the `filters[...]` / `options[...]`

        nested query-param syntax. Used internally by the web and mobile apps.
      parameters:
        - in: query
          name: filters[search]
          description: Search value among tenant emails, first and last name, current and
            future lease property address.
          schema:
            type: string
        - in: query
          name: filters[active]
          description: When `true`, returns residents with a current and/or a future
            lease. When `false`, returns residents with a past lease or no lease
            information. HTTP query strings are always strings, so the value is
            compared against the literal `'true'` / `'false'`.
          schema:
            type: string
            enum:
              - "true"
              - "false"
        - in: query
          name: filters[deleted]
          description: >
            When the literal string `'true'`, deleted residents are included.
            Any

            other value (including `'false'`, missing, or anything else) is

            treated as `false`. There is no explicit enum validation, so unknown

            values are silently normalised to `false` rather than rejected as 400.
          schema:
            type: string
            example: "false"
            default: "false"
        - in: query
          name: filters[emails]
          description: Email(s) to filter residents by. Multiple values should be
            comma-separated.
          schema:
            type: string
        - in: query
          name: filters[property_id]
          description: Filters all residents for a specific property.
          schema:
            type: string
        - in: query
          name: filters[occupancy_status][value]
          description: Occupancy status(es) to filter residents by. Multiple values should
            be concatenated with a `|`. Allowed individual values are `future`,
            `past`, and `current`.
          schema:
            type: string
            pattern: ^(future|past|current)(\|(future|past|current))*$
        - in: query
          name: filters[occupancy_status][condition]
          description: Condition to apply to the occupancy_status filter. The validator
            only accepts `any_of`. Note also that `filters[active]` and
            `filters[occupancy_status]` are mutually exclusive — supplying both
            is rejected by the request validator.
          schema:
            type: string
            enum:
              - any_of
        - in: query
          name: occupancy_status[]
          deprecated: true
          schema:
            type: array
            items:
              type: string
          description: |
            Legacy multi-value occupancy status filter, still accepted. Prefer
            `filters[occupancy_status][value]`.
        - in: query
          name: options[pagination][page_size]
          description: How many items to be returned for each page.
          schema:
            type: integer
            default: 20
        - in: query
          name: options[pagination][page_number]
          description: Zero-indexed page number.
          schema:
            type: integer
            default: 0
        - in: query
          name: options[sort][sort_type]
          description: |
            Sort list in ascending or descending order. Accepted
            case-insensitively.
          schema:
            type: string
            default: ASC
            enum:
              - ASC
              - DESC
              - asc
              - desc
        - in: query
          name: options[sort][sort_by]
          description: Sort list by a specific property.
          schema:
            type: string
            default: first_name
            enum:
              - first_name
              - last_name
              - emails
              - property_address
              - lease_start
              - lease_end
              - status
      responses:
        "200":
          description: Returns all residents
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/resident_v3_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "400":
          description: >
            The query parameters failed validation. Common causes:
            `filters[active]` and

            `filters[occupancy_status]` supplied together (rejected as mutually exclusive), an

            `options[sort][sort_by]` value outside the enum, or a malformed

            `filters[occupancy_status][value]` string.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 400
                  error:
                    type: string
                    example: '"options.sort.sort_by" must be one of [first_name, last_name, emails,
                      property_address, lease_start, lease_end, status]'
        "401":
          $ref: "#/components/responses/401"
  "/v1/residents/{residentId}":
    get:
      x-internal: true
      deprecated: true
      summary: Get a resident by ID (v1, deprecated)
      tags:
        - Residents (deprecated)
      description: >
        Returns a single v1 resident by id. Prefer the v2 (`/v2/residents/{id}`)
        and v3

        (`/v3/residents`) endpoints, which return the richer tenant + occupancies model.
      parameters:
        - name: residentId
          in: path
          description: Resident ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested resident.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/resident_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `resident not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: resident not found
    put:
      x-internal: true
      deprecated: true
      summary: Update a resident (v1, deprecated)
      tags:
        - Residents (deprecated)
      description: >
        Updates a v1 resident document. Prefer `PUT /v2/residents/{id}` for new
        integrations.
      parameters:
        - name: residentId
          in: path
          description: Resident ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/resident_update_request_model"
      responses:
        "200":
          description: Returns the updated resident.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/resident_response_model"
        "400":
          description: >
            Bad request — request body failed validation. Possible messages:
              - `emails has an invalid value: empty` (empty or non-array `emails`)
              - `email has an invalid value: <value>` (an email fails format validation)
              - `phone_numbers must be an array`
              - `phone number has an invalid value: <value>` (a phone number fails format validation)
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: "emails has an invalid value: empty"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `resident not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: resident not found
  /v1/sales/rev-share-prospects:
    post:
      x-internal: true
      security: []
      summary: Create a rev-share prospect
      tags:
        - Account Settings
      description: >
        Onboards a new revenue-share prospect: provisions a RentCheck user with
        a

        temporary password for the supplied email, attaches the requested Chargebee

        plan + addons to the user's `prospect_data`, persists the self-reported

        portfolio size, and triggers the rev-share-prospect welcome email.


        Reachable without an authenticated session; the endpoint is invoked by the

        public RentCheck marketing flow.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/rev_share_prospect_create_request_model"
      responses:
        "200":
          description: Returns the prospect's RentCheck user profile after enrollment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: object
                    description: The RentCheck user profile for the newly-created prospect, with the
                      rev-share `prospect_data` block populated.
                    additionalProperties: true
        "400":
          description: |
            Bad request — request body validation failed, or the supplied
            Chargebee plan / addons could not be resolved against the live
            Chargebee catalog. Examples:
              - `"prospect_email" is required`
              - `"prospect_email" must be a valid email`
              - `"total_properties" is required`
              - `"chargebee_plan_id" is required`
              - `"chargebee_addon_ids" is required`
              - `Can't find selected plan`
              - `Some of the addons couldn't be found, found these [...]`
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"prospect_email" must be a valid email'
        "409":
          description: |
            Conflict — a RentCheck user already exists for the supplied
            `prospect_email`. Example: `user already exists`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 409
                  error:
                    type: string
                    example: user already exists
  "/v1/inspections/{inspectionId}/signature-requests":
    patch:
      x-internal: true
      summary: Accept all open signature requests for an inspection
      tags:
        - Inspections
      description: >
        Marks every open signature request on this inspection as accepted by the

        authenticated recipient. Used when a single user has multiple pending requests

        against the same inspection (e.g. additional-signature loops).
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the list of (now accepted) signature requests.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/api_inspection_request"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Total number of accepted signature requests.
        "401":
          $ref: "#/components/responses/401"
    post:
      x-internal: true
      summary: Create signature request(s) for an inspection
      tags:
        - Inspections
      description: >
        Creates one signature request per email in `emails`. Each request is
        dispatched

        via email and tracked individually in the inspection timeline. The response is the

        list of created requests.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/signature_request_create_request_model"
      responses:
        "200":
          description: Returns the list of created signature requests.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/api_inspection_request"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "400":
          description: >
            Bad request — request body validation failed. When `emails` is empty
            the

            response is `"data.emails" must contain at least 1 items`; when it is omitted

            the response is `"data.emails" is required`. Emails are not format-validated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.emails" must contain at least 1 items'
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `inspection not found` or `user not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection not found
  "/v1/inspections/{inspectionId}/signature-requests/{requestId}/claim":
    post:
      x-internal: true
      summary: Claim a signature request from a deep link
      tags:
        - Inspections
      description: >
        Claims a pending additional-signature request from a Branch deep link,

        reassigning `recipient_email` to the authenticated caller. The linked

        request and every other pending additional-signature request addressed to

        the original recipient's email are reassigned to the caller.


        Possession of the high-entropy request id is treated as the capability to

        claim the request (the deep link acts as a bearer token), so — unlike the

        GET endpoint — this endpoint does not require the caller's email to match

        the request's recipient. Returns the affected requests so the apps can

        surface the newly-claimed inspections.
      parameters:
        - name: inspectionId
          in: path
          description: |
            Inspection ID. Pass the literal `*`; the deep link only carries the
            request id and the inspection is resolved from the request
            itself.
          required: true
          schema:
            type: string
        - name: requestId
          in: path
          description: Signature request ID from the deep link.
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The requests reassigned to the caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/api_inspection_request"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Total number of requests reassigned to the caller.
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `inspection signature request not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection signature request not found
  "/v1/inspections/{inspectionId}/signature-requests/{requestId}":
    get:
      x-internal: true
      summary: Get a signature request by ID
      tags:
        - Inspections
      description: >
        Returns a single signature request attached to an inspection. Used by
        the web/mobile

        apps to render the signature-request detail view.
      parameters:
        - name: inspectionId
          in: path
          description: |
            Inspection ID. Pass the literal `*` to look the signature request
            up across all inspections the caller can access.
          required: true
          schema:
            type: string
        - name: requestId
          in: path
          description: Signature request ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested signature request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_inspection_request"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `inspection signature request not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection signature request not found
  "/v1/inspections/{inspectionId}/signature-requests/sign":
    post:
      x-internal: true
      summary: Complete a signature request by submitting a signature
      tags:
        - Inspections
      description: >
        Completes the signature request that the authenticated recipient
        currently has

        open against this inspection. Either `signature_base64` (drawn signature image) or

        `signature_text` (typed signature) must be supplied. Returns the updated list of

        signature requests on the inspection.
      parameters:
        - name: inspectionId
          in: path
          description: Inspection ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/signature_request_complete_request_model"
      responses:
        "200":
          description: Returns the updated signature requests for the inspection.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/api_inspection_request"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "400":
          description: |
            Bad request — request body failed validation. Examples:
              - `either signature_base64 or signature_text is required` (neither
                field supplied)
              - `"data.platform" is required`
              - `"data.platform" must be one of [WEB, MOBILE]`
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: either signature_base64 or signature_text is required
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `inspection not found` or `inspection signature not
            found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: inspection signature not found
  /v1/subscriptions/trial:
    post:
      x-internal: true
      summary: Create a free trial subscription
      tags:
        - Subscriptions
      description: >
        Starts a free trial subscription for the caller. Reads the `x-app-id`
        header to determine which

        Chargebee site / app the trial should be created in.
      parameters:
        - in: header
          name: x-app-id
          required: true
          schema:
            type: string
          description: >
            App id identifying which Chargebee site the trial should be created
            in.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/subscription_create_trial_request_model"
      responses:
        "200":
          description: Returns the newly-created trial subscription.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/subscription_get_by_user_response_model"
        "400":
          description: |
            Bad request — request body validation rejected the body. The
            `x-app-id` header is also required, so a missing header surfaces
            here rather than as a 401.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.user_name" is required'
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — the authenticated token's user id does not
            resolve to a profile.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - user not found
                    example: user not found
  /v1/subscriptions/me:
    get:
      x-internal: true
      summary: Get subscription
      tags:
        - Subscriptions
      description: >
        Returns the subscription associated with the authenticated user. Used by
        the web and mobile apps

        to render plan / billing information for the logged-in user.
      responses:
        "200":
          description: Returns the user's subscription.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/subscription_get_by_user_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — the authenticated user has no subscription record.
            No `user not found` 404 is raised on this path (a
            missing profile flows through as an empty subscription lookup).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - subscription not found
                    example: subscription not found
  "/v1/subscriptions/invoices/{invoiceId}/get-download-url":
    get:
      x-internal: true
      summary: Get a download URL for an invoice
      tags:
        - Subscriptions
      description: >
        Returns a short-lived URL that can be used to download the PDF for the
        supplied Chargebee invoice.

        The invoice must belong to the caller's subscription.
      parameters:
        - name: invoiceId
          in: path
          description: Chargebee invoice id.
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the download URL for the invoice PDF.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: string
                    description: Pre-signed download URL for the invoice PDF.
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `subscription not found` or `invoice not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - subscription not found
                      - invoice not found
                    example: invoice not found
  /v1/subscriptions/invoices:
    get:
      x-internal: true
      summary: Get invoices for the caller's subscription
      tags:
        - Subscriptions
      description: >
        Lists Chargebee invoices associated with the caller's subscription.
        Returns the curated subset of

        Chargebee invoice fields surfaced by the web app; refer to Chargebee's API docs for the full set.
      responses:
        "200":
          description: Returns the list of invoices for the caller's subscription.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/subscription_invoice_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `subscription not found` for the caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: subscription not found
  /v1/subscriptions/me/requests:
    post:
      x-internal: true
      summary: Request a new plan / trial for the caller's subscription
      tags:
        - Subscriptions
      description: >
        Submits an internal request for sales / support to switch the caller's
        subscription to the

        supplied Chargebee plan. Optional `note` is forwarded to the team that fulfills the request.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/subscription_create_request_request_model"
      responses:
        "200":
          description: The request has been submitted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: "null"
                    description: |
                      Always `null` — the response returns no data on success.
        "400":
          description: Bad request — request body validation rejected the body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.plan_id" is required'
        "401":
          description: >
            Unauthorized — a standard auth-token failure, or the caller's

            subscription state disallows the request (`unauthorized` message) when:

            - the caller already has permission to edit the plan directly, or

            - the caller requested the plan they are already on and the
              subscription is active or in trial.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 401
                  error:
                    type: string
                    enum:
                      - token is not valid
                      - authorization header is empty
                      - authorization header is not valid
                      - "format for Authorization must be: Bearer [token]"
                      - refresh token is not valid for resources
                      - not well-formed token
                      - unauthorized
                    example: unauthorized
        "404":
          description: >
            Not Found — one of the resources needed to fulfil the request could

            not be located:

            - `subscription not found` — the caller has no subscription attached.

            - `plan not found` — the supplied `plan_id` does not resolve in Chargebee.

            - `permission group not found` — no permission group on the subscription
              grants `allow_billing_plan_editing`, so there is no billing admin to notify.
            - `admin users not found` — the billing admin permission group exists
              but has no members.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - subscription not found
                      - plan not found
                      - permission group not found
                      - admin users not found
                    example: subscription not found
  /v1/subscriptions:
    put:
      x-internal: true
      summary: Update the caller's subscription
      tags:
        - Subscriptions
      description: >
        Updates the Chargebee plan and/or addons on the caller's subscription.
        Supply only the keys you

        want to change; both `plan` and `addons` are optional. Returns the updated subscription.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/subscription_update_request_model"
      responses:
        "200":
          description: Returns the updated subscription.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/subscription_get_by_user_response_model"
        "400":
          description: Bad request — Chargebee rejected the update (invalid plan id, addon
            id, etc.).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `subscription not found` for the caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: subscription not found
  /v1/teams/invites/accept:
    post:
      summary: Accept all team invites
      tags:
        - Teams
      description: This resource accepts all team invitations for the user that calls it.
      responses:
        "200":
          description: Returns all invites that were accepted by user.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/team_invite_accept_all_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "401":
          $ref: "#/components/responses/401"
  "/v1/teams/{teamId}/invites":
    post:
      summary: Create team invite
      tags:
        - Teams
      description: Invite a user to team
      parameters:
        - name: teamId
          in: path
          description: Team ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/team_invite_create_request_model"
      responses:
        "200":
          description: Returns the updated team info along two lists of users that were
            and weren't invited
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/team_invite_create_response_model"
        "400":
          description: |
            Bad request — request body validation failed, or the target
            email resolves to a `System` account. Examples:
              - `"data.emails" is required`
              - `"data.emails" must be an array`
              - `user type has an invalid value: System`
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.emails" is required'
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: |
            Forbidden — the caller is not permitted to invite for this team, or
            the underlying subscription is missing/inactive.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    enum:
                      - user must be an admin
                      - user must be a member of the team
                      - subscription not found or inactive
                    example: user must be an admin
        "404":
          description: >
            Not Found — the caller or the target team could not be resolved.

            `invite not found` is returned when the caller's user record is

            missing; `team not found` is returned when the target team is missing.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - team not found
                      - invite not found
                    example: team not found
        "409":
          description: |
            Conflict — the caller has more than one active subscription and the
            business rules require exactly one.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 409
                  error:
                    type: string
                    enum:
                      - too many subscriptions
                    example: too many subscriptions
    get:
      summary: Get all team invites
      tags:
        - Teams
      description: This resource retrieves information about team invites that have
        yet not been accepted.
      parameters:
        - name: teamId
          in: path
          description: Team ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns all invites for a specific team
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/team_invite_get_all_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "401":
          $ref: "#/components/responses/401"
  "/v1/users/{userId}/invites":
    delete:
      x-internal: true
      summary: Delete all open invites of a user
      tags:
        - Teams
      description: >
        Cancels every outstanding (not yet accepted) team invite addressed to
        the target user. Used by

        admin / cleanup flows that need to revoke a user's pending invitations across all teams in one

        call.
      parameters:
        - name: userId
          in: path
          description: ID of the user whose pending invites should be cancelled.
          required: true
          schema:
            type: string
      responses:
        "204":
          description: All outstanding invites for the user have been cancelled.
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: |
            Forbidden — the target user is already a member of a team, so their
            outstanding invites cannot be cancelled through this endpoint.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    enum:
                      - user is already part of a team
                    example: user is already part of a team
        "404":
          description: >
            Not Found — the target user has no cancellable invites, or one of
            the

            invites references a team that no longer exists. `invite not found`

            is returned both for the top-level "no invites for this user" case

            and for any per-invite failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - invite not found
                    example: invite not found
  "/v1/teams/{teamId}/invites/{inviteId}":
    delete:
      summary: Delete team invite
      tags:
        - Teams
      description: Delete invite sent to user.
      parameters:
        - name: teamId
          in: path
          description: Team ID
          required: true
          schema:
            type: string
        - name: inviteId
          in: path
          description: Invite ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the updated team info
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/team_get_by_id_response_model"
        "400":
          description: |
            Bad request — the invite is not in `sent` state, so it cannot be
            removed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: "status has an invalid value: accepted"
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: |
            Forbidden — the caller is not an admin of the team.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    enum:
                      - invalid user role
                    example: invalid user role
        "404":
          description: |
            Not Found — the invite could not be resolved for the caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - invite not found
                    example: invite not found
    put:
      summary: Update team invite
      tags:
        - Teams
      description: Update a team invite that hasn't been accepted yet.
      parameters:
        - name: teamId
          in: path
          description: Team ID
          required: true
          schema:
            type: string
        - name: inviteId
          in: path
          description: Invite ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/team_invite_update_request_model"
      responses:
        "200":
          description: Returns the updated team invite.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/team_invite_update_response_model"
        "400":
          description: |
            Bad request — the invite is not in `sent` state, so it cannot be
            updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: "status has an invalid value: accepted"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — the team or invite could not be resolved for the caller.
            `team not found` is returned when the team cannot be resolved and
            `invite not found` when the invite cannot be resolved.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - team not found
                      - invite not found
                    example: invite not found
  "/v1/teams/{teamId}/invites/{inviteId}/reminders":
    post:
      summary: Team invite reminder
      tags:
        - Teams
      description: Sends an email to users that have not yet accepted specified team invite.
      parameters:
        - name: teamId
          in: path
          description: Team ID
          required: true
          schema:
            type: string
        - name: inviteId
          in: path
          description: Invite ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the invite that was called.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/team_invite_reminder_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — `team not found` is returned when the team cannot be
            resolved for the caller, and `invite not found` is returned when the
            invite id does not resolve, or when it resolves to a different team.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - team not found
                      - invite not found
                    example: invite not found
  "/v1/users/{userId}/invites/reminders":
    post:
      x-internal: true
      summary: Send reminders for all open invites of a user
      tags:
        - Teams
      description: >
        Re-sends invite reminders for every outstanding (not yet accepted) team
        invite addressed to the

        target user. Used by admin flows that want to nudge a single invitee about all the teams they

        have pending invitations for.
      parameters:
        - name: userId
          in: path
          description: ID of the user whose pending invites should be reminded.
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the list of team invites that were reminded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/team_invite_reminder_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — no team invites addressed to the target user could be
            reminded. `invite not found` is returned when the target user has no
            open invites, and any invite that has already been claimed/expired
            surfaces the same error. `team not found` can also occur when the
            invite's referenced team was deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - invite not found
                      - team not found
                    example: invite not found
  "/v1/teams/{teamId}/members/{memberId}":
    delete:
      summary: Delete team member
      tags:
        - Teams
      description: Delete member/user from the team.
      parameters:
        - name: teamId
          in: path
          description: Team ID
          required: true
          schema:
            type: string
        - name: memberId
          in: path
          description: Member ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the updated team info
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/team_get_by_id_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — the requested team or member could not be resolved for
            the caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - team not found
                      - member not found
                    example: member not found
    put:
      summary: Update team member
      tags:
        - Teams
      description: Update team member information
      parameters:
        - name: teamId
          in: path
          description: Team ID
          required: true
          schema:
            type: string
        - name: memberId
          in: path
          description: Member ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/team_member_update_request_model"
      responses:
        "200":
          description: Returns the updated team info
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/team_get_by_id_response_model"
        "400":
          description: Bad request — the supplied role / body was rejected by validation.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.role" must be one of [admin, manager, none]'
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `team not found` or `member not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - team not found
                      - member not found
                    example: team not found
  /v1/teams:
    post:
      summary: Create a team
      tags:
        - Teams
      description: Create a team
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/team_create_request_model"
      responses:
        "200":
          description: Returns the updated team info
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/team_get_by_id_response_model"
        "400":
          description: Bad request — request body validation failed (for example, `name`
            or `internal_label` missing or empty), or the caller is in a
            subscription state that does not allow team creation.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - '"data.name" is required'
                      - '"data.internal_label" is required'
                      - '"data.name" is not allowed to be empty'
                      - '"data.internal_label" is not allowed to be empty'
                      - there is no subscription for this user
                      - user has more than one active subscription
                    example: '"data.name" is required'
        "401":
          $ref: "#/components/responses/401"
    get:
      summary: Get all teams
      tags:
        - Teams
      description: >
        This resource allows you to retrieve information about Team entities.
        Teams are a way to organize groups of users and the properties they
        manage.


        By default the response excludes the caller's "All teams" pseudo-team. Pass `filters[include_all_teams]=true` to include it.
      parameters:
        - in: query
          name: filters[include_all_teams]
          schema:
            type: string
            enum:
              - "true"
              - "false"
            default: "false"
          description: When `true`, include the caller's "All teams" pseudo-team in the
            result.
      responses:
        "200":
          description: Returns all teams
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/team_get_all_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Total amount of teams matching the request (irrespective of
                      paging).
        "401":
          $ref: "#/components/responses/401"
  "/v1/teams/{teamId}":
    delete:
      x-internal: true
      summary: Delete a team
      tags:
        - Teams
      description: >
        Hard-deletes a team. The caller must be an admin of the team. The team
        cannot be deleted if any

        of the following apply: it is the only team on the subscription, it still contains other

        members, it still has properties associated with it, it is the only team referenced by an

        inspection template, or it is currently bound to a Latchel/Rentvine/AppFolio/RentManager

        integration mapping.
      parameters:
        - name: teamId
          in: path
          description: Team ID
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Team successfully deleted.
        "400":
          description: >
            Bad request — examples:
              - `Only admin users can delete teams.`
              - `You cannot delete '<name>' because every subscription requires at least one team. ...`
              - `Cannot delete team. Remove all other team members before deleting.`
              - `This team contains <N> properties that need to be moved before deletion. ...`
              - `This team cannot be deleted because it's currently the only team that has access to the following inspection template(s): <names>. ...`
              - `This team is linked to the Latchel integration. Update the integration's team, then try again.`
              - `This team is linked to the <Rentvine|AppFolio|RentManager> integration. Update the integration's team assignment, then try again.`
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `team not found` or `subscription not found` (the team
            is not in any of the caller's subscriptions).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: team not found
  "/v1/teams/{id}":
    get:
      summary: Get a team by ID
      tags:
        - Teams
      description: Get a team by ID
      parameters:
        - name: id
          in: path
          description: Team ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested team
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/team_get_by_id_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — the team does not exist.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: team not found
    put:
      summary: Update team information
      tags:
        - Teams
      description: Update team information
      parameters:
        - name: id
          in: path
          description: Team ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/team_update_request_model"
      responses:
        "200":
          description: Returns the updated team info
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/team_get_by_id_response_model"
        "400":
          description: |
            Bad request — request body validation failed. All string fields are
            optional but must be strings when supplied. Example:
            `"data.name" must be a string`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.name" must be a string'
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — the requested team could not be resolved for the caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - team not found
                    example: team not found
  /v1/latchel/integrations:
    get:
      x-internal: true
      summary: Get all Latchel integrations
      tags:
        - Teams
      description: This resource allows you to retrieve information about Latchel
        entities for the user that called the endpoint.
      responses:
        "200":
          description: Returns all Latchel integrations
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/team_integration_latchel_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "401":
          $ref: "#/components/responses/401"
    post:
      x-internal: true
      summary: Integrate Latchel to RentCheck
      tags:
        - Teams
      description: This resource allows you to save your Latchel's credentials into a Team.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/team_integration_latchel_request_model"
      responses:
        "201":
          description: Successful integration.
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    enum:
                      - team_ids is missing
                      - api_key is missing
                      - invalid user_id
                      - invalid team_ids
                      - invalid api_key
                      - user doesn't have permission to all team_ids
                    example: api_key is missing
        "401":
          $ref: "#/components/responses/401"
    delete:
      x-internal: true
      summary: Delete Latchel integrations
      tags:
        - Teams
      description: >
        Removes a Latchel API key from one or more teams. The integration is
        identified by `api_key`

        and `team_ids` are the specific teams the integration should be removed from. Returns the

        teams the Latchel integration was removed from.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/latchel_integration_delete_request_model"
      responses:
        "200":
          description: Returns the teams the Latchel integration was removed from.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/latchel_integration_delete_response_model"
        "400":
          description: Bad request — request body validation failed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"data.team_ids" is required'
        "401":
          $ref: "#/components/responses/401"
  /v1/units:
    post:
      summary: Create a unit
      tags:
        - Units
      description: This resource allows you to create a Unit. Units are single
        properties that can be occupied by one or more residents.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/unit_create_request_model"
      responses:
        "200":
          description: Returns the created unit
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/unit_response_model"
        "400":
          description: >
            Bad request — request body validation failed, or one of the
            post-validation

            checks rejected the create. Examples:
              - `"address" is required`
              - `"city" is required`
              - `"zip_code" is required`
              - `"bedrooms" is required`
              - `"full_bathrooms" is required`
              - `"property_type" is required`
              - `"property_type" contains an invalid value` — the four disallowed
                values `Building`, `Community`, `building`, `community` are
                explicitly rejected.
              - `team_id is missing` — surfaces when the caller has at least one
                active subscription but did not supply a `team_id`.
              - `team_id has an invalid value` — the supplied `team_id` is not
                one of the teams the caller can access.
              - `"bedrooms" must be less than or equal to 10` /
                `"full_bathrooms" must be less than or equal to 10` /
                `"half_bathrooms" must be less than or equal to 10` — an upper
                bound of `10` is enforced on each bathroom/bedroom count.
              - `max units reached: 1/1` — free-tier callers are limited to one
                unit; the error string carries the caller's current unit count in
                the `&lt;current&gt;/&lt;max&gt;` prefix, so the smallest reachable
                value is `1/1`.
              - `"value" contains a conflict between optional exclusive peers [rooms, rooms_info]` —
                supplying both `rooms` and `rooms_info` at the same time is
                rejected.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: '"address" is required'
        "401":
          $ref: "#/components/responses/401"
    delete:
      summary: Delete units
      tags:
        - Units
      description: This resource allows you to delete multiple units at once.
      parameters:
        - in: query
          name: id[]
          required: true
          style: form
          explode: true
          schema:
            type: array
            minItems: 1
            items:
              type: string
          description: |
            The ids of the units to be deleted. Repeat the parameter to send
            multiple values (e.g. `?id[]=abc&id[]=def`). The query string must
            contain `id[]`, and a missing array
            surfaces as a 500. Individual entries are not validated; ids the
            caller cannot access are silently skipped.
      responses:
        "200":
          description: Returns the count of deleted units.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: object
                    required:
                      - totalRemoved
                    properties:
                      totalRemoved:
                        type: integer
                        description: Number of units that were removed.
                        example: 1
        "401":
          $ref: "#/components/responses/401"
        "403":
          $ref: "#/components/responses/synced_property_delete_forbidden"
  "/v1/units/{unitId}":
    get:
      summary: Get a unit by ID
      tags:
        - Units
      description: Get a unit by ID
      parameters:
        - name: unitId
          in: path
          description: Unit ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested unit
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/unit_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `unit not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: unit not found
    put:
      summary: Update a unit
      tags:
        - Units
      description: Update parameters of a unit
      parameters:
        - name: unitId
          in: path
          description: Unit ID
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/unit_update_request_model"
      responses:
        "200":
          description: Returns the updated unit
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/unit_response_model"
        "400":
          description: |
            Bad request — request body validation failed or the supplied
            `team_id` is not one the caller can access. The same constraints as
            on create apply on update — every key is optional but when supplied
            it is validated the same way:
              - `"bedrooms" must be less than or equal to 10` /
                `"full_bathrooms" must be less than or equal to 10` /
                `"half_bathrooms" must be less than or equal to 10`
              - `"property_type" contains an invalid value` — the four
                disallowed values are `Building`, `Community`, `building`,
                `community`.
              - `"value" contains a conflict between optional exclusive peers
                [rooms, rooms_info]` — `rooms` and `rooms_info` are mutually
                exclusive.
              - `team_id has an invalid value` — the supplied `team_id` is not
                one of the teams the caller can access.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: team_id has an invalid value
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `unit not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: unit not found
  /v1/users/permission-groups:
    put:
      x-internal: true
      summary: Bulk-edit users' permission groups
      tags:
        - Users
      description: >
        Reconciles the supplied users' permission group memberships against the
        supplied permission

        group ids — each user is set to belong to exactly the requested permission groups (and removed

        from any others on the same subscription). Returns the updated user digests.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/user_bulk_edit_permission_groups_request_model"
      responses:
        "200":
          description: Returns the updated user digests.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/api_user_digest"
        "400":
          description: Bad request — request body validation failed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
  /v1/users:
    post:
      x-internal: true
      summary: Create a user account
      tags:
        - Users
      security:
        - x-app-id: []
          x-app-secret: []
      description: >
        Creates a new user account. Used by the sign-up flow. The endpoint does
        not require an

        authenticated session — the caller authenticates the resulting user separately. If the supplied

        email is malformed or looks like a typo the response will return an `email_suggestion` block

        instead of creating the account.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/user_create_request_model"
      responses:
        "200":
          description: User successfully created (or email suggestion returned).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/user_create_response_model"
        "400":
          description: >
            Bad request — request body validation failed (missing required
            field, invalid `user_type`,

            email already in use, password does not meet complexity requirements, etc.).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
    get:
      summary: Get all users
      tags:
        - Users
      description: Get all users
      parameters:
        - in: query
          name: filters[search]
          description: Search by user name, email or id.
          schema:
            type: string
        - in: query
          name: filters[team_ids]
          description: Filter by team ids. It accept multiple ids separated by comma.
          schema:
            type: string
        - in: query
          name: filters[permission_group_ids]
          description: Filter by permission group ids. It accept multiple ids separated by
            comma
          schema:
            type: string
        - in: query
          name: filters[status]
          description: Filter by user status.
          schema:
            type: string
            enum:
              - Active
              - Invited
        - in: query
          name: options[pagination][page_size]
          description: How many items to be returned for each page.
          schema:
            type: integer
            default: 20
        - in: query
          name: options[pagination][page_number]
          description: Zero-indexed page number.
          schema:
            type: integer
            default: 0
        - in: query
          name: options[sort][sort_type]
          description: Sort list in ascending or descending order.
          schema:
            type: string
            default: ASC
            enum:
              - ASC
              - DESC
        - in: query
          name: options[sort][sort_by]
          description: Sort list by a specific property.
          schema:
            type: string
            default: user_name
            enum:
              - user_name
              - email
      responses:
        "200":
          description: Returns all users
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/api_user_digest"
                  count:
                    type: integer
                    description: Amount of elements in `data` on the current page.
                  total_results:
                    type: integer
                    description: Total amount of users matching the filter criteria (irrespective of
                      paging).
        "401":
          $ref: "#/components/responses/401"
  "/v1/users/{userId}":
    delete:
      x-internal: true
      summary: Deactivate a user
      tags:
        - Users
      description: >
        Deactivates the target user — removing them from every team and
        permission group on the caller's

        subscription, deleting their pending team invites, and unassigning them from automations. The

        caller must (a) share an active subscription with the target user and (b) be an admin of at

        least one team on that subscription.
      parameters:
        - name: userId
          in: path
          description: ID of the user to deactivate.
          required: true
          schema:
            type: string
      responses:
        "204":
          description: User successfully deactivated.
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: Forbidden — `user needs to be an admin` (the caller is not an admin
            on any team in the shared subscription).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: user needs to be an admin
        "404":
          description: >
            Not Found — `user not found` (the caller or target profile does not
            exist) or `subscription not found`

            (the caller and target do not share a single active subscription).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: user not found
  "/v1/users/{id}":
    get:
      x-internal: true
      summary: Get a user by ID
      tags:
        - Users
      description: Get a user by ID
      parameters:
        - name: id
          in: path
          description: User ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested user
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/user_get_by_id_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — the user does not exist.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: user not found
  /v1/users/invite:
    post:
      x-internal: true
      summary: Invite users
      tags:
        - Users
      description: >
        Invites one or more users by email, attaching them to the supplied teams
        and permission groups

        with the given role. Each email is processed independently — the response groups successfully

        created user digests under `success.users` and per-email failure reasons under `failure.errors`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/user_invite_request_model"
      responses:
        "200":
          description: Per-email invite results.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/user_invite_response_model"
        "400":
          description: Bad request — request body validation failed (missing required
            field, invalid role, etc.).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
  "/v1/users/{userId}/access":
    put:
      x-internal: true
      summary: Update a user's team and permission group access
      tags:
        - Users
      description: >
        Reconciles the target user's team memberships and permission groups
        against the supplied lists,

        and updates the role they hold in each team. The caller must share a subscription with the

        target user and be an admin on the relevant teams.
      parameters:
        - name: userId
          in: path
          description: ID of the user whose access is being updated.
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/user_update_access_request_model"
      responses:
        "200":
          description: Returns the updated user digest.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/api_user_digest"
        "400":
          description: Bad request — request body validation failed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: Not Found — `user not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: user not found
  "/v1/work_orders/providers/{provider}":
    post:
      x-internal: true
      summary: Create a work order
      tags:
        - Work Orders
      description: |
        Creates a Work Order in RentCheck and dispatches it to the requested
        integration provider. The path parameter `provider` selects the
        destination integration; the request body shape varies per provider.
      parameters:
        - in: path
          name: provider
          required: true
          schema:
            type: string
            enum:
              - appfolio
              - jenark
              - latchel
              - rentmanager
              - rentvine
          description: |
            The integration provider that should fulfill this work order.
        - in: header
          name: x-app-id
          required: false
          description: |
            Creation-source identifier. Persisted as `creation_source` on each
            created work order.
          schema:
            type: string
      requestBody:
        required: true
        description: |
          Request body shape varies per `{provider}`. The provider in the path
          determines which validation runs against the body:
            * `latchel`, `rentmanager`, `jenark` — the body is validated against
              the provider-specific schema.
            * `appfolio` / `rentvine` — the body is forwarded to the integration
              provider without additional validation.
        content:
          application/json:
            schema:
              anyOf:
                - $ref: "#/components/schemas/work_order_create_latchel_request_model"
                - $ref: "#/components/schemas/work_order_create_rent_manager_request_model"
                - $ref: "#/components/schemas/work_order_create_jenark_request_model"
                - $ref: "#/components/schemas/work_order_create_appfolio_request_model"
                - $ref: "#/components/schemas/work_order_create_rentvine_request_model"
      responses:
        "200":
          description: Returns the created work order
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/work_order_response_model"
        "400":
          description: >
            Bad request — request body validation failed, or the supplied

            features span more than one inspection. Validation messages are raw, e.g.:
              - `"data.work_order_name" is required`
              - `"data.work_order_description" is required`
              - `"data.maintenance_flags" must contain at least 1 items`
              - `"data.maintenance_flags" must be an array`
              - `"data.latchel_property_id" is required` (`latchel`)
              - `"data.ok_to_enter" is required` (`rentmanager`)
              - `"data.priority" must be one of [Low, Normal, High]` (`rentmanager`)
              - `"data.work_order_code" is required` (`jenark`)
            The cross-inspection guard returns

            `Inspection must be consistent. All features should belong to the same inspection.`

            The Rent Manager integration also surfaces

            `property not synced with integration` and

            `No rentmanager integration is configured for this account` as 400

            when its preflight checks fail.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
        "401":
          description: >
            Unauthorized — standard auth failures, plus the per-work-order
            permission check

            (`User does not have permissions to create work order` is emitted as 401,

            not 400).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 401
                  error:
                    type: string
                    example: User does not have permissions to create work order
        "404":
          description: >
            Not Found — `subscription not found` (caller has no subscription),

            `appfolio integration not found`, `rentvine integration not found`,

            `latchel integration not found` (the destination integration is not

            configured for the caller's subscription), `resident not found (<id>)`

            (the supplied `resident_id` does not exist), or `property not found`

            (the property associated with the flagged features is not synced to

            the requested integration).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    description: |
                      One of the static integration / subscription / property
                      not-found strings, OR `resident not found (<id>)` carrying
                      the supplied resident id.
                    oneOf:
                      - enum:
                          - subscription not found
                          - appfolio integration not found
                          - rentvine integration not found
                          - latchel integration not found
                          - property not found
                      - pattern: ^resident not found \(.+\)$
                    example: appfolio integration not found
        "500":
          description: >
            Internal server error. Sources:
              - Unknown `provider` — `invalid provider` is returned when the path
                parameter is not one of the registered providers. The
                path-parameter `enum` documents the supported set; unsupported
                providers should be rejected before processing.
              - Jenark — `property not synced with Jenark` is returned when the
                property is not synced through the Jenark integration. (The
                analogous Rent Manager check surfaces as 400 — see the 400
                response above.)
              - AppFolio / Rentvine — `Must have work order sync switched ON in
                order to create work orders. Please adjust your data sync
                settings.` is returned when the integration exists but work-order
                sync is disabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 500
                  error:
                    type: string
                    enum:
                      - invalid provider
                      - property not synced with Jenark
                      - Must have work order sync switched ON in order to create
                        work orders. Please adjust your data sync settings.
                    example: invalid provider
  /v2/work_orders:
    get:
      x-internal: true
      summary: Get all work orders
      tags:
        - Work Orders V2
      description: Get all work orders
      parameters:
        - in: query
          name: page_number
          schema:
            type: integer
            default: 0
          description: The page number of the collection. Zero-indexed.
        - in: query
          name: page_size
          schema:
            type: integer
            default: 20
          description: The number of elements in the data collection.
        - in: query
          name: sort_by
          schema:
            type: string
            default: created_date
            enum:
              - created_date
              - property_address
              - property_address_2
              - property_city
              - property_region
              - property_zip_code
              - inspection_type
              - inspection_template
              - name
              - status
          description: Sort results by one of the available fields.
        - in: query
          name: sort_type
          schema:
            type: string
            default: DESC
            pattern: ^(?:[Aa][Ss][Cc]|[Dd][Ee][Ss][Cc])$
            example: DESC
          description: |
            Sort direction. Accepted case-insensitively — any casing
            of `asc` / `desc` is silently normalised to `ASC` / `DESC`.
        - in: query
          name: search
          schema:
            type: string
          description: Search string to match in several work order's fields
        - in: query
          name: property_id
          schema:
            type: string
          description: Property ID to filter work orders by. Only work orders whose source
            inspection lives on this property are returned.
        - in: query
          name: inspection_type[]
          style: form
          explode: true
          schema:
            type: array
            items:
              type: string
          description: >
            List of inspection types to filter work orders. Repeat the parameter

            to send multiple values (e.g. `?inspection_type[]=Move-In&inspection_type[]=Move-Out`).
        - in: query
          name: status[]
          style: form
          explode: true
          schema:
            type: array
            items:
              type: string
              enum:
                - Created
                - In Progress
                - Completed
                - Canceled
          description: |
            List of work order statuses to filter by. Repeat the parameter to
            send multiple values. Allowed individual values are `Created`,
            `In Progress`, `Completed`, and `Canceled`. The raw array is not
            validated, so unknown values are silently ignored — they simply
            return zero matches instead of a 400.
        - in: query
          name: start_created_date
          schema:
            type: string
          description: Value to filter work order creation date by a starting date
        - in: query
          name: end_created_date
          schema:
            type: string
          description: Value to filter work order creation date by an ending date
      responses:
        "200":
          description: Returns all work orders
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/work_order_response_model"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
                  total_results:
                    type: integer
                    description: Amount of total elements according to search criteria.
        "401":
          $ref: "#/components/responses/401"
  "/v1/work_orders/{workOrderId}":
    get:
      x-internal: true
      summary: Get a work order by ID
      tags:
        - Work Orders
      description: Get a work order by ID
      parameters:
        - name: workOrderId
          in: path
          description: work order ID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Returns the requested work order
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/work_order_response_model"
        "401":
          description: >
            Unauthorized — standard auth failures, plus the per-work-order
            permission check.

            Returns HTTP 401 with message `User does not have permissions to get work order` when

            the caller's role doesn't allow read access.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 401
                  error:
                    type: string
                    example: User does not have permissions to get work order
        "404":
          description: Not Found — `work order not found`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: work order not found
  /v1/work_orders/skills:
    get:
      x-internal: true
      summary: Get all work order skills
      tags:
        - Work Orders
      description: >
        Returns the list of skills that can be selected when creating a work
        order. The

        same static list is always returned, regardless of the caller's enabled integrations.
      responses:
        "200":
          description: Returns the list of work order skills.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/api_work_order_skill"
                  count:
                    type: integer
                    description: Amount of elements in `data`.
        "401":
          $ref: "#/components/responses/401"
  /v1/zapier/inspections:
    post:
      x-internal: true
      summary: Create an inspection from a Zapier trigger
      tags:
        - Inspections
      description: >
        Zapier-flavoured entry point for the v2 inspection create flow. The

        Zapier request body carries a flat inspection payload; the property is

        resolved by (`address` + `postal_code`) within the caller's user +

        organizations — matched against the resolved inspection template's

        `property_type` — then the up-to-five `resident_email_N` fields are

        translated into the v2 `recipients` shape (`{ type: 'emails', emails }`

        when any resident email is provided, otherwise `{ type: 'self-perform' }`)

        and delegated to the v2 create flow.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/zapier_inspection_create_request_model"
      responses:
        "200":
          description: |
            Returns the standard v2 create envelope — an array of successful
            inspections plus per-property error / warning / conflict payloads.
            Individual property-level failures do not surface as 4xx; they are
            recorded in `errors[].reason` on the 200 response.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/inspection_v2_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — the resolved inspection template or property could not
            be located within the caller's user + organizations. Raised when the
            template id does not resolve, or when no property matches
            (`address` + `postal_code` + `property_type`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    example: property not found
  /v1/zapier/properties:
    post:
      x-internal: true
      summary: Create a property (unit) from a Zapier trigger
      tags:
        - Units
      description: |
        Zapier-flavoured entry point for the v1 unit create flow. The Zapier
        request body carries a flat property payload; `full_bathrooms` /
        `half_bathrooms` are derived from the single `bathrooms` value, the
        region name is resolved from the state, and the request is delegated to
        the v1 unit create flow. The `orgId` is taken from the caller's decoded
        auth token, falling back to the `orgId` query parameter when the token
        has none.
      parameters:
        - in: query
          name: orgId
          schema:
            type: string
          description: |
            Optional organization id override. Only used when the caller's
            auth token does not already carry `orgId`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/zapier_property_create_request_model"
      responses:
        "200":
          description: Returns the created unit.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/unit_response_model"
        "400":
          description: |
            Bad request — validation on the unit body failed. Common causes:
            unknown `property_type`, `bedrooms` out of the `[0, 10]` range,
            missing required unit field.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    example: property_type has an invalid value
        "401":
          $ref: "#/components/responses/401"
        "403":
          description: |
            Forbidden — the caller has hit the max-units limit for their plan.
            Returned by the v1 unit create flow when the subscription's unit
            quota is exhausted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 403
                  error:
                    type: string
                    example: "max units reached: 100/100"
  /v1/zapier/residents:
    post:
      x-internal: true
      summary: Create a resident from a Zapier trigger
      tags:
        - Residents (deprecated)
      description: |
        Zapier-flavoured entry point for the v1 resident create flow. The Zapier
        request body carries a single flat resident payload; the property is
        resolved by (`address` + `postal_code`) within the caller's user +
        organizations and the request is then delegated to the v1 resident
        create flow. The response payload mirrors the classic
        `resident_response_model`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/zapier_resident_create_request_model"
      responses:
        "200":
          description: Returns the created resident.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 200
                  data:
                    $ref: "#/components/schemas/resident_response_model"
        "401":
          $ref: "#/components/responses/401"
        "404":
          description: |
            Not Found — no property matched the supplied `address` +
            `postal_code` within the caller's user + organizations.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 404
                  error:
                    type: string
                    enum:
                      - Property not found
                    example: Property not found
        "409":
          description: |
            Conflict — a resident with the supplied email already exists on a
            property owned by the caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    description: HTTP status code
                    example: 409
                  error:
                    type: string
                    example: Resident already exists
